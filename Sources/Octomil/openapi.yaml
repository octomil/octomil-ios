openapi: 3.1.0
info:
  title: Octomil Contract
  version: 1.28.0
  description: Generated from octomil-contracts (RPC-style HTTP).
  contact:
    name: Octomil API
    email: support@octomil.com
    url: https://octomil.com/support
  license:
    name: Proprietary
    url: https://octomil.com/terms
servers:
- url: https://api.octomil.com
  description: Production
- url: http://localhost:8000
  description: Local development
x-octomil-min-server: 1.0.0
x-octomil-namespaces:
  stable:
  - models
  - responses
  - control
  - capabilities
  - telemetry
  - chat
  - devices
  - artifacts
  - federation
  - training
  - audio
  - text
  - billing
  - monitoring
  - settings
  - cache
  - admin
  - agents
  - jobs
  compatibility: []
  experimental:
  - workflows
  - routing
  - runtime
x-octomil-release-gate:
  conformance_required: true
  minimum_fixture_pass: 100%
  parity_score_floor: 60%
  lifecycle_parity_score_floor: 60%
x-octomil-sdks:
  python:
    package: octomil
    min_contract: 1.0.0
    max_tested: 1.0.0
  ios:
    package: Octomil
    min_contract: 1.0.0
    max_tested: 1.0.0
  android:
    package: ai.octomil
    min_contract: 1.0.0
    max_tested: 1.0.0
  browser:
    package: '@octomil/browser'
    min_contract: 1.0.0
    max_tested: 1.0.0
  node:
    package: '@octomil/sdk'
    min_contract: 1.0.0
    max_tested: 1.0.0
tags:
- name: admin
- name: agents
- name: apps
- name: artifacts
- name: audio
- name: auth
- name: catalog
- name: chat
- name: cloud
- name: deploy
- name: deployments
- name: device_groups
- name: devices
- name: eval
- name: feature_flags
- name: federation
- name: federations
- name: inference
- name: jobs
- name: logs
- name: models
- name: monitoring
- name: providers
- name: recommendations
- name: responses
- name: route
- name: routing
- name: runtime
- name: settings
- name: telemetry
- name: traces
- name: training
- name: usage
paths:
  /api/v1/admin/billing/zoho-export-status:
    get:
      operationId: admin_billing_get_zoho_export_status
      summary: Read-only ops view of Zoho Books invoice export rows for an org.
      tags:
      - admin
      x-octomil-method: admin.billing.get_zoho_export_status
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Read-only ops view of Zoho Books invoice export rows for an org.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          default: 20
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/entries/{entry_id}/approve:
    post:
      operationId: admin_catalog_sync_approve_entry
      summary: Approve a pending catalog sync entry, triggering catalog upsert (family, variant, and version creation or linking).
        Re-a
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.approve_entry
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Approve a pending catalog sync entry, triggering catalog upsert (family, variant, and version creation
        or linking). Re-approving an already-mapped entry is a no-op at the catalog layer. Returns 404 if the entry does not
        exist. Requires JWT auth with admin or owner role.
      parameters:
      - name: entry_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Mapping result for the approved entry.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/sources:
    post:
      operationId: admin_catalog_sync_create_source
      summary: Create a new provider catalog sync source. Returns the created source with 201. Requires JWT auth with admin
        or owner ro
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.create_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new provider catalog sync source. Returns the created source with 201. Requires JWT auth with
        admin or owner role. auth_config is write-only (credentials are not returned in subsequent GET responses).
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 128
                provider_type:
                  type: string
                  maxLength: 32
                  default: huggingface
                enabled:
                  type: boolean
                  default: true
                base_url:
                  type: string
                  maxLength: 512
                  default: https://huggingface.co
                auth_config:
                  type: object
                  nullable: true
                  additionalProperties: true
                search_filters:
                  type: object
                  nullable: true
                  additionalProperties: true
                list_refresh_interval:
                  type: integer
                  minimum: 300
                  default: 21600
                  description: Seconds between list refreshes.
                detail_refresh_interval:
                  type: integer
                  minimum: 300
                  default: 86400
                  description: Seconds between detail refreshes.
                max_concurrent_fetches:
                  type: integer
                  minimum: 1
                  maximum: 20
                  default: 5
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 201
                type: object
                description: Created catalog sync source.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: admin_catalog_sync_list_sources
      summary: 'List all provider catalog sync sources (e.g. HuggingFace). Returns every source ordered by provider_type.
        No pagination '
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.list_sources
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List all provider catalog sync sources (e.g. HuggingFace). Returns every source ordered by provider_type.\
        \ No pagination \u2014 source count is bounded by registered providers. Requires JWT auth with admin or owner role."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: List of catalog sync sources.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/sources/{source_id}:
    delete:
      operationId: admin_catalog_sync_delete_source
      summary: 'Delete a catalog sync source. Returns 204 No Content on success. Returns 404 if the source does not exist.
        Requires JWT '
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.delete_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete a catalog sync source. Returns 204 No Content on success. Returns 404 if the source does not exist.
        Requires JWT auth with admin or owner role.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 204
                type: 'null'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: admin_catalog_sync_update_source
      summary: Partially update a catalog sync source. Only supplied fields are updated (exclude_unset semantics). Requires
        JWT auth wi
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.update_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Partially update a catalog sync source. Only supplied fields are updated (exclude_unset semantics). Requires
        JWT auth with admin or owner role. Returns 404 if the source does not exist.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 128
                enabled:
                  type: boolean
                base_url:
                  type: string
                  maxLength: 512
                auth_config:
                  type: object
                  nullable: true
                  additionalProperties: true
                search_filters:
                  type: object
                  nullable: true
                  additionalProperties: true
                list_refresh_interval:
                  type: integer
                  minimum: 300
                detail_refresh_interval:
                  type: integer
                  minimum: 300
                max_concurrent_fetches:
                  type: integer
                  minimum: 1
                  maximum: 20
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Updated catalog sync source.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/runs/{run_id}:
    get:
      operationId: admin_catalog_sync_get_run
      summary: Fetch a single catalog sync run by ID. Returns 404 if the run does not exist. Requires JWT auth with admin
        or owner role
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.get_run
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single catalog sync run by ID. Returns 404 if the run does not exist. Requires JWT auth with admin
        or owner role.
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Catalog sync run detail.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/health:
    get:
      operationId: admin_catalog_sync_health
      summary: Return an aggregate health summary across all catalog sync sources. Always returns 200 even when all sources
        are in erro
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.health
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return an aggregate health summary across all catalog sync sources. Always returns 200 even when all sources\
        \ are in error state \u2014 health status is embedded in the payload. Requires JWT auth with admin or owner role."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Aggregate health summary for all catalog sync sources.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/sources/{source_id}/entries:
    get:
      operationId: admin_catalog_sync_list_entries
      summary: List catalog sync entries discovered from an external source (e.g. HuggingFace). Supports filtering by mapping_status.
        U
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.list_entries
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List catalog sync entries discovered from an external source (e.g. HuggingFace). Supports filtering by\
        \ mapping_status. Uses offset pagination (STYLE_GUIDE \xA74 grandfathered). Does not return 404 if source_id is unknown\
        \ \u2014 returns empty array. Requires JWT auth with admin or owner role."
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: mapping_status
        in: query
        required: false
        schema:
          type: string
        description: Filter by mapping_status (e.g. pending, mapped, failed).
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Catalog sync entries for the source.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/sources/{source_id}/runs:
    get:
      operationId: admin_catalog_sync_list_runs
      summary: 'List the most recent sync runs for a catalog sync source, ordered by started_at descending. Hardcoded limit
        of 50 runs. '
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.list_runs
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List the most recent sync runs for a catalog sync source, ordered by started_at descending. Hardcoded\
        \ limit of 50 runs. Does not return 404 if the source_id is unknown \u2014 returns an empty array. Requires JWT auth\
        \ with admin or owner role."
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Up to 50 most recent sync runs for this source, newest first.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/catalog-sync/sources/{source_id}/sync:
    post:
      operationId: admin_catalog_sync_trigger_sync
      summary: Trigger a manual catalog sync for the given source. Enqueues a background job (priority=600) and returns 202
        with the jo
      tags:
      - admin
      x-octomil-method: admin.catalog_sync.trigger_sync
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Trigger a manual catalog sync for the given source. Enqueues a background job (priority=600) and returns
        202 with the job_id immediately. The resulting CatalogSyncRun will have trigger='manual'. Requires JWT auth with admin
        or owner role.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                force_full:
                  type: boolean
                  default: false
                  description: If true, ignore incremental state and do a full re-scan.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 202
                type: object
                properties:
                  job_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - queued
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/mappings:
    post:
      operationId: admin_cloud_catalog_create_mapping
      summary: Create a curated mapping from an internal catalog variant to a cloud provider model ID. Returns 201 with the
        created map
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.create_mapping
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a curated mapping from an internal catalog variant to a cloud provider model ID. Returns 201 with
        the created mapping. mapping_type defaults to 'curated' for manually created mappings. Requires JWT auth with admin
        or owner role.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - variant_id
              - cloud_model_id
              properties:
                variant_id:
                  type: string
                  format: uuid
                  description: Internal catalog variant ID to map.
                cloud_model_id:
                  type: string
                  description: External cloud provider model ID (e.g. gpt-4o, claude-3-5-sonnet-20241022).
                mapping_type:
                  type: string
                  enum:
                  - curated
                  - auto
                  default: curated
                confidence:
                  type: number
                  nullable: true
                  description: Confidence score (0-1) for auto-suggested mappings.
                notes:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 201
                type: object
                description: Created variant mapping.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: admin_cloud_catalog_list_mappings
      summary: "List all variant-to-cloud-model mappings, ordered by created_at descending. No pagination \u2014 mapping count\
        \ is bounded by "
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.list_mappings
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List all variant-to-cloud-model mappings, ordered by created_at descending. No pagination \u2014 mapping\
        \ count is bounded by the number of catalog variants. Requires JWT auth with admin or owner role."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: All variant-to-cloud-model mappings, newest first.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources:
    post:
      operationId: admin_cloud_catalog_create_source
      summary: 'Create a new cloud provider catalog source. Returns the created source with 201. provider_key must be unique
        across all '
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.create_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new cloud provider catalog source. Returns the created source with 201. provider_key must be unique
        across all cloud catalog sources. Requires JWT auth with admin or owner role.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - provider_key
              - display_name
              properties:
                provider_key:
                  type: string
                  description: Stable provider key (e.g. openai, anthropic).
                display_name:
                  type: string
                source_config:
                  type: object
                  nullable: true
                  additionalProperties: true
                models_refresh_interval:
                  type: integer
                  default: 21600
                  description: Seconds between model list refreshes.
                pricing_refresh_interval:
                  type: integer
                  default: 43200
                  description: Seconds between pricing refreshes.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 201
                type: object
                description: Created cloud catalog source.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: admin_cloud_catalog_list_sources
      summary: "List all cloud provider catalog sources (e.g. OpenAI, Anthropic), ordered by provider_key. No pagination \u2014\
        \ source count "
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.list_sources
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List all cloud provider catalog sources (e.g. OpenAI, Anthropic), ordered by provider_key. No pagination\
        \ \u2014 source count is bounded by registered cloud providers. Requires JWT auth with admin or owner role."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: List of cloud catalog sources.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/mappings/{mapping_id}:
    delete:
      operationId: admin_cloud_catalog_delete_mapping
      summary: Delete a variant-to-cloud-model mapping. Returns 204 No Content on success. Returns 404 if the mapping does
        not exist. R
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.delete_mapping
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete a variant-to-cloud-model mapping. Returns 204 No Content on success. Returns 404 if the mapping
        does not exist. Requires JWT auth with admin or owner role.
      parameters:
      - name: mapping_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 204
                type: 'null'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources/{source_id}:
    delete:
      operationId: admin_cloud_catalog_delete_source
      summary: Delete a cloud catalog source. Returns 204 No Content on success. Returns 404 if the source does not exist.
        Requires JWT
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.delete_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete a cloud catalog source. Returns 204 No Content on success. Returns 404 if the source does not exist.
        Requires JWT auth with admin or owner role.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 204
                type: 'null'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: admin_cloud_catalog_update_source
      summary: Partially update a cloud catalog source. Only supplied fields are updated (exclude_unset semantics). provider_key
        is imm
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.update_source
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Partially update a cloud catalog source. Only supplied fields are updated (exclude_unset semantics). provider_key
        is immutable. Returns 404 if the source does not exist. Requires JWT auth with admin or owner role.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                enabled:
                  type: boolean
                source_config:
                  type: object
                  nullable: true
                  additionalProperties: true
                models_refresh_interval:
                  type: integer
                pricing_refresh_interval:
                  type: integer
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Updated cloud catalog source.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/runs/{run_id}:
    get:
      operationId: admin_cloud_catalog_get_run
      summary: Fetch a single cloud catalog sync run by ID. Returns 404 if the run does not exist. Requires JWT auth with
        admin or owne
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.get_run
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single cloud catalog sync run by ID. Returns 404 if the run does not exist. Requires JWT auth with
        admin or owner role.
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Cloud catalog sync run detail.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/health:
    get:
      operationId: admin_cloud_catalog_health
      summary: "Return an aggregate health summary across all cloud catalog sources. Always returns 200 \u2014 health status\
        \ is embedded in t"
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.health
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return an aggregate health summary across all cloud catalog sources. Always returns 200 \u2014 health\
        \ status is embedded in the payload. total_models counts all CloudCatalogModel rows regardless of stale status. Requires\
        \ JWT auth with admin or owner role."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Aggregate health summary for all cloud catalog sources.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources/{source_id}/runs:
    get:
      operationId: admin_cloud_catalog_list_runs
      summary: List the most recent cloud catalog sync runs for a source, ordered by started_at descending. Hardcoded limit
        of 50 runs.
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.list_runs
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "List the most recent cloud catalog sync runs for a source, ordered by started_at descending. Hardcoded\
        \ limit of 50 runs. Does not return 404 if the source_id is unknown \u2014 returns an empty array. Requires JWT auth\
        \ with admin or owner role."
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Up to 50 most recent cloud catalog sync runs for this source, newest first.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources/{source_id}/models:
    get:
      operationId: admin_cloud_catalog_list_source_models
      summary: List all cloud models for a source including stale ones.
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.list_source_models
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - cloud_catalog_source_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: List all cloud models for a source including stale ones.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources/{source_id}/pricing:
    get:
      operationId: admin_cloud_catalog_list_source_pricing
      summary: Latest non-stale pricing per model for a source.
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.list_source_pricing
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - cloud_catalog_source_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Latest non-stale pricing per model for a source.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/mappings/suggest:
    post:
      operationId: admin_cloud_catalog_suggest_mappings
      summary: Auto-suggest variant-to-cloud-model mappings. Results not persisted.
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.suggest_mappings
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Auto-suggest variant-to-cloud-model mappings. Results not persisted.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/cloud-catalog/sources/{source_id}/sync:
    post:
      operationId: admin_cloud_catalog_trigger_sync
      summary: Trigger a manual cloud catalog sync for the given source (both models and pricing phases). Enqueues a background
        job (pr
      tags:
      - admin
      x-octomil-method: admin.cloud_catalog.trigger_sync
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Trigger a manual cloud catalog sync for the given source (both models and pricing phases). Enqueues a background
        job (priority=500, phase='both') and returns 202 with job_id immediately. The resulting CloudCatalogSyncRun will have
        trigger='manual'. Requires JWT auth with admin or owner role.
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                status: 202
                type: object
                properties:
                  job_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - queued
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/admin/devices/backfill-classes:
    post:
      operationId: admin_devices_backfill_classes
      summary: Backfill device_class for NULL-classified devices. Safe to re-run.
      tags:
      - admin
      x-octomil-method: admin.devices.backfill_classes
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Sets device_class.
      - Ensures DeviceGroups.
      - Applies auto-assignment.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Backfill device_class for NULL-classified devices. Safe to re-run.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  classified:
                    type: integer
                  skipped:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/admin/devices:
    get:
      operationId: admin_devices_search
      summary: Search and filter devices across the fleet for a given org. Supports filtering by device_identifier (partial
        ILIKE match
      tags:
      - admin
      x-octomil-method: admin.devices.search
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - insufficient_scope
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Search and filter devices across the fleet for a given org. Supports filtering by device_identifier (partial\
        \ ILIKE match), app_version, active_model_version, platform, and status. Uses offset pagination (STYLE_GUIDE \xA7\
        4 grandfathered). Accepts JWT or API-key auth; requires devices:read scope in both cases."
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
        description: Organization to search within.
      - name: device_identifier
        in: query
        required: false
        schema:
          type: string
        description: Partial (ILIKE) match on device_identifier.
      - name: app_version
        in: query
        required: false
        schema:
          type: string
        description: Exact app version match.
      - name: model_version
        in: query
        required: false
        schema:
          type: string
        description: Exact active_model_version match.
      - name: platform
        in: query
        required: false
        schema:
          type: string
        description: Filter by platform (e.g. ios, android).
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Filter by device status.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 1000
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Paginated device list with devices[], total, limit, and offset.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/approval_requests/{approval_id}/resolve:
    post:
      operationId: agents_approval_requests_resolve
      summary: Resolve a pending approval request. Validates parent run belongs to authenticated org. Sets status, reason,
        resolved_by,
      tags:
      - agents
      x-octomil-method: agents.approval_requests.resolve
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Updates ApprovalRequest.status, .reason, .resolved_by, .resolved_at.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - approval_not_found
      - approval_already_resolved
      - invalid_input
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Resolve a pending approval request. Validates parent run belongs to authenticated org. Sets status, reason,
        resolved_by, resolved_at.
      parameters:
      - name: approval_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - action
              properties:
                action:
                  type: string
                  enum:
                  - approve
                  - reject
                reason:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - approval_id
                - status
                properties:
                  approval_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - approved
                    - rejected
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/manifest:
    get:
      operationId: agents_manifest_get
      summary: "Return metadata for all agents in AgentRegistry. Does NOT require the model client \u2014 only the registry.\
        \ For frontend dis"
      tags:
      - agents
      x-octomil-method: agents.manifest.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return metadata for all agents in AgentRegistry. Does NOT require the model client \u2014 only the registry.\
        \ For frontend discovery."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentManifest'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/runs/{run_id}/cancel:
    post:
      operationId: agents_runs_cancel
      summary: Cancel a run in queued, running, or awaiting_approval status. Sets status=cancelled and completed_at=now().
      tags:
      - agents
      x-octomil-method: agents.runs.cancel
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Updates Run.status to cancelled and sets completed_at.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - run_not_found
      - run_state_invalid
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Cancel a run in queued, running, or awaiting_approval status. Sets status=cancelled and completed_at=now().
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - run_id
                - status
                properties:
                  run_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - cancelled
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/runs/{run_id}:
    get:
      operationId: agents_runs_get
      summary: Retrieve a run by ID with steps (by step_index) and pending ApprovalRequests. Scoped to the authenticated org.
      tags:
      - agents
      x-octomil-method: agents.runs.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - run_not_found
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a run by ID with steps (by step_index) and pending ApprovalRequests. Scoped to the authenticated
        org.
      parameters:
      - name: run_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Run'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/sessions/{session_id}/complete:
    post:
      operationId: agents_sessions_complete
      summary: 'Mark a client-side agent session as completed. Updates Run: status=completed, summary, confidence, completed_at=now().
        e'
      tags:
      - agents
      x-octomil-method: agents.sessions.complete
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Updates Run.status to completed, sets summary, confidence, completed_at.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - run_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Mark a client-side agent session as completed. Updates Run: status=completed, summary, confidence, completed_at=now().
        evidence and next_steps not persisted.'
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - summary
              properties:
                summary:
                  type: string
                confidence:
                  type: number
                  nullable: true
                  minimum: 0
                  maximum: 1
                evidence:
                  type: array
                  items:
                    type: string
                  default: []
                next_steps:
                  type: array
                  items:
                    type: string
                  default: []
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - session_id
                - status
                properties:
                  session_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - completed
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/sessions:
    post:
      operationId: agents_sessions_create
      summary: Create a client-side agent tool session. Server resolves the model, selects deployment lane, reads system prompt,
        builds
      tags:
      - agents
      x-octomil-method: agents.sessions.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Inserts a Run row in agent_runs with status=running.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a client-side agent tool session. Server resolves the model, selects deployment lane, reads system
        prompt, builds tool gateway, and creates a Run row (status=running).
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - agent_type
              - query
              properties:
                agent_type:
                  type: string
                  description: Agent type key. Returns 400 if unknown.
                query:
                  type: string
                  maxLength: 2000
                context:
                  type: object
                  additionalProperties: true
                  default: {}
                device_id:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateSessionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/sessions/{session_id}/execute:
    post:
      operationId: agents_sessions_execute
      summary: Execute a single tool call within a client-side agent session. Records a Step row. Returns tool result as JSON
        string.
      tags:
      - agents
      x-octomil-method: agents.sessions.execute
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Inserts a Step row in agent_steps.
      - Side effects of the tool itself.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - run_not_found
      - invalid_input
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Execute a single tool call within a client-side agent session. Records a Step row. Returns tool result
        as JSON string.
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - tool_name
              - tool_call_id
              properties:
                tool_name:
                  type: string
                arguments:
                  type: object
                  nullable: true
                  additionalProperties: true
                  description: Null is coerced to {}.
                tool_call_id:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - tool_call_id
                - content
                - is_error
                properties:
                  tool_call_id:
                    type: string
                  content:
                    type: string
                    description: JSON-serialised tool result.
                  is_error:
                    type: boolean
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/sessions/{session_id}/responses:
    post:
      operationId: agents_sessions_responses_create
      summary: Run one inference turn within an active agent session. Validates session ownership and model match. Runs optional
        intent
      tags:
      - agents
      x-octomil-method: agents.sessions.responses.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Populates in-process response cache for multi-turn threading.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - run_not_found
      - invalid_input
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Run one inference turn within an active agent session. Validates session ownership and model match. Runs
        optional intent gate. Builds context from previous_response_id cache.
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - input
              properties:
                model:
                  type: string
                  nullable: true
                  description: Must match session's resolved_model. Mismatch returns 400.
                input:
                  oneOf:
                  - type: string
                  - type: array
                    items:
                      type: object
                      additionalProperties: true
                tools:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                  default: []
                previous_response_id:
                  type: string
                  nullable: true
                stream:
                  type: boolean
                  default: false
                max_output_tokens:
                  type: integer
                  nullable: true
                  default: 4096
                temperature:
                  type: number
                  nullable: true
                instructions:
                  type: string
                  nullable: true
                metadata:
                  type: object
                  additionalProperties:
                    type: string
                  default: {}
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'stream=false: response payload (output, tool_calls, usage). stream=true: text/event-stream with
                  text_delta, tool_call_delta, done, error.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/threads:
    post:
      operationId: agents_threads_create
      summary: Create a new conversation thread for the specified agent_type. Validates against AgentRegistry.supported_agents()
        and in
      tags:
      - agents
      x-octomil-method: agents.threads.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Inserts a Thread row in chat_threads.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new conversation thread for the specified agent_type. Validates against AgentRegistry.supported_agents()
        and inserts a Thread row in chat_threads.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - agent_type
              properties:
                agent_type:
                  type: string
                  description: Agent type key. Must be in AgentRegistry. Returns 400 if unknown.
                title:
                  type: string
                  nullable: true
                metadata:
                  type: object
                  additionalProperties: true
                  default: {}
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Thread'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: agents_threads_list
      summary: List threads for the authenticated org, optionally filtered by agent_type. Scoped to org_id, ordered by updated_at
        DESC,
      tags:
      - agents
      x-octomil-method: agents.threads.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: List threads for the authenticated org, optionally filtered by agent_type. Scoped to org_id, ordered by
        updated_at DESC, hard-capped at 50.
      parameters:
      - name: agent_type
        in: query
        required: false
        schema:
          type: string
        description: Filter by agent_type. Omit to return all types.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  schema: agents/Thread.json
                description: Array of Thread objects ordered by updated_at DESC, capped at 50.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/threads/{thread_id}:
    get:
      operationId: agents_threads_get
      summary: Retrieve a thread by ID with full message history. Scoped to the authenticated org; 404 for cross-org access.
      tags:
      - agents
      x-octomil-method: agents.threads.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - thread_not_found
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a thread by ID with full message history. Scoped to the authenticated org; 404 for cross-org access.
      parameters:
      - name: thread_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Thread'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/agents/threads/{thread_id}/runs:
    post:
      operationId: agents_threads_runs_create
      summary: Start a new agent run within an existing thread. Validates thread ownership, opens SSE stream via start_agent_run().
        Fir
      tags:
      - agents
      x-octomil-method: agents.threads.runs.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - server_error
      x-octomil-side-effects:
      - Inserts Run row in agent_runs.
      - Inserts Message and Step rows as orchestrator progresses.
      - Inserts ApprovalRequest rows for high-risk tools.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - thread_not_found
      - agent_system_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Start a new agent run within an existing thread. Validates thread ownership, opens SSE stream via start_agent_run().
        First event is run_created with run_id. Stream closes after done.
      parameters:
      - name: thread_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - query
              properties:
                query:
                  type: string
                  maxLength: 2000
                context:
                  type: object
                  additionalProperties: true
                  default: {}
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: string
                format: text/event-stream
                description: 'SSE stream. Event types: run_created, message_delta, tool_call, run_completed, run_failed, done.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/activity:
    get:
      operationId: apps_activity
      summary: Return the activity feed scoped to a single app, newest first. Backed by activity_feed_service.get_feed() with
        app_id fi
      tags:
      - apps
      x-octomil-method: apps.activity
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the activity feed scoped to a single app, newest first. Backed by activity_feed_service.get_feed()
        with app_id filter. Provides the same event types as the org-level activity feed (`/api/v1/activity`) but restricted
        to this app's events. Used by the app detail page's activity timeline.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
      - name: before
        in: query
        required: false
        schema:
          type: string
        description: 'Opaque cursor returned by a prior response. Pass to retrieve events older than the cursor. Omit to start
          from the most recent events.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: "Unified activity feed for the app \u2014 deployment events and cloud usage \u2014 ordered newest\
                  \ first.\n"
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/archive:
    post:
      operationId: apps_archive
      summary: Archive an App. Soft-delete; targets + slots are preserved for historical / audit purposes but the App is hidden
        from li
      tags:
      - apps
      x-octomil-method: apps.archive
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets App.status to "archived"
      - all keys bound to the App are revoked (key_records reject auth attempts)
      - emits telemetry event (app.archived)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Archive an App. Soft-delete; targets + slots are preserved for historical / audit purposes but the App
        is hidden from list endpoints by default and rejected by SDK auth flows.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The App row with status = "archived".
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/bootstrap:
    post:
      operationId: apps_bootstrap
      summary: 'First-run bootstrap: create an app, register a target for the given platform, and generate an app key (plus
        publishable '
      tags:
      - apps
      x-octomil-method: apps.bootstrap
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates App record
      - creates AppTarget/integration record for the selected platform (server target registered with identifier `{slug}-default`)
      - creates ApiKey record (key_type=app_key, environment=live)
      - creates ApiKey record (key_type=publishable_key, environment=live) for ios/android/browser/macos platforms
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'First-run bootstrap: create an app, register a target for the given platform, and generate an app key
        (plus publishable key for mobile/browser platforms) in a single atomic transaction. Intended for the onboarding quickstart
        flow so the SDK integration instructions can display a ready-to-use API key immediately.

        The entire operation is atomic. If any step fails, the transaction is rolled back and no records are persisted.'
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - platform
              properties:
                name:
                  type: string
                  description: Human-readable app name. Used to auto-generate slug if not provided.
                slug:
                  type: string
                  description: 'URL-safe slug. If omitted the server auto-generates one from `name` with a short random suffix
                    to ensure uniqueness.

                    '
                description:
                  type: string
                  nullable: true
                platform:
                  type: string
                  description: 'Target platform for the initial target registration. Valid values: `ios | android | browser
                    | macos | server | node`.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - app
                - app_key
                - app_key_id
                - app_key_prefix
                - quickstart
                properties:
                  app:
                    type: object
                    additionalProperties: true
                    description: Created app object (same shape as apps.get response).
                  integration:
                    type: object
                    nullable: true
                    additionalProperties: true
                    description: 'Registered target/integration for the selected platform. Null if the platform did not produce
                      a target record.

                      '
                  app_key:
                    type: string
                    description: 'Plaintext app API key. Shown once at creation time only; callers MUST store this value.
                      Subsequent requests cannot retrieve it.

                      '
                  app_key_id:
                    type: string
                    description: Stable UUID for the created ApiKey record.
                  app_key_prefix:
                    type: string
                    description: First 21 characters of the app key, safe to log/display.
                  publishable_key:
                    type: string
                    nullable: true
                    description: 'Plaintext publishable key for mobile/browser platforms (`ios`, `android`, `browser`, `macos`).
                      Null for `server`/`node`. Shown once only.

                      '
                  publishable_key_id:
                    type: string
                    nullable: true
                    description: Stable UUID for the created publishable ApiKey record.
                  quickstart:
                    type: object
                    required:
                    - platform
                    - has_capability
                    - has_first_request
                    properties:
                      platform:
                        type: string
                      has_capability:
                        type: boolean
                        description: Whether the app already has at least one slot configured.
                      has_first_request:
                        type: boolean
                        description: Whether any inference request has been received for this app.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/capabilities/{capability_key}/check-optimization:
    get:
      operationId: apps_capabilities_check_optimization
      summary: Check if a better model variant is available for this capability. Runs CapabilityOrchestratorService.check_optimization(
      tags:
      - apps
      x-octomil-method: apps.capabilities.check_optimization
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Check if a better model variant is available for this capability. Runs CapabilityOrchestratorService.check_optimization()
        which compares the currently deployed model variant against available alternatives, accounting for the device class
        and platform distribution of the app's registered targets. Returns a recommendation with the suggested variant and
        expected improvement, without making any changes.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
      - name: device_class
        in: query
        required: false
        schema:
          type: string
        description: 'Filter optimization check to a specific device class (e.g. `high_end`, `mid_range`). Omit to check across
          all device classes.

          '
      - name: platform
        in: query
        required: false
        schema:
          type: string
        description: 'Filter optimization check to a specific platform (e.g. `ios`, `android`). Omit to check across all platforms.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "CheckOptimizationResponse \u2014 whether a better model exists for this capability given the\
                  \ fleet's current device class / platform composition.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/capabilities/{capability_key}/enable:
    post:
      operationId: apps_capabilities_enable
      summary: Enable a capability on an app. Runs CapabilityOrchestratorService.enable_capability() which resolves the best
        model vari
      tags:
      - apps
      x-octomil-method: apps.capabilities.enable
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - may create a Deployment record for this app+capability+environment
      - may create DeviceAssignment records for all registered targets
      - may start the deployment (transition to active/rolling_out)
      - creates or updates a CapabilitySlot for the capability
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Enable a capability on an app. Runs CapabilityOrchestratorService.enable_capability() which resolves the
        best model variant, creates a deployment scoped to this app+capability+environment, creates device assignments for
        all registered targets, and starts the deployment. This is the primary onboarding action for an app''s first capability.

        Idempotent in intent: if a deployment already exists for this app+capability+environment, the orchestrator updates
        it rather than creating a duplicate. However, the endpoint is marked non-idempotent because the exact side effects
        depend on the current state.'
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                variant_id:
                  type: string
                  nullable: true
                  description: 'Specific model variant to use. If omitted, the orchestrator selects the best available variant
                    for the app''s registered targets.

                    '
                environment:
                  type: string
                  default: staging
                  description: Deployment environment. Defaults to `staging`.
                serving_policy:
                  type: object
                  nullable: true
                  additionalProperties: true
                  description: Optional serving policy overrides for the created deployment.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "EnableCapabilityResponse \u2014 includes status, the created or updated deployment, assigned\
                  \ model variant, and any warnings.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/capabilities/{capability_key}/optimize:
    post:
      operationId: apps_capabilities_optimize
      summary: Re-optimize a capability by starting a canary rollout to a better model variant. Runs CapabilityOrchestratorService.opti
      tags:
      - apps
      x-octomil-method: apps.capabilities.optimize
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - may update existing Deployment record to target a new model variant
      - may create a new canary Rollout record
      - may update DeviceAssignment records for the new variant
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Re-optimize a capability by starting a canary rollout to a better model variant. Runs CapabilityOrchestratorService.optimize_capability()\
        \ which: 1. Identifies the best available model variant for the fleet composition. 2. Creates or updates the staging/production\
        \ deployment to target the\n   new variant at a low initial rollout percentage (canary).\n3. Returns the optimization\
        \ result with per-tier details for multi-tier fleets.\nThe rollout proceeds via the normal deployment automator \u2014\
        \ the operator monitors health and advances/rolls back via the standard deployment endpoints."
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
      - name: device_class
        in: query
        required: false
        schema:
          type: string
      - name: platform
        in: query
        required: false
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "OptimizeCapabilityResponse \u2014 result of the optimization attempt, including status and any\
                  \ tier-level details for multi-tier fleets.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/capabilities/{capability_key}/preview:
    post:
      operationId: apps_capabilities_preview
      summary: Side-effect-free preview of what `apps.capabilities.enable` would do. Runs the CapabilityOrchestratorService.preview_cap
      tags:
      - apps
      x-octomil-method: apps.capabilities.preview
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Side-effect-free preview of what `apps.capabilities.enable` would do. Runs the CapabilityOrchestratorService.preview_capability()
        flow to resolve the best model variant and target assignments, then returns the recommendation without persisting
        anything. Use this before calling enable to confirm the orchestrator's choices.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                variant_id:
                  type: string
                  nullable: true
                  description: 'Specific model variant to preview. If omitted, the orchestrator selects the best available
                    variant for the app''s targets.

                    '
                environment:
                  type: string
                  nullable: true
                  description: Target deployment environment (e.g. `staging`, `production`).
                serving_policy:
                  type: object
                  nullable: true
                  additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "EnableCapabilityResponse \u2014 the orchestrator's recommendation for how to enable this capability,\
                  \ without actually creating any records. Includes status, recommended model variant, and any warnings.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps:
    post:
      operationId: apps_create
      summary: Create a new App. The App is the parent of AppTargets (one per platform / bundle ID) and CapabilitySlots (one
        per ModelC
      tags:
      - apps
      x-octomil-method: apps.create
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts an App row
      - emits telemetry event (app.created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new App. The App is the parent of AppTargets (one per platform / bundle ID) and CapabilitySlots
        (one per ModelCapability the App uses).
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - slug
              properties:
                name:
                  type: string
                  description: Display name for the dashboard.
                slug:
                  type: string
                  description: URL-safe identifier, unique per org. Used as `{app_slug}` in routes.
                description:
                  type: string
                status:
                  type: string
                  description: Defaults to "active".
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The persisted App row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: apps_list
      summary: List Apps in the caller's org.
      tags:
      - apps
      x-octomil-method: apps.list
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List Apps in the caller's org.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 \"active\" or \"archived\"."
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of apps entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/targets:
    post:
      operationId: apps_create_target
      summary: 'Register a (platform, identifier) target for the App. Unique per (org_id, platform, identifier); duplicate
        registration '
      tags:
      - apps
      x-octomil-method: apps.create_target
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts an AppTarget row
      - emits telemetry event (app.target_added)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Register a (platform, identifier) target for the App. Unique per (org_id, platform, identifier); duplicate
        registration returns 409.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - platform
              - identifier
              properties:
                platform:
                  type: string
                  description: '"ios" | "android" | "macos" | "browser" | "server".'
                identifier:
                  type: string
                  description: Bundle ID / package name (e.g. "com.octomil.canary-demo").
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The newly-created AppTarget row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: apps_list_targets
      summary: "List every AppTarget registered to the App. Each target binds the App to a (platform, identifier) tuple \u2014\
        \ e.g. (\"ios\", \""
      tags:
      - apps
      x-octomil-method: apps.list_targets
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "List every AppTarget registered to the App. Each target binds the App to a (platform, identifier) tuple\
        \ \u2014 e.g. (\"ios\", \"com.octomil.canary-demo\") \u2014 which lets the platform join devices + deployments back\
        \ to the App via bundle ID."
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of targets entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/slots/{capability_key}:
    delete:
      operationId: apps_delete_slot
      summary: Remove a capability slot from an App.
      tags:
      - apps
      x-octomil-method: apps.delete_slot
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes the CapabilitySlot row
      - inference requests on this capability route via the App return 404 immediately
      - emits telemetry event (app.slot_deleted)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Remove a capability slot from an App.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Empty object on success.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: apps_get_slot
      summary: Fetch a single capability slot for an app by capability key. Returns 404 if the app exists but has no slot
        for the reque
      tags:
      - apps
      x-octomil-method: apps.get_slot
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - capability_not_configured
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single capability slot for an app by capability key. Returns 404 if the app exists but has no slot
        for the requested capability. Used by dashboard panels that need per-capability slot configuration without loading
        all slots.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
        description: Capability key (e.g. `chat`, `transcription`, `tts`).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'CapabilitySlot object for the requested capability.

                  '
                required:
                - id
                - app_id
                - capability
                - is_required
                properties:
                  id:
                    type: string
                  app_id:
                    type: string
                  capability:
                    type: string
                  is_required:
                    type: boolean
                    description: 'Whether this slot must be configured before the app can serve requests for the capability.
                      Matches apps.upsert_slot''s request shape.

                      '
                  active_variant_id:
                    type: string
                    nullable: true
                  active_version_id:
                    type: string
                    nullable: true
                  serving_policy:
                    type: object
                    nullable: true
                    additionalProperties: true
                  created_at:
                    type: string
                  updated_at:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      operationId: apps_upsert_slot
      summary: Configure or update a capability slot on an App. PUT (not PATCH) because the operation replaces the slot's
        serving polic
      tags:
      - apps
      x-octomil-method: apps.upsert_slot
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts or replaces CapabilitySlot for (app_id, capability)
      - re-resolves the App's routing decisions for the affected capability
      - emits telemetry event (app.slot_upserted)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - model_not_found
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Configure or update a capability slot on an App. PUT (not PATCH) because the operation replaces the slot's
        serving policy + bindings wholesale; partial updates are not supported (saves us from confusing serving-policy diff
        semantics).
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: capability_key
        in: path
        required: true
        schema:
          type: string
        description: ModelCapability value (e.g. "vision", "chat", "transcription").
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - capability
              properties:
                capability:
                  type: string
                  description: ModelCapability value identifying the slot (e.g. "vision", "chat", "transcription").
                active_variant_id:
                  type: string
                active_version_id:
                  type: string
                is_required:
                  type: boolean
                  default: true
                serving_policy:
                  type: object
                  description: 'Inline ServingPolicy, OR a preset reference like {"preset": "local_first"} where preset is
                    one of local_first / performance_first / cloud_first / private.'
                tier_variants:
                  type: object
                  description: "Per-scope variant overrides \u2014 e.g. `{\"flagship,high\": \"variant-a\"}`."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The upserted CapabilitySlot.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/targets/{target_id}:
    delete:
      operationId: apps_delete_target
      summary: 'Remove a target (app integration/platform registration) from an app. Triggers two reconciliation passes before
        deleting:'
      tags:
      - apps
      x-octomil-method: apps.delete_target
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - removes the target's target_id from any deployment.targeting.app_target_ids arrays (reconcile_app_target_deployments)
      - re-runs label-selector materializer for all remaining app deployments (rematerialize_app_deployments)
      - deletes AppTarget record
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Remove a target (app integration/platform registration) from an app. Triggers two reconciliation passes
        before deleting: first, any `app_target_ids` deployments that reference this target are updated to remove the target
        assignment; second, label-selector materializer is re-run across all app deployments so device assignment counts stay
        accurate. The target is then deleted and the transaction is committed.'
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: target_id
        in: path
        required: true
        schema:
          type: string
        description: UUIDv4 of the AppTarget to remove.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Empty 204 response on success; no body.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/deployments:
    get:
      operationId: apps_deployments_list
      summary: 'List deployments scoped to a single app, identified by slug. Equivalent to `deployments.list` filtered by
        `app_id`, but '
      tags:
      - apps
      x-octomil-method: apps.deployments.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List deployments scoped to a single app, identified by slug. Equivalent to `deployments.list` filtered
        by `app_id`, but addressed via the app slug directly. The dashboard's app detail page uses this to render the deployments
        tab without requiring a prior app-id lookup.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
        description: URL-safe slug identifying the app.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by deployment status. Valid values mirror the deployment lifecycle: `draft | active | paused
          | rolling_out | failed | archived`. Omit to return all statuses.

          '
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - deployments
                - total
                properties:
                  deployments:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                      description: Deployment object as returned by deployments.get.
                  total:
                    type: integer
                    description: Total count of deployments matching the filter before pagination.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/first-request-status:
    get:
      operationId: apps_first_request_status
      summary: Check whether any inference request has been received for this app. Counts both cloud requests (CloudUsageLog)
        and on-de
      tags:
      - apps
      x-octomil-method: apps.first_request_status
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Check whether any inference request has been received for this app. Counts both cloud requests (CloudUsageLog)
        and on-device/SDK runtime requests (RuntimeRouteEvent). The quickstart indicator in the dashboard and onboarding flows
        uses this endpoint to flip to "success" on first SDK call regardless of execution path (cloud vs local).
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - received
                properties:
                  received:
                    type: boolean
                    description: 'True if at least one inference request has been received for this app, from either cloud
                      (CloudUsageLog) or on-device/SDK (RuntimeRouteEvent).

                      '
                  first_request_at:
                    type: string
                    nullable: true
                    description: 'ISO-8601 UTC timestamp of the earliest observed request. Null if `received` is false.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}:
    get:
      operationId: apps_get
      summary: Fetch one App by slug. Powers the App detail page.
      tags:
      - apps
      x-octomil-method: apps.get
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch one App by slug. Powers the App detail page.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
        description: App.slug (unique per org).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Full App row with eager-loaded targets + slots.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: apps_update
      summary: Partial update of an App.
      tags:
      - apps
      x-octomil-method: apps.update
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates App row per request body
      - emits telemetry event (app.updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Partial update of an App.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                status:
                  type: string
                  description: '"active" or "archived". Prefer apps.archive over flipping directly.'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated App row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/health:
    get:
      operationId: apps_health
      summary: "Return slot coverage health for an app \u2014 whether each configured capability slot has sufficient active\
        \ deployment covera"
      tags:
      - apps
      x-octomil-method: apps.health
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return slot coverage health for an app \u2014 whether each configured capability slot has sufficient active\
        \ deployment coverage. Computed by AppService.check_slot_coverage(). Powers the app health badge in the dashboard\
        \ list view and the health detail panel on the app detail page."
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: environment
        in: query
        required: false
        schema:
          type: string
        description: 'Filter health check to a specific deployment environment (e.g. `staging`, `production`). Omit to check
          coverage across all environments.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Slot coverage health report for the app. Shape is service-defined and includes coverage status
                  per capability/slot. Clients should treat as opaque and forward unknown fields.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/inference-config:
    delete:
      operationId: apps_inference_config_delete
      summary: Delete inference configuration for an app.
      tags:
      - apps
      x-octomil-method: apps.inference_config.delete
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Deletes InferenceConfig row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete inference configuration for an app.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: apps_inference_config_get
      summary: Get inference configuration for an app.
      tags:
      - apps
      x-octomil-method: apps.inference_config.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Get inference configuration for an app.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      operationId: apps_inference_config_upsert
      summary: Create or update inference configuration for an app.
      tags:
      - apps
      x-octomil-method: apps.inference_config.upsert
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Upserts InferenceConfig row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create or update inference configuration for an app.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/slots:
    get:
      operationId: apps_list_slots
      summary: List every CapabilitySlot configured on the App. Each slot pairs a ModelCapability (e.g. `vision`, `chat`,
        `transcriptio
      tags:
      - apps
      x-octomil-method: apps.list_slots
      x-octomil-since: 1.10.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List every CapabilitySlot configured on the App. Each slot pairs a ModelCapability (e.g. `vision`, `chat`,
        `transcription`) with an active catalog variant + serving policy.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of slots entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/routing/decisions:
    get:
      operationId: apps_routing_decisions
      summary: List recent routing decisions scoped to a single app. Backed by the same list_decisions_db service as the org-level
        deci
      tags:
      - apps
      x-octomil-method: apps.routing.decisions
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List recent routing decisions scoped to a single app. Backed by the same list_decisions_db service as the
        org-level decisions endpoint, but filtered to app_id. Used by the app detail page's routing panel to show the decision
        history (local vs cloud, model chosen, latency).
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'Array of routing decision objects scoped to this app, ordered by decision time descending. Each
                  object represents one routing choice made by the routing layer for a request to this app.

                  '
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/apps/{app_slug}/routing/stats:
    get:
      operationId: apps_routing_stats
      summary: Return aggregated routing statistics scoped to a single app. Backed by the same routing_telemetry service as
        the org-lev
      tags:
      - apps
      x-octomil-method: apps.routing.stats
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated routing statistics scoped to a single app. Backed by the same routing_telemetry service
        as the org-level stats endpoint but filtered to `app_id`. Used by the app detail page routing panel.
      parameters:
      - name: app_slug
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated routing statistics for the app. Shape mirrors the org-level routing stats but scoped
                  to this app''s traffic. Includes decision counts by outcome (local/cloud/fallback), latency summaries, and
                  model-level breakdown.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/artifacts/{artifact_id}/download-urls:
    post:
      operationId: artifacts_download_urls
      summary: Generate pre-signed download URLs for artifact files (optionally per-chunk for resumable downloads). Returned
        URLs are s
      tags:
      - artifacts
      x-octomil-method: artifacts.download_urls
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server generates short-lived signed URLs against CDN storage
      - server logs URL generation for audit
      x-octomil-errors:
      - model_not_found
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Generate pre-signed download URLs for artifact files (optionally per-chunk for resumable downloads). Returned
        URLs are short-lived; re-call if a download resumes after expiry.
      parameters:
      - name: artifact_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - files
              properties:
                files:
                  type: array
                  items:
                    type: string
                  description: List of file paths within the artifact to generate URLs for.
                chunk_indices:
                  type: object
                  additionalProperties:
                    type: array
                    items:
                      type: integer
                  description: 'Per-file chunk indices for resumable downloads. Shape: `{<file_path>: [chunk_idx, ...]}`.
                    If absent or omitted for a file, URLs for all chunks of that file are returned.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - artifact_id
                - urls
                properties:
                  artifact_id:
                    type: string
                  urls:
                    type: array
                    items:
                      type: object
                      required:
                      - path
                      - chunk_index
                      - url
                      - expires_at
                      properties:
                        path:
                          type: string
                          description: File path within the artifact.
                        chunk_index:
                          type: integer
                          default: 0
                        url:
                          type: string
                          format: uri
                        expires_at:
                          type: string
                          format: date-time
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/artifacts/{artifact_id}/manifest:
    get:
      operationId: artifacts_manifest
      summary: Fetch the chunked download manifest for a specific artifact, including per-chunk integrity hashes.
      tags:
      - artifacts
      x-octomil-method: artifacts.manifest
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server records manifest fetch timestamp for this device/artifact pair
      x-octomil-errors:
      - model_not_found
      - device_not_registered
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch the chunked download manifest for a specific artifact, including per-chunk integrity hashes.
      parameters:
      - name: artifact_id
        in: path
        required: true
        schema:
          type: string
      - name: deviceId
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/artifact_manifest'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /v1/audio/speech:
    post:
      operationId: audio_speech_create
      summary: "Synthesize speech from text. Routed through the unified facade \u2014 local sherpa-onnx (Kokoro/Piper) for\
        \ on-device executio"
      tags:
      - audio
      x-octomil-method: audio.speech.create
      x-octomil-since: 1.15.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - consumes compute (local or cloud)
      - emits telemetry events (inference.started, inference.completed or inference.failed)
      - cloud execution writes cloud_usage_logs and increments hosted_requests_monthly
      - local execution emits route telemetry only (no cloud usage rows)
      x-octomil-errors:
      - model_not_found
      - model_not_ready
      - runtime_unavailable
      - local_tts_runtime_unavailable
      - format_not_supported_for_local_tts
      - voice_not_supported_for_locality
      - cloud_execution_disallowed
      - cloud_fallback_disabled
      - capability_not_supported_on_platform
      - input_too_large
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      - authentication_failed
      - forbidden
      - policy_local_only
      description: "Synthesize speech from text. Routed through the unified facade \u2014 local sherpa-onnx (Kokoro/Piper)\
        \ for on-device execution, hosted gateway for cloud execution. App refs (@app/<slug>/tts) carry the routing policy."
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/audio_speech_request'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/audio_speech_result'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/delete-account:
    post:
      operationId: auth_account_delete
      summary: Permanently deletes the authenticated user's account and all associated data. Clears auth cookies on success.
        This actio
      tags:
      - auth
      x-octomil-method: auth.account.delete
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - permanently deletes the User row and all associated data via AccountDeletionService
      - clears httpOnly auth cookies on the response
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Permanently deletes the authenticated user's account and all associated data. Clears auth cookies on success.
        This action is irreversible.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - confirmation
              properties:
                confirmation:
                  type: string
                  description: 'Must be exactly the string "DELETE MY ACCOUNT" (case-sensitive) to confirm intentional deletion.
                    Any other value returns 400.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                    default: true
                  report:
                    type: object
                    description: 'Summary of what was deleted. Shape is implementation-defined (from AccountDeletionService.delete_account);
                      fields indicate counts of deleted rows per entity type.

                      '
                    additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/set-password:
    post:
      operationId: auth_account_set_password
      summary: Sets a password on an account that has none (OAuth-only accounts). This is the onboarding path for social login
        users wh
      tags:
      - auth
      x-octomil-method: auth.account.set_password
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets User.password_hash
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Sets a password on an account that has none (OAuth-only accounts). This is the onboarding path for social
        login users who want to add password auth. To change an existing password use auth.password.change.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - password
              properties:
                password:
                  type: string
                  minLength: 8
                  description: New password in plaintext (transmitted over TLS).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    default: Password set.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/account-setup:
    get:
      operationId: auth_account_setup_status
      summary: 'Returns onboarding/account-setup status for the authenticated user: whether their email is verified and whether
        they hav'
      tags:
      - auth
      x-octomil-method: auth.account.setup_status
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Returns onboarding/account-setup status for the authenticated user: whether their email is verified and
        whether they have a password set. Used by the dashboard onboarding flow to determine which steps remain incomplete.'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - email
                - email_verified
                - has_password
                properties:
                  email:
                    type: string
                    format: email
                    description: Current email address on the account.
                  email_verified:
                    type: boolean
                    description: Whether the email has been verified via a 6-digit code.
                  has_password:
                    type: boolean
                    description: Whether the account has a password set (false for OAuth-only accounts).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/cli/authorize:
    post:
      operationId: auth_cli_authorize
      summary: Browser-based CLI authorization. After a user logs in via the dashboard, the dashboard calls this endpoint.
        The server c
      tags:
      - auth
      x-octomil-method: auth.cli.authorize
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates an ApiKey row scoped to CLI permissions
      - may create a publishable ApiKey row if none exists for the org
      - records an audit event (action=cli_authorize)
      - records a log entry
      - emits PostHog cli_install event
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Browser-based CLI authorization. After a user logs in via the dashboard, the dashboard calls this endpoint.
        The server creates a scoped API key, then returns a redirect_url that delivers the key to the CLI via localhost callback.
        Optionally auto-creates a publishable key if none exists for the org.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - callback_url
              - state
              properties:
                callback_url:
                  type: string
                  description: 'Local CLI callback URL. MUST start with "http://127.0.0.1:" or "http://localhost:". The server
                    redirects the result to this URL.

                    '
                state:
                  type: string
                  minLength: 1
                  description: CSRF token generated by the CLI to validate the callback.
                hostname:
                  type: string
                  default: unknown
                  description: CLI machine hostname; used to generate the API key display name.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  redirect_url:
                    type: string
                    description: 'callback_url with query params: key, state, org, org_id, and optionally pub_key (if a publishable
                      key was auto-created).

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/cli/dashboard-token:
    post:
      operationId: auth_cli_dashboard_token
      summary: 'Creates a short-lived single-use dashboard token. The CLI uses this to open a browser session from a terminal:
        it exchan'
      tags:
      - auth
      x-octomil-method: auth.cli.dashboard_token
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - stores the token in server in-memory store (keyed by token, value includes org_id)
      - evicts any expired tokens from the in-memory store on each call
      x-octomil-errors:
      - authentication_failed
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Creates a short-lived single-use dashboard token. The CLI uses this to open a browser session from a terminal:
        it exchanges the token at the session endpoint, which sets auth cookies and redirects to the dashboard.'
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    description: 'Short-lived (5 minute TTL), single-use, URL-safe random token. Exchange via GET /api/v1/auth/cli/session?token={token}
                      to obtain browser cookies.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/cli/session:
    get:
      operationId: auth_cli_session
      summary: Exchanges a single-use dashboard token for browser session cookies. Resolves the org's first member, mints
        an access + r
      tags:
      - auth
      x-octomil-method: auth.cli.session
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - marks the token as used and removes it from in-memory store
      - mints access + refresh tokens
      - sets httpOnly auth cookies on the 302 response
      x-octomil-errors:
      - rate_limited
      - server_error
      description: Exchanges a single-use dashboard token for browser session cookies. Resolves the org's first member, mints
        an access + refresh token pair for that user, and issues a 302 redirect to the dashboard. Tokens that are invalid,
        expired, or already used redirect to /login.
      parameters:
      - name: token
        in: query
        required: true
        schema:
          type: string
        description: Single-use dashboard token from auth.cli.dashboard_token.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                description: 'Browser redirect (HTTP 302). On success: redirects to OCTOMIL_FRONTEND_URL/dashboard with auth
                  cookies set. On failure (invalid/expired/used token): redirects to OCTOMIL_FRONTEND_URL/login with no cookies
                  set.

                  '
                type: object
                properties:
                  location:
                    type: string
                    description: Redirect target URL (in the Location header).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/send-verification-code:
    post:
      operationId: auth_email_send_verification_code
      summary: Sends a 6-digit verification code to the authenticated user's email address. Invalidates any previously issued
        verificat
      tags:
      - auth
      x-octomil-method: auth.email.send_verification_code
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes all existing AuthChallenge rows for this user with purpose=email_verification
      - persists a new AuthChallenge row (6-digit code, TTL=15 minutes)
      - sends verification email via email service
      x-octomil-errors:
      - authentication_failed
      - email_already_verified
      - server_error
      - rate_limited
      - network_unavailable
      - request_timeout
      description: Sends a 6-digit verification code to the authenticated user's email address. Invalidates any previously
        issued verification challenges for this user before issuing a new one.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    default: Verification code sent.
                  code:
                    type: string
                    nullable: true
                    description: '6-digit code returned only in non-production environments (OCTOMIL_ENVIRONMENT != "production")
                      to enable integration testing without email delivery.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/update-email:
    post:
      operationId: auth_email_update
      summary: Changes the authenticated user's email address. Only permitted before the email has been verified. Invalidates
        any pendi
      tags:
      - auth
      x-octomil-method: auth.email.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates User.email (lowercased + trimmed)
      - sets User.email_verified = false
      - sets User.email_verified_at = null
      - deletes all AuthChallenge rows for this user with purpose=email_verification
      x-octomil-errors:
      - authentication_failed
      - email_already_verified
      - email_already_in_use
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Changes the authenticated user's email address. Only permitted before the email has been verified. Invalidates
        any pending verification codes for the old email and resets email_verified to false.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 255
                  description: New email address for the account.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  email:
                    type: string
                    description: The email now on the account (lowercased, trimmed).
                  message:
                    type: string
                    default: Email updated.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/verify-email-code:
    post:
      operationId: auth_email_verify_code
      summary: 'Verifies the 6-digit code sent by auth.email.send_verification_code. Marks the user''s email as verified and
        deletes the '
      tags:
      - auth
      x-octomil-method: auth.email.verify_code
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets User.email_verified = true
      - sets User.email_verified_at to current timestamp
      - deletes the consumed AuthChallenge row
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Verifies the 6-digit code sent by auth.email.send_verification_code. Marks the user's email as verified
        and deletes the challenge row.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
                  minLength: 6
                  maxLength: 6
                  description: 6-digit numeric code from the verification email.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  verified:
                    type: boolean
                  message:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/link/confirm-transfer:
    post:
      operationId: auth_link_confirm_transfer
      summary: Confirms transfer of an OAuth identity that is currently linked to another user account into the current user's
        account.
      tags:
      - auth
      x-octomil-method: auth.link.confirm_transfer
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - reassigns the OAuthAccount.user_id to the current authenticated user
      - updates OAuthAccount.email to the provider's email
      - deletes the consumed AuthChallenge row
      - records an audit event (action=oauth_link_transfer)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - oauth_provider_not_linked
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Confirms transfer of an OAuth identity that is currently linked to another user account into the current
        user's account. Used when an OAuth provider email matches a different existing account; the server issues a transfer
        challenge that the intended recipient must confirm.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
                  minLength: 8
                  description: 'Transfer code from an AuthChallenge with purpose=link_transfer. The challenge value format
                    is "{code}:{json_payload}".

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    description: '"<provider> account transferred and linked"'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/link/{provider}/start:
    get:
      operationId: auth_link_provider_start
      summary: Begins an OAuth account-linking flow for an already-authenticated user. Sets a linking_user_id session cookie
        so the sub
      tags:
      - auth
      x-octomil-method: auth.link.provider.start
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - stores linking_user_id in the server-side session for the callback
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Begins an OAuth account-linking flow for an already-authenticated user. Sets a linking_user_id session
        cookie so the subsequent OAuth callback knows to associate the incoming identity with the existing account rather
        than starting a new login flow. The flow completes via the shared /oauth/{provider}/callback handler.
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
        description: 'OAuth provider slug (google, apple, github). Must be configured server-side; returns 404 if the provider
          is unknown or disabled.

          '
      - name: Authorization
        in: header
        required: true
        schema:
          type: string
        description: Bearer JWT for the authenticated user initiating the link.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                description: "HTTP 302 redirect to the provider's OAuth authorization endpoint. The browser follows this redirect;\
                  \ no JSON body is returned. The real shape is an empty response with a `Location` header \u2014 declared\
                  \ as an open object here only to satisfy OpenAPI 3 schema validation; SDKs MUST treat this endpoint as browser-only\
                  \ (see notes below).\n"
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/link/{provider}:
    delete:
      operationId: auth_link_unlink
      summary: Unlinks an OAuth provider from the authenticated user's account. Guards against unlinking the user's only authentication
      tags:
      - auth
      x-octomil-method: auth.link.unlink
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes the OAuthAccount row for this user + provider
      - records an audit event (action=oauth_unlink)
      x-octomil-errors:
      - authentication_failed
      - oauth_provider_not_linked
      - last_auth_method
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Unlinks an OAuth provider from the authenticated user's account. Guards against unlinking the user's only
        authentication method.
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
        description: OAuth provider name (e.g. "google", "github", "apple").
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    description: '"<provider> account unlinked"'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/linked-accounts:
    get:
      operationId: auth_linked_accounts_list
      summary: List the OAuth providers currently linked to the authenticated user. Used by the dashboard's account settings
        page to re
      tags:
      - auth
      x-octomil-method: auth.linked_accounts.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: List the OAuth providers currently linked to the authenticated user. Used by the dashboard's account settings
        page to render the "Connected accounts" section and to compute which providers are available to disconnect.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - accounts
                properties:
                  accounts:
                    type: array
                    description: 'One entry per OAuth provider the user has linked to their Octomil account. Empty array if
                      no providers are linked (e.g. user signed up with password only).

                      '
                    items:
                      type: object
                      required:
                      - provider
                      properties:
                        provider:
                          type: string
                          description: 'Provider identifier from `OAuthProvider` (`google | apple | github`). Matches the
                            `name` field in `auth.providers`.

                            '
                        email:
                          type: string
                          nullable: true
                          description: 'The email the user has on file with that provider, which may differ from their Octomil
                            login email. `null` if the provider didn''t return an email at link time.

                            '
                        linked_at:
                          type: string
                          format: date-time
                          nullable: true
                          description: 'ISO-8601 UTC timestamp of when the link was established (`OAuthAccount.created_at`).
                            `null` for legacy rows created before the column existed.

                            '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/login:
    post:
      operationId: auth_login
      summary: Password-based login. Returns a session token + refresh token. Browsers also get an httpOnly cookie; server-to-server
        ca
      tags:
      - auth
      x-octomil-method: auth.login
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates a UserSession row
      - mints access + refresh tokens
      - sets httpOnly cookies on the response
      - emits telemetry event (auth.login_success / auth.login_failed)
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Password-based login. Returns a session token + refresh token. Browsers also get an httpOnly cookie; server-to-server
        callers use the returned token in Authorization headers.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  description: Plaintext over TLS. Server hashes for comparison; never stored.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Session payload \u2014 typically `{user_id, org_id, token, refresh_token, expires_at}`. Token\
                  \ is also set as an httpOnly cookie for browser sessions.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/logout:
    post:
      operationId: auth_logout
      summary: "Revoke the current session \u2014 invalidates both the access token and the refresh-token lineage. The next\
        \ API call from the"
      tags:
      - auth
      x-octomil-method: auth.logout
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - invalidates the current UserSession + lineage
      - clears httpOnly auth cookies on the response
      - emits telemetry event (auth.logged_out)
      x-octomil-errors:
      - authentication_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: "Revoke the current session \u2014 invalidates both the access token and the refresh-token lineage. The\
        \ next API call from the same session returns 401 and the SDK must re-authenticate."
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Empty body. Server uses the access token (cookie or header) to identify the session.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Empty / minimal ack. Cookies are also cleared on the response.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/me:
    get:
      operationId: auth_me
      summary: 'Return the caller''s AuthContext. Polled on every dashboard page load to keep the UI''s permission model in
        sync with the '
      tags:
      - auth
      x-octomil-method: auth.me
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the caller's AuthContext. Polled on every dashboard page load to keep the UI's permission model
        in sync with the server-side scope.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Current AuthContext shape \u2014 `{auth_type, user_id, email, name, org_id, role, scopes, ...}`\
                  \ per server/app/dependencies/auth/types.py. Used by every dashboard request to populate the UI's \"who\
                  \ am I\" banner and to compute permission gating client-side.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/oauth/exchange:
    post:
      operationId: auth_oauth_exchange
      summary: Final step of the OAuth login flow. The browser (or SDK) calls this POST with the `code` it received on the
        OAuth callba
      tags:
      - auth
      x-octomil-method: auth.oauth.exchange
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - Deletes the `AuthChallenge` row backing the `code` (single-use).
      - Sets `HttpOnly` access + refresh cookies on the response.
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Final step of the OAuth login flow. The browser (or SDK) calls this POST with the `code` it received on\
        \ the OAuth callback redirect; the server validates the code, deletes the underlying `AuthChallenge` row, and sets\
        \ the access + refresh token cookies on the response. The tokens themselves are never returned in the response body\
        \ \u2014 they live in `HttpOnly` cookies that subsequent calls (including `auth.me`) authenticate against."
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
                  minLength: 8
                  description: 'Single-use exchange code from the `?code=` query parameter on the OAuth callback redirect.
                    The dashboard (or an embedding client) receives this on the `/auth/callback` page after the user completes
                    provider consent. Codes are URL-safe random strings; treat them as opaque.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - authenticated
                - message
                properties:
                  authenticated:
                    type: boolean
                    description: Always `true` on a 2xx response. Reserved for future negotiated-MFA cases where exchange
                      may succeed-but-require-step-up.
                  message:
                    type: string
                    description: "Human-readable status, e.g. `\"Authenticated\"`. Do not parse \u2014 render directly."
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/passkeys/authenticate/options:
    post:
      operationId: auth_passkeys_authenticate_options
      summary: Step 1 of WebAuthn passkey authentication. Generates a server-side challenge and optionally scopes allowCredentials
        to t
      tags:
      - auth
      x-octomil-method: auth.passkeys.authenticate.options
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists an AuthChallenge row with purpose=auth_passkey
      - challenge may be user-scoped (email provided) or anonymous (no email)
      x-octomil-errors:
      - passkey_credential_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Step 1 of WebAuthn passkey authentication. Generates a server-side challenge and optionally scopes allowCredentials
        to the given email.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: 'Optional. If provided, the response includes an allowCredentials list scoped to this user.
                    If omitted, the authenticator performs a discoverable-credential (resident key) assertion.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'WebAuthn PublicKeyCredentialRequestOptions payload. Fields follow camelCase as required by navigator.credentials.get().

                  '
                properties:
                  challenge:
                    type: string
                    description: base64url-encoded challenge bytes.
                  timeout:
                    type: integer
                  rpId:
                    type: string
                  allowCredentials:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                  userVerification:
                    type: string
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/passkeys/authenticate/verify:
    post:
      operationId: auth_passkeys_authenticate_verify
      summary: 'Step 2 of WebAuthn passkey authentication. Verifies the assertion response against the stored credential and
        challenge, '
      tags:
      - auth
      x-octomil-method: auth.passkeys.authenticate.verify
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates PasskeyCredential.sign_count
      - updates User.last_login_at
      - deletes the consumed AuthChallenge row
      - mints access + refresh tokens
      - sets httpOnly cookies on the response
      - ensures user has an org_id
      x-octomil-errors:
      - authentication_failed
      - passkey_challenge_expired
      - passkey_credential_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Step 2 of WebAuthn passkey authentication. Verifies the assertion response against the stored credential
        and challenge, updates the sign_count, and issues an authenticated session.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - credential
              properties:
                email:
                  type: string
                  format: email
                  description: 'Optional. Used to look up the user when provided. If omitted the server resolves the user
                    from credential.rawId.

                    '
                credential:
                  type: object
                  description: 'Raw WebAuthn AuthenticatorAssertionResponse as returned by navigator.credentials.get(). Passed
                    verbatim to py-webauthn verify_authentication_response(). Must include rawId or id for user resolution
                    when email is omitted.

                    '
                  additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  authenticated:
                    type: boolean
                    default: true
                  message:
                    type: string
                    default: Authenticated
                description: 'Session payload. Access and refresh tokens are also set as httpOnly cookies.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/passkeys/register/options:
    post:
      operationId: auth_passkeys_register_options
      summary: Step 1 of WebAuthn passkey registration. Generates and persists a server-side challenge. If no account exists
        for the gi
      tags:
      - auth
      x-octomil-method: auth.passkeys.register.options
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - may auto-create a User row if the email is new
      - persists an AuthChallenge row with purpose=register_passkey
      - emits PostHog signup_completed if a new user was created
      x-octomil-errors:
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Step 1 of WebAuthn passkey registration. Generates and persists a server-side challenge. If no account
        exists for the given email, the server auto-creates one and emits a signup_completed PostHog event.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              properties:
                email:
                  type: string
                  format: email
                  description: User's email address. If no account exists one is auto-created.
                name:
                  type: string
                  description: Display name for a newly-created account; ignored if account exists.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'WebAuthn PublicKeyCredentialCreationOptions payload (base64url-encoded challenge, RP info, user
                  info, pubKeyCredParams, excludeCredentials, timeout). The exact shape is defined by the WebAuthn Level 3
                  spec. Fields follow camelCase as required by the browser navigator.credentials.create() API.

                  '
                properties:
                  challenge:
                    type: string
                    description: base64url-encoded challenge bytes. Stored server-side for verify step.
                  timeout:
                    type: integer
                    description: Suggested client timeout in milliseconds.
                  rp:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                        description: base64url-encoded user ID bytes.
                      name:
                        type: string
                      displayName:
                        type: string
                  pubKeyCredParams:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        alg:
                          type: integer
                          description: COSE algorithm identifier (-7 = ES256, -257 = RS256).
                  excludeCredentials:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/passkeys/register/verify:
    post:
      operationId: auth_passkeys_register_verify
      summary: Step 2 of WebAuthn passkey registration. Verifies the attestation response, persists the new PasskeyCredential,
        and issu
      tags:
      - auth
      x-octomil-method: auth.passkeys.register.verify
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists a PasskeyCredential row for the user
      - deletes the AuthChallenge row consumed by this verification
      - mints access + refresh tokens
      - sets httpOnly cookies on the response
      - ensures user has an org_id (creates Org + OrgMember if missing)
      x-octomil-errors:
      - authentication_failed
      - passkey_challenge_expired
      - passkey_credential_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Step 2 of WebAuthn passkey registration. Verifies the attestation response, persists the new PasskeyCredential,
        and issues an authenticated session.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - credential
              properties:
                email:
                  type: string
                  format: email
                  description: Email address of the user completing registration.
                credential:
                  type: object
                  description: 'Raw WebAuthn AuthenticatorAttestationResponse as returned by navigator.credentials.create().
                    Passed verbatim to py-webauthn verify_registration_response(). Field names follow the W3C PublicKeyCredential
                    interface (camelCase).

                    '
                  additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  authenticated:
                    type: boolean
                    default: true
                  message:
                    type: string
                    default: Authenticated
                description: 'Session payload. Access and refresh tokens are also set as httpOnly cookies on the response.
                  SDKs should treat the cookie path as the primary session mechanism for browser contexts.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/change-password:
    post:
      operationId: auth_password_change
      summary: Change the authenticated user's password. Requires the current password for verification. The new password
        must be diffe
      tags:
      - auth
      x-octomil-method: auth.password.change
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates User.password_hash
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Change the authenticated user's password. Requires the current password for verification. The new password
        must be different from the current one.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - current_password
              - new_password
              properties:
                current_password:
                  type: string
                  minLength: 1
                  description: Current plaintext password for verification.
                new_password:
                  type: string
                  minLength: 8
                  description: New plaintext password. Must differ from current password.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    default: Password changed.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/continue:
    post:
      operationId: auth_password_continue
      summary: Unified login-or-register flow. If the email has no account, auto-creates one (requires password >= 8 chars).
        If the acc
      tags:
      - auth
      x-octomil-method: auth.password.continue
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - may create a User row
      - may create an Org + OrgMember if a new user is created
      - mints access + refresh tokens
      - sets httpOnly cookies on the response
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Unified login-or-register flow. If the email has no account, auto-creates one (requires password >= 8 chars).
        If the account exists with a password, verifies it. If the account exists but is OAuth-only (no password), returns
        401 directing to social login.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 1
                name:
                  type: string
                  nullable: true
                  description: Optional display name; only used when auto-creating a new account.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  authenticated:
                    type: boolean
                    default: true
                  message:
                    type: string
                    description: '"Signed in" for existing users, "Account created" for new registrations.'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/forgot-password:
    post:
      operationId: auth_password_forgot
      summary: Initiates password reset. Persists a reset token challenge and sends a reset link via email. Always returns
        200 regardle
      tags:
      - auth
      x-octomil-method: auth.password.forgot
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists an AuthChallenge row with purpose=password_reset, TTL=30 minutes
      - sends a password reset email (may fail silently to avoid revealing account existence)
      x-octomil-errors:
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Initiates password reset. Persists a reset token challenge and sends a reset link via email. Always returns
        200 regardless of whether the email matches an account, to prevent user enumeration.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              properties:
                email:
                  type: string
                  format: email
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    default: If that email exists, a reset link has been sent.
                  reset_token:
                    type: string
                    nullable: true
                    description: 'Present only in non-production environments (OCTOMIL_ENVIRONMENT != "production"). Contains
                      the raw reset token to allow integration testing without email delivery.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/reset-password:
    post:
      operationId: auth_password_reset
      summary: Completes password reset using a valid, unexpired reset token. Hashes and stores the new password, then deletes
        the used
      tags:
      - auth
      x-octomil-method: auth.password.reset
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates User.password_hash
      - deletes the consumed AuthChallenge row
      x-octomil-errors:
      - invalid_token
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Completes password reset using a valid, unexpired reset token. Hashes and stores the new password, then
        deletes the used token.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - token
              - password
              properties:
                token:
                  type: string
                  minLength: 1
                  description: Reset token from the emailed link or forgot-password response.
                password:
                  type: string
                  minLength: 8
                  description: New password in plaintext (transmitted over TLS).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    default: Password has been reset.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/providers:
    get:
      operationId: auth_providers
      summary: List available authentication providers (OAuth + passkeys) and whether each is enabled on this deployment.
        Called by the
      tags:
      - auth
      x-octomil-method: auth.providers
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: List available authentication providers (OAuth + passkeys) and whether each is enabled on this deployment.
        Called by the dashboard's login screen to decide which buttons to render, and intended for SDK consumption to discover
        the auth surface programmatically before initiating an OAuth or passkey flow.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - providers
                - passkeys_enabled
                properties:
                  providers:
                    type: array
                    description: 'One entry per supported authentication provider. `enabled=true` means the server has the
                      corresponding client credentials configured and the provider''s OAuth start URL (`/api/v1/auth/oauth/{provider}/start`)
                      will work. `enabled=false` means the provider is offered as a build-time option but is not currently
                      usable on this deployment.

                      '
                    items:
                      type: object
                      required:
                      - name
                      - enabled
                      properties:
                        name:
                          type: string
                          description: 'Provider identifier. Stable values: `google`, `apple`, `github`, `passkeys`. The first
                            three are members of the server-side `OAuthProvider` enum; `passkeys` is included alongside them
                            so a single endpoint covers the full "what login methods are available?" question.

                            '
                        enabled:
                          type: boolean
                  passkeys_enabled:
                    type: boolean
                    description: 'Duplicate of the `passkeys` entry in `providers[]`, surfaced at the top level so the dashboard''s
                      pre-login UI can branch on it without iterating the array. True iff at least one `PasskeyCredential`
                      row exists in the database.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/refresh:
    post:
      operationId: auth_refresh
      summary: Exchange the refresh-token cookie for a fresh access token + a rotated refresh token. Cookies are re-set on
        the response
      tags:
      - auth
      x-octomil-method: auth.refresh
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - invalidates the presented refresh token
      - mints + sets new access + refresh httpOnly cookies
      - emits telemetry event (auth.refreshed)
      x-octomil-errors:
      - authentication_failed
      - token_expired
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Exchange the refresh-token cookie for a fresh access token + a rotated refresh token. Cookies are re-set
        on the response; no token material is returned in the body. Browser-only by design.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - authenticated
                - message
                properties:
                  authenticated:
                    type: boolean
                    description: Always true on success (route returns 401 otherwise).
                  message:
                    type: string
                    description: Static "Session refreshed".
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/signup:
    post:
      operationId: auth_signup
      summary: Create a new user account. Default behavior is to also create a single-user organization with the user as owner.
        For inv
      tags:
      - auth
      x-octomil-method: auth.signup
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates User + Organization + OrganizationMember rows
      - sends a verification email
      - emits telemetry event (auth.signup)
      - logs the new user in immediately (returns a session token)
      x-octomil-errors:
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new user account. Default behavior is to also create a single-user organization with the user
        as owner. For invited flows, see auth.complete_invitation (separate route).
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                name:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Newly-created user + organization (single-user org by default) + initial session token.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/auth/sso/start:
    post:
      operationId: auth_sso_start
      summary: Discovery endpoint for enterprise SSO. The dashboard's login form calls this with the user's email after they
        type it; S
      tags:
      - auth
      x-octomil-method: auth.sso.start
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Discovery endpoint for enterprise SSO. The dashboard's login form calls this with the user's email after
        they type it; SDKs can call it before invoking password login to branch the UX onto SSO when the user's org requires
        it.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              properties:
                email:
                  type: string
                  minLength: 3
                  description: 'User email. The server extracts the domain and looks up `OrgSettings.sso_domain` to determine
                    whether SSO is available for that domain.

                    '
                redirect:
                  type: string
                  nullable: true
                  description: "Optional client-supplied redirect target appended to the SSO start URL. Validated server-side\
                    \ against the allow-list \u2014 an invalid value is silently dropped, not echoed back.\n"
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - sso_available
                properties:
                  sso_available:
                    type: boolean
                    description: "`true` iff: (1) an `OrgSettings` row matches the email's domain with `identity_mode == \"\
                      sso\"`, (2) Stytch is configured on the server, (3) the org has a usable SSO connection. False otherwise\
                      \ \u2014 the SDK should fall back to password/OAuth flows.\n"
                  start_url:
                    type: string
                    nullable: true
                    description: 'Fully-qualified URL the client should redirect the user''s browser to in order to initiate
                      SSO. `null` when `sso_available == false`.

                      '
                  provider:
                    type: string
                    nullable: true
                    description: 'Configured SSO provider name from `OrgSettings.sso_provider` (e.g. `"okta"`, `"azure_ad"`).
                      Useful for rendering provider- specific branding on the "Continue with SSO" button. `null` when `sso_available
                      == false`.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/browse:
    post:
      operationId: catalog_browse
      summary: Return the catalog manifest annotated with per-variant compatibility against the supplied device capabilities.
        Despite t
      tags:
      - catalog
      x-octomil-method: catalog.browse
      x-octomil-since: 2.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return the catalog manifest annotated with per-variant compatibility against the supplied device capabilities.\
        \ Despite the name, this is NOT a faceted-search endpoint \u2014 it's a device-capability-aware manifest fetch the\
        \ device-agent SDK uses to discover which variants it can actually serve."
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - platform
              properties:
                platform:
                  type: string
                  description: "Device platform \u2014 ios / android / macos / linux / windows / browser."
                model:
                  type: string
                  description: Device model identifier (e.g. iPhone16,1, SM-S928B).
                manufacturer:
                  type: string
                cpu_architecture:
                  type: string
                  description: arm64 / x86_64.
                os_version:
                  type: string
                total_memory_mb:
                  type: integer
                  minimum: 0
                gpu_available:
                  type: boolean
                  default: false
                npu_available:
                  type: boolean
                  default: false
                npu_tops:
                  type: number
                  minimum: 0
                  description: NPU performance in TOPS.
                supported_runtimes:
                  type: array
                  items:
                    type: string
                  description: Runtime executors the device supports (coreml, litert, etc.).
                nnapi_version:
                  type: integer
                  minimum: 0
                  description: Android NNAPI version (27+).
                compute_units:
                  type: string
                  description: "iOS compute units \u2014 all / cpu_and_gpu / cpu_only."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Catalog manifest filtered by `platform`, with each variant annotated for compatibility against
                  the supplied device capabilities. Resource URIs are stripped to enforce read-only browsing.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/cloud-models:
    get:
      operationId: catalog_cloud_models_list
      summary: Paginated cloud model list with provider/capability filters.
      tags:
      - catalog
      x-octomil-method: catalog.cloud_models.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Paginated cloud model list with provider/capability filters.
      parameters:
      - name: provider
        in: query
        required: false
        schema:
          type: string
      - name: capability
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/families:
    post:
      operationId: catalog_families_create
      summary: Create a model family. Duplicate name returns catalog_slug_conflict.
      tags:
      - catalog
      x-octomil-method: catalog.families.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Creates ModelFamily row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_slug_conflict
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a model family. Duplicate name returns catalog_slug_conflict.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - vendor
              properties:
                name:
                  type: string
                  description: Canonical slug (e.g. gemma-2). Must be unique.
                vendor:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: catalog_list_families
      summary: List ModelFamilies in the v2 catalog. A family groups related variants (e.g. "llama-3" with 8b/70b variants);
        the catalo
      tags:
      - catalog
      x-octomil-method: catalog.list_families
      x-octomil-since: 2.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List ModelFamilies in the v2 catalog. A family groups related variants (e.g. "llama-3" with 8b/70b variants);
        the catalog uses families as the discovery unit for browsing.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 if set, returns families visible to the org (global + org-owned)."
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of families entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/families/{family_id}:
    get:
      operationId: catalog_get_family
      summary: Fetch a single family by id. Powers the family detail page in the catalog browser.
      tags:
      - catalog
      x-octomil-method: catalog.get_family
      x-octomil-since: 2.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single family by id. Powers the family detail page in the catalog browser.
      parameters:
      - name: family_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: ModelFamily with eager-loaded variants list + metadata (lineage, license, capabilities).
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/families/{family_id}/variants:
    get:
      operationId: catalog_list_family_variants
      summary: List the CatalogModelVariants under a family. Each variant is a concrete model + size (e.g. "llama-3-8b").
      tags:
      - catalog
      x-octomil-method: catalog.list_family_variants
      x-octomil-since: 2.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the CatalogModelVariants under a family. Each variant is a concrete model + size (e.g. "llama-3-8b").
      parameters:
      - name: family_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of variants entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/manifest:
    get:
      operationId: catalog_manifest_get
      summary: Resolved client manifest of active families and variants.
      tags:
      - catalog
      x-octomil-method: catalog.manifest.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Resolved client manifest of active families and variants.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/packages:
    post:
      operationId: catalog_packages_create
      summary: Create an artifact package under a catalog version.
      tags:
      - catalog
      x-octomil-method: catalog.packages.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Creates ArtifactPackage row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_version_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Create an artifact package under a catalog version.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - version_id
              - platform
              - artifact_format
              - runtime_executor
              properties:
                version_id:
                  type: string
                platform:
                  type: string
                artifact_format:
                  type: string
                runtime_executor:
                  type: string
                is_default:
                  type: boolean
                  default: false
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/providers:
    get:
      operationId: catalog_providers_list
      summary: List cloud providers with model counts.
      tags:
      - catalog
      x-octomil-method: catalog.providers.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: List cloud providers with model counts.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/resources:
    post:
      operationId: catalog_resources_create
      summary: Create an artifact resource within a package.
      tags:
      - catalog
      x-octomil-method: catalog.resources.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Creates ArtifactResource row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_package_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Create an artifact resource within a package.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - artifact_package_id
              - kind
              - uri
              properties:
                artifact_package_id:
                  type: string
                kind:
                  type: string
                uri:
                  type: string
                checksum_sha256:
                  type: string
                required:
                  type: boolean
                  default: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/routing-summary:
    get:
      operationId: catalog_routing_summary_get
      summary: Variant routing summary with local packages and cloud options.
      tags:
      - catalog
      x-octomil-method: catalog.routing_summary.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: Variant routing summary with local packages and cloud options.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/summary:
    get:
      operationId: catalog_summary_get
      summary: Flat family+variant summary for dashboard display.
      tags:
      - catalog
      x-octomil-method: catalog.summary.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: Flat family+variant summary for dashboard display.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/variants:
    post:
      operationId: catalog_variants_create
      summary: Create a model variant. Capabilities inherit from parent family unless overridden.
      tags:
      - catalog
      x-octomil-method: catalog.variants.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Creates CatalogModelVariant row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_family_not_found
      - catalog_slug_conflict
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a model variant. Capabilities inherit from parent family unless overridden.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - family_id
              - name
              - parameter_count
              properties:
                family_id:
                  type: string
                name:
                  type: string
                parameter_count:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/versions:
    post:
      operationId: catalog_versions_create
      summary: Create a catalog model version. lifecycle defaults to active.
      tags:
      - catalog
      x-octomil-method: catalog.versions.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Creates CatalogModelVersion row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_variant_not_found
      - catalog_lifecycle_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a catalog model version. lifecycle defaults to active.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - variant_id
              - version
              properties:
                variant_id:
                  type: string
                version:
                  type: string
                lifecycle:
                  type: string
                  default: active
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/catalog/versions/{version_id}/lifecycle:
    patch:
      operationId: catalog_versions_update_lifecycle
      summary: Transition version lifecycle. Returns catalog_lifecycle_invalid on illegal transition.
      tags:
      - catalog
      x-octomil-method: catalog.versions.update_lifecycle
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Updates lifecycle on CatalogModelVersion.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - catalog_version_not_found
      - catalog_lifecycle_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Transition version lifecycle. Returns catalog_lifecycle_invalid on illegal transition.
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - lifecycle
              properties:
                lifecycle:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/chat/threads:
    post:
      operationId: chat_threads_create
      summary: Create a new chat thread.
      tags:
      - chat
      x-octomil-method: chat.threads.create
      x-octomil-since: 1.3.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates a persistent thread with a thread_ prefixed ID
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      - authentication_failed
      - forbidden
      description: Create a new chat thread.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat_thread'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat_thread'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: chat_threads_list
      summary: List chat threads.
      tags:
      - chat
      x-octomil-method: chat.threads.list
      x-octomil-since: 1.3.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      - authentication_failed
      - forbidden
      description: List chat threads.
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 20
      - name: order
        in: query
        required: false
        schema:
          type: string
          enum:
          - asc
          - desc
          default: desc
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/chat_thread'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/chat/threads/{thread_id}:
    get:
      operationId: chat_threads_get
      summary: Retrieve a thread and its messages.
      tags:
      - chat
      x-octomil-method: chat.threads.get
      x-octomil-since: 1.3.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      - authentication_failed
      - forbidden
      description: Retrieve a thread and its messages.
      parameters:
      - name: thread_id
        in: path
        required: true
        schema:
          type: string
          pattern: ^thread_
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat_thread'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/chat/threads/{thread_id}/tracking:
    get:
      operationId: chat_threads_tracking
      summary: Return ordered tracking events for a conversation thread. Requires admin or internal scope; returns 403 for
        other scopes
      tags:
      - chat
      x-octomil-method: chat.threads.tracking
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Return ordered tracking events for a conversation thread. Requires admin or internal scope; returns 403
        for other scopes.
      parameters:
      - name: thread_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                  - id
                  - threadId
                  - eventType
                  properties:
                    id:
                      type: string
                    threadId:
                      type: string
                    messageId:
                      type: string
                      nullable: true
                    eventType:
                      type: string
                    role:
                      type: string
                      nullable: true
                    toolName:
                      type: string
                      nullable: true
                    toolStatus:
                      type: string
                      nullable: true
                    latencyMs:
                      type: integer
                      nullable: true
                    sequenceNumber:
                      type: integer
                      nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/chat/threads/{thread_id}/turns:
    post:
      operationId: chat_turn
      summary: Submit a user message and receive a complete assistant response.
      tags:
      - chat
      x-octomil-method: chat.turn
      x-octomil-since: 1.3.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - appends user and assistant messages to the thread
      - consumes compute (local or cloud)
      - emits telemetry events (inference.started, inference.completed or inference.failed)
      x-octomil-errors:
      - model_not_found
      - model_disabled
      - runtime_unavailable
      - model_load_failed
      - inference_failed
      - insufficient_memory
      - rate_limited
      - invalid_input
      - cancelled
      - network_unavailable
      - request_timeout
      - server_error
      - authentication_failed
      - forbidden
      description: Submit a user message and receive a complete assistant response.
      parameters:
      - name: thread_id
        in: path
        required: true
        schema:
          type: string
          pattern: ^thread_
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat_turn_request'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat_turn_result'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cloud/chat/completions:
    post:
      operationId: cloud_chat_completions
      summary: OpenAI-compatible chat completions via the Octomil cloud gateway. Resolves the credential for the inferred/specified
        pro
      tags:
      - cloud
      x-octomil-method: cloud.chat.completions
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - records usage in billing ledger
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: OpenAI-compatible chat completions via the Octomil cloud gateway. Resolves the credential for the inferred/specified
        provider, applies the org admission gate, and forwards to the provider. Supports both blocking and streaming responses.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model
              - messages
              properties:
                model:
                  type: string
                  description: External model identifier (e.g. "gpt-4o").
                messages:
                  type: array
                  items:
                    type: object
                    required:
                    - role
                    - content
                    properties:
                      role:
                        type: string
                      content:
                        type: string
                provider:
                  type: string
                  nullable: true
                  description: 'Provider key override. When omitted, provider is inferred from the model name.

                    '
                stream:
                  type: boolean
                  nullable: true
                  description: If true, returns a Server-Sent Events stream.
                temperature:
                  type: number
                  nullable: true
                max_tokens:
                  type: integer
                  nullable: true
                top_p:
                  type: number
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                description: 'OpenAI-compatible chat completion response. When stream=true, returns Server-Sent Events with
                  delta chunks in the same shape as the OpenAI streaming API.

                  '
                type: object
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/cloud/models:
    get:
      operationId: cloud_models_list
      summary: List cloud models available via the Octomil gateway. Returns active models from the cloud catalog. Requires
        cloud scope.
      tags:
      - cloud
      x-octomil-method: cloud.models.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: List cloud models available via the Octomil gateway. Returns active models from the cloud catalog. Requires
        cloud scope.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - object
                - data
                properties:
                  object:
                    type: string
                    enum:
                    - list
                  data:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - provider
                      properties:
                        id:
                          type: string
                          description: External model identifier (e.g. "gpt-4o").
                        provider:
                          type: string
                          description: Provider key (e.g. "openai").
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair/{code}/benchmark:
    post:
      operationId: deploy_pair_benchmark
      summary: "Device submits benchmark results after running inference. No authentication required \u2014 the code is the\
        \ credential. Merge"
      tags:
      - deploy
      x-octomil-method: deploy.pair.benchmark
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - merges benchmark data into pairing session
      - creates DeviceAssignment if session transitions to done
      x-octomil-errors:
      - resource_not_found
      - conflict
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Device submits benchmark results after running inference. No authentication required \u2014 the code is\
        \ the credential. Merges benchmark data with any existing session payload (does not overwrite origin metadata). Transitions\
        \ session from \"deploying\" to \"done\" on first submission."
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - ttft_ms
              - tpot_ms
              - tokens_per_second
              - p50_latency_ms
              - p95_latency_ms
              - p99_latency_ms
              - memory_peak_bytes
              - inference_count
              properties:
                ttft_ms:
                  type: number
                  description: Time to first token in milliseconds.
                tpot_ms:
                  type: number
                  description: Time per output token in milliseconds.
                tokens_per_second:
                  type: number
                p50_latency_ms:
                  type: number
                p95_latency_ms:
                  type: number
                p99_latency_ms:
                  type: number
                  description: 99th-percentile inference latency in milliseconds.
                memory_peak_bytes:
                  type: integer
                  description: Peak memory usage in bytes during the benchmark run.
                inference_count:
                  type: integer
                  description: Number of inference iterations run during the benchmark.
                peak_memory_mb:
                  type: number
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - model_name
                - status
                - expires_at
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                  model_name:
                    type: string
                  status:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair/{code}/cancel:
    post:
      operationId: deploy_pair_cancel
      summary: Cancel a pairing session. Auth required. Returns 409 if the session is already "done" or "cancelled".
      tags:
      - deploy
      x-octomil-method: deploy.pair.cancel
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - transitions pairing session to cancelled
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - conflict
      - network_unavailable
      - request_timeout
      - server_error
      description: Cancel a pairing session. Auth required. Returns 409 if the session is already "done" or "cancelled".
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - model_name
                - status
                - expires_at
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                  model_name:
                    type: string
                  status:
                    type: string
                    description: "Session status after cancellation \u2014 \"cancelled\"."
                  expires_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair/{code}/connect:
    post:
      operationId: deploy_pair_connect
      summary: "Device connects to a pairing session. No authentication required \u2014 the code is the credential. Transitions\
        \ the session f"
      tags:
      - deploy
      x-octomil-method: deploy.pair.connect
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates pairing session with device metadata
      x-octomil-errors:
      - resource_not_found
      - gone
      - conflict
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Device connects to a pairing session. No authentication required \u2014 the code is the credential. Transitions\
        \ the session from \"pending\" to \"connected\". Returns 410 if expired, 409 if session is not in pending state."
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - platform
              properties:
                device_id:
                  type: string
                platform:
                  type: string
                  description: Device platform (ios / android).
                device_name:
                  type: string
                  nullable: true
                chip_family:
                  type: string
                  nullable: true
                ram_gb:
                  type: number
                  nullable: true
                os_version:
                  type: string
                  nullable: true
                npu_available:
                  type: boolean
                  nullable: true
                gpu_available:
                  type: boolean
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - model_name
                - status
                - expires_at
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                  model_name:
                    type: string
                  status:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
                  device_id:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair:
    post:
      operationId: deploy_pair_create
      summary: Create a new pairing session for phone-based model deployment. Returns a short code the user enters on their
        device to c
      tags:
      - deploy
      x-octomil-method: deploy.pair.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates a pairing session record
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new pairing session for phone-based model deployment. Returns a short code the user enters on
        their device to connect. Auth required.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_name
              properties:
                model_name:
                  type: string
                  description: Name of the model to deploy via pairing.
                model_version:
                  type: string
                  nullable: true
                app_slug:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - status
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                    description: Short alphanumeric pairing code displayed to the user.
                  status:
                    type: string
                    description: "Session status \u2014 pending / connected / deploying / done / error / cancelled."
                  created_at:
                    type: string
                    format: date-time
                  model_name:
                    type: string
                    nullable: true
                  model_version:
                    type: string
                    nullable: true
                  expires_at:
                    type: string
                    format: date-time
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair/{code}/deploy:
    post:
      operationId: deploy_pair_deploy
      summary: Trigger deployment to the connected device. Resolves the model, determines optimal format for the device capabilities,
        g
      tags:
      - deploy
      x-octomil-method: deploy.pair.deploy
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - resolves model format and generates presigned download URL
      - transitions pairing session to deploying
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - conflict
      - network_unavailable
      - request_timeout
      - server_error
      description: Trigger deployment to the connected device. Resolves the model, determines optimal format for the device
        capabilities, generates a download URL, and transitions the session to "deploying". Auth required. Session must be
        in "connected" state or returns 409.
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - model_name
                - status
                - expires_at
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                  model_name:
                    type: string
                  status:
                    type: string
                    description: "Updated session status \u2014 \"deploying\"."
                  expires_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
                  download_url:
                    type: string
                    nullable: true
                    description: Presigned URL for the device to download the model artifact.
                  model_id:
                    type: string
                    nullable: true
                  model_version:
                    type: string
                    nullable: true
                  format:
                    type: string
                    nullable: true
                  quantization:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deploy/pair/{code}:
    get:
      operationId: deploy_pair_get
      summary: "Poll pairing session status by code. No authentication required \u2014 the code itself is the secret. Returns\
        \ 404 if the code"
      tags:
      - deploy
      x-octomil-method: deploy.pair.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - resource_not_found
      - gone
      - network_unavailable
      - request_timeout
      - server_error
      description: "Poll pairing session status by code. No authentication required \u2014 the code itself is the secret.\
        \ Returns 404 if the code does not exist, 410 if the session has expired."
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Pairing session code (case-insensitive).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - code
                - status
                - created_at
                properties:
                  id:
                    type: string
                    description: Pairing session UUID.
                  code:
                    type: string
                  status:
                    type: string
                    description: "Session status \u2014 pending / connected / deploying / done / error / cancelled / expired."
                  created_at:
                    type: string
                    format: date-time
                  model_name:
                    type: string
                    nullable: true
                  model_version:
                    type: string
                    nullable: true
                  device_id:
                    type: string
                    nullable: true
                  expires_at:
                    type: string
                    format: date-time
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/actions/{action_id}/dismiss:
    post:
      operationId: deployments_actions_dismiss
      summary: Dismiss a proposed operations action, recording the dismissing user and reason. Only actions in `proposed`
        status can be
      tags:
      - deployments
      x-octomil-method: deployments.actions.dismiss
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets OperationsAction.status to "dismissed"
      - records dismissed_at timestamp and dismissed_by user_id
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - action_not_found
      - action_state_invalid
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Dismiss a proposed operations action, recording the dismissing user and reason. Only actions in `proposed`
        status can be dismissed. Dismissal is an acknowledgement that the operator has reviewed the action and chosen not
        to execute it; it does not alter deployment state.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: action_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - reason
              properties:
                reason:
                  type: string
                  description: 'Human-readable explanation for why the action was dismissed. Stored on the action record for
                    audit purposes.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'The updated OperationsAction with status = "dismissed".

                  '
                required:
                - id
                - deployment_id
                - action_type
                - status
                - dismissed_at
                - dismissed_by
                properties:
                  id:
                    type: string
                  deployment_id:
                    type: string
                  action_type:
                    type: string
                  status:
                    type: string
                    description: Always `dismissed` after a successful call.
                  dismissed_at:
                    type: string
                    description: ISO-8601 UTC timestamp of dismissal.
                  dismissed_by:
                    type: string
                    nullable: true
                    description: user_id of the authenticated user who dismissed the action.
                  reason:
                    type: string
                  created_at:
                    type: string
                  updated_at:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/actions/{action_id}/execute:
    post:
      operationId: deployments_actions_execute
      summary: Execute a proposed operations action on behalf of the authenticated user. The server applies the action's effect
        immedia
      tags:
      - deployments
      x-octomil-method: deployments.actions.execute
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets OperationsAction.status to "manually_executed"
      - records executed_at timestamp
      - "for action_type=pause \u2014 transitions Deployment.status to \"paused\" (same side effects as deployments.pause)"
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - action_not_found
      - action_state_invalid
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Execute a proposed operations action on behalf of the authenticated user. The server applies the action's\
        \ effect immediately and synchronously, then marks the action as `manually_executed`. Only `proposed` actions with\
        \ an `action_type` in the server's executable set can be executed via this endpoint.\n**Current executable action\
        \ types**: `pause` \u2014 triggers the same logic as `deployments.pause`, transitioning the deployment to `paused`\
        \ status.\nThe call returns immediately with the updated action record; it does NOT poll for downstream propagation.\
        \ Callers that need to observe the deployment's new status should follow up with `deployments.get`."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: action_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without re-executing side effects.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'The updated OperationsAction with status = "manually_executed" and executed_at set.

                  '
                required:
                - id
                - deployment_id
                - action_type
                - status
                - executed_at
                properties:
                  id:
                    type: string
                  deployment_id:
                    type: string
                  action_type:
                    type: string
                  status:
                    type: string
                    description: Always `manually_executed` after a successful call.
                  executed_at:
                    type: string
                    description: ISO-8601 UTC timestamp when the action was executed.
                  reason:
                    type: string
                  proposed_change:
                    type: object
                    nullable: true
                    additionalProperties: true
                  created_at:
                    type: string
                  updated_at:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/actions:
    get:
      operationId: deployments_actions_list
      summary: List proposed and historical operations actions for a deployment. The automator engine creates `proposed` actions
        when i
      tags:
      - deployments
      x-octomil-method: deployments.actions.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - deployment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List proposed and historical operations actions for a deployment. The automator engine creates `proposed`
        actions when it detects conditions that warrant operator intervention (e.g. high error rate triggering a suggested
        pause). This endpoint is the primary feed for the dashboard's "Recommended Actions" panel.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: 'Filter by action status. Valid values: `proposed`, `auto_executed`, `manually_executed`, `dismissed`,
          `expired`. Omit to return all.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'Array of OperationsAction objects for this deployment, ordered by created_at descending. Capped
                  at 50 rows per request.

                  '
                items:
                  type: object
                  required:
                  - id
                  - deployment_id
                  - action_type
                  - status
                  - reason
                  - created_at
                  properties:
                    id:
                      type: string
                      description: UUIDv4 action identifier.
                    org_id:
                      type: string
                    deployment_id:
                      type: string
                    app_id:
                      type: string
                      nullable: true
                    capability:
                      type: string
                      nullable: true
                    action_type:
                      type: string
                      description: 'One of: `pause`, `rollback`, `reduce_percentage`, `switch_routing`, `recommend_model`,
                        `create_incident`.

                        '
                    reason:
                      type: string
                      description: Human-readable reason the action was proposed.
                    proposed_change:
                      type: object
                      nullable: true
                      additionalProperties: true
                      description: Structured payload describing the proposed change.
                    status:
                      type: string
                      description: 'Lifecycle status: `proposed | auto_executed | manually_executed | dismissed | expired`.

                        '
                    auto_executable:
                      type: boolean
                      description: Whether the system is permitted to execute this action automatically.
                    executed_at:
                      type: string
                      nullable: true
                      description: ISO-8601 UTC timestamp when the action was executed.
                    dismissed_at:
                      type: string
                      nullable: true
                      description: ISO-8601 UTC timestamp when the action was dismissed.
                    dismissed_by:
                      type: string
                      nullable: true
                      description: user_id of the dismissing user.
                    created_at:
                      type: string
                    updated_at:
                      type: string
                      nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/advance:
    post:
      operationId: deployments_advance
      summary: Manually advance the active rollout by one step. Used when auto_advance is off, OR when an operator wants to
        step past a
      tags:
      - deployments
      x-octomil-method: deployments.advance
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - bumps Rollout.current_percentage by step_percentage
      - materializer re-evaluates DeviceAssignment cohort for the new percentage
      - emits telemetry event (rollout.advanced) with the new percentage
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Manually advance the active rollout by one step. Used when auto_advance is off, OR when an operator wants
        to step past a soak interval the automator is still waiting on.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                step_percentage:
                  type: number
                  description: 'Optional override of the rollout''s configured step_percentage for this single advance call.
                    Useful for moving past a stuck step or accelerating after a manual review.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment + advanced rollout state.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/approve:
    post:
      operationId: deployments_approve
      summary: Approve a deployment that's pending review. The approving user must have the approval role (org admin / governance
        revie
      tags:
      - deployments
      x-octomil-method: deployments.approve
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Deployment.approval_status to "approved"
      - records approver user_id + comment + timestamp
      - emits telemetry event (deployment.approved)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Approve a deployment that's pending review. The approving user must have the approval role (org admin /
        governance reviewer) and must not be the same user who created or requested approval (separation-of-duties enforced
        server-side).
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                comment:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment with approval_status = "approved".
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments:
    post:
      operationId: deployments_create
      summary: Create a new deployment. Validates that the model_variant_id exists in the catalog (visible to the caller's
        org) and the
      tags:
      - deployments
      x-octomil-method: deployments.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a Deployment row in `draft` status
      - emits telemetry event (deployment.created)
      - if `targeting` is set, the materializer may begin populating DeviceAssignment rows asynchronously
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - model_not_found
      - version_not_found
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new deployment. Validates that the model_variant_id exists in the catalog (visible to the caller's
        org) and the optional desired/active version ids belong to that variant.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - key
              - name
              - model_variant_id
              properties:
                key:
                  type: string
                  description: Stable slug, unique per org.
                name:
                  type: string
                description:
                  type: string
                environment:
                  type: string
                  description: '"development" / "staging" / "production".'
                platforms:
                  type: array
                  items:
                    type: string
                  description: e.g. ["ios", "android"].
                app_ids:
                  type: array
                  items:
                    type: string
                  description: Bundle IDs the deployment targets. Joins to AppTarget.identifier.
                model_variant_id:
                  type: string
                  description: Stable CatalogModelVariant id. Versions move under this.
                desired_model_version_id:
                  type: string
                active_model_version_id:
                  type: string
                targeting:
                  type: object
                  description: 'Cohort selector. Shape: `{device_group_ids: [...]}` for named groups OR `{label_selector:
                    {matchLabels: {...}}}` for materializer-driven dynamic targeting.

                    '
                rollout_policy:
                  type: object
                settings:
                  type: object
                serving_policy:
                  type: object
                  description: ServingPolicy preset or inline (see schemas/core/serving_policy.json).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The persisted Deployment row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: deployments_list
      summary: List deployments in the caller's org. Offset-paginated.
      tags:
      - deployments
      x-octomil-method: deployments.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List deployments in the caller's org. Offset-paginated.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
        description: "Org scope. Required \u2014 the server has no implicit-from-auth fallback on this route."
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: "Filter on deployment status \u2014 `draft | active | paused | rolling_out | failed | archived`."
      - name: model_variant_id
        in: query
        required: false
        schema:
          type: string
      - name: app_id
        in: query
        required: false
        schema:
          type: string
      - name: capability
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - deployments
                - total
                properties:
                  deployments:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
                    description: Unfiltered count for "showing N of M" UI.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/assignments:
    post:
      operationId: deployments_create_assignment
      summary: 'Manually pin a device to a deployment cohort. Used to force a specific device into the canary cohort for QA,
        OR to keep '
      tags:
      - deployments
      x-octomil-method: deployments.create_assignment
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a DeviceAssignment row
      - the affected device picks up the new assignment on its next devices.sync
      - emits telemetry event (deployment.assignment_created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - device_not_registered
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Manually pin a device to a deployment cohort. Used to force a specific device into the canary cohort for
        QA, OR to keep a device on a previous version when a rollout shouldn't touch it. Manual assignments survive materializer
        re-runs.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - model_id
              properties:
                device_id:
                  type: string
                model_id:
                  type: string
                  description: Internal model UUID to assign to the device.
                variant:
                  type: string
                  description: "Operator-chosen \u2014 typically \"canary\" or \"baseline\"."
                version:
                  type: string
                  description: Override the cohort default for this device.
                assignment_reason:
                  type: string
                  description: 'Free-text; defaults to "manual". The materializer leaves rows with non-"label_selector" reasons
                    untouched, so manually-created assignments survive auto-reconciliation.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The created DeviceAssignment row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: deployments_list_assignments
      summary: List the DeviceAssignment rows for a deployment. Powers the assignments-table view + the deployment health
        computations.
      tags:
      - deployments
      x-octomil-method: deployments.list_assignments
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the DeviceAssignment rows for a deployment. Powers the assignments-table view + the deployment health
        computations.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: variant
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 typically \"canary\" or \"baseline\"."
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - assignments
                properties:
                  assignments:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  has_more:
                    type: boolean
                  next_cursor:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/allocations:
    patch:
      operationId: deployments_experiments_allocations_update
      summary: Partially updates variant traffic allocations for a running or paused experiment. Returns the updated Experiment.
        Alloca
      tags:
      - deployments
      x-octomil-method: deployments.experiments.allocations.update
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Calls ExperimentCRUD.update_variant_allocations(), which updates traffic_allocation on ExperimentVariant rows.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: "Partially updates variant traffic allocations for a running or paused experiment. Returns the updated\
        \ Experiment. Allocation changes take effect immediately on the server \u2014 traffic routing updates on the next\
        \ device sync cycle."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - variants
              properties:
                variants:
                  type: array
                  description: 'List of variant allocation updates. Each entry is a free-form dict passed directly to ExperimentCRUD.update_variant_allocations().
                    At minimum each entry should contain the variant id and the new traffic_allocation value.

                    '
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Variant UUID.
                      traffic_allocation:
                        type: number
                        minimum: 0
                        maximum: 100
                        description: New traffic allocation percentage for this variant.
                    additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics:
    get:
      operationId: deployments_experiments_analytics_get
      summary: 'Returns a full analytics summary for the experiment: per-variant sample counts, confidence intervals, the
        Welch t-test s'
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.get
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Returns a full analytics summary for the experiment: per-variant sample counts, confidence intervals,
        the Welch t-test significance result, and observed statistical power. The significance_result and statistical_power
        fields are null when either variant has fewer than 2 observations.'
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: metric_name
        in: query
        required: false
        schema:
          type: string
        description: 'Metric to analyse. When omitted the server uses experiment.primary_metric. Must match a metric name
          for which the experiment has recorded events.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentAnalytics'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/guardrails:
    post:
      operationId: deployments_experiments_analytics_guardrails
      summary: Evaluates each supplied metric against a configurable threshold. Returns pass/violation status per metric and
        an aggrega
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.guardrails
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Evaluates each supplied metric against a configurable threshold. Returns pass/violation status per metric
        and an aggregate any_violation flag. Does not mutate experiment state.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - metrics
              properties:
                metrics:
                  type: array
                  description: 'One entry per guardrail metric to evaluate. Each entry is a free-form dict passed through
                    to StatisticalAnalyzer.check_guardrails().

                    '
                  items:
                    type: object
                    required:
                    - metric_name
                    properties:
                      metric_name:
                        type: string
                        description: Metric name to check. Must match recorded experiment events.
                      direction:
                        type: string
                        default: higher_is_better
                        description: '''higher_is_better'' or ''lower_is_better''. Controls which direction of delta is a
                          guardrail violation. Defaults to ''higher_is_better''.

                          '
                      threshold_pct:
                        type: number
                        default: 5.0
                        description: 'Maximum allowed relative degradation (%) before violation is flagged. Defaults to 5.0.

                          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentGuardrailsResult'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/nonparametric:
    get:
      operationId: deployments_experiments_analytics_nonparametric
      summary: 'Runs a Mann-Whitney U test on the experiment''s metric values and tests each variant for normality. The recommended_test '
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.nonparametric
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Runs a Mann-Whitney U test on the experiment's metric values and tests each variant for normality. The
        recommended_test field signals whether a parametric (t-test) or nonparametric test is more appropriate given the data
        distribution.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: metric_name
        in: query
        required: false
        schema:
          type: string
        description: 'Metric to analyse. When omitted the server uses experiment.primary_metric.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentNonparametric'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/sample-size:
    get:
      operationId: deployments_experiments_analytics_sample_size
      summary: Computes the required sample size for the experiment's primary metric given the current baseline mean and standard
        devia
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.sample_size
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Computes the required sample size for the experiment's primary metric given the current baseline mean and
        standard deviation observed in the control group. Also returns enrollment progress toward the required total.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: minimum_effect
        in: query
        required: false
        schema:
          type: number
          default: 5.0
        description: 'Minimum detectable effect (percentage). Passed to StatisticalAnalyzer.calculate_sample_size(). Defaults
          to 5.0.

          '
      - name: power
        in: query
        required: false
        schema:
          type: number
          default: 0.8
        description: 'Target statistical power in the range (0, 1). Defaults to 0.80.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentSampleSize'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/segments:
    get:
      operationId: deployments_experiments_analytics_segments
      summary: Partitions experiment events by a device metadata key and runs per-segment significance analysis to detect
        heterogeneity
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.segments
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Partitions experiment events by a device metadata key and runs per-segment significance analysis to detect
        heterogeneity of treatment effect across device cohorts (e.g. platform, OS version).
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: segment_by
        in: query
        required: false
        schema:
          type: string
          default: platform
        description: 'Device metadata key to partition events by. Defaults to ''platform''. The key is looked up in event.metadata
          (a JSON dict). Events with no matching metadata key are bucketed under the segment value ''unknown''.

          '
      - name: metric_name
        in: query
        required: false
        schema:
          type: string
        description: 'Metric to analyse. When omitted the server uses experiment.primary_metric.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentSegmentAnalysis'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/sequential:
    get:
      operationId: deployments_experiments_analytics_sequential
      summary: Performs a group-sequential test against the experiment's metric data using the chosen alpha-spending function.
        The resp
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.sequential
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Performs a group-sequential test against the experiment's metric data using the chosen alpha-spending function.
        The response shape is returned verbatim from StatisticalAnalyzer.sequential_test().
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: spending_function
        in: query
        required: false
        schema:
          type: string
          default: obrien_fleming
        description: 'Alpha-spending function for the group-sequential test. Passed directly to StatisticalAnalyzer.sequential_test().
          Defaults to ''obrien_fleming''. Other values accepted by the analyzer (e.g. ''pocock'') are forwarded as-is.

          '
      - name: metric_name
        in: query
        required: false
        schema:
          type: string
        description: 'Metric to analyse. When omitted the server uses experiment.primary_metric.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentSequentialAnalysis'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/analytics/timeseries:
    get:
      operationId: deployments_experiments_analytics_timeseries
      summary: 'Returns per-variant time-bucketed metric means for charting experiment progression over time. Each data point
        holds the '
      tags:
      - deployments
      x-octomil-method: deployments.experiments.analytics.timeseries
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns per-variant time-bucketed metric means for charting experiment progression over time. Each data
        point holds the mean metric value and event count for the bucket window.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      - name: interval
        in: query
        required: false
        schema:
          type: string
          default: day
        description: 'Time bucket granularity. Accepted values: ''hour'', ''day'', ''week''. Any unrecognised value is silently
          treated as ''day'' by the server.

          '
      - name: metric_name
        in: query
        required: false
        schema:
          type: string
        description: 'Metric to plot. When omitted the server uses experiment.primary_metric.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentTimeseries'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/cancel:
    post:
      operationId: deployments_experiments_cancel
      summary: Cancel an experiment. Stamps `ended_at`, terminates variant traffic allocation, and locks the experiment from
        further st
      tags:
      - deployments
      x-octomil-method: deployments.experiments.cancel
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Sets experiment `status="cancelled"` and `ended_at=now`.
      - Variant traffic allocation stops; assignments revert to deployment baseline.
      - "Experiment is locked \u2014 no further state transitions accepted."
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - experiment_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Cancel an experiment. Stamps `ended_at`, terminates variant traffic allocation, and locks the experiment
        from further state transitions. Use this when the experiment must be aborted (bug, safety issue, business decision)
        rather than completing naturally.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/complete:
    post:
      operationId: deployments_experiments_complete
      summary: Mark an experiment as completed. Stamps `ended_at`, terminates variant traffic allocation, and locks the experiment
        from
      tags:
      - deployments
      x-octomil-method: deployments.experiments.complete
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Sets experiment `status="completed"` and `ended_at=now`.
      - Variant traffic allocation stops; assignments revert to deployment baseline.
      - "Experiment is locked \u2014 no further start/pause/resume/cancel transitions accepted."
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - experiment_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Mark an experiment as completed. Stamps `ended_at`, terminates variant traffic allocation, and locks the
        experiment from further state transitions. Use this when the experiment has reached its statistical-significance threshold
        or planned duration.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments:
    post:
      operationId: deployments_experiments_create
      summary: "Create an experiment under a deployment. The experiment starts in `draft` status \u2014 no traffic flows until\
        \ `deployments.e"
      tags:
      - deployments
      x-octomil-method: deployments.experiments.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Inserts an `Experiment` row in `draft` status.
      - When `variants` is supplied, also inserts an `ExperimentVariant` row per entry.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - deployment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: "Create an experiment under a deployment. The experiment starts in `draft` status \u2014 no traffic flows\
        \ until `deployments.experiments.start` is called. Returns the full Experiment object (with default values populated\
        \ server-side)."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without creating a duplicate experiment.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - model_id
              properties:
                name:
                  type: string
                  description: Human-readable experiment name; shown in dashboard panels and reports.
                model_id:
                  type: string
                  description: "Internal Model registry UUID this experiment is bound to (NOT the catalog `CatalogModelVariant.id`\
                    \ \u2014 see workspace memory `Model Registries`)."
                traffic_percentage:
                  type: number
                  default: 100.0
                  minimum: 0
                  maximum: 100
                  description: Percentage of eligible devices included in the experiment.
                experiment_type:
                  type: string
                  default: version
                  description: e.g. `"version"` to compare model versions, `"variant"` to compare named variants.
                variants:
                  type: array
                  nullable: true
                  description: 'Optional per-variant configuration. When supplied, the server routes via `ExperimentService.create_experiment`
                    (variant-aware path) instead of `DeploymentService.create_experiment` (deployment-scoped, single-variant
                    path). Pass either, not both.

                    '
                  items:
                    type: object
                    required:
                    - name
                    - traffic_allocation
                    properties:
                      name:
                        type: string
                      model_version:
                        type: string
                        default: ''
                      traffic_allocation:
                        type: number
                        minimum: 0
                        maximum: 100
                      is_control:
                        type: boolean
                        default: false
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: deployments_experiments_list
      summary: List all experiments belonging to a deployment, regardless of status. Returns the full experiment objects (including
        var
      tags:
      - deployments
      x-octomil-method: deployments.experiments.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: List all experiments belonging to a deployment, regardless of status. Returns the full experiment objects
        (including variants and target groups) so the dashboard's experiments panel doesn't need per-row fetches.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentListResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/pause:
    post:
      operationId: deployments_experiments_pause
      summary: "Pause a running experiment. Stops variant traffic allocation without ending the experiment \u2014 callers\
        \ can resume later wi"
      tags:
      - deployments
      x-octomil-method: deployments.experiments.pause
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Sets experiment `status="paused"`.
      - Variant traffic allocation stops; assignments fall back to the deployment's baseline model.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - experiment_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: "Pause a running experiment. Stops variant traffic allocation without ending the experiment \u2014 callers\
        \ can resume later with `deployments.experiments.resume`."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/resume:
    post:
      operationId: deployments_experiments_resume
      summary: Resume a paused experiment. Restores variant traffic allocation using the same configuration that was active
        when paused
      tags:
      - deployments
      x-octomil-method: deployments.experiments.resume
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Sets experiment `status="running"`.
      - Variant traffic allocation becomes effective immediately.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - experiment_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Resume a paused experiment. Restores variant traffic allocation using the same configuration that was active
        when paused.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/start:
    post:
      operationId: deployments_experiments_start
      summary: "Start a `draft` experiment. Transitions status `draft \u2192 running`, stamps `started_at`. Returns the updated\
        \ Experiment."
      tags:
      - deployments
      x-octomil-method: deployments.experiments.start
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Sets experiment `status="running"` and `started_at=now`.
      - Variant traffic allocation becomes effective immediately.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - experiment_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: "Start a `draft` experiment. Transitions status `draft \u2192 running`, stamps `started_at`. Returns the\
        \ updated Experiment."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Experiment'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/experiments/{experiment_id}/target-groups:
    get:
      operationId: deployments_experiments_target_groups_get
      summary: Returns the device groups currently targeted by the experiment. target_groups is an empty array when no groups
        have been
      tags:
      - deployments
      x-octomil-method: deployments.experiments.target_groups.get
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns the device groups currently targeted by the experiment. target_groups is an empty array when no
        groups have been configured.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentTargetGroups'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      operationId: deployments_experiments_target_groups_set
      summary: "Replaces the experiment's target group list with the supplied group_ids. This is a full replacement (PUT semantics)\
        \ \u2014 gr"
      tags:
      - deployments
      x-octomil-method: deployments.experiments.target_groups.set
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Calls ExperimentService.set_target_groups(), which replaces the experiment's target_groups association.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - experiment_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: "Replaces the experiment's target group list with the supplied group_ids. This is a full replacement (PUT\
        \ semantics) \u2014 groups not in the list are removed. Returns the updated target groups list."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: Deployment UUID.
      - name: experiment_id
        in: path
        required: true
        schema:
          type: string
        description: Experiment UUID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - group_ids
              properties:
                group_ids:
                  type: array
                  description: 'Complete replacement list of device group UUIDs to target. An empty array clears all target
                    groups (experiment becomes untargeted).

                    '
                  items:
                    type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExperimentTargetGroups'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}:
    get:
      operationId: deployments_get
      summary: Fetch a single deployment by id. Powers the deployment detail page.
      tags:
      - deployments
      x-octomil-method: deployments.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single deployment by id. Powers the deployment detail page.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Full Deployment row with eager-loaded current rollout.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: deployments_update
      summary: Partial update of a deployment. Used by the dashboard for both label-level edits (name / description / settings)
        and sub
      tags:
      - deployments
      x-octomil-method: deployments.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates Deployment row per request body
      - if `targeting` changed, the materializer re-evaluates DeviceAssignment membership
      - emits telemetry event (deployment.updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - version_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: "Partial update of a deployment. Used by the dashboard for both label-level edits (name / description /\
        \ settings) and substantive changes (target_version / targeting / rollout_policy). Mutating a rollout-affecting field\
        \ while a rollout is active returns 409 \u2014 operators must pause first."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: 'All fields optional; only declared fields are mutated. Fields that affect an active rollout (model_variant_id,
                desired_model_version_id, targeting) require the rollout to be paused first or the route returns 409.

                '
              properties:
                name:
                  type: string
                description:
                  type: string
                environment:
                  type: string
                platforms:
                  type: array
                  items:
                    type: string
                app_ids:
                  type: array
                  items:
                    type: string
                desired_model_version_id:
                  type: string
                active_model_version_id:
                  type: string
                targeting:
                  type: object
                rollout_policy:
                  type: object
                settings:
                  type: object
                serving_policy:
                  type: object
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated Deployment row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/device-health:
    get:
      operationId: deployments_get_device_health
      summary: 'Aggregate device health metrics for a deployment. Drives the dashboard''s HealthSummaryPanel "Device Health"
        section and '
      tags:
      - deployments
      x-octomil-method: deployments.get_device_health
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Aggregate device health metrics for a deployment. Drives the dashboard's HealthSummaryPanel "Device Health"
        section and the auto-pause flow's secondary signals (a deployment with high download_failures is unhealthy even if
        inference is succeeding elsewhere).
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Per-bucket device health for the deployment. Counts devices by operational status (online /
                  stale / offline) plus runtime-state buckets (model_active / download_failures / load_failures / fallback_active
                  / battery_constrained / thermally_constrained).

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/health:
    get:
      operationId: deployments_get_health
      summary: Return current health for a deployment. Drives the dashboard's HealthSummaryPanel; the auto-pause flow polls
        this at the
      tags:
      - deployments
      x-octomil-method: deployments.get_health
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return current health for a deployment. Drives the dashboard's HealthSummaryPanel; the auto-pause flow
        polls this at the automator's tick interval to make its gate decision.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - deployment_id
                - status
                - assigned_devices
                properties:
                  deployment_id:
                    type: string
                  status:
                    type: string
                    description: "Deployment.status \u2014 one of `draft | active | paused | rolling_out | failed | archived`."
                  assigned_devices:
                    type: integer
                    description: Total count of DeviceAssignment rows for this deployment.
                  current_rollout_id:
                    type: string
                    nullable: true
                  rollout_percentage:
                    type: number
                    nullable: true
                    description: Active rollout's current_percentage if any.
                  rollout_status:
                    type: string
                    nullable: true
                  health_snapshot:
                    type: object
                    nullable: true
                    additionalProperties: true
                    description: "Most recent automator tick's snapshot \u2014 gate_passed, violations, inference + bindings\
                      \ + device_health blocks. `null` until the rollout automator has run at least once.\n"
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/latest-thumbnails:
    get:
      operationId: deployments_list_latest_thumbnails
      summary: List the latest camera frame metadata for every device assigned to a deployment. Backs the deployment detail
        page's "Lat
      tags:
      - deployments
      x-octomil-method: deployments.list_latest_thumbnails
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the latest camera frame metadata for every device assigned to a deployment. Backs the deployment detail
        page's "Latest frames" grid. Returned entries are stable per device_id so client-side rendering can use device_id
        as a stable React key.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
        description: 'Deployment.id whose assigned devices'' thumbnails to list. The caller''s org must own the deployment
          (the route enforces deployment.org_id == auth.org_id and joins through Device.org_id so a misconfigured deployment
          can''t leak foreign device_ids).

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    description: 'One entry per device assigned to the deployment. Devices that have not uploaded a thumbnail
                      yet are still included with `updated_at: null` so the dashboard can render a placeholder cell rather
                      than skipping the device from the grid.

                      '
                    items:
                      type: object
                      required:
                      - device_id
                      properties:
                        device_id:
                          type: string
                          description: Device.id (UUID).
                        model_version:
                          type: string
                          description: 'The version currently resolved for this device by the rollout (DeviceAssignment.resolved_model_version).
                            Drives the per-cell version badge in the dashboard tile.

                            '
                        thumbnail_url:
                          type: string
                          nullable: true
                          description: 'Path to GET the device''s latest frame, or null if the device hasn''t uploaded one
                            yet. Currently maps to devices.get_latest_thumbnail.

                            '
                        updated_at:
                          type: number
                          nullable: true
                          description: 'Unix epoch seconds when the device''s latest frame was stored. `null` when no frame
                            exists. The dashboard renders a "stale" badge if updated_at is older than 30s.

                            '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/rollouts:
    get:
      operationId: deployments_list_rollout_history
      summary: List the rollout history for a deployment.
      tags:
      - deployments
      x-octomil-method: deployments.list_rollout_history
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the rollout history for a deployment.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'Chronological list of rollouts this deployment has had. Latest first. Each entry includes status
                  / from_version / to_version / started_at / completed_at / rollback_reason if any. Used by the dashboard''s
                  "Status History Timeline" view.

                  '
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/materialize-assignments:
    post:
      operationId: deployments_materialize_assignments
      summary: Force the assignment materializer to (re)run for this deployment. Normally the materializer runs automatically
        after dep
      tags:
      - deployments
      x-octomil-method: deployments.materialize_assignments
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts/updates/removes DeviceAssignment rows to match the deployment's targeting + rollout percentage
      - emits telemetry event (deployment.materialized) with the inserted/updated/removed counts
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Force the assignment materializer to (re)run for this deployment. Normally the materializer runs automatically
        after deployment create / targeting change / start; this route is for the "something is off and I want to recompute
        now" case.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Materialization summary (created / updated / removed counts).
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/pause:
    post:
      operationId: deployments_pause
      summary: Pause the active rollout. Devices that have already been bumped to the new version stay there; remaining devices
        keep th
      tags:
      - deployments
      x-octomil-method: deployments.pause
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Rollout.status to "paused" via DeploymentService.pause_deployment
      - the automator stops advancing (next_advance_at cleared)
      - emits telemetry event (rollout.paused)
      - any Deployment writes that would have been blocked by an active rollout (target_version change, targeting change)
        become allowed
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Pause the active rollout. Devices that have already been bumped to the new version stay there; remaining\
        \ devices keep the previous version. No teardown \u2014 resume picks up where pause left off."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment / rollout in their new paused state.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/promote:
    post:
      operationId: deployments_promote
      summary: "Promote a deployment from one environment to another (typically staging \u2192 production). Creates a new\
        \ Deployment row in t"
      tags:
      - deployments
      x-octomil-method: deployments.promote
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a new Deployment row in target_environment
      - copies model_variant_id / desired_version / targeting / rollout_policy from source (with overrides)
      - "the new deployment starts in `draft` \u2014 production gates still apply (approval, quality_gate)"
      - emits telemetry event (deployment.promoted)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: "Promote a deployment from one environment to another (typically staging \u2192 production). Creates a\
        \ new Deployment row in the target environment cloning the source's configuration, with promoted_from_id linking back."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                target_environment:
                  type: string
                  description: "Destination environment \u2014 \"staging\" or \"production\"."
                overrides:
                  type: object
                  additionalProperties: true
                  description: 'Field-level overrides for the promoted deployment (e.g. tighter rollout_policy for prod, different
                    targeting). The source deployment''s fields are copied otherwise.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The newly-created deployment in the target environment.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/reject:
    post:
      operationId: deployments_reject
      summary: Reject a pending deployment approval request. Reason is required for the audit trail.
      tags:
      - deployments
      x-octomil-method: deployments.reject
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Deployment.approval_status to "rejected"
      - records rejector user_id + reason + timestamp
      - emits telemetry event (deployment.rejected) including reason
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Reject a pending deployment approval request. Reason is required for the audit trail.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - reason
              properties:
                reason:
                  type: string
                  description: Required free-text. Surfaced to the requester in the dashboard so they understand what to fix.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment with approval_status = "rejected".
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/request-approval:
    post:
      operationId: deployments_request_approval
      summary: Open a governance approval flow for a deployment that requires reviewer sign-off before starting. Used when
        org policy m
      tags:
      - deployments
      x-octomil-method: deployments.request_approval
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Deployment.approval_status to "pending"
      - notifies the configured reviewers (email + Slack if integrated)
      - emits telemetry event (deployment.approval_requested)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Open a governance approval flow for a deployment that requires reviewer sign-off before starting. Used
        when org policy mandates approval for production deploys.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Free-text justification. Surfaced to reviewers in the dashboard's ApprovalBanner.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment with approval_status = "pending".
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/resume:
    post:
      operationId: deployments_resume
      summary: Resume a paused rollout. Picks up at the current_percentage where pause left off; the automator's next_advance_at
        is re-
      tags:
      - deployments
      x-octomil-method: deployments.resume
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Rollout.status back to "in_progress"
      - re-arms next_advance_at if auto_advance is true
      - emits telemetry event (rollout.resumed)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Resume a paused rollout. Picks up at the current_percentage where pause left off; the automator's next_advance_at
        is re-armed using the configured step_interval_minutes from now.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment + resumed rollout state.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/rollback:
    post:
      operationId: deployments_rollback
      summary: Roll a deployment back to its from_model_version. All devices that were bumped to the new version are immediately
        re-tar
      tags:
      - deployments
      x-octomil-method: deployments.rollback
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - "sets Rollout.status to \"rolled_back\" via DeploymentService.rollback_deployment (terminal \u2014 a new rollout is\
        \ required to re-attempt)"
      - sets Rollout.current_percentage to 0
      - device-agent sync returns the original (from_model_version) on next poll for every device that was bumped to the new
        version
      - emits telemetry event (rollout.rolled_back)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Roll a deployment back to its from_model_version. All devices that were bumped to the new version are immediately
        re-targeted to the previous version on their next sync. Used by the auto-pause flow AND by operator-initiated emergency
        rollback.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment / closed rollout in their rolled-back state.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/start:
    post:
      operationId: deployments_start
      summary: Move a deployment from `draft` to `active`/`rolling_out` and create a Rollout if rollout_policy is configured.
        Materiali
      tags:
      - deployments
      x-octomil-method: deployments.start
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - flips Deployment.status
      - inserts a Rollout row when rollout_policy is non-trivial
      - kicks off the materializer to populate DeviceAssignment rows
      - emits telemetry event (deployment.started)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Move a deployment from `draft` to `active`/`rolling_out` and create a Rollout if rollout_policy is configured.
        Materializer runs to populate DeviceAssignment rows for the cohort.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The deployment in its new `rolling_out` / `active` status, including the newly-created Rollout
                  if applicable.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/deployments/{deployment_id}/rollout:
    patch:
      operationId: deployments_update_current_rollout
      summary: Partial update of the current rollout's configuration. Used by the dashboard's slider for manual percentage
        edits and by
      tags:
      - deployments
      x-octomil-method: deployments.update_current_rollout
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates Rollout row per request body
      - if current_percentage moved, materializer re-evaluates DeviceAssignment cohort
      - emits telemetry event (rollout.config_updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Partial update of the current rollout's configuration. Used by the dashboard's slider for manual percentage
        edits and by the "edit cadence" controls during paused-state changes.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - percentage
              description: "Mutate fields on the currently-active rollout without recreating it. Useful for adjusting cadence\
                \ mid-flight (e.g. shortening step_interval after a successful early ramp) or flipping auto-flags. Mutating\
                \ current_percentage directly is allowed but noisier than deployments.advance \u2014 prefer the latter unless\
                \ jumping multiple steps.\n"
              properties:
                percentage:
                  type: number
                  minimum: 0
                  maximum: 100
                  description: Target rollout percentage to set on the active rollout.
                current_percentage:
                  type: number
                  minimum: 0
                  maximum: 100
                target_percentage:
                  type: number
                  minimum: 0
                  maximum: 100
                step_percentage:
                  type: number
                step_interval_minutes:
                  type: integer
                auto_advance:
                  type: boolean
                auto_rollback:
                  type: boolean
                reason:
                  type: string
                  description: Free-text recorded with the mutation for the audit trail.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated Rollout.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-groups/{group_id}/members:
    post:
      operationId: device_groups_add_member
      summary: Manually add a device to a (static or hybrid) group. Returns 409 if the group is purely dynamic.
      tags:
      - device_groups
      x-octomil-method: device_groups.add_member
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a device_group_memberships row with assignment_type=manual
      - emits telemetry event (device_group.member_added)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - device_not_registered
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Manually add a device to a (static or hybrid) group. Returns 409 if the group is purely dynamic.
      parameters:
      - name: group_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_ids
              properties:
                device_ids:
                  type: array
                  items:
                    type: string
                  description: One or more device identifiers to add to the group.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The created device_group_memberships row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-groups:
    post:
      operationId: device_groups_create
      summary: Create a new DeviceGroup. Static groups have manual members; dynamic groups evaluate auto_assignment_rules.matchLabels
        a
      tags:
      - device_groups
      x-octomil-method: device_groups.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a DeviceGroup row
      - for dynamic/hybrid groups, immediately evaluates rules to compute initial membership
      - emits telemetry event (device_group.created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new DeviceGroup. Static groups have manual members; dynamic groups evaluate auto_assignment_rules.matchLabels
        at query time.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              properties:
                name:
                  type: string
                description:
                  type: string
                group_type:
                  type: string
                  description: '"static" | "dynamic" | "hybrid". Defaults to "static".'
                auto_assignment_rules:
                  type: object
                  description: "For dynamic/hybrid \u2014 `{matchLabels: {key: value}}` selector style."
                tags:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The created DeviceGroup row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: device_groups_list
      summary: List DeviceGroups in the caller's org.
      tags:
      - device_groups
      x-octomil-method: device_groups.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List DeviceGroups in the caller's org.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
      - name: group_type
        in: query
        required: false
        schema:
          type: string
        description: '"static" | "dynamic" | "hybrid".'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - groups
                properties:
                  groups:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  has_more:
                    type: boolean
                  next_cursor:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-groups/{group_id}:
    get:
      operationId: device_groups_get
      summary: Fetch a single DeviceGroup. Powers the group detail page.
      tags:
      - device_groups
      x-octomil-method: device_groups.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single DeviceGroup. Powers the group detail page.
      parameters:
      - name: group_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Full DeviceGroup row with current member count + first N member ids.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: device_groups_update
      summary: Partial update of a DeviceGroup. Changing auto_assignment_rules re-evaluates membership.
      tags:
      - device_groups
      x-octomil-method: device_groups.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates DeviceGroup row
      - if auto_assignment_rules changed, dynamic-style group is re-evaluated
      - emits telemetry event (device_group.updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Partial update of a DeviceGroup. Changing auto_assignment_rules re-evaluates membership.
      parameters:
      - name: group_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                is_active:
                  type: boolean
                auto_assignment_rules:
                  type: object
                tags:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated DeviceGroup row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-groups/{group_id}/devices:
    get:
      operationId: device_groups_list_devices
      summary: List the devices that are members of a group. Static membership is direct; dynamic is re-evaluated at query
        time.
      tags:
      - device_groups
      x-octomil-method: device_groups.list_devices
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the devices that are members of a group. Static membership is direct; dynamic is re-evaluated at query
        time.
      parameters:
      - name: group_id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - devices
                properties:
                  devices:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  has_more:
                    type: boolean
                  next_cursor:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-groups/{group_id}/members/{device_id}:
    delete:
      operationId: device_groups_remove_member
      summary: Remove a manual member from a group. Has no effect on dynamic members (which are recomputed from rules).
      tags:
      - device_groups
      x-octomil-method: device_groups.remove_member
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes the device_group_memberships row
      - emits telemetry event (device_group.member_removed)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Remove a manual member from a group. Has no effect on dynamic members (which are recomputed from rules).
      parameters:
      - name: group_id
        in: path
        required: true
        schema:
          type: string
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Empty / minimal ack.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/desired-state:
    get:
      operationId: devices_desired_state
      summary: Fetch the server-authoritative desired state for a device, including active binding, model entries, and policy
        config.
      tags:
      - devices
      x-octomil-method: devices.desired_state
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server records device sync timestamp
      - emits telemetry event (device.sync_requested)
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch the server-authoritative desired state for a device, including active binding, model entries, and
        policy config.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: knownStateVersion
        in: query
        required: false
        schema:
          type: string
        description: Last applied desired state version; server may return 304-equivalent if unchanged.
      - name: appId
        in: query
        required: false
        schema:
          type: string
      - name: appVersion
        in: query
        required: false
        schema:
          type: string
      - name: sdkVersion
        in: query
        required: false
        schema:
          type: string
      - name: platform
        in: query
        required: false
        schema:
          type: string
      - name: deviceClass
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/desired_state'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}:
    get:
      operationId: devices_get
      summary: Fetch a single device by id. Dashboard's device detail page, plus any operator workflow that needs the full
        row (assignm
      tags:
      - devices
      x-octomil-method: devices.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - device_not_registered
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single device by id. Dashboard's device detail page, plus any operator workflow that needs the
        full row (assignments, labels, capability profile, last heartbeat).
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device.id (UUID).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Full Device row, including its current assignment / observed state / capability profile if available.
                  Shape mirrors the Device model plus eager-loaded relationships.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: devices_update
      summary: Partial update of a device. Operators use this to tag devices with labels (steering them into DeviceGroup label-selector
      tags:
      - devices
      x-octomil-method: devices.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates Device row fields per request body
      - if `labels` changed, re-evaluates DeviceGroup label-selector membership for any group with a matching rule
      - emits telemetry event (device.updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - device_not_registered
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Partial update of a device. Operators use this to tag devices with labels (steering them into DeviceGroup\
        \ label-selector membership), rename, or flip status (e.g. archive a decommissioned camera). Cannot rotate auth tokens\
        \ \u2014 use devices.revoke + devices.register for that."
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: "All fields optional; only declared fields are mutated. Other Device fields (org_id, platform,\
                \ manufacturer, etc.) cannot be changed via this route \u2014 they're set at registration time.\n"
              properties:
                model:
                  type: string
                  description: Human-readable model name (e.g. "iPad Pro 12.9").
                os_version:
                  type: string
                sdk_version:
                  type: string
                capabilities:
                  type: object
                  additionalProperties: true
                  description: "ModelCapability \u2192 runtime support map. Re-probing capabilities (e.g. when an OS update\
                    \ enables a new accelerator) should be followed by a PATCH so the materializer's targeting can pick up\
                    \ the change without waiting for the next devices.register.\n"
                labels:
                  type: object
                  additionalProperties:
                    type: string
                  description: 'Free-form key/value labels. DeviceGroup rules with `auto_assignment_rules.matchLabels` re-evaluate
                    on every change. To clear a label, set its value to null (which the server interprets as a delete).

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated Device row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/latest-thumbnail:
    get:
      operationId: devices_get_latest_thumbnail
      summary: 'Return the most recently uploaded camera frame for a device. Backs the dashboard''s "Latest frames" tile +
        device detail '
      tags:
      - devices
      x-octomil-method: devices.get_latest_thumbnail
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the most recently uploaded camera frame for a device. Backs the dashboard's "Latest frames" tile
        + device detail page. Polled at ~5s cadence; matches the iPad SDK's upload interval so faster polling has no benefit.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device.id of the camera whose latest frame to fetch.
      responses:
        '200':
          description: Success
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
                description: 'The raw JPEG bytes of the device''s most recently uploaded frame. Response also carries an `X-Thumbnail-Updated-At`
                  header (unix epoch seconds) so the dashboard can render a "stale" badge without a separate HEAD request.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/heartbeat:
    put:
      operationId: devices_heartbeat
      summary: Periodic device-health ping. Reports operational telemetry and pulls server-side control directives. PUT (not
        POST) beca
      tags:
      - devices
      x-octomil-method: devices.heartbeat
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates Device.last_heartbeat_at + the snapshot fields above
      - may trigger fleet rebucketing if a label or capability changed
      - emits telemetry event (device.heartbeat_received)
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Periodic device-health ping. Reports operational telemetry and pulls server-side control directives. PUT\
        \ (not POST) because every call is idempotent \u2014 replaying the same heartbeat shouldn't double-count anything;\
        \ the server uses the latest payload as the device's current state."
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device.id (UUID) returned by devices.register.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                battery_pct:
                  type: integer
                  minimum: 0
                  maximum: 100
                charging:
                  type: boolean
                thermal_state:
                  type: string
                  description: 'One of the values in enums/thermal_state.yaml (nominal | fair | serious | critical). Reported
                    so the server can avoid scheduling heavy work on overheating devices.

                    '
                network_type:
                  type: string
                  description: "Coarse classification \u2014 \"wifi\" / \"cellular\" / \"none\"."
                available_memory_mb:
                  type: integer
                available_storage_mb:
                  type: integer
                power_saver:
                  type: boolean
                model_status:
                  type: string
                  description: 'Operational status of the device''s currently-assigned model (active | downloading | failed
                    | unloaded). Drives the deployment health panel''s "model_active" device count.

                    '
                active_model_version:
                  type: string
                  description: 'Semver of the version the device currently has loaded. The health gate compares this against
                    the deployment''s resolved_model_version to compute cohort drift.

                    '
                runtime_executor:
                  type: string
                  description: Engine slug actually serving inference (e.g. "coreml", "litert", "llamacpp").
                fallback_active:
                  type: boolean
                last_error_code:
                  type: string
                  description: Most recent error code the SDK saw, if any.
                last_inference_at:
                  type: string
                  format: date-time
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                - device_id
                properties:
                  status:
                    type: string
                    description: Literal "ok".
                  device_id:
                    type: string
                  connectivity_status:
                    type: string
                    description: Recomputed by `device.compute_connectivity_status()` after the heartbeat payload is applied.
                  health_status:
                    type: string
                    description: Computed by `device.compute_health_status()` at heartbeat time.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices:
    get:
      operationId: devices_list
      summary: List devices for the authenticated org with optional filters. Results are enriched with the winning DeviceAssignment.
        Su
      tags:
      - devices
      x-octomil-method: devices.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'List devices for the authenticated org with optional filters. Results are enriched with the winning DeviceAssignment.
        Supports offset-based pagination. Label filters use bracket notation: label[key]=value.'
      parameters:
      - name: platform
        in: query
        required: false
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
      - name: deployment_id
        in: query
        required: false
        schema:
          type: string
        description: 'Filter devices assigned to a specific deployment via scalar subquery on DeviceAssignment.

          '
      - name: battery_low
        in: query
        required: false
        schema:
          type: boolean
        description: 'true = battery_pct < 20; false = battery_pct >= 20 OR IS NULL.

          '
      - name: search
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
      - name: offset
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - devices
                - total
                properties:
                  devices:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                      description: 'Device row enriched with the winning DeviceAssignment (active deployment preferred; most
                        recent assigned_at as tiebreaker).

                        '
                  total:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/activity:
    get:
      operationId: devices_list_activity
      summary: Return a chronological activity feed for a device. Powers the "Activity" timeline on the device detail page.
      tags:
      - devices
      x-octomil-method: devices.list_activity
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - device_not_registered
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return a chronological activity feed for a device. Powers the "Activity" timeline on the device detail
        page.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
        description: 'Opaque pagination cursor from a previous response''s `next_cursor`. Activities are ordered newest-first;
          the cursor encodes the last-seen activity_id + timestamp.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: "Array of activity entries for the device \u2014 most recent first. Each entry combines control-plane\
                  \ events (assignment changes, revocation, registration), agent reports (heartbeat, sync_completed), and\
                  \ operator actions (label edit, manual rollback). Schema is intentionally open-ended so the dashboard timeline\
                  \ can render heterogeneous event types from a single feed.\n"
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/groups:
    get:
      operationId: devices_list_groups
      summary: List every DeviceGroup the device belongs to. Powers the "Groups" panel on the device detail page.
      tags:
      - devices
      x-octomil-method: devices.list_groups
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List every DeviceGroup the device belongs to. Powers the "Groups" panel on the device detail page.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - groups
                - count
                properties:
                  groups:
                    type: array
                    description: 'DeviceGroup memberships, one entry per group. Each item is a DeviceGroupMembershipItem with
                      the group''s metadata + current device_count.

                      '
                    items:
                      type: object
                      required:
                      - id
                      - name
                      - group_type
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          nullable: true
                        group_type:
                          type: string
                          description: '"static" | "dynamic" | "hybrid".'
                        is_active:
                          type: boolean
                        tags:
                          type: array
                          items:
                            type: string
                        device_count:
                          type: integer
                          description: Member count of the group (not just this device's membership).
                  count:
                    type: integer
                    description: Number of groups returned.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/observed-state:
    post:
      operationId: devices_observed_state
      summary: Report the device's current observed state to the server. Used by the control plane for fleet health monitoring
        and drif
      tags:
      - devices
      x-octomil-method: devices.observed_state
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server updates device fleet record with observed model statuses
      - server may trigger rollout or re-assignment logic based on reported drift
      - emits telemetry event (device.observed_state_reported)
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Report the device's current observed state to the server. Used by the control plane for fleet health monitoring
        and drift detection.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/observed_state'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  ack:
                    type: boolean
                  serverTimestamp:
                    type: string
                    format: date-time
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/profiles:
    get:
      operationId: devices_profiles
      summary: Retrieve device capability profiles. Profiles are the output of the device-classifier service that scores devices
        agains
      tags:
      - devices
      x-octomil-method: devices.profiles
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve device capability profiles. Profiles are the output of the device-classifier service that scores
        devices against catalog variants.
      parameters:
      - name: device_id
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - profiles
                properties:
                  profiles:
                    type: array
                    description: Device capability profiles with classification scores.
                    items:
                      type: object
                      additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/register:
    post:
      operationId: devices_register
      summary: 'Register or re-register a device with the org. Idempotent on (org_id, device_identifier): re-registration
        updates fields'
      tags:
      - devices
      x-octomil-method: devices.register
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - inserts or updates the Device row
      - mints a fresh device access + refresh token pair
      - emits telemetry event (device.registered)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      description: 'Register or re-register a device with the org. Idempotent on (org_id, device_identifier): re-registration
        updates fields and rotates tokens but keeps the same Device.id. SDKs call this once at first launch and again whenever
        the persisted token is invalidated.'
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Re-submitting the same body with the same key within 24h replays the
          original response (and the same minted token pair) without re-executing the registration. SDKs that retry on network
          errors SHOULD send an Idempotency-Key so a half-completed registration doesn''t produce two Device rows.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - platform
              properties:
                device_identifier:
                  type: string
                  description: 'Stable per-install identifier the SDK persists across launches (vendor-id on iOS, install-id
                    on Android). The server uses this to deduplicate registrations from the same install.

                    '
                platform:
                  type: string
                  description: "Platform slug \u2014 \"ios\" / \"android\" / \"macos\" / \"browser\"."
                installation_id:
                  type: string
                app_id:
                  type: string
                  description: 'Bundle ID / package name. Joins to AppTarget.identifier so the registered device inherits
                    the App''s capability slots.

                    '
                app_version:
                  type: string
                manufacturer:
                  type: string
                model:
                  type: string
                cpu_architecture:
                  type: string
                gpu_available:
                  type: boolean
                  default: false
                total_memory_mb:
                  type: integer
                available_storage_mb:
                  type: integer
                os_version:
                  type: string
                sdk_version:
                  type: string
                locale:
                  type: string
                region:
                  type: string
                timezone:
                  type: string
                metadata:
                  type: object
                  additionalProperties: true
                capabilities:
                  type: object
                  additionalProperties: true
                  description: "ModelCapability \u2192 runtime support map the SDK probed at boot. Used by the device-classifier\
                    \ service to score the device against catalog variants.\n"
                feature_schema:
                  type: array
                  items:
                    type: string
                battery_pct:
                  type: integer
                  minimum: 0
                  maximum: 100
                charging:
                  type: boolean
                labels:
                  type: object
                  additionalProperties:
                    type: string
                  description: 'Free-form key/value labels the operator can target via DeviceGroup label-selector rules (e.g.
                    {"role": "checkout"}).

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Returns the persisted Device row plus a freshly-minted device access/refresh token pair the
                  SDK uses for all subsequent calls.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/revoke:
    post:
      operationId: devices_revoke
      summary: Mark a device as revoked. Sets Device.status to "revoked" and recomputes connectivity_status. The server does
        NOT invali
      tags:
      - devices
      x-octomil-method: devices.revoke
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Device.status to "revoked"
      - recomputes Device.connectivity_status
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Mark a device as revoked. Sets Device.status to \"revoked\" and recomputes connectivity_status. The server\
        \ does NOT invalidate outstanding device tokens at this layer \u2014 token revocation is a separate concern handled\
        \ by DeviceTokenService.revoke_all_sessions, which this route does not call. SDKs that need both should additionally\
        \ invoke the token-revocation flow."
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device.id of the device to flip to revoked status.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                - device_id
                properties:
                  status:
                    type: string
                    description: Literal "revoked".
                  device_id:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/sync:
    post:
      operationId: devices_sync
      summary: Unified sync endpoint that combines desired state fetch and observed state report into a single round-trip.
        Client sends
      tags:
      - devices
      x-octomil-method: devices.sync
      x-octomil-since: 1.12.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server records device sync timestamp
      - server ingests model inventory and observed state
      - server may advance rollout logic based on observed state
      - emits telemetry event (device.sync_completed)
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Unified sync endpoint that combines desired state fetch and observed state report into a single round-trip.
        Client sends inventory + observed state, server returns desired state + directives. Supersedes separate devices.desired_state
        and devices.observed_state calls for new SDKs.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_sync_request'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_sync_response'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/device-agent/frame-thumbnails/{device_id}:
    post:
      operationId: devices_upload_frame_thumbnail
      summary: Upload the latest camera frame as a JPEG. Replaces any previously uploaded frame for this device. Used by the
        iPad / And
      tags:
      - devices
      x-octomil-method: devices.upload_frame_thumbnail
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - replaces the device's latest stored thumbnail
      - emits telemetry event (device.thumbnail_uploaded)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Upload the latest camera frame as a JPEG. Replaces any previously uploaded frame for this device. Used
        by the iPad / Android SDKs' `ThumbnailUploader` to feed the dashboard's deployment-detail "Latest frames" tile.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device.id of the camera uploading the frame.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              content_type: multipart/form-data
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: "JPEG payload, \u2264256 KB. The server's _MAX_THUMBNAIL_BYTES enforces the cap; the iPad SDK\
                    \ compresses to ~50 KB at quality=0.7 before upload.\n"
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/eval/{eval_id}:
    get:
      operationId: eval_get
      summary: Retrieve a previously-run quality evaluation by eval_id. Returns summary including overall_score and pass/fail
        verdict.
      tags:
      - eval
      x-octomil-method: eval.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a previously-run quality evaluation by eval_id. Returns summary including overall_score and pass/fail
        verdict.
      parameters:
      - name: eval_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - eval_id
                - model_id
                - overall_score
                - status
                properties:
                  eval_id:
                    type: string
                  model_id:
                    type: string
                  overall_score:
                    type: number
                  passed:
                    type: boolean
                  recommendation:
                    type: string
                    nullable: true
                  status:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/eval/history/{model_id}:
    get:
      operationId: eval_history
      summary: List all past quality evaluation runs for a given model, newest first.
      tags:
      - eval
      x-octomil-method: eval.history
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: List all past quality evaluation runs for a given model, newest first.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum number of eval history entries (1\u2013200, default 50)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                  - eval_id
                  - model_id
                  - overall_score
                  - status
                  properties:
                    eval_id:
                      type: string
                    model_id:
                      type: string
                    overall_score:
                      type: number
                    passed:
                      type: boolean
                    recommendation:
                      type: string
                      nullable: true
                    status:
                      type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/eval:
    post:
      operationId: eval_run
      summary: Run a quality evaluation comparing on-device and cloud inference outputs. The evaluation is persisted and its
        eval_id ca
      tags:
      - eval
      x-octomil-method: eval.run
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists quality evaluation result
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Run a quality evaluation comparing on-device and cloud inference outputs. The evaluation is persisted and
        its eval_id can be retrieved via eval.get.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              - test_inputs
              properties:
                model_id:
                  type: string
                test_inputs:
                  type: array
                  description: List of input/expected-output pairs to evaluate.
                  items:
                    type: object
                    required:
                    - input
                    properties:
                      input:
                        type: string
                      expected_output:
                        type: string
                        nullable: true
                metrics:
                  type: array
                  items:
                    type: string
                  description: Metric names to compute (e.g. bleu, rouge, exact_match).
                quality_threshold:
                  type: number
                  nullable: true
                  description: Threshold in [0, 1] for pass/fail verdict.
                device_id:
                  type: string
                  nullable: true
                reference_provider:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - eval_id
                - model_id
                - status
                - summary
                - per_input_results
                properties:
                  eval_id:
                    type: string
                  model_id:
                    type: string
                  status:
                    type: string
                    description: Run status. e.g. "completed", "failed", "in_progress".
                  summary:
                    type: object
                    description: 'Aggregated quality metrics (overall_score, passed verdict, per-metric breakdown). Shape
                      determined server-side by the metrics requested; `additionalProperties: true` until pinned.

                      '
                    additionalProperties: true
                  per_input_results:
                    type: array
                    description: Per-input comparison results (cloud vs device output, scores).
                    items:
                      type: object
                      additionalProperties: true
                  statistical_significance:
                    type: object
                    nullable: true
                    description: 'Optional A/B significance analysis (p-value, sample size, effect size) when the eval compared
                      two model variants. Null when only a single model was evaluated.

                      '
                    additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/feature-flags:
    get:
      operationId: feature_flags_get
      summary: Returns boolean feature flags evaluated for the authenticated org. Flag keys and values vary by org configuration;
        unkno
      tags:
      - feature_flags
      x-octomil-method: feature_flags.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns boolean feature flags evaluated for the authenticated org. Flag keys and values vary by org configuration;
        unknown flags should default to false on the client side.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Boolean feature flags evaluated for the authenticated organization.
                additionalProperties:
                  type: boolean
                example:
                  new_onboarding: false
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/rounds/{round_id}/heartbeat:
    post:
      operationId: federation_heartbeat
      summary: Send a liveness and progress heartbeat to the server during federated round participation.
      tags:
      - federation
      x-octomil-method: federation.heartbeat
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - server updates participation record with current state and progress
      - may extend participation deadline if server detects progress
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Send a liveness and progress heartbeat to the server during federated round participation.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - participation_id
              properties:
                device_id:
                  type: string
                  description: Device identifier submitting the heartbeat.
                participation_id:
                  type: string
                  description: Participation record identifier for this round.
                deviceId:
                  type: string
                  description: Alias for device_id (camelCase legacy field).
                participationToken:
                  type: string
                  description: Alias for participation_id (camelCase legacy field).
                state:
                  type: string
                  description: Current FederatedParticipationState enum value.
                progressPercent:
                  type: number
                  minimum: 0
                  maximum: 100
                  description: Training progress 0-100 if state is local_training.
                samplesProcessed:
                  type: integer
                  minimum: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  ack:
                    type: boolean
                  abort:
                    type: boolean
                    description: If true, server requests the device abort participation for this round.
                  abortReason:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/rounds/{round_id}/join:
    post:
      operationId: federation_join
      summary: Accept a federated round participation offer. Server records the participation and returns ids the device uses
        for subse
      tags:
      - federation
      x-octomil-method: federation.join
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a FederationParticipation row
      - emits telemetry event (federation.device_joined)
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      - training_failed
      description: Accept a federated round participation offer. Server records the participation and returns ids the device
        uses for subsequent federation.* calls.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              properties:
                device_id:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - participation_id
                - round_id
                - accepted
                properties:
                  participation_id:
                    type: string
                    description: ID for this device's participation in the round.
                  round_id:
                    type: string
                  accepted:
                    type: boolean
                  deadline:
                    type: string
                    format: date-time
                    nullable: true
                  plan_id:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/rounds/offers:
    get:
      operationId: federation_offers
      summary: Poll for available federated round participation offers for this device.
      tags:
      - federation
      x-octomil-method: federation.offers
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server records device poll timestamp
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Poll for available federated round participation offers for this device.
      parameters:
      - name: deviceId
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  offers:
                    type: array
                    items:
                      type: object
                      required:
                      - roundId
                      - jobId
                      - expiresAt
                      properties:
                        roundId:
                          type: string
                        jobId:
                          type: string
                        modelId:
                          type: string
                          description: Base model the round targets; device can pre-check eligibility.
                        expiresAt:
                          type: string
                          format: date-time
                        estimatedDurationSeconds:
                          type: integer
                          description: Server estimate for local training wall time.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/plans/{plan_id}:
    get:
      operationId: federation_plan
      summary: Fetch the training plan for a federated round after joining. Contains hyperparameters, privacy config, and
        data constrai
      tags:
      - federation
      x-octomil-method: federation.plan
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server records plan fetch timestamp for this participation
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - training_not_supported
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch the training plan for a federated round after joining. Contains hyperparameters, privacy config,
        and data constraints.
      parameters:
      - name: plan_id
        in: path
        required: true
        schema:
          type: string
      - name: deviceId
        in: query
        required: true
        schema:
          type: string
      - name: participationToken
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/training_plan'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/rounds/{round_id}/updates/{upload_id}/complete:
    post:
      operationId: federation_upload_complete
      summary: Notify the server that the training-update payload upload is complete. Server verifies integrity (sha256 match
        against t
      tags:
      - federation
      x-octomil-method: federation.upload_complete
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server verifies payload sha256 against upload session record
      - server marks device contribution as received
      - emits telemetry event (federation.update_received)
      - may trigger aggregation if quorum is reached
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - weight_upload_failed
      - training_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Notify the server that the training-update payload upload is complete. Server verifies integrity (sha256
        match against the initiation record) and marks the contribution as received.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: upload_id
        in: path
        required: true
        schema:
          type: string
        description: Upload session identifier from federation.upload_initiate.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - participation_id
              properties:
                device_id:
                  type: string
                participation_id:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - upload_id
                properties:
                  upload_id:
                    type: string
                  verified:
                    type: boolean
                    description: True once the server has verified the uploaded payload's sha256 against the initiation manifest.
                  message:
                    type: string
                    description: Human-readable status.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federation/rounds/{round_id}/updates/initiate:
    post:
      operationId: federation_upload_initiate
      summary: Initiate a training-update upload session, returning a pre-signed PUT URL for the encrypted payload.
      tags:
      - federation
      x-octomil-method: federation.upload_initiate
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - server creates an upload session record
      - server generates a short-lived pre-signed PUT URL
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - training_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Initiate a training-update upload session, returning a pre-signed PUT URL for the encrypted payload.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - participation_id
              - update_bytes
              - update_sha256
              properties:
                device_id:
                  type: string
                participation_id:
                  type: string
                update_bytes:
                  type: integer
                  minimum: 1
                  description: Total size in bytes of the encrypted training update payload.
                update_sha256:
                  type: string
                  pattern: ^[a-fA-F0-9]{64}$
                  description: SHA-256 hex digest of the encrypted update payload.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - upload_id
                - upload_url
                - expires_at
                properties:
                  upload_id:
                    type: string
                    description: Opaque upload session identifier.
                  upload_url:
                    type: string
                    format: uri
                    description: Pre-signed PUT URL for uploading the encrypted payload.
                  expires_at:
                    type: string
                    format: date-time
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations:
    post:
      operationId: federations_create
      summary: Create a new Federation. The creating org becomes the owner; other orgs can join via federations.join with
        a token issue
      tags:
      - federations
      x-octomil-method: federations.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a Federation row + the owner's FederationMember
      - mints an initial invite token
      - emits telemetry event (federation.created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new Federation. The creating org becomes the owner; other orgs can join via federations.join with
        a token issued by federations.invite.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_id
              - name
              properties:
                org_id:
                  type: string
                  description: Identifier of the org creating and owning the federation.
                name:
                  type: string
                description:
                  type: string
                privacy_policy:
                  type: object
                  description: Differential-privacy, secure-aggregation, etc. settings. See schemas/core/federation_privacy_policy.json.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The persisted Federation row, including a join token the owner can share with member orgs.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: federations_list
      summary: List Federations the caller's org belongs to or owns.
      tags:
      - federations
      x-octomil-method: federations.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List Federations the caller's org belongs to or owns.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of federations entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations/{federation_id}:
    get:
      operationId: federations_get
      summary: Fetch a single Federation by id.
      tags:
      - federations
      x-octomil-method: federations.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single Federation by id.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Federation row with eager-loaded member orgs + active rounds count.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations/{federation_id}/invite:
    post:
      operationId: federations_invite
      summary: Invite one or more organizations to join a federation. Each org_id receives a FederationMember row with status=invited
        i
      tags:
      - federations
      x-octomil-method: federations.invite
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates FederationMember rows with status=invited for new org_ids
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Invite one or more organizations to join a federation. Each org_id receives a FederationMember row with
        status=invited if none exists. If a member row already exists for an org_id, it is returned unchanged (idempotent
        per org). The invited org must subsequently call federations.join to become an active member.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
        description: ID of the federation to which organizations are being invited.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_ids
              properties:
                org_ids:
                  type: array
                  items:
                    type: string
                  minItems: 1
                  description: "List of organization IDs to invite. Each org receives a FederationMember row with status=invited.\
                    \ Idempotent per org_id \u2014 already-existing members are returned unchanged.\n"
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    federation_id:
                      type: string
                    org_id:
                      type: string
                    status:
                      type: string
                      description: Member status. Invited orgs start at "invited".
                    created_at:
                      type: string
                      format: date-time
                    updated_at:
                      type: string
                      format: date-time
                description: 'Array of FederationMember records for all org_ids in the request, including any that were already
                  members.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations/{federation_id}/join:
    post:
      operationId: federations_join
      summary: Join a Federation as a member. The caller's org becomes a member; subsequent training rounds can include the
        org's devic
      tags:
      - federations
      x-octomil-method: federations.join
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a FederationMember row (org_id, role=member)
      - invalidates the consumed invite token
      - emits telemetry event (federation.joined)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Join a Federation as a member. The caller's org becomes a member; subsequent training rounds can include
        the org's devices.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_id
              properties:
                org_id:
                  type: string
                  description: Identifier of the org joining the federation.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The newly-created FederationMember row + acknowledgment.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations/{federation_id}/members:
    get:
      operationId: federations_list_members
      summary: List orgs that are members of the federation. Powers the federation detail page's member roster.
      tags:
      - federations
      x-octomil-method: federations.list_members
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List orgs that are members of the federation. Powers the federation detail page's member roster.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of members entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/federations/{federation_id}/models:
    get:
      operationId: federations_models_list
      summary: Returns all models shared with a federation. Federation members use this to discover which models they can
        contribute tr
      tags:
      - federations
      x-octomil-method: federations.models.list
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns all models shared with a federation. Federation members use this to discover which models they
        can contribute training updates to.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
        description: ID of the federation whose shared models to list.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    name:
                      type: string
                    org_id:
                      type: string
                    framework:
                      type: string
                    use_case:
                      type: string
                    federation_id:
                      type: string
                    description:
                      type: string
                      nullable: true
                description: 'List of models that have been shared with the specified federation (i.e., all Model rows where
                  federation_id matches).

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      operationId: federations_models_share
      summary: 'Associates a model with a federation so that federation members can contribute training updates to it. Sets
        the model''s '
      tags:
      - federations
      x-octomil-method: federations.models.share
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets Model.federation_id on the target model row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Associates a model with a federation so that federation members can contribute training updates to it.
        Sets the model's federation_id field. Only the owning organization of both the model and the federation may perform
        this operation.
      parameters:
      - name: federation_id
        in: path
        required: true
        schema:
          type: string
        description: ID of the federation to share the model with.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              properties:
                model_id:
                  type: string
                  description: 'ID of the model to share. The model''s owning org must match the federation''s owning org.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  org_id:
                    type: string
                  framework:
                    type: string
                  use_case:
                    type: string
                  federation_id:
                    type: string
                    description: Set to the federation_id after sharing.
                  description:
                    type: string
                    nullable: true
                description: 'The updated model record with federation_id populated.

                  '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/inference:
    post:
      operationId: inference_create
      summary: 'Single non-streaming cloud inference. Server proxies to the configured LLM backend and returns the full response
        in one '
      tags:
      - inference
      x-octomil-method: inference.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - consumes compute (cloud)
      - emits inference.started + inference.completed (or inference.failed) telemetry
      - writes an InferenceSession row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - model_disabled
      - inference_failed
      - context_too_large
      - rate_limited
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Single non-streaming cloud inference. Server proxies to the configured LLM backend and returns the full
        response in one shot.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              properties:
                model_id:
                  type: string
                  description: Model identifier (catalog variant slug + optional version).
                input_data:
                  description: 'Raw prompt or structured input. Mutually-exclusive with `messages`; one must be supplied or
                    the server returns 422.

                    '
                messages:
                  type: array
                  description: 'Chat-completions format. Use either this or `input_data`, not both. Each entry is `{role,
                    content}`.

                    '
                  items:
                    type: object
                    required:
                    - role
                    - content
                    properties:
                      role:
                        type: string
                        description: system / user / assistant.
                      content:
                        type: string
                parameters:
                  type: object
                  additionalProperties: true
                  description: Inference parameters (temperature, max_tokens, etc.).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - output
                - latency_ms
                - provider
                - model_id
                properties:
                  output:
                    description: Inference output. Shape depends on the model + input format.
                  latency_ms:
                    type: number
                    description: End-to-end latency in ms.
                  provider:
                    type: string
                    description: Backend that served the request (octomil / openai / anthropic / groq / together).
                  model_id:
                    type: string
                  session_id:
                    type: string
                    nullable: true
                    description: Telemetry session ID for correlating with InferenceSession rows.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/inference/kv-cache-config:
    get:
      operationId: inference_kv_cache_config
      summary: Return the recommended KV cache configuration for a model/device-class combination. No authentication required.
        The runt
      tags:
      - inference
      x-octomil-method: inference.kv_cache_config
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the recommended KV cache configuration for a model/device-class combination. No authentication required.
        The runtime_config object is intended to be merged directly into engine init parameters.
      parameters:
      - name: runtime
        in: query
        required: true
        schema:
          type: string
        description: Runtime executor slug (e.g. llama_cpp, mlx, nnapi).
      - name: ram_gb
        in: query
        required: true
        schema:
          type: number
        description: Device RAM in GB (must be > 0).
      - name: context_length
        in: query
        required: false
        schema:
          type: integer
        description: Planned context length in tokens. Defaults to 8192.
      - name: model
        in: query
        required: false
        schema:
          type: string
        description: Optional model name for model-specific planning.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - enabled
                - compression_enabled
                - strategy
                - residual_window_tokens
                - recent_window_tokens
                - backend
                - maturity
                - runtime_config
                properties:
                  enabled:
                    type: boolean
                  compression_enabled:
                    type: boolean
                  strategy:
                    type: string
                  bits:
                    type: integer
                    nullable: true
                  residual_window_tokens:
                    type: integer
                  recent_window_tokens:
                    type: integer
                  estimated_compression_ratio:
                    type: number
                    nullable: true
                  projected_context_multiplier:
                    type: number
                    nullable: true
                  backend:
                    type: string
                  maturity:
                    type: string
                  backend_profile:
                    type: string
                    nullable: true
                  runtime_config:
                    type: object
                    additionalProperties: true
                  notes:
                    type: array
                    items:
                      type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/inference/speculative-config:
    get:
      operationId: inference_speculative_config
      summary: Return the recommended speculative decoding configuration for a model/device combination. No authentication
        required. en
      tags:
      - inference
      x-octomil-method: inference.speculative_config
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the recommended speculative decoding configuration for a model/device combination. No authentication
        required. enabled=true requires >= 6 GB RAM and a supported chip family.
      parameters:
      - name: model_id
        in: query
        required: false
        schema:
          type: string
      - name: device_class
        in: query
        required: false
        schema:
          type: string
      - name: total_memory_mb
        in: query
        required: false
        schema:
          type: integer
      - name: chip_family
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - enabled
                properties:
                  enabled:
                    type: boolean
                    description: 'Whether speculative decoding is recommended for this device/model. true requires >= 6 GB
                      RAM and a supported chip family.

                      '
                  draft_model_id:
                    type: string
                    nullable: true
                    description: Recommended draft model for speculative decoding, if enabled.
                  config:
                    type: object
                    additionalProperties: true
                    nullable: true
                    description: Speculative decoding configuration parameters.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/inference/stream:
    post:
      operationId: inference_stream
      summary: Streaming inference. Same routing semantics as inference.create but the response is text/event-stream and the
        SDK can st
      tags:
      - inference
      x-octomil-method: inference.stream
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - stream_interrupted
      x-octomil-side-effects:
      - consumes compute
      - emits inference.started + (incremental inference.chunk events) + inference.completed/failed telemetry
      - increments usage counters as tokens are produced (per-token billing model)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - model_disabled
      - version_not_found
      - inference_failed
      - context_too_large
      - rate_limited
      - cancelled
      - stream_interrupted
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Streaming inference. Same routing semantics as inference.create but the response is text/event-stream and
        the SDK can start consuming tokens before the model is finished.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              properties:
                model_id:
                  type: string
                  description: Model identifier (catalog variant slug + optional version).
                input_data:
                  description: Raw prompt or structured input. Mutually-exclusive with messages.
                messages:
                  type: array
                  description: Chat-completions format; use either this or input_data.
                  items:
                    type: object
                    required:
                    - role
                    - content
                    properties:
                      role:
                        type: string
                      content:
                        type: string
                parameters:
                  type: object
                  additionalProperties: true
      responses:
        '200':
          description: 'Server-Sent Events stream. Each event is a JSON-encoded ResponseStreamEvent. Terminal event has `type:
            "done"`; SDKs should close the connection after receiving it.

            '
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/response_stream_event'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      x-octomil-stream: sse
      x-octomil-stream-event-schema: response_stream_event
      x-octomil-stream-terminal-event: done
  /api/v1/jobs/{job_id}/cancel:
    post:
      operationId: jobs_cancel
      summary: Cancel a non-terminal job. Sets status=cancelled and finished_at=now(). Jobs in 'completed' or 'failed' status
        cannot be
      tags:
      - jobs
      x-octomil-method: jobs.cancel
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Updates JobRecord.status to cancelled and sets finished_at.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - job_not_found
      - job_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Cancel a non-terminal job. Sets status=cancelled and finished_at=now(). Jobs in 'completed' or 'failed'
        status cannot be cancelled (400).
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Job'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/jobs:
    post:
      operationId: jobs_create
      summary: 'Enqueue a new background job. For job_type=''model.conversion'', checks PlanGateService.check_and_consume()
        first; 402 if '
      tags:
      - jobs
      x-octomil-method: jobs.create
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - Inserts a JobRecord row.
      - Decrements model_conversions_monthly for job_type='model.conversion'.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - plan_limit_exceeded
      - network_unavailable
      - request_timeout
      - server_error
      description: Enqueue a new background job. For job_type='model.conversion', checks PlanGateService.check_and_consume()
        first; 402 if limit exceeded. scheduled_at = now() + delay_seconds.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - job_type
              properties:
                job_type:
                  type: string
                  description: Dotted type. 'model.conversion' triggers plan gate check.
                payload:
                  type: object
                  additionalProperties: true
                  default: {}
                priority:
                  type: integer
                  default: 100
                max_attempts:
                  type: integer
                  default: 3
                delay_seconds:
                  type: integer
                  default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Job'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: jobs_list
      summary: List background jobs for the specified org. job_type filter is applied in-process after the DB fetch.
      tags:
      - jobs
      x-octomil-method: jobs.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List background jobs for the specified org. job_type filter is applied in-process after the DB fetch.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
      - name: job_type
        in: query
        required: false
        schema:
          type: string
        description: Applied in-process after DB query.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  schema: jobs/Job.json
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/jobs/{job_id}:
    get:
      operationId: jobs_get
      summary: Retrieve a single job by ID. No org scoping.
      tags:
      - jobs
      x-octomil-method: jobs.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - job_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a single job by ID. No org scoping.
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Job'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/jobs/{job_id}/logs:
    get:
      operationId: jobs_logs
      summary: Return log entries associated with a job. Fetched via ILIKE '%{job_id}%' on LogEntry.message, ordered by created_at
        DESC
      tags:
      - jobs
      x-octomil-method: jobs.logs
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "Return log entries associated with a job. Fetched via ILIKE '%{job_id}%' on LogEntry.message, ordered\
        \ by created_at DESC. No FK relationship \u2014 string-match heuristic."
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  schema: jobs/JobLogEntry.json
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/jobs/overview:
    get:
      operationId: jobs_overview
      summary: Return aggregated job queue summary. Fetches up to 500 jobs and computes counts by status and job_type in memory.
        active
      tags:
      - jobs
      x-octomil-method: jobs.overview
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated job queue summary. Fetches up to 500 jobs and computes counts by status and job_type
        in memory. active_total = running + in_progress; recent_jobs = first 10 of 500.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobsOverview'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/jobs/{job_id}/retry:
    post:
      operationId: jobs_retry
      summary: Retry a failed or cancelled job. Resets status=queued, clears error_message, result, progress_pct, scheduled_at=now().
        D
      tags:
      - jobs
      x-octomil-method: jobs.retry
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - 'Updates JobRecord: status=queued, error_message=null, result=null, progress_pct=0, scheduled_at=now(), started_at=null,
        finished_at=null.'
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - job_not_found
      - job_state_invalid
      - network_unavailable
      - request_timeout
      - server_error
      description: Retry a failed or cancelled job. Resets status=queued, clears error_message, result, progress_pct, scheduled_at=now().
        Does NOT reset the attempts counter.
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Job'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/logs/audit:
    get:
      operationId: logs_audit
      summary: Retrieve the audit trail of security-relevant log entries for an org. Subset of the full log stream limited
        to audit-cla
      tags:
      - logs
      x-octomil-method: logs.audit
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve the audit trail of security-relevant log entries for an org. Subset of the full log stream limited
        to audit-class events.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
      - name: offset
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - events
                - total
                properties:
                  events:
                    type: array
                    description: List of audit log entries.
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/logs/{event_id}:
    get:
      operationId: logs_get
      summary: Retrieve a single log entry by event_id.
      tags:
      - logs
      x-octomil-method: logs.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a single log entry by event_id.
      parameters:
      - name: event_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - level
                - message
                - org_id
                properties:
                  id:
                    type: string
                  org_id:
                    type: string
                  level:
                    type: string
                  message:
                    type: string
                  service:
                    type: string
                    nullable: true
                  trace_id:
                    type: string
                    nullable: true
                  device_id:
                    type: string
                    nullable: true
                  metadata:
                    type: object
                    additionalProperties: true
                  created_at:
                    type: string
                    format: date-time
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/log-entries:
    post:
      operationId: logs_ingest_runtime
      summary: Ingest a batch of runtime log entries from octomil serve. Creates LogEntry records that appear in the device
        activity fe
      tags:
      - logs
      x-octomil-method: logs.ingest_runtime
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - writes LogEntry rows
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Ingest a batch of runtime log entries from octomil serve. Creates LogEntry records that appear in the device
        activity feed. Logs are accepted and stored regardless of whether the device_id is registered.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - device_id
              - entries
              properties:
                device_id:
                  type: string
                  description: 'Device identifier. Logs are stored even when the device is not registered; the field is required
                    but the device need not be registered.

                    '
                org_id:
                  type: string
                  description: Org identifier. Defaults to "default" if omitted.
                model_id:
                  type: string
                  nullable: true
                entries:
                  type: array
                  description: Batch of log entries to ingest.
                  items:
                    type: object
                    required:
                    - service
                    - message
                    properties:
                      service:
                        type: string
                      level:
                        type: string
                        description: Log level (debug / info / warning / error / critical). Defaults to "info".
                      message:
                        type: string
                      metadata:
                        type: object
                        additionalProperties: true
                        nullable: true
                      timestamp:
                        type: number
                        nullable: true
                        description: Unix epoch seconds. Server time used if omitted.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - created
                properties:
                  created:
                    type: integer
                    description: Number of log entries successfully ingested.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/logs:
    get:
      operationId: logs_list
      summary: List log entries for an org with optional level, service, device, trace, and full-text filters. Supports offset-based
        pa
      tags:
      - logs
      x-octomil-method: logs.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List log entries for an org with optional level, service, device, trace, and full-text filters. Supports
        offset-based pagination.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: level
        in: query
        required: false
        schema:
          type: string
        description: Filter by log level (debug / info / warning / error / critical).
      - name: service
        in: query
        required: false
        schema:
          type: string
      - name: query
        in: query
        required: false
        schema:
          type: string
        description: Full-text filter on log message.
      - name: device_id
        in: query
        required: false
        schema:
          type: string
      - name: trace_id
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum entries per page (1\u2013500, default 100)."
      - name: offset
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - entries
                - total
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - level
                      - message
                      - org_id
                      properties:
                        id:
                          type: string
                        org_id:
                          type: string
                        level:
                          type: string
                        message:
                          type: string
                        service:
                          type: string
                          nullable: true
                        trace_id:
                          type: string
                          nullable: true
                        device_id:
                          type: string
                          nullable: true
                        metadata:
                          type: object
                          additionalProperties: true
                        created_at:
                          type: string
                          format: date-time
                          nullable: true
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/logs/query:
    post:
      operationId: logs_query
      summary: 'Advanced log query using a request body for complex filter combinations. Equivalent to logs.list but accepts
        filters as '
      tags:
      - logs
      x-octomil-method: logs.query
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Advanced log query using a request body for complex filter combinations. Equivalent to logs.list but accepts
        filters as a POST body.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org_id
              properties:
                org_id:
                  type: string
                level:
                  type: string
                  nullable: true
                service:
                  type: string
                  nullable: true
                query:
                  type: string
                  nullable: true
                  description: Full-text filter on log message.
                device_id:
                  type: string
                  nullable: true
                trace_id:
                  type: string
                  nullable: true
                limit:
                  type: integer
                  nullable: true
                offset:
                  type: integer
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - entries
                - total
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/compatibility:
    get:
      operationId: models_check_compatibility
      summary: Check whether the model's latest catalog version is compatible with the specified device profiles. The server
        resolves t
      tags:
      - models
      x-octomil-method: models.check_compatibility
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Check whether the model's latest catalog version is compatible with the specified device profiles. The
        server resolves the model's latest size from its CatalogModelVersion artifact packages, then calls OptimizationService.check_compatibility_from_metadata
        with the model's framework and optional device targets.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: devices
        in: query
        required: false
        schema:
          type: string
        description: Comma-separated list of device profile keys to check against. When omitted, all known device profiles
          are evaluated.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Compatibility report produced by OptimizationService.check_compatibility_from_metadata(). Shape
                  is service-defined. Clients should treat the payload as opaque and render only fields they recognise.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models:
    post:
      operationId: models_create
      summary: "Create a new Model in the internal registry (distinct from CatalogModelVariant \u2014 see workspace memory\
        \ \"Model Registries\""
      tags:
      - models
      x-octomil-method: models.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a Model row
      - emits telemetry event (model.created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: "Create a new Model in the internal registry (distinct from CatalogModelVariant \u2014 see workspace memory\
        \ \"Model Registries\" for the two-registry distinction). Operators create Models when they want to track a custom\
        \ or fine-tuned model that isn't in the global catalog."
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              properties:
                name:
                  type: string
                  description: Catalog identifier (joins to CatalogModelVariant.name for enrichment).
                slug:
                  type: string
                description:
                  type: string
                framework:
                  type: string
                use_case:
                  type: string
                catalog_variant_id:
                  type: string
                enabled:
                  type: boolean
                  default: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The persisted Model row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: models_list
      summary: List Models in the caller's org with extensive filter support for the dashboard's model browser.
      tags:
      - models
      x-octomil-method: models.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List Models in the caller's org with extensive filter support for the dashboard's model browser.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: framework
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 e.g. \"coreml\", \"litert\", \"llamacpp\"."
      - name: use_case
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 e.g. \"object_detection\", \"transcription\"."
      - name: capability
        in: query
        required: false
        schema:
          type: string
        description: Filter via CatalogModelVariant.task_taxonomy.
      - name: deployment_state
        in: query
        required: false
        schema:
          type: string
        description: "\"deployed\" | \"not_deployed\" \u2014 joins to active deployments."
      - name: include_auto_bridge
        in: query
        required: false
        schema:
          type: boolean
          default: true
      - name: search
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - models
                - total
                properties:
                  models:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
                    description: Unfiltered count for "showing N of M" UI.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions:
    post:
      operationId: models_create_version
      summary: 'Register a new Version for a Model. Artifacts (concrete weights / package files) are uploaded separately via
        the upload '
      tags:
      - models
      x-octomil-method: models.create_version
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a ModelVersion row in "draft" status
      - emits telemetry event (model.version_created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Register a new Version for a Model. Artifacts (concrete weights / package files) are uploaded separately
        via the upload routes; creating a Version is metadata-only.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - version
              - storage_path
              - format
              - checksum
              - size_bytes
              properties:
                version:
                  type: string
                  description: Semver string (e.g. "1.2.0"). Unique per Model.
                storage_path:
                  type: string
                  description: Path to the model artifact in object storage.
                format:
                  type: string
                  description: Serialization format of the model (e.g. onnx, gguf, coreml).
                checksum:
                  type: string
                  description: SHA-256 hex digest of the model artifact for integrity verification.
                size_bytes:
                  type: integer
                  description: Size of the model artifact in bytes.
                description:
                  type: string
                lifecycle_status:
                  type: string
                  description: Starts as "draft"; published via models.publish_version.
                metadata:
                  type: object
                  additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The created ModelVersion row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: models_list_versions
      summary: List every Version for a Model. Powers the version timeline on the model detail page.
      tags:
      - models
      x-octomil-method: models.list_versions
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List every Version for a Model. Powers the version timeline on the model detail page.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of versions entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}:
    delete:
      operationId: models_delete
      summary: Delete a Model. Cascades to all versions and their artifact records; does NOT delete the underlying R2 objects
        (left for
      tags:
      - models
      x-octomil-method: models.delete
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes Model + all ModelVersion + ModelArtifact rows
      - active deployments using this model become unhealthy (model_not_found at runtime); operators should rollback / re-target
        first
      - emits telemetry event (model.deleted)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete a Model. Cascades to all versions and their artifact records; does NOT delete the underlying R2
        objects (left for the cleanup job to GC) so an accidental delete can be recovered manually within the 7-day R2 lifecycle
        window.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Deletion summary (deleted Model.id + count of versions cascaded).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: models_get
      summary: Fetch a single model by id. Powers the model detail page.
      tags:
      - models
      x-octomil-method: models.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single model by id. Powers the model detail page.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Model row with eager-loaded versions list.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: models_update
      summary: 'Partial update of a Model. Flipping `enabled: false` immediately blocks new deployments from using the model
        but doesn''t'
      tags:
      - models
      x-octomil-method: models.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - mutates Model row per request body
      - if `enabled` flipped to false, catalog routing decisions exclude this model on next decision tick
      - emits telemetry event (model.updated)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Partial update of a Model. Flipping `enabled: false` immediately blocks new deployments from using the
        model but doesn''t affect in-flight rollouts.'
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                framework:
                  type: string
                use_case:
                  type: string
                enabled:
                  type: boolean
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The updated Model row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/{version_id}:
    delete:
      operationId: models_delete_version
      summary: Delete a Version + cascade artifacts.
      tags:
      - models
      x-octomil-method: models.delete_version
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - deletes ModelVersion + ModelArtifact rows
      - emits telemetry event (model.version_deleted)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Delete a Version + cascade artifacts.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Deletion summary.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/filter-options:
    get:
      operationId: models_filter_options
      summary: Return the set of filter values available for the models list page. Distinct frameworks come from Model.framework;
        capab
      tags:
      - models
      x-octomil-method: models.filter_options
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the set of filter values available for the models list page. Distinct frameworks come from Model.framework;
        capabilities come from CatalogModelVariant.task_taxonomy flattened across all variants whose name appears in the org's
        Model rows. Used to populate the filter panel dropdowns without a round-trip per option.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
        description: Organization ID to scope filter option enumeration.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Enumerated framework and capability values present in the org's model catalog.
                properties:
                  frameworks:
                    type: array
                    items:
                      type: string
                    description: Distinct non-null framework strings across all org models (sorted ascending).
                  capabilities:
                    type: array
                    items:
                      type: string
                    description: Distinct task_taxonomy values across all catalog variants matched to org models (sorted).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/artifacts/{artifact_id}/download-url:
    get:
      operationId: models_get_artifact_download_url
      summary: Generate a presigned download URL for the primary weights ArtifactResource associated with an artifact package.
        The mode
      tags:
      - models
      x-octomil-method: models.get_artifact_download_url
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - generates_presigned_url
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - download_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Generate a presigned download URL for the primary weights ArtifactResource associated with an artifact
        package. The model_id is verified first to enforce org-scoped access. URL expires in 3600 seconds.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
        description: Internal Model UUID. Used to verify the model exists and the caller has access.
      - name: artifact_id
        in: path
        required: true
        schema:
          type: string
        description: ArtifactPackage UUID.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  download_url:
                    type: string
                    description: Presigned S3 URL (expires in 3600 seconds).
                  filename:
                    type: string
                    description: Suggested filename derived from ArtifactResource.path or the storage key basename.
                  size_bytes:
                    type: integer
                    description: Size of the weights resource file.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/inference-metrics:
    get:
      operationId: models_get_inference_metrics
      summary: Return aggregated inference telemetry for a model within a time window. Combines InferenceSession rows (org-scoped,
        with
      tags:
      - models
      x-octomil-method: models.get_inference_metrics
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated inference telemetry for a model within a time window. Combines InferenceSession rows
        (org-scoped, within the window) with fleet Device health status. Latency metrics are computed as percentile distributions.
        The fleet block summarises device availability independent of this model's inference history.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: hours
        in: query
        required: false
        schema:
          type: integer
        description: "Lookback window in hours (1\u2013720, default 24)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  model_id:
                    type: string
                  model_name:
                    type: string
                  window_hours:
                    type: integer
                  inference:
                    type: object
                    properties:
                      total_sessions:
                        type: integer
                      completed:
                        type: integer
                      failed:
                        type: integer
                      success_rate:
                        type: number
                        description: "Percentage of completed sessions (0\u2013100)."
                      unique_devices:
                        type: integer
                      modality_breakdown:
                        type: object
                        additionalProperties:
                          type: integer
                      ttfc_ms:
                        type: object
                        description: Time-to-first-chunk latency percentiles.
                        additionalProperties: true
                      chunk_latency_ms:
                        type: object
                        additionalProperties: true
                      throughput_tokens_sec:
                        type: object
                        additionalProperties: true
                      total_duration_ms:
                        type: object
                        additionalProperties: true
                  fleet:
                    type: object
                    properties:
                      total_devices:
                        type: integer
                      online:
                        type: integer
                      stale:
                        type: integer
                      offline:
                        type: integer
                      platform_breakdown:
                        type: object
                        additionalProperties:
                          type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/optimize:
    post:
      operationId: models_get_optimization_plan
      summary: 'Generate an optimization plan for a model. The server resolves the model''s latest catalog version size and
        delegates to '
      tags:
      - models
      x-octomil-method: models.get_optimization_plan
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Generate an optimization plan for a model. The server resolves the model's latest catalog version size
        and delegates to OptimizationService.get_optimization_plan_from_metadata(). The plan describes recommended quantization
        strategies, format targets, and expected size/accuracy tradeoffs per device profile.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Optional optimization parameters.
              properties:
                target_devices:
                  type: array
                  items:
                    type: string
                  description: Device profile keys to optimize for.
                accuracy_threshold:
                  type: number
                  description: "Minimum acceptable accuracy score (0\u20131). Defaults to 0.95."
                size_budget_mb:
                  type: number
                  description: Maximum artifact size budget in megabytes. Null means no budget constraint.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Optimization plan produced by OptimizationService.get_optimization_plan_from_metadata(). Shape
                  is service-defined. Clients should treat the payload as opaque.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/optimized-config/{device_type}:
    get:
      operationId: models_get_optimized_config
      summary: Return the pre-computed optimized configuration for a specific device type. Configurations are produced by
        running `octo
      tags:
      - models
      x-octomil-method: models.get_optimized_config
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the pre-computed optimized configuration for a specific device type. Configurations are produced
        by running `octomil optimize` locally and uploading the result. This endpoint fetches the stored config artifact for
        the given model_id + device_type combination.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: device_type
        in: path
        required: true
        schema:
          type: string
        description: Device type key (e.g. iphone_15, pixel_8, generic_android).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Device-specific optimized configuration produced by OptimizationService.get_device_config().
                  Shape is service-defined.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/performance:
    get:
      operationId: models_get_performance
      summary: Return aggregated training-update statistics for a model, broken down by catalog version. Includes per-version
        update co
      tags:
      - models
      x-octomil-method: models.get_performance
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated training-update statistics for a model, broken down by catalog version. Includes per-version
        update counts, sample counts, device counts, and the latest recorded metrics dict from TrainingUpdate. Scoped to the
        caller's org (Model.org_id == auth.org_id).
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated training-update performance across all catalog versions for this model.

                  '
                properties:
                  model_id:
                    type: string
                  model_name:
                    type: string
                  versions_count:
                    type: integer
                  total_updates:
                    type: integer
                  total_devices:
                    type: integer
                  versions:
                    type: array
                    items:
                      type: object
                      properties:
                        version:
                          type: string
                        status:
                          type: string
                        created_at:
                          type: string
                        updates_count:
                          type: integer
                        samples_count:
                          type: integer
                        devices_count:
                          type: integer
                        metrics:
                          type: object
                          nullable: true
                          additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/model-versions/{version_id}:
    get:
      operationId: models_get_version
      summary: 'Fetch a Version by id without needing the parent Model.id. Used by deployment + rollout views that hold version
        ids but '
      tags:
      - models
      x-octomil-method: models.get_version
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a Version by id without needing the parent Model.id. Used by deployment + rollout views that hold
        version ids but don't always track the parent model.
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: ModelVersion row with eager-loaded artifacts list + lifecycle metadata.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/{version}/performance:
    get:
      operationId: models_get_version_performance
      summary: Return detailed training-update statistics for a specific model version. Computes per-metric percentiles (p50,
        p95, p99)
      tags:
      - models
      x-octomil-method: models.get_version_performance
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return detailed training-update statistics for a specific model version. Computes per-metric percentiles
        (p50, p95, p99) and per-device breakdowns from all TrainingUpdate rows for the model_id + version combination. Returns
        a zero-count response (not 404) when no updates exist for the version.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: version
        in: path
        required: true
        schema:
          type: string
        description: Semver version string (e.g. 1.0.0).
      - name: limit_devices
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum number of devices to include in device_breakdown (1\u2013100, default 10)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  model_id:
                    type: string
                  version:
                    type: string
                  total_updates:
                    type: integer
                  total_samples:
                    type: integer
                  total_devices:
                    type: integer
                  metrics_summary:
                    type: array
                    description: Per-metric aggregation across all training updates for this version.
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        count:
                          type: integer
                        mean:
                          type: number
                        std_dev:
                          type: number
                        min_value:
                          type: number
                        max_value:
                          type: number
                        p50:
                          type: number
                        p95:
                          type: number
                        p99:
                          type: number
                  device_breakdown:
                    type: array
                    description: Per-device update statistics, sorted by updates_count DESC, limited by limit_devices.
                    items:
                      type: object
                      additionalProperties: true
                  training_rounds:
                    type: integer
                  first_update_at:
                    type: string
                    nullable: true
                  last_update_at:
                    type: string
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/artifacts:
    get:
      operationId: models_list_artifacts
      summary: 'Return all artifact packages across every catalog version for a model. Uses CatalogModelVariant.resolve_by_name_for_org '
      tags:
      - models
      x-octomil-method: models.list_artifacts
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return all artifact packages across every catalog version for a model. Uses CatalogModelVariant.resolve_by_name_for_org
        to select the correct variant row (org-override beats blessed row on name collision). Returns [] when no catalog variant
        exists for the model name.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
        description: Internal Model UUID.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'All artifact packages across all catalog versions for this model. Each entry is one ArtifactPackage
                  row joined with its version metadata.

                  '
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    version:
                      type: string
                    status:
                      type: string
                      description: CatalogModelVersion.lifecycle (e.g. active, deprecated, preview).
                    platform:
                      type: string
                    artifact_format:
                      type: string
                    runtime_executor:
                      type: string
                    quantization:
                      type: string
                    support_tier:
                      type: string
                    total_size_bytes:
                      type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/manifest:
    get:
      operationId: models_manifest
      summary: 'Return a manifest of all models as a name-keyed map. SDK clients use this for local caching: check the ETag
        header befor'
      tags:
      - models
      x-octomil-method: models.manifest
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Return a manifest of all models as a name-keyed map. SDK clients use this for local caching: check the
        ETag header before re-parsing. The endpoint is public (no auth required) to allow CLI and CI tooling to fetch the
        manifest without an API key.'
      parameters:
      - name: platform
        in: query
        required: false
        schema:
          type: string
        description: Optional platform filter (e.g. android, ios, linux). When omitted, all platforms are included.
      - name: If-None-Match
        in: header
        required: false
        schema:
          type: string
        description: ETag from a prior response. Server returns 304 Not Modified when the manifest has not changed.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Map of model name \u2192 model entry dict. Shape of each entry is determined by Model.to_dict().\
                  \ Clients must treat unknown fields as opaque. 304 Not Modified is returned when the ETag matches.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/model-versions/{version_id}/publish:
    post:
      operationId: models_publish_version
      summary: Publish a draft ModelVersion. Validates that at least one artifact has been uploaded + checksums; flips lifecycle_status
      tags:
      - models
      x-octomil-method: models.publish_version
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets ModelVersion.lifecycle_status to "published"
      - sets released_at to now()
      - emits telemetry event (model.version_published)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Publish a draft ModelVersion. Validates that at least one artifact has been uploaded + checksums; flips
        lifecycle_status to "published"; makes the version visible to SDK clients for download.
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The published ModelVersion row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/resolve:
    get:
      operationId: models_resolve
      summary: Resolve a model by name and return its manifest entry. Used by SDK CLI tooling to look up a canonical model
        entry withou
      tags:
      - models
      x-octomil-method: models.resolve
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Resolve a model by name and return its manifest entry. Used by SDK CLI tooling to look up a canonical model
        entry without knowing its UUID. Searches Model rows by name (case-sensitive exact match).
      parameters:
      - name: name
        in: query
        required: true
        schema:
          type: string
        description: Model name to resolve.
      - name: platform
        in: query
        required: false
        schema:
          type: string
        description: Optional platform hint (currently informational; not used in server-side resolution logic).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Resolved model entry.
                properties:
                  name:
                    type: string
                  entry:
                    type: object
                    description: Model.to_dict() output for the resolved model.
                    additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/model-versions/{version_id}/deprecate:
    post:
      operationId: models_version_deprecate
      summary: Transition a catalog version's lifecycle to "deprecated". Only versions with lifecycle == "active" can be deprecated.
        Th
      tags:
      - models
      x-octomil-method: models.version.deprecate
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - modifies_catalog_version_lifecycle
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Transition a catalog version's lifecycle to "deprecated". Only versions with lifecycle == "active" can
        be deprecated. The operation sets CatalogModelVersion.lifecycle = "deprecated" and commits.
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Updated version dict (same shape as models.get_version response).
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/model-versions/{version_id}/download-urls:
    get:
      operationId: models_version_get_download_urls
      summary: Generate presigned download URLs for all stored resources of a catalog version. Iterates all ArtifactPackage
        rows for th
      tags:
      - models
      x-octomil-method: models.version.get_download_urls
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - generates_presigned_url
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - download_failed
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Generate presigned download URLs for all stored resources of a catalog version. Iterates all ArtifactPackage
        rows for the version, then all ArtifactResource rows with a non-null storage_key. Resources that fail presigning are
        silently skipped. Returns {source: "catalog", resources: []} when no resources have storage_keys.'
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
      - name: expires_in
        in: query
        required: false
        schema:
          type: integer
        description: "Presigned URL TTL in seconds (60\u201386400, default 3600)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  source:
                    type: string
                    description: Always "catalog" for v2 catalog versions.
                  resources:
                    type: array
                    description: Presigned download URLs for all ArtifactResource rows with storage_key set.
                    items:
                      type: object
                      properties:
                        resource_id:
                          type: string
                        kind:
                          type: string
                          description: ArtifactResourceKind (weights, tokenizer, model_config, etc.).
                        filename:
                          type: string
                        size_bytes:
                          type: integer
                        download_url:
                          type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/model-versions/{version_id}/artifacts:
    get:
      operationId: models_version_list_artifacts
      summary: 'List all artifact packages for a specific catalog version. Does not require model_id; the version_id directly
        addresses '
      tags:
      - models
      x-octomil-method: models.version.list_artifacts
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - version_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List all artifact packages for a specific catalog version. Does not require model_id; the version_id directly
        addresses the CatalogModelVersion row. Returns [] when no packages exist for the version.
      parameters:
      - name: version_id
        in: path
        required: true
        schema:
          type: string
        description: CatalogModelVersion UUID.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: All ArtifactPackage rows for this version.
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    version_id:
                      type: string
                    platform:
                      type: string
                    artifact_format:
                      type: string
                    runtime_executor:
                      type: string
                    quantization:
                      type: string
                    support_tier:
                      type: string
                    total_size_bytes:
                      type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/confirm-upload:
    post:
      operationId: models_versions_confirm_upload
      summary: Create the catalog version record after a successful presigned PUT upload. Verifies the object exists in S3
        before creat
      tags:
      - models
      x-octomil-method: models.versions.confirm_upload
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates_catalog_version
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - download_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Create the catalog version record after a successful presigned PUT upload. Verifies the object exists
        in S3 before creating the ArtifactPackage and ArtifactResource rows. Auto-creates the internal Model row on first
        confirm for slug-shaped model_ids (OCT-46 #3). Framework is derived from the storage_key file extension.'
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              content_type: application/x-www-form-urlencoded
              required:
              - version
              - storage_key
              properties:
                version:
                  type: string
                storage_key:
                  type: string
                file_size:
                  type: integer
                description:
                  type: string
                platform:
                  type: string
                capability:
                  type: string
              fields:
                version:
                  type: string
                  required: true
                  description: Semantic version string matching what was used to get the presigned URL.
                storage_key:
                  type: string
                  required: true
                  description: The storage_key returned by models.versions.get_upload_url.
                file_size:
                  type: integer
                  required: false
                  description: File size in bytes (default 0).
                description:
                  type: string
                  required: false
                platform:
                  type: string
                  required: false
                capability:
                  type: string
                  required: false
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Version record created in the catalog.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/upload-url:
    post:
      operationId: models_versions_get_upload_url
      summary: Return a presigned S3 PUT URL so the client can upload a model artifact directly to object storage, bypassing
        the server
      tags:
      - models
      x-octomil-method: models.versions.get_upload_url
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - generates_presigned_url
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return a presigned S3 PUT URL so the client can upload a model artifact directly to object storage, bypassing
        the server. The storage key is derived from org_id/model_name/version/format+extension. After a successful PUT, call
        models.versions.confirm_upload to create the version record.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              content_type: application/x-www-form-urlencoded
              required:
              - version
              - filename
              properties:
                version:
                  type: string
                filename:
                  type: string
                content_type:
                  type: string
              fields:
                version:
                  type: string
                  required: true
                  description: Semantic version string.
                filename:
                  type: string
                  required: true
                  description: Filename of the artifact to upload (used to derive storage key and format).
                content_type:
                  type: string
                  required: false
                  description: MIME type of the artifact (default "application/octet-stream").
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  upload_url:
                    type: string
                    description: Presigned S3 PUT URL.
                  storage_key:
                    type: string
                    description: Object storage key where the file should be PUT.
                  method:
                    type: string
                    description: Always "PUT".
                  expires_in:
                    type: integer
                    description: URL TTL in seconds (always 3600).
                  headers:
                    type: object
                    description: Headers that must be included in the PUT request (includes Content-Type).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/upload:
    post:
      operationId: models_versions_upload
      summary: Upload a model weights file and convert it to one or more mobile artifact formats server-side. Supports PyTorch
        (.pt) an
      tags:
      - models
      x-octomil-method: models.versions.upload
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates_catalog_version
      - uploads_to_object_storage
      - consumes_plan_quota
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - plan_limit_exceeded
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Upload a model weights file and convert it to one or more mobile artifact formats server-side. Supports
        PyTorch (.pt) and ONNX (.onnx) inputs. Server runs Docker-based conversion for .pt files. Enforces plan gate (model_versions_monthly)
        before accepting the upload. Auto-creates the internal Model row on first upload for slug-shaped model_ids (OCT-46
        #3).'
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
        description: Internal Model UUID or slug. Auto-creates a Model row if slug is not a UUID and does not exist.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              content_type: multipart/form-data
              required:
              - file
              - version
              properties:
                file:
                  type: string
                  format: binary
                version:
                  type: string
                onnx_data:
                  type: string
                  format: binary
                description:
                  type: string
                formats:
                  type: string
                architecture:
                  type: string
                input_dim:
                  type: integer
                hidden_dim:
                  type: integer
                output_dim:
                  type: integer
                platform:
                  type: string
                capability:
                  type: string
              fields:
                file:
                  type: file
                  required: true
                  description: Model weights file (.pt TorchScript recommended; .safetensors, .onnx supported).
                onnx_data:
                  type: file
                  required: false
                  description: Optional ONNX external data file (.onnx_data).
                version:
                  type: string
                  required: true
                  description: Semantic version string (e.g. 1.0.0).
                description:
                  type: string
                  required: false
                formats:
                  type: string
                  required: false
                  description: Comma-separated target artifact formats (default "onnx").
                architecture:
                  type: string
                  required: false
                  description: Model architecture name for TorchScript export (e.g. tiny_mlp).
                input_dim:
                  type: integer
                  required: false
                  description: Input dimension for ONNX export (default 16).
                hidden_dim:
                  type: integer
                  required: false
                  description: Hidden dimension for tiny MLP (default 8).
                output_dim:
                  type: integer
                  required: false
                  description: Output dimension (default 4).
                platform:
                  type: string
                  required: false
                  description: Target deployment platform (android, ios, linux, macos, windows, browser). Inferred from file
                    format when omitted.
                capability:
                  type: string
                  required: false
                  description: Model capability (chat, transcription, embedding, etc.). Inferred from format when omitted.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/upload-bundle:
    post:
      operationId: models_versions_upload_bundle
      summary: 'Upload a primary weights file plus N sidecar files as a single coherent artifact bundle. All files are grouped
        into ONE '
      tags:
      - models
      x-octomil-method: models.versions.upload_bundle
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates_catalog_version
      - uploads_to_object_storage
      - consumes_plan_quota
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - plan_limit_exceeded
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Upload a primary weights file plus N sidecar files as a single coherent artifact bundle. All files are
        grouped into ONE ArtifactPackage row with N+1 ArtifactResource rows. This is required for models that need sidecar
        files to be co-located at runtime (e.g. piper-vits TTS: model.onnx + tokens.txt + espeak-ng-data). Enforces plan gate
        (model_versions_monthly). len(extras) MUST equal len(extras_kind); 422 on mismatch.'
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              content_type: multipart/form-data
              required:
              - file
              - version
              properties:
                file:
                  type: string
                  format: binary
                version:
                  type: string
                extras:
                  type: array
                  items:
                    type: string
                    format: binary
                extras_kind:
                  type: array
                  items:
                    type: string
                platform:
                  type: string
                capability:
                  type: string
              fields:
                file:
                  type: file
                  required: true
                  description: Primary weights file (.onnx, .gguf, etc.).
                extras:
                  type: array
                  items:
                    type: file
                  required: false
                  description: Sidecar files (tokenizer, vocab, espeak-ng data, etc.).
                extras_kind:
                  type: array
                  items:
                    type: string
                  required: false
                  description: 'One ArtifactResourceKind value per sidecar file, in the same order as extras. Valid values:
                    tokenizer, tokenizer_config, model_config, vocab, generation_config, processor, merges, adapter, manifest,
                    signature, projector, metadata.

                    '
                version:
                  type: string
                  required: true
                platform:
                  type: string
                  required: false
                capability:
                  type: string
                  required: false
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/models/{model_id}/versions/upload-converted:
    post:
      operationId: models_versions_upload_converted
      summary: "Upload pre-converted model artifact files \u2014 no server-side conversion. Creates one ArtifactPackage row\
        \ per file. Use thi"
      tags:
      - models
      x-octomil-method: models.versions.upload_converted
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates_catalog_version
      - uploads_to_object_storage
      - consumes_plan_quota
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - plan_limit_exceeded
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Upload pre-converted model artifact files \u2014 no server-side conversion. Creates one ArtifactPackage\
        \ row per file. Use this path when artifacts are produced locally via `octomil convert`. Enforces plan gate (model_versions_monthly).\
        \ Note: for coherent multi-file bundles (e.g. ONNX + tokenizer + sidecar) use models.versions.upload_bundle instead,\
        \ which groups all files into a single ArtifactPackage."
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
        description: Internal Model UUID or slug.
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              content_type: multipart/form-data
              required:
              - files
              - version
              properties:
                files:
                  type: array
                  items:
                    type: string
                    format: binary
                version:
                  type: string
                description:
                  type: string
                metadata_json:
                  type: string
                platform:
                  type: string
                capability:
                  type: string
              fields:
                files:
                  type: array
                  items:
                    type: file
                  required: true
                  description: One or more already-converted model artifact files.
                version:
                  type: string
                  required: true
                  description: Semantic version string.
                description:
                  type: string
                  required: false
                metadata_json:
                  type: string
                  required: false
                  description: Optional JSON string with extra metadata. Validated for syntax; not persisted.
                platform:
                  type: string
                  required: false
                  description: Target deployment platform. Inferred from file extension when omitted.
                capability:
                  type: string
                  required: false
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/activity:
    get:
      operationId: monitoring_activity
      summary: 'Unified org-wide activity feed of deployment events and cloud usage, newest first. Supports cursor-based pagination
        via '
      tags:
      - monitoring
      x-octomil-method: monitoring.activity
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Unified org-wide activity feed of deployment events and cloud usage, newest first. Supports cursor-based
        pagination via the before parameter.
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum events to return (1\u2013100, default 20)."
      - name: before
        in: query
        required: false
        schema:
          type: string
        description: "Cursor \u2014 return events before this timestamp or ID."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: Activity feed event (deployment event or cloud usage record).
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/alerts:
    post:
      operationId: monitoring_alerts_create
      summary: 'Create an alert rule that watches a named metric and fires when it crosses the configured threshold over the
        evaluation '
      tags:
      - monitoring
      x-octomil-method: monitoring.alerts.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Inserts a new `AlertRule` row.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create an alert rule that watches a named metric and fires when it crosses the configured threshold over
        the evaluation window. Returns the new rule's identity; call `monitoring.alerts.get` for the full object.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without creating a duplicate alert rule.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - metric_name
              - threshold
              properties:
                name:
                  type: string
                  description: Human-readable rule name; surfaced in alert notifications.
                alert_type:
                  type: string
                  default: threshold
                  description: e.g. "threshold", "anomaly". Default `"threshold"`.
                severity:
                  type: string
                  default: warning
                  description: e.g. "warning", "critical". Default `"warning"`.
                metric_name:
                  type: string
                  description: Name of the metric the rule watches (matches metric names emitted into the logs pipeline).
                threshold:
                  type: number
                  description: Numeric threshold; the rule fires when the metric crosses this.
                window_minutes:
                  type: integer
                  default: 5
                  description: Evaluation window in minutes (maps to server-side `threshold_duration_minutes`).
                enabled:
                  type: boolean
                  default: true
                notification_channels:
                  type: array
                  items:
                    type: string
                  description: List of channel IDs/URIs to notify when the rule fires. Server currently stores but does not
                    yet route on this field.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - name
                - alert_type
                - enabled
                properties:
                  id:
                    type: string
                    description: Newly created alert rule UUID.
                  name:
                    type: string
                  alert_type:
                    type: string
                  enabled:
                    type: boolean
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: monitoring_alerts_list
      summary: List alert rules in an org. Returns a bare array (not envelope) because alert rules are bounded and the dashboard
        render
      tags:
      - monitoring
      x-octomil-method: monitoring.alerts.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List alert rules in an org. Returns a bare array (not envelope) because alert rules are bounded and the
        dashboard renders them in a single table.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: enabled
        in: query
        required: false
        schema:
          type: boolean
        description: 'Optional filter. `true` returns only enabled rules; `false` only disabled. Omit to return both.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: "Bare array of alert-rule summary objects, newest first by `created_at`. NOT paginated \u2014\
                  \ alert rules are bounded per org (typically O(10s)), so a single response is fine.\n"
                items:
                  type: object
                  required:
                  - id
                  - name
                  - enabled
                  properties:
                    id:
                      type: string
                      description: Alert rule UUID.
                    name:
                      type: string
                    alert_type:
                      type: string
                      description: e.g. "threshold", "anomaly". String form, not enum-typed server-side.
                    severity:
                      type: string
                      description: e.g. "warning", "critical".
                    metric_name:
                      type: string
                    threshold:
                      type: number
                      description: Legacy alias for `threshold_value`. Both fields carry the same number; new clients should
                        prefer `threshold_value`.
                    threshold_value:
                      type: number
                    window_minutes:
                      type: integer
                      description: Legacy alias for `threshold_duration_minutes`.
                    threshold_duration_minutes:
                      type: integer
                    enabled:
                      type: boolean
                    created_at:
                      type: string
                      format: date-time
                      nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/alerts/{rule_id}:
    delete:
      operationId: monitoring_alerts_delete
      summary: Delete an alert rule. Verifies org_id ownership before deletion.
      tags:
      - monitoring
      x-octomil-method: monitoring.alerts.delete
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - deletes alert_rules row
      x-octomil-errors:
      - authentication_failed
      - alert_rule_not_found
      description: Delete an alert rule. Verifies org_id ownership before deletion.
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: monitoring_alerts_get
      summary: Get full details for a single alert rule. Scoped by org_id.
      tags:
      - monitoring
      x-octomil-method: monitoring.alerts.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - alert_rule_not_found
      description: Get full details for a single alert rule. Scoped by org_id.
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRuleResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: monitoring_alerts_update
      summary: Partially update an alert rule. Only whitelisted fields are applied.
      tags:
      - monitoring
      x-octomil-method: monitoring.alerts.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates alert_rules row
      x-octomil-errors:
      - authentication_failed
      - alert_rule_not_found
      - invalid_input
      description: Partially update an alert rule. Only whitelisted fields are applied.
      parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAlertRuleRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRuleResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}:
    get:
      operationId: monitoring_apps_get
      summary: Return aggregated monitoring metrics for a single app. Computed by MonitoringService.get_app_metrics(). Powers
        the app d
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated monitoring metrics for a single app. Computed by MonitoringService.get_app_metrics().
        Powers the app detail page monitoring tab.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated monitoring metrics for an app. Shape is service-defined. Clients must treat the payload
                  as opaque.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/capabilities/{capability}:
    get:
      operationId: monitoring_apps_get_capability
      summary: Return monitoring metrics for a specific capability within an app. Computed by MonitoringService.get_capability_metrics(
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_capability
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - capability_not_configured
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return monitoring metrics for a specific capability within an app. Computed by MonitoringService.get_capability_metrics(app_id,
        capability). Enables per-capability drill-down on the app monitoring page.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: capability
        in: path
        required: true
        schema:
          type: string
        description: Capability key (e.g. chat, transcription, embedding).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Monitoring metrics scoped to a specific capability within an app. Shape is service-defined.
                  Clients must treat the payload as opaque.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/capabilities/{capability}/timeseries:
    get:
      operationId: monitoring_apps_get_capability_timeseries
      summary: Return time-series monitoring data scoped to a specific capability within an app. Calls MonitoringService.get_capability
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_capability_timeseries
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - capability_not_configured
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return time-series monitoring data scoped to a specific capability within an app. Calls MonitoringService.get_capability_metrics_timeseries(
        app_id, capability, start=now-hours). Enables per-capability trend charts.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: capability
        in: path
        required: true
        schema:
          type: string
      - name: hours
        in: query
        required: false
        schema:
          type: integer
        description: "Lookback window in hours (1\u2013720, default 24)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Time-bucketed monitoring metrics for a specific capability within an app.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/events:
    get:
      operationId: monitoring_apps_get_events
      summary: Return recent deployment events for an app. Calls MonitoringService.get_app_deployment_events(app_id, event_type=...,
        li
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_events
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return recent deployment events for an app. Calls MonitoringService.get_app_deployment_events(app_id, event_type=...,
        limit=...). Covers events such as version deploys, rollbacks, and status changes.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: event_type
        in: query
        required: false
        schema:
          type: string
        description: Optional event type filter.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum number of events to return (1\u2013500, default 100)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Deployment events for the app, newest first.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/runtime/events:
    get:
      operationId: monitoring_apps_get_runtime_events
      summary: List recent runtime route events for an app with pagination. Returns RouteEventListResponse with events[],
        total, limit,
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_runtime_events
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List recent runtime route events for an app with pagination. Returns RouteEventListResponse with events[],
        total, limit, offset. Each event is built from RuntimeRouteEvent.to_event_dict() via _build_event_response(). Distinct
        from monitoring.apps.get_events which returns deployment events.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: timeframe
        in: query
        required: false
        schema:
          type: string
        description: Time window enum (one_hour, six_hours, twenty_four_hours [default], seven_days, thirty_days).
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum events to return (1\u2013100, default 20)."
      - name: offset
        in: query
        required: false
        schema:
          type: integer
        description: Pagination offset (default 0).
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/runtime:
    get:
      operationId: monitoring_apps_get_runtime_summary
      summary: Return aggregated runtime request telemetry for an app, derived from RuntimeRouteEvent records. Loads up to
        10,000 event
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_runtime_summary
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated runtime request telemetry for an app, derived from RuntimeRouteEvent records. Loads up
        to 10,000 events within the timeframe and passes them to _build_monitoring_summary_response(). Distinct from monitoring.apps.get
        which reads from MonitoringService.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: timeframe
        in: query
        required: false
        schema:
          type: string
        description: 'Time window for runtime event aggregation. Enum values match the Timeframe enum in runtime_monitoring.py:
          one_hour, six_hours, twenty_four_hours (default), seven_days, thirty_days.

          '
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/runtime/timeseries:
    get:
      operationId: monitoring_apps_get_runtime_timeseries
      summary: Return bucketed runtime request count time-series for an app. Loads up to 10,000 RuntimeRouteEvent records
        within the ti
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_runtime_timeseries
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return bucketed runtime request count time-series for an app. Loads up to 10,000 RuntimeRouteEvent records
        within the timeframe and passes them to _build_runtime_timeseries_response(). Used to render throughput trend charts
        on the app monitoring page.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: timeframe
        in: query
        required: false
        schema:
          type: string
        description: Time window enum (one_hour, six_hours, twenty_four_hours [default], seven_days, thirty_days).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: "Bucketed runtime request counts for the app within the timeframe. Each item is a RuntimeTimeseriesPoint.\
                  \ The bucket granularity is determined by the timeframe (e.g. 1h \u2192 5 min buckets, 7d \u2192 1h buckets).\n"
                items:
                  type: object
                  description: "RuntimeTimeseriesPoint \u2014 timestamp bucket + aggregated counts."
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/apps/{app_id}/timeseries:
    get:
      operationId: monitoring_apps_get_timeseries
      summary: Return time-series monitoring data for an app. Calls MonitoringService.get_app_metrics_timeseries(app_id, start=now-hour
      tags:
      - monitoring
      x-octomil-method: monitoring.apps.get_timeseries
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - app_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return time-series monitoring data for an app. Calls MonitoringService.get_app_metrics_timeseries(app_id,
        start=now-hours). Used to render metric trend charts on the app monitoring page.
      parameters:
      - name: app_id
        in: path
        required: true
        schema:
          type: string
      - name: hours
        in: query
        required: false
        schema:
          type: integer
        description: "Lookback window in hours (1\u2013720, default 24)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'Time-bucketed monitoring metrics for an app. Each item is a time-bucket dict. Shape is service-defined.
                  Clients must treat item fields as opaque.

                  '
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/deployments/{deployment_id}/events:
    get:
      operationId: monitoring_deployments_events
      summary: "List monitoring events for a deployment \u2014 lifecycle transitions, rollout steps, health gate evaluations,\
        \ and governance "
      tags:
      - monitoring
      x-octomil-method: monitoring.deployments.events
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - deployment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "List monitoring events for a deployment \u2014 lifecycle transitions, rollout steps, health gate evaluations,\
        \ and governance actions. Designed for the deployment detail page's activity/events timeline. Events are fetched from\
        \ MonitoringService.get_deployment_events(), which aggregates across the deployment_events table and any correlated\
        \ rollout history entries."
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      - name: event_type
        in: query
        required: false
        schema:
          type: string
        description: 'Filter to a specific event type string (e.g. `approval_requested`, `started`, `paused`, `rolled_back`).
          Omit to return all event types.

          '
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
        description: Maximum number of events to return.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'Array of deployment event objects ordered by event time descending.

                  '
                items:
                  type: object
                  additionalProperties: true
                  description: 'Event shape is service-defined. Expected common fields include event_type, created_at, user_id,
                    and a payload object.

                    '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/deployments/{deployment_id}:
    get:
      operationId: monitoring_deployments_get
      summary: Return aggregated runtime monitoring metrics for a single deployment. Computed by MonitoringService.get_deployment_metri
      tags:
      - monitoring
      x-octomil-method: monitoring.deployments.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - deployment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated runtime monitoring metrics for a single deployment. Computed by MonitoringService.get_deployment_metrics()
        from the most recent telemetry ingested for this deployment_id. This endpoint drives the deployment detail page's
        "Monitoring" tab.
      parameters:
      - name: deployment_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated monitoring metrics for the deployment. Shape is service-defined and may include inference
                  counts, latency percentiles, error rates, and device assignment breakdowns. Clients should treat the payload
                  as an opaque object and render only fields they recognise.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/devices/{device_id}:
    get:
      operationId: monitoring_devices_get
      summary: Return aggregated monitoring metrics for a single device. Computed by MonitoringService.get_device_metrics().
        Covers inf
      tags:
      - monitoring
      x-octomil-method: monitoring.devices.get
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated monitoring metrics for a single device. Computed by MonitoringService.get_device_metrics().
        Covers inference activity, heartbeat health, and device assignment state as available in the monitoring service.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated monitoring metrics for a device. Shape is service-defined. Clients must treat the
                  payload as opaque and render only known fields.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/incidents:
    post:
      operationId: monitoring_incidents_create
      summary: Create an incident, optionally linked to an alert rule. On success notifies verified status-page subscribers
        and dispatc
      tags:
      - monitoring
      x-octomil-method: monitoring.incidents.create
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Inserts a new `Incident` row with `status="open"`.
      - Queues incident-notification email to all verified status-page subscribers.
      - Dispatches `incident.created` event to all configured org webhooks.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create an incident, optionally linked to an alert rule. On success notifies verified status-page subscribers
        and dispatches `incident.created` to configured webhooks (Slack, PagerDuty, etc.) in the background.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: 'Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original
          response without creating a duplicate incident.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - title
              properties:
                title:
                  type: string
                  description: Short headline shown in incident lists and status-page emails.
                description:
                  type: string
                  nullable: true
                  description: Long-form context for responders and external subscribers.
                severity:
                  type: string
                  default: medium
                  description: 'Free-form server-side string. Conventional values match the `IncidentSeverity` enum (`info
                    | warning | error | critical`) but `"medium"` is the default. The legacy default predates enum tightening;
                    SDKs that want strict enum values should pass one of the enum members explicitly.

                    '
                alert_rule_id:
                  type: string
                  nullable: true
                  description: Optional UUID of the alert rule that triggered this incident. Use for incidents auto-created
                    from a fired alert; null for manually-opened incidents.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Incident'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: monitoring_incidents_list
      summary: 'List incidents for an org. Used by the dashboard''s incidents panel and by SRE consoles aggregating across
        orgs (per-org '
      tags:
      - monitoring
      x-octomil-method: monitoring.incidents.list
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List incidents for an org. Used by the dashboard's incidents panel and by SRE consoles aggregating across
        orgs (per-org call).
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: 'Optional filter. Server-side enum values: `open | acknowledged | investigating | resolved | closed`.
          Omit to return all statuses.

          '
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
        description: Maximum incidents returned.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncidentListResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/incidents/stats:
    get:
      operationId: monitoring_incidents_stats
      summary: Rollup statistics for the incidents panel. Single aggregated call so the dashboard doesn't need to compute
        MTTR / resolu
      tags:
      - monitoring
      x-octomil-method: monitoring.incidents.stats
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Rollup statistics for the incidents panel. Single aggregated call so the dashboard doesn't need to compute
        MTTR / resolution rate client-side.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: days
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 365
          default: 30
        description: Look-back window in days.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - timestamp
                - org_id
                - window_days
                - total_incidents
                - by_status
                - by_severity
                - mttr_hours
                - resolution_rate_pct
                properties:
                  timestamp:
                    type: string
                    format: date-time
                    description: ISO-8601 UTC time the stats were computed (now).
                  org_id:
                    type: string
                  window_days:
                    type: integer
                    description: Echo of the `days` query parameter.
                  total_incidents:
                    type: integer
                    description: Total incidents in the window (sum of by_status counts).
                  by_status:
                    type: object
                    additionalProperties:
                      type: integer
                    description: 'Count keyed by incident status (e.g. {"open": 3, "resolved": 12}).'
                  by_severity:
                    type: object
                    additionalProperties:
                      type: integer
                    description: Count keyed by severity. Severity NULL is bucketed as "unknown".
                  mttr_hours:
                    type: number
                    description: Mean Time To Resolve in hours, averaged across incidents that have a non-null `resolved_at`
                      in the window. `0` when no resolved incidents exist.
                  resolution_rate_pct:
                    type: number
                    description: Percentage of incidents in the window that have been resolved (`resolved_at` set). `0` when
                      `total_incidents == 0`.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/incidents/{incident_id}:
    put:
      operationId: monitoring_incidents_update
      summary: 'Update an incident''s status, severity, assignee, or resolution notes. Transitions to `resolved` send a resolution
        email '
      tags:
      - monitoring
      x-octomil-method: monitoring.incidents.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - Mutates the `Incident` row's status/severity/assignee/resolution_notes.
      - Stamps `acknowledged_at|by`, `resolved_at|by`, `closed_at|by` on first transition.
      - Queues resolution email to status-page subscribers on first transition to `resolved`.
      - Dispatches `incident.{new_status}` webhook on any status change.
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - incident_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Update an incident's status, severity, assignee, or resolution notes. Transitions to `resolved` send a
        resolution email to verified status-page subscribers; any status change dispatches `incident.{status}` to configured
        webhooks.
      parameters:
      - name: incident_id
        in: path
        required: true
        schema:
          type: string
        description: Incident UUID.
      - name: user_id
        in: query
        required: true
        schema:
          type: string
        description: 'UUID of the user performing the update. Server stamps this into `acknowledged_by` / `resolved_by` /
          `closed_by` based on the status transition. Required because the audit trail is per-actor.

          '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: 'Partial-update body. Only the following keys are honored; anything else is silently ignored. Pass
                only the fields you want to change.

                '
              additionalProperties: false
              properties:
                status:
                  type: string
                  enum:
                  - open
                  - acknowledged
                  - investigating
                  - resolved
                  - closed
                  description: 'Transitioning to `acknowledged`, `resolved`, or `closed` auto-stamps the corresponding `*_at`
                    and `*_by` columns the first time the transition fires.

                    '
                severity:
                  type: string
                  description: Per `IncidentSeverity` enum (`info | warning | error | critical`).
                assigned_to:
                  type: string
                  description: User ID to assign the incident to. Pass empty string to unassign.
                resolution_notes:
                  type: string
                  description: Free-form notes appended to the incident when resolving.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Incident'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/metrics/query:
    get:
      operationId: monitoring_metrics_query
      summary: Query time-series metrics for an org within a lookback window. Returns an array of metric records; schema depends
        on the
      tags:
      - monitoring
      x-octomil-method: monitoring.metrics.query
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Query time-series metrics for an org within a lookback window. Returns an array of metric records; schema
        depends on the metric type.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: window_hours
        in: query
        required: false
        schema:
          type: integer
        description: "Lookback window in hours (1\u2013720, default 24)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: Metric data point or series.
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/models/{model_id}:
    get:
      operationId: monitoring_models_get
      summary: Return aggregated runtime monitoring metrics for a single model. Computed by MonitoringService.get_model_metrics().
        Dist
      tags:
      - monitoring
      x-octomil-method: monitoring.models.get
      x-octomil-since: 1.27.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return aggregated runtime monitoring metrics for a single model. Computed by MonitoringService.get_model_metrics().
        Distinct from models.get_inference_metrics which computes from InferenceSession rows; this endpoint delegates to the
        MonitoringService abstraction layer.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: 'Aggregated monitoring metrics for a model. Shape is service-defined and may include inference
                  counts, latency percentiles, and error rates. Clients must treat the payload as opaque and render only known
                  fields.

                  '
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/monitoring/overview:
    get:
      operationId: monitoring_overview
      summary: Single-call summary for the dashboard's monitoring landing page. Polled on page mount; not intended for high-frequency
        S
      tags:
      - monitoring
      x-octomil-method: monitoring.overview
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Single-call summary for the dashboard's monitoring landing page. Polled on page mount; not intended for
        high-frequency SDK polling.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Org-wide monitoring summary aggregated by `MonitoringService.get_overview()`. Returns the headline\
                  \ numbers the dashboard's monitoring landing page renders: deployment health rollup, recent incident counts,\
                  \ active alerts count, request volume / error-rate windowed over the last 24h. The exact field set is server-driven\
                  \ (see `server/app/services/monitoring_service.py`) and can grow without a breaking change \u2014 clients\
                  \ should tolerate unknown keys.\n"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/providers:
    get:
      operationId: providers_list
      summary: List all supported cloud providers with their auth schemes, key format hints, and managed pool status. Includes
        provider
      tags:
      - providers
      x-octomil-method: providers.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: List all supported cloud providers with their auth schemes, key format hints, and managed pool status.
        Includes providers without active pools.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - providers
                properties:
                  providers:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - display_name
                      - auth_schemes
                      - status
                      properties:
                        id:
                          type: string
                          description: Provider identifier (e.g. "openai", "anthropic").
                        display_name:
                          type: string
                        auth_schemes:
                          type: array
                          items:
                            type: string
                        status:
                          type: string
                          description: Provider availability status.
                        managed_available:
                          type: boolean
                          nullable: true
                        managed_pool_active:
                          type: boolean
                          description: Whether a managed pool is currently active for this provider.
                        key_format:
                          type: object
                          nullable: true
                          properties:
                            prefix:
                              type: string
                              nullable: true
                            min_length:
                              type: integer
                              nullable: true
                            example:
                              type: string
                              nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/recommendations/apply:
    post:
      operationId: recommendations_apply
      summary: Apply a model recommendation by creating or updating a deployment for the specified app, capability, and environment.
        Re
      tags:
      - recommendations
      x-octomil-method: recommendations.apply
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates or updates a deployment
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - resource_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Apply a model recommendation by creating or updating a deployment for the specified app, capability, and
        environment. Returns the resulting deployment record.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - variant_id
              - version
              - app_id
              - capability
              properties:
                variant_id:
                  type: string
                version:
                  type: string
                app_id:
                  type: string
                capability:
                  type: string
                  description: Capability slot to configure (max 64 chars).
                device_group_id:
                  type: string
                  nullable: true
                environment:
                  type: string
                  description: Target environment (staging / production / development).
                  enum:
                  - staging
                  - production
                  - development
                  nullable: true
                serving_policy:
                  type: object
                  nullable: true
                  additionalProperties: true
                rollout_policy:
                  type: object
                  nullable: true
                  additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Created or updated deployment record.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/recommendations/group:
    post:
      operationId: recommendations_group
      summary: Get model recommendations for a device group and capability. Returns ranked catalog variants with coverage,
        quantization
      tags:
      - recommendations
      x-octomil-method: recommendations.group
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Get model recommendations for a device group and capability. Returns ranked catalog variants with coverage,
        quantization, and format hints.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - capability
              properties:
                capability:
                  type: string
                  description: Capability to find model recommendations for.
                device_group_id:
                  type: string
                  nullable: true
                app_id:
                  type: string
                  nullable: true
                limit:
                  type: integer
                  nullable: true
                  description: Maximum recommendations to return.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                  - variant_id
                  - variant_name
                  - family_name
                  properties:
                    variant_id:
                      type: string
                    internal_model_id:
                      type: string
                      nullable: true
                    variant_name:
                      type: string
                    family_name:
                      type: string
                    parameter_count:
                      type: integer
                      nullable: true
                    version_id:
                      type: string
                      nullable: true
                    version:
                      type: string
                      nullable: true
                    coverage_pct:
                      type: number
                      nullable: true
                    platform_breakdown:
                      type: object
                      additionalProperties: true
                    recommended_quantization:
                      type: string
                      nullable: true
                    recommended_format:
                      type: string
                      nullable: true
                    total_size_bytes:
                      type: integer
                      nullable: true
                    support_tier:
                      type: string
                      nullable: true
                    score:
                      type: number
                      nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /v1/responses:
    post:
      operationId: responses_stream
      summary: Run inference and stream tokens as they are produced.
      tags:
      - responses
      x-octomil-method: responses.stream
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - consumes compute (local or cloud)
      - emits telemetry events (inference.started, inference.chunk_produced, inference.completed or inference.failed)
      - caches final response by id for previousResponseId lookups
      x-octomil-errors:
      - model_not_found
      - model_disabled
      - runtime_unavailable
      - model_load_failed
      - inference_failed
      - insufficient_memory
      - rate_limited
      - invalid_input
      - cancelled
      - network_unavailable
      - request_timeout
      - server_error
      - authentication_failed
      - forbidden
      description: Run inference and stream tokens as they are produced.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/response_request'
      responses:
        '200':
          description: Streaming response (SSE).
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/response_stream_event'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      x-octomil-stream: sse
      x-octomil-stream-event-schema: response_stream_event
      x-octomil-stream-terminal-event: done
  /api/v1/route:
    post:
      operationId: route_decide
      summary: Compute and return a routing decision. Given model parameters and device capabilities, returns the optimal
        execution tar
      tags:
      - route
      x-octomil-method: route.decide
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists routing decision for audit
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Compute and return a routing decision. Given model parameters and device capabilities, returns the optimal
        execution target (device vs cloud), format, engine, and quantization. When deployment_id is supplied the deployment's
        serving_policy overrides the prefer hint; an active serving_policy experiment takes precedence over the deployment
        policy.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              - model_size_mb
              properties:
                model_id:
                  type: string
                  description: Model identifier used to select the routing decision.
                model_size_mb:
                  type: number
                  description: Model size in megabytes used for device capability matching.
                model_params:
                  type: object
                  description: Model identifier and runtime requirements.
                  additionalProperties: true
                device_capabilities:
                  type: object
                  description: 'Device hardware capabilities used to determine optimal execution target. Provide total_memory_mb
                    or ram_gb; npu_tops, supported_runtimes optional.

                    '
                  additionalProperties: true
                prefer:
                  type: string
                  description: "Execution preference hint \u2014 \"device\" or \"cloud\". When omitted and deployment_id is\
                    \ supplied, the deployment's serving_policy governs.\n"
                deployment_id:
                  type: string
                  nullable: true
                  description: 'When provided, the deployment''s serving_policy and any active serving_policy experiment override
                    the prefer field.

                    '
                device_id:
                  type: string
                  nullable: true
                  description: Device identifier used as the bucketing key for experiments.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - id
                - target
                - engine
                - device_class
                properties:
                  id:
                    type: string
                    description: Routing decision ID.
                  target:
                    type: string
                    description: "Resolved execution target \u2014 \"device\" or \"cloud\"."
                  engine:
                    type: string
                    description: Engine identifier selected for execution.
                  device_class:
                    type: string
                    description: Classified device tier (e.g. high, mid, low).
                  format:
                    type: string
                    nullable: true
                  quantization:
                    type: string
                    nullable: true
                  reason:
                    type: string
                    nullable: true
                  routing_mode:
                    type: string
                    nullable: true
                  decision_id:
                    type: string
                    nullable: true
                    description: Persisted decision identifier for audit retrieval.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/route/decision/{decision_id}:
    get:
      operationId: route_decision_get
      summary: Retrieve a routing decision from the in-memory decision store by decision_id. The in-memory store is ephemeral;
        decision
      tags:
      - route
      x-octomil-method: route.decision.get
      x-octomil-since: 1.28.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - server_error
      - network_unavailable
      - request_timeout
      description: Retrieve a routing decision from the in-memory decision store by decision_id. The in-memory store is ephemeral;
        decisions may expire or be evicted. For persistent history use route.decisions.list.
      parameters:
      - name: decision_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - decision_id
                - model_id
                - target
                - format
                - engine
                - quantization
                - reason
                - device_class
                - preference
                - model_size_mb
                - model_params
                - routing_latency_us
                - timestamp
                properties:
                  decision_id:
                    type: string
                  model_id:
                    type: string
                  device_id:
                    type: string
                    nullable: true
                  target:
                    type: string
                  format:
                    type: string
                  engine:
                    type: string
                  quantization:
                    type: string
                  reason:
                    type: string
                  device_class:
                    type: string
                  preference:
                    type: string
                  model_size_mb:
                    type: number
                  model_params:
                    type: integer
                  routing_latency_us:
                    type: integer
                  timestamp:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/route/policy:
    get:
      operationId: route_policy_get
      summary: Retrieve the active routing policy for the org. Supports conditional GET via If-None-Match / ETag to avoid
        re-fetching a
      tags:
      - route
      x-octomil-method: route.policy.get
      x-octomil-since: 1.28.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Retrieve the active routing policy for the org. Supports conditional GET via If-None-Match / ETag to avoid
        re-fetching an unchanged policy. Response includes Cache-Control: private, max-age=300.'
      parameters:
      - name: If-None-Match
        in: header
        required: false
        schema:
          type: string
        description: ETag from a prior response for conditional GET.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - version
                - thresholds
                - tiers
                - tier_models
                - deterministic_enabled
                - deterministic_operations
                - simple_patterns
                - complex_indicators
                - updated_at
                properties:
                  version:
                    type: integer
                  thresholds:
                    type: array
                    items:
                      type: number
                  tiers:
                    type: array
                    items:
                      type: string
                  tier_models:
                    type: object
                    additionalProperties:
                      type: string
                  deterministic_enabled:
                    type: boolean
                  deterministic_operations:
                    type: array
                    items:
                      type: string
                  simple_patterns:
                    type: array
                    items:
                      type: string
                  complex_indicators:
                    type: array
                    items:
                      type: string
                  updated_at:
                    type: string
                  etag:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/route/query:
    post:
      operationId: route_query
      summary: Route a query (OpenAI-style message list) to the appropriate model tier based on complexity scoring. When decompose=true
      tags:
      - route
      x-octomil-method: route.query
      x-octomil-since: 1.28.0
      x-octomil-stability: experimental
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Route a query (OpenAI-style message list) to the appropriate model tier based on complexity scoring. When
        decompose=true the server may return a DecomposedQueryRouteResponse with sub-decisions instead.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - messages
              properties:
                messages:
                  type: array
                  description: OpenAI-style message list. At least one user message is required.
                  items:
                    type: object
                    required:
                    - role
                    - content
                    properties:
                      role:
                        type: string
                        description: 'Message role: system, user, assistant'
                      content:
                        type: string
                decompose:
                  type: boolean
                  description: 'If true, attempt query decomposition for multi-task queries. Defaults to false. Response shape
                    changes to DecomposedQueryRouteResponse when decomposition is triggered.

                    '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - model_name
                - complexity_score
                - tier
                properties:
                  model_name:
                    type: string
                    description: Selected model name. Empty string if deterministic routing applies.
                  complexity_score:
                    type: number
                    description: Computed complexity score in [0.0, 1.0].
                  tier:
                    type: string
                    description: 'Assigned tier: fast / balanced / quality / deterministic.'
                  strategy:
                    type: string
                    description: Routing strategy used.
                  fallback_chain:
                    type: array
                    items:
                      type: string
                  deterministic_result:
                    type: object
                    nullable: true
                    description: Present when tier=deterministic; contains answer, method, confidence.
                    properties:
                      answer:
                        type: string
                      method:
                        type: string
                      confidence:
                        type: number
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/serving-policy/presets:
    get:
      operationId: route_serving_policy_presets
      summary: List the named serving policy presets with their canonical configurations. Presets are the authoritative source
        for serv
      tags:
      - route
      x-octomil-method: route.serving_policy_presets
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: List the named serving policy presets with their canonical configurations. Presets are the authoritative
        source for serving policy defaults; use these names when configuring deployments.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                  - name
                  - policy
                  properties:
                    name:
                      type: string
                      description: Canonical preset name (e.g. "device_only", "cloud_first").
                    policy:
                      type: object
                      description: Serving policy configuration object for this preset.
                      additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/route/stats/{model_id}:
    get:
      operationId: route_stats_get
      summary: "Get aggregated routing statistics for a specific model \u2014 device vs cloud percentages, total decision\
        \ count, and common r"
      tags:
      - route
      x-octomil-method: route.stats.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "Get aggregated routing statistics for a specific model \u2014 device vs cloud percentages, total decision\
        \ count, and common routing reasons."
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
        description: Model identifier to retrieve routing statistics for.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - model_id
                - total_decisions
                properties:
                  model_id:
                    type: string
                  total_decisions:
                    type: integer
                  device_count:
                    type: integer
                  cloud_count:
                    type: integer
                  device_pct:
                    type: number
                    description: Percentage of decisions routed to device, in [0, 100].
                  cloud_pct:
                    type: number
                    description: Percentage of decisions routed to cloud, in [0, 100].
                  common_reasons:
                    type: array
                    items:
                      type: string
                  avg_routing_latency_us:
                    type: number
                    nullable: true
                    description: Average routing decision latency in microseconds.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/routing/decisions:
    get:
      operationId: routing_decisions_list
      summary: 'List persisted routing decisions from the database, optionally filtered by model. Returns newest-first. Maximum
        500 per '
      tags:
      - routing
      x-octomil-method: routing.decisions.list
      x-octomil-since: 1.28.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: List persisted routing decisions from the database, optionally filtered by model. Returns newest-first.
        Maximum 500 per call.
      parameters:
      - name: model_id
        in: query
        required: false
        schema:
          type: string
        description: Filter decisions by model identifier.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
        description: "Maximum number of decisions to return (1\u2013500, default 100)."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: Persisted routing decision record.
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/routing/stats:
    get:
      operationId: routing_stats_get
      summary: "Returns aggregated routing statistics from the database \u2014 local vs cloud decision counts, fallback rate,\
        \ and average lat"
      tags:
      - routing
      x-octomil-method: routing.stats.get
      x-octomil-since: 1.28.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: "Returns aggregated routing statistics from the database \u2014 local vs cloud decision counts, fallback\
        \ rate, and average latency broken down by locality."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Aggregated routing statistics across all models.
                properties:
                  local_count:
                    type: integer
                  cloud_count:
                    type: integer
                  fallback_rate:
                    type: number
                    nullable: true
                  avg_latency_by_locality:
                    type: object
                    additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/benchmarks:
    post:
      operationId: runtime_benchmarks_submit
      summary: Submit real inference benchmark results from a device.
      tags:
      - runtime
      x-octomil-method: runtime.benchmarks.submit
      x-octomil-since: 1.15.0
      x-octomil-stability: experimental
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - persists benchmark data to the database
      - future plan requests may re-rank candidates based on submitted benchmarks
      x-octomil-errors:
      - invalid_request
      - authentication_failed
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Submit real inference benchmark results from a device.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/runtime_benchmark_submission_schema'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runtime_benchmark_submission_response_schema'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/defaults:
    get:
      operationId: runtime_defaults
      summary: Returns public engine defaults, supported capabilities, and routing policies. No authentication is required.
        SDKs call t
      tags:
      - runtime
      x-octomil-method: runtime.defaults
      x-octomil-since: 1.27.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns public engine defaults, supported capabilities, and routing policies. No authentication is required.
        SDKs call this on first launch to populate defaults without needing a full plan request. Response is static for a
        given server version and is safe to cache for plan_ttl_seconds.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  default_engines:
                    type: object
                    description: 'Map of capability name to default engine slug (e.g. {"chat": "llamacpp", "stt": "whisper"}).
                      SDKs use this to populate engine selection UI and set fallback defaults.

                      '
                    additionalProperties:
                      type: string
                  supported_capabilities:
                    type: array
                    items:
                      type: string
                    description: 'Canonical capability names the planner can route (e.g. chat, stt, vision, embedding). Matches
                      the capability field on apps and runtime events.

                      '
                  supported_policies:
                    type: array
                    items:
                      type: string
                    description: 'Sorted list of routing policy strings the planner accepts (e.g. local_first, cloud_only,
                      private, local_only, cloud_first).

                      '
                  plan_ttl_seconds:
                    type: integer
                    description: 'Recommended client-side cache TTL for a plan response in seconds. Default is 604800 (7 days).
                      SDKs SHOULD re-plan after this TTL or when device hardware changes.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/events/{route_id}:
    get:
      operationId: runtime_events_get
      summary: 'Retrieve a single route telemetry event by route_id. Scoped to the authenticated caller''s org_id. Used by
        the dashboard '
      tags:
      - runtime
      x-octomil-method: runtime.events.get
      x-octomil-since: 1.27.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve a single route telemetry event by route_id. Scoped to the authenticated caller's org_id. Used
        by the dashboard and SDK debugging tools to inspect routing decisions for a specific request.
      parameters:
      - name: route_id
        in: path
        required: true
        schema:
          type: string
        description: 'The route_id of the event to retrieve. This is the same value the SDK submitted via runtime.events.ingest.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  route_id:
                    type: string
                  plan_id:
                    type: string
                    nullable: true
                  request_id:
                    type: string
                  app_id:
                    type: string
                    nullable: true
                  app_slug:
                    type: string
                    nullable: true
                  capability:
                    type: string
                    nullable: true
                  deployment_id:
                    type: string
                    nullable: true
                  experiment_id:
                    type: string
                    nullable: true
                  variant_id:
                    type: string
                    nullable: true
                  policy:
                    type: string
                    nullable: true
                  planner_source:
                    type: string
                    nullable: true
                  model_ref:
                    type: string
                    nullable: true
                  model_ref_kind:
                    type: string
                    nullable: true
                  candidate_attempts:
                    type: integer
                  fallback_used:
                    type: boolean
                  fallback_trigger_code:
                    type: string
                    nullable: true
                  fallback_trigger_stage:
                    type: string
                    nullable: true
                  selected_locality:
                    type: string
                    nullable: true
                  final_locality:
                    type: string
                    nullable: true
                  final_mode:
                    type: string
                    nullable: true
                  engine:
                    type: string
                    nullable: true
                  artifact_id:
                    type: string
                    nullable: true
                  cache_status:
                    type: string
                    nullable: true
                  ttft_ms:
                    type: number
                    nullable: true
                  tokens_per_second:
                    type: number
                    nullable: true
                  total_tokens:
                    type: integer
                    nullable: true
                  duration_ms:
                    type: number
                    nullable: true
                  quality_evaluator_name:
                    type: string
                    nullable: true
                  quality_score:
                    type: number
                    nullable: true
                  quality_reason_code:
                    type: string
                    nullable: true
                  advisory_failures:
                    type: array
                    nullable: true
                    items:
                      type: object
                  gate_failure_count:
                    type: integer
                    nullable: true
                  output_visible_before_failure:
                    type: boolean
                    nullable: true
                  attempt_details:
                    type: array
                    nullable: true
                    items:
                      type: object
                  ingested_at:
                    type: string
                    format: date-time
                    description: ISO 8601 timestamp when the event was persisted.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/events:
    post:
      operationId: runtime_events_ingest
      summary: 'Ingest a route telemetry event from an SDK. The event records the outcome of a routing decision: which engine
        was select'
      tags:
      - runtime
      x-octomil-method: runtime.events.ingest
      x-octomil-since: 1.27.0
      x-octomil-stability: experimental
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - persists a RuntimeRouteEvent row scoped to the caller's org_id
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Ingest a route telemetry event from an SDK. The event records the outcome of a routing decision: which
        engine was selected, whether fallback occurred, performance metrics, and gate evaluation results. The server validates
        enum-constrained fields, strips privacy-sensitive content, and stores the event for monitoring queries. Returns 202
        Accepted synchronously.'
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - route_id
              - request_id
              properties:
                route_id:
                  type: string
                  description: Unique identifier for this route decision. Used for correlation.
                plan_id:
                  type: string
                  nullable: true
                  description: ID of the runtime plan that produced this route, if any.
                request_id:
                  type: string
                  description: Originating inference request identifier.
                app_id:
                  type: string
                  nullable: true
                app_slug:
                  type: string
                  nullable: true
                capability:
                  type: string
                  nullable: true
                  description: 'Capability being routed (chat, stt, vision, embedding, etc.).

                    '
                deployment_id:
                  type: string
                  nullable: true
                experiment_id:
                  type: string
                  nullable: true
                variant_id:
                  type: string
                  nullable: true
                policy:
                  type: string
                  nullable: true
                  description: Routing policy applied (local_first, cloud_only, etc.).
                planner_source:
                  type: string
                  nullable: true
                model_ref:
                  type: string
                  nullable: true
                  description: Model identifier as provided by the caller.
                model_ref_kind:
                  type: string
                  nullable: true
                  enum:
                  - model
                  - app
                  - capability
                  - deployment
                  - experiment
                  - alias
                  - default
                  - unknown
                  description: How model_ref was resolved.
                candidate_attempts:
                  type: integer
                  default: 0
                  description: Number of candidate engines attempted before a final selection.
                fallback_used:
                  type: boolean
                  default: false
                  description: Whether the router fell back from the primary candidate.
                fallback_trigger_code:
                  type: string
                  nullable: true
                  description: Canonical error code that triggered the fallback.
                fallback_trigger_stage:
                  type: string
                  nullable: true
                  enum:
                  - policy
                  - prepare
                  - download
                  - verify
                  - load
                  - benchmark
                  - gate
                  - inference
                  description: Stage at which the fallback was triggered.
                selected_locality:
                  type: string
                  nullable: true
                  enum:
                  - local
                  - cloud
                  description: Locality selected by the planner.
                final_locality:
                  type: string
                  nullable: true
                  enum:
                  - local
                  - cloud
                  description: Locality that actually served the request.
                final_mode:
                  type: string
                  nullable: true
                  enum:
                  - sdk_runtime
                  - hosted_gateway
                  - external_endpoint
                engine:
                  type: string
                  nullable: true
                  description: Engine slug that served the request (e.g. llamacpp, whisper).
                artifact_id:
                  type: string
                  nullable: true
                cache_status:
                  type: string
                  nullable: true
                  enum:
                  - hit
                  - miss
                  - downloaded
                  - not_applicable
                  - unavailable
                ttft_ms:
                  type: number
                  nullable: true
                  description: Time-to-first-token in milliseconds.
                tokens_per_second:
                  type: number
                  nullable: true
                total_tokens:
                  type: integer
                  nullable: true
                duration_ms:
                  type: number
                  nullable: true
                  description: Total inference duration in milliseconds.
                quality_evaluator_name:
                  type: string
                  nullable: true
                quality_score:
                  type: number
                  nullable: true
                quality_reason_code:
                  type: string
                  nullable: true
                advisory_failures:
                  type: array
                  nullable: true
                  items:
                    type: object
                  description: Non-blocking gate failures logged for observability.
                gate_failure_count:
                  type: integer
                  nullable: true
                output_visible_before_failure:
                  type: boolean
                  nullable: true
                attempt_details:
                  type: array
                  nullable: true
                  items:
                    type: object
                  description: Per-candidate attempt breakdown (see RouteAttemptDetail shape).
              additionalProperties: true
              description: 'Route telemetry event from the SDK after a routing decision completes. The server strips any fields
                matching the privacy forbidden list before persistence (prompt, input, output, content, messages, audio, etc.).

                '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Always "accepted".
                  route_id:
                    type: string
                    description: Echo of the submitted route_id.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: runtime_events_list
      summary: List route telemetry events for the authenticated org with comprehensive filtering. Supports windowed queries
        by timefra
      tags:
      - runtime
      x-octomil-method: runtime.events.list
      x-octomil-since: 1.27.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List route telemetry events for the authenticated org with comprehensive filtering. Supports windowed queries
        by timeframe and filtering by app, deployment, experiment, variant, route_id, request_id, fallback status, locality,
        mode, and gate taxonomy fields. Returns newest-first.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  events:
                    type: array
                    items:
                      type: object
                    description: Page of RouteEventResponse objects (see runtime.events.get for shape).
                  total:
                    type: integer
                    description: 'Total matching event count before pagination. When gate_code or gate_class filters are active,
                      total reflects the post-filter count.

                      '
                  limit:
                    type: integer
                  offset:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/monitoring/summary:
    get:
      operationId: runtime_monitoring_summary
      summary: Returns aggregated runtime monitoring statistics for the authenticated org within the specified timeframe.
        Aggregates up
      tags:
      - runtime
      x-octomil-method: runtime.monitoring.summary
      x-octomil-since: 1.27.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Returns aggregated runtime monitoring statistics for the authenticated org within the specified timeframe.
        Aggregates up to 10,000 events per call. Provides operational insight into routing decisions, fallback rates, local-vs-cloud
        split, top fallback triggers, per-context breakdowns, and gate class distribution. Used by the dashboard monitoring
        view.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  total_requests:
                    type: integer
                    description: Total route events in the timeframe (after filters).
                  fallback_rate_pct:
                    type: number
                    description: Percentage of requests where fallback_used=true.
                  local_failure_reasons:
                    type: object
                    additionalProperties:
                      type: integer
                    description: 'Map of fallback_trigger_code to count for all events in the window.

                      '
                  runtime_unavailable_rate_pct:
                    type: number
                    description: 'Percentage of requests with fallback_trigger_code=runtime_unavailable.

                      '
                  artifact_cache_miss_count:
                    type: integer
                  artifact_download_count:
                    type: integer
                  policy_denied_cloud_fallback_count:
                    type: integer
                  platform_breakdown:
                    type: object
                    additionalProperties:
                      type: integer
                    description: Request counts keyed by engine slug.
                  avg_ttft_ms:
                    type: number
                    nullable: true
                  avg_tokens_per_second:
                    type: number
                    nullable: true
                  timeframe:
                    type: string
                    description: The timeframe used for this summary ("1h", "24h", or "7d").
                  app_id:
                    type: string
                    nullable: true
                  deployment_id:
                    type: string
                    nullable: true
                  experiment_id:
                    type: string
                    nullable: true
                  locality_split:
                    type: object
                    nullable: true
                    properties:
                      local_count:
                        type: integer
                      cloud_count:
                        type: integer
                      local_pct:
                        type: number
                      cloud_pct:
                        type: number
                    description: Local vs cloud request split breakdown.
                  top_fallback_triggers:
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                        count:
                          type: integer
                    description: Top 10 fallback trigger codes by frequency, sorted descending.
                  app_breakdown:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        total:
                          type: integer
                        fallback_count:
                          type: integer
                        fallback_rate_pct:
                          type: number
                    description: Per-app request and fallback counts (top 20 by volume).
                  deployment_breakdown:
                    type: array
                    items:
                      type: object
                    description: Per-deployment request and fallback counts (top 20 by volume).
                  experiment_breakdown:
                    type: array
                    items:
                      type: object
                    description: Per-experiment request and fallback counts (top 20 by volume).
                  gate_class_breakdown:
                    type: object
                    additionalProperties:
                      type: integer
                    description: 'Count of events (not gate occurrences) that contain each gate_class in attempt_details or
                      advisory_failures.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/runtime/plan:
    post:
      operationId: runtime_plan
      summary: Request an optimized runtime execution plan for a model on a specific device.
      tags:
      - runtime
      x-octomil-method: runtime.plan
      x-octomil-since: 1.15.0
      x-octomil-stability: experimental
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - reads benchmark data from database
      - may emit telemetry events (plan.requested, plan.resolved)
      x-octomil-errors:
      - model_not_found
      - invalid_request
      - authentication_failed
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Request an optimized runtime execution plan for a model on a specific device.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/runtime_plan_request_schema'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runtime_plan_response_schema'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/api-keys:
    post:
      operationId: settings_api_keys_create
      summary: Create a new org-scoped API key with the given scopes. The full raw key value is returned exactly once in the
        api_key fi
      tags:
      - settings
      x-octomil-method: settings.api_keys.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts api_key row
      - raw key generated and returned once
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - app_not_found
      description: Create a new org-scoped API key with the given scopes. The full raw key value is returned exactly once
        in the api_key field and cannot be retrieved again.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateApiKeyRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApiKeyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_api_keys_list
      summary: List all API keys belonging to the organization, across all key types and states. The raw key value is never
        returned; o
      tags:
      - settings
      x-octomil-method: settings.api_keys.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: List all API keys belonging to the organization, across all key types and states. The raw key value is
        never returned; only the non-secret prefix is included.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/api-key-scopes:
    get:
      operationId: settings_api_keys_get_scopes
      summary: Return the canonical scope registry, key-type grouped scope views, and named scope templates. Intended for
        dashboard UIs
      tags:
      - settings
      x-octomil-method: settings.api_keys.get_scopes
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the canonical scope registry, key-type grouped scope views, and named scope templates. Intended
        for dashboard UIs rendering scope selectors. Scope registry is org-agnostic; no org_id required.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyScopesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/api-keys/{key_id}:
    delete:
      operationId: settings_api_keys_revoke
      summary: Revoke an API key by setting its revoked_at timestamp. The key immediately stops authenticating. Returns the
        updated key
      tags:
      - settings
      x-octomil-method: settings.api_keys.revoke
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets revoked_at on the api_key row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - api_key_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Revoke an API key by setting its revoked_at timestamp. The key immediately stops authenticating. Returns
        the updated key record.
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
        description: Key identifier (UUIDv4).
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/api-keys/{key_id}/rotate:
    post:
      operationId: settings_api_keys_rotate
      summary: Atomically revoke the given key and create a replacement with identical configuration (name, key_type, scopes,
        environme
      tags:
      - settings
      x-octomil-method: settings.api_keys.rotate
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - sets revoked_at on the old api_key row
      - inserts a new api_key row
      - new raw key generated and returned once
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - api_key_not_found
      - api_key_already_revoked
      - network_unavailable
      - request_timeout
      - server_error
      description: Atomically revoke the given key and create a replacement with identical configuration (name, key_type,
        scopes, environment, federation_id, model_id, app_id). The new raw key value is returned exactly once in the api_key
        field.
      parameters:
      - name: key_id
        in: path
        required: true
        schema:
          type: string
        description: Identifier of the key to rotate (UUIDv4).
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApiKeyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/app-keys:
    post:
      operationId: settings_app_keys_create
      summary: Create an app key (oct_app_live_... or oct_app_test_...) for server-side inference tied to a specific Octomil
        App. App k
      tags:
      - settings
      x-octomil-method: settings.app_keys.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts api_key row (key_type=app_key)
      - raw key generated and returned once
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - app_not_found
      description: Create an app key (oct_app_live_... or oct_app_test_...) for server-side inference tied to a specific Octomil
        App. App keys inherit the app's inference configuration without requiring explicit model selection per call.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAppKeyRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateAppKeyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing/checkout/reconcile:
    post:
      operationId: settings_billing_checkout_reconcile
      summary: Reconcile a Stripe Checkout session after the user returns from the Stripe-hosted payment page. Re-fetches
        the session f
      tags:
      - settings
      x-octomil-method: settings.billing.checkout.reconcile
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - projects billing_subscriptions row via advisory-lock-protected seam (same as webhook)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - model_not_found
      - checkout_not_complete
      - server_error
      - network_unavailable
      - request_timeout
      description: Reconcile a Stripe Checkout session after the user returns from the Stripe-hosted payment page. Re-fetches
        the session from Stripe, verifies org ownership, acquires the per-(org, customer) advisory lock, and projects the
        resulting subscription via the canonical BillingSubscriptionSyncService seam (the same path used by the webhook handler).
        Returns the projected subscription state.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReconcileCheckoutRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReconcileCheckoutResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing/checkout:
    post:
      operationId: settings_billing_create_checkout_session
      summary: Create a Stripe checkout session for plan upgrade.
      tags:
      - settings
      x-octomil-method: settings.billing.create_checkout_session
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - server_error
      x-octomil-side-effects:
      - may create Stripe customer if none exists
      - creates Stripe checkout session
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - server_error
      description: Create a Stripe checkout session for plan upgrade.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckoutRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing/portal:
    post:
      operationId: settings_billing_create_portal_session
      summary: Create a Stripe billing portal session for subscription management.
      tags:
      - settings
      x-octomil-method: settings.billing.create_portal_session
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - server_error
      x-octomil-side-effects:
      - creates Stripe portal session
      x-octomil-errors:
      - authentication_failed
      - invalid_input
      - server_error
      description: Create a Stripe billing portal session for subscription management.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortalRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing:
    get:
      operationId: settings_billing_get
      summary: Return the current billing subscription state for the organization. This is a local projection of the Stripe
        subscriptio
      tags:
      - settings
      x-octomil-method: settings.billing.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the current billing subscription state for the organization. This is a local projection of the Stripe
        subscription cache; may lag by up to one webhook delivery cycle.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_billing_update
      summary: "Update local billing plan. Admin/dev override only \u2014 production plan changes should go through Stripe\
        \ checkout."
      tags:
      - settings
      x-octomil-method: settings.billing.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates or creates BillingSubscription row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      description: "Update local billing plan. Admin/dev override only \u2014 production plan changes should go through Stripe\
        \ checkout."
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBillingRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing/invoices:
    get:
      operationId: settings_billing_list_invoices
      summary: List recent Stripe invoices for the organization. Fetched live from Stripe on demand and cached server-side
        for 90 secon
      tags:
      - settings
      x-octomil-method: settings.billing.list_invoices
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - billing_customer_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: List recent Stripe invoices for the organization. Fetched live from Stripe on demand and cached server-side
        for 90 seconds per Stripe customer ID. Returns up to 20 invoices ordered by creation date, most recent first.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingInvoicesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/billing/support-context:
    get:
      operationId: settings_billing_support_context_get
      summary: Return a billing context bundle for support handoff. Includes plan, subscription status, and the latest Stripe
        invoice s
      tags:
      - settings
      x-octomil-method: settings.billing.support_context.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: Return a billing context bundle for support handoff. Includes plan, subscription status, and the latest
        Stripe invoice summary so support can triage a billing issue without follow-up. Latest invoice is fetched via stripe.Invoice.list(limit=1)
        and cached server-side for 60 seconds per customer.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingSupportContextResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/connections:
    post:
      operationId: settings_cloud_connections_create
      summary: Create a new cloud provider connection for the org. The server immediately verifies the credential after creation
        and se
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts connection row
      - attempts live credential verification (verification_status set on response)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - upstream_provider_unavailable
      - server_error
      description: Create a new cloud provider connection for the org. The server immediately verifies the credential after
        creation and sets verification_status accordingly.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConnectionRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_cloud_connections_list
      summary: List all cloud provider connections for the calling org. org_id is resolved from the auth context (JWT). Optionally
        filt
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: List all cloud provider connections for the calling org. org_id is resolved from the auth context (JWT).
        Optionally filter by provider.
      parameters:
      - name: provider
        in: query
        required: false
        schema:
          type: string
        description: Filter to a specific provider, e.g. 'openai'.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/connections/{connection_id}:
    delete:
      operationId: settings_cloud_connections_delete
      summary: Delete a cloud provider connection. Returns 204 No Content on success.
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.delete
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - deletes connection row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - connection_not_found
      - server_error
      description: Delete a cloud provider connection. Returns 204 No Content on success.
      parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_cloud_connections_get
      summary: Fetch a single cloud provider connection by id. API key is never returned.
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - connection_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single cloud provider connection by id. API key is never returned.
      parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_cloud_connections_update
      summary: Partially update a cloud provider connection. If api_key is provided, the server re-verifies the credential
        after update
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates connection row
      - may attempt live credential re-verification if api_key is updated
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - connection_not_found
      - invalid_input
      - upstream_provider_unavailable
      - server_error
      description: Partially update a cloud provider connection. If api_key is provided, the server re-verifies the credential
        after update and reflects the result in verification_status.
      parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConnectionRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/connections/{connection_id}/models:
    get:
      operationId: settings_cloud_connections_list_models
      summary: Fetch the list of model ids available from a cloud provider connection by making a live request to the provider's
        models
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.list_models
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - connection_not_found
      - invalid_input
      - upstream_provider_unavailable
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch the list of model ids available from a cloud provider connection by making a live request to the
        provider's models endpoint. Returns sorted model ids.
      parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectionModelsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/connections/{connection_id}/test:
    post:
      operationId: settings_cloud_connections_test
      summary: Active-probe a cloud provider connection by making a live models-listing request to the provider. On success,
        updates th
      tags:
      - settings
      x-octomil-method: settings.cloud.connections.test
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects:
      - may update last_verified_at and verification_latency_ms on the connection row
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - connection_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Active-probe a cloud provider connection by making a live models-listing request to the provider. On success,
        updates the connection's last_verified_at and verification_latency_ms in the database. Returns success=false (not
        an error status) for non-200 provider responses so the caller can surface the diagnostic.
      parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/credentials:
    post:
      operationId: settings_cloud_credentials_create
      summary: 'Store a new BYOK cloud provider credential for the org. The API key is encrypted at rest and never returned.
        Records an '
      tags:
      - settings
      x-octomil-method: settings.cloud.credentials.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts provider_credential row (encrypted api_key)
      - records audit event (action=create_cloud_credential)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - server_error
      description: Store a new BYOK cloud provider credential for the org. The API key is encrypted at rest and never returned.
        Records an audit event on success.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCredentialRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_cloud_credentials_list
      summary: "List BYOK cloud provider credentials for the org. Optionally filter by provider. API keys are never returned\
        \ \u2014 only meta"
      tags:
      - settings
      x-octomil-method: settings.cloud.credentials.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: "List BYOK cloud provider credentials for the org. Optionally filter by provider. API keys are never returned\
        \ \u2014 only metadata."
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: provider
        in: query
        required: false
        schema:
          type: string
        description: Filter results to a specific provider identifier, e.g. 'openai'.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/credentials/{credential_id}:
    delete:
      operationId: settings_cloud_credentials_delete
      summary: Delete a BYOK cloud provider credential. Records an audit event on success.
      tags:
      - settings
      x-octomil-method: settings.cloud.credentials.delete
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - deletes provider_credential row
      - records audit event (action=delete_cloud_credential)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - credential_not_found
      - server_error
      description: Delete a BYOK cloud provider credential. Records an audit event on success.
      parameters:
      - name: credential_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - status
                - id
                properties:
                  status:
                    type: string
                    const: deleted
                  id:
                    type: string
                additionalProperties: false
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_cloud_credentials_get
      summary: Fetch a single BYOK cloud provider credential by id. API key is never returned.
      tags:
      - settings
      x-octomil-method: settings.cloud.credentials.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - credential_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a single BYOK cloud provider credential by id. API key is never returned.
      parameters:
      - name: credential_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_cloud_credentials_update
      summary: Partially update a BYOK cloud provider credential. Providing api_key replaces the stored encrypted key. Records
        an audit
      tags:
      - settings
      x-octomil-method: settings.cloud.credentials.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates provider_credential row
      - records audit event (action=update_cloud_credential, api_key excluded from metadata)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - credential_not_found
      - invalid_input
      - server_error
      description: Partially update a BYOK cloud provider credential. Providing api_key replaces the stored encrypted key.
        Records an audit event on success (api_key field is never included in audit metadata).
      parameters:
      - name: credential_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCredentialRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/cloud/policy:
    get:
      operationId: settings_cloud_policy_get
      summary: 'Fetch the cloud fallback policy for the org: whether cloud inference is enabled, which credential sources
        are permitted,'
      tags:
      - settings
      x-octomil-method: settings.cloud.policy.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Fetch the cloud fallback policy for the org: whether cloud inference is enabled, which credential sources
        are permitted, provider allowlist, and default fallback model.'
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudPolicyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_cloud_policy_update
      summary: Partially update the cloud fallback policy for the org. Records an audit event on success.
      tags:
      - settings
      x-octomil-method: settings.cloud.policy.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_settings row (cloud policy fields)
      - records audit event (action=update_cloud_policy)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - server_error
      description: Partially update the cloud fallback policy for the org. Records an audit event on success.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCloudPolicyRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudPolicyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/config:
    get:
      operationId: settings_execution_config_get
      summary: 'Return the full execution configuration for the org: all local runtime targets, all cloud targets, and the
        current routi'
      tags:
      - settings
      x-octomil-method: settings.execution.config.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Return the full execution configuration for the org: all local runtime targets, all cloud targets, and
        the current routing strategy and cooldown settings.'
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionConfigResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/health:
    get:
      operationId: settings_execution_health_get
      summary: 'Return live health metrics for all execution targets: request rates, inflight counts, success/failure rates,
        cooldown st'
      tags:
      - settings
      x-octomil-method: settings.execution.health.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: 'Return live health metrics for all execution targets: request rates, inflight counts, success/failure
        rates, cooldown state, and last verification timestamps.'
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionHealthResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/local:
    post:
      operationId: settings_execution_local_create
      summary: Register a new local runtime target for the org. Records an audit event on success.
      tags:
      - settings
      x-octomil-method: settings.execution.local.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts org_local_runtime row
      - records audit event (action=create_local_runtime)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - server_error
      description: Register a new local runtime target for the org. Records an audit event on success.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLocalRuntimeRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocalTargetResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_execution_local_list
      summary: 'List all local runtime targets for the org: active, disabled, and the env-fallback sentinel row if present.'
      tags:
      - settings
      x-octomil-method: settings.execution.local.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: 'List all local runtime targets for the org: active, disabled, and the env-fallback sentinel row if present.'
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LocalTargetResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/local/{runtime_id}:
    delete:
      operationId: settings_execution_local_delete
      summary: Delete a local runtime target. Records an audit event on success. Returns 204 No Content.
      tags:
      - settings
      x-octomil-method: settings.execution.local.delete
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - deletes org_local_runtime row
      - records audit event (action=delete_local_runtime)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - local_runtime_not_found
      - server_error
      description: Delete a local runtime target. Records an audit event on success. Returns 204 No Content.
      parameters:
      - name: runtime_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_execution_local_update
      summary: Partially update a local runtime target. Records an audit event on success.
      tags:
      - settings
      x-octomil-method: settings.execution.local.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_local_runtime row
      - records audit event (action=update_local_runtime)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - local_runtime_not_found
      - invalid_input
      - server_error
      description: Partially update a local runtime target. Records an audit event on success.
      parameters:
      - name: runtime_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateLocalRuntimeRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocalTargetResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/local/{runtime_id}/test:
    post:
      operationId: settings_execution_local_test
      summary: Active-probe a local runtime endpoint by making a live request. Returns success, error message, and latency.
        Non-2xx res
      tags:
      - settings
      x-octomil-method: settings.execution.local.test
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - local_runtime_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Active-probe a local runtime endpoint by making a live request. Returns success, error message, and latency.
        Non-2xx responses from the runtime are returned as success=false, not as HTTP errors.
      parameters:
      - name: runtime_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestConnectionResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/execution/routing:
    patch:
      operationId: settings_execution_routing_update
      summary: Update the cloud routing strategy and/or cooldown seconds. Records an audit event on success.
      tags:
      - settings
      x-octomil-method: settings.execution.routing.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_settings row (cloud_routing_strategy, cloud_cooldown_seconds)
      - records audit event (action=update_execution_routing)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      - server_error
      description: Update the cloud routing strategy and/or cooldown seconds. Records an audit event on success.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRoutingRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateRoutingResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings:
    get:
      operationId: settings_get
      summary: 'Return the governance and operational settings for an organization. If no settings record exists yet the server
        upserts '
      tags:
      - settings
      x-octomil-method: settings.get
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the governance and operational settings for an organization. If no settings record exists yet the
        server upserts a default record and returns it.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
        description: Organization identifier. Must match the authenticated principal's org.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgSettingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_update
      summary: Partially update org-level governance and operational settings. Only fields present in the request body are
        applied. Rec
      tags:
      - settings
      x-octomil-method: settings.update
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_settings row
      - records audit_log entry (action=update_settings)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      description: Partially update org-level governance and operational settings. Only fields present in the request body
        are applied. Records an audit log entry on every successful mutation.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSettingsRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgSettingsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/auth:
    get:
      operationId: settings_get_auth_config
      summary: Return the authentication and identity provider configuration for the organization. OAuth provider availability
        (google_
      tags:
      - settings
      x-octomil-method: settings.get_auth_config
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: Return the authentication and identity provider configuration for the organization. OAuth provider availability
        (google_enabled, apple_enabled, github_enabled) reflects server-level credential configuration and cannot be set per-org.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthConfigResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_update_auth_config
      summary: 'Partially update org-level authentication configuration: SSO, SCIM, and primary identity mode. Records an
        audit log entr'
      tags:
      - settings
      x-octomil-method: settings.update_auth_config
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_settings row (auth fields)
      - records audit_log entry (action=update_auth_config)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      description: 'Partially update org-level authentication configuration: SSO, SCIM, and primary identity mode. Records
        an audit log entry on every successful mutation.'
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuthConfigRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthConfigResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/policies:
    get:
      operationId: settings_get_policies
      summary: 'Return org-level serving and data-handling policy configuration including the active serving policy, telemetry
        emission '
      tags:
      - settings
      x-octomil-method: settings.get_policies
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: Return org-level serving and data-handling policy configuration including the active serving policy, telemetry
        emission toggle, and privacy mode.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PoliciesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_update_policies
      summary: Partially update org-level serving and data-handling policies.
      tags:
      - settings
      x-octomil-method: settings.update_policies
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates org_settings row (policy fields)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      description: Partially update org-level serving and data-handling policies.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePoliciesRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PoliciesResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/integrations:
    post:
      operationId: settings_integrations_create
      summary: Create a new notification or data integration for the organization. Enabled by default on creation. Use settings.update-
      tags:
      - settings
      x-octomil-method: settings.integrations.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts integration row (enabled=true)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      description: Create a new notification or data integration for the organization. Enabled by default on creation. Use
        settings.update-integration to configure provider-specific fields after.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIntegrationRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_integrations_list
      summary: List all integrations configured for the organization. Returns summary records; use settings.get-integration
        for provide
      tags:
      - settings
      x-octomil-method: settings.integrations.list
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - network_unavailable
      - request_timeout
      - server_error
      description: List all integrations configured for the organization. Returns summary records; use settings.get-integration
        for provider-specific detail fields.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/integrations/{integration_id}:
    delete:
      operationId: settings_integrations_delete
      summary: Delete an integration. Verifies org_id ownership before deletion.
      tags:
      - settings
      x-octomil-method: settings.integrations.delete
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - deletes integration row
      x-octomil-errors:
      - authentication_failed
      - integration_not_found
      description: Delete an integration. Verifies org_id ownership before deletion.
      parameters:
      - name: integration_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: settings_integrations_get
      summary: Get full details for a single integration. Scoped by org_id.
      tags:
      - settings
      x-octomil-method: settings.integrations.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - integration_not_found
      description: Get full details for a single integration. Scoped by org_id.
      parameters:
      - name: integration_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationDetailResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    patch:
      operationId: settings_integrations_update
      summary: Partially update an integration. Only whitelisted fields are applied.
      tags:
      - settings
      x-octomil-method: settings.integrations.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - updates integration row
      x-octomil-errors:
      - authentication_failed
      - integration_not_found
      - invalid_input
      description: Partially update an integration. Only whitelisted fields are applied.
      parameters:
      - name: integration_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateIntegrationRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationDetailResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/integrations/{integration_id}/validate:
    post:
      operationId: settings_integrations_validate
      summary: "Validate integration configuration. Local validation only \u2014 does not test live connectivity."
      tags:
      - settings
      x-octomil-method: settings.integrations.validate
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - integration_not_found
      description: "Validate integration configuration. Local validation only \u2014 does not test live connectivity."
      parameters:
      - name: integration_id
        in: path
        required: true
        schema:
          type: string
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationTestResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/publishable-keys:
    post:
      operationId: settings_publishable_keys_create
      summary: Create a publishable key (oct_pub_live_... or oct_pub_test_...) for client-side SDK use. Publishable keys have
        a restric
      tags:
      - settings
      x-octomil-method: settings.publishable_keys.create
      x-octomil-since: 1.26.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - inserts api_key row (key_type=publishable_key)
      - raw key generated and returned once
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - missing_org_context
      - invalid_input
      description: Create a publishable key (oct_pub_live_... or oct_pub_test_...) for client-side SDK use. Publishable keys
        have a restricted scope set, optional origin restrictions, and a rate limit. The full raw key is returned exactly
        once.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: Optional client-generated UUIDv4. Repeated requests within 24h replay the original response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePublishableKeyRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePublishableKeyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/settings/usage-limits:
    get:
      operationId: settings_usage_limits_get
      summary: Retrieve usage limits for an organization.
      tags:
      - settings
      x-octomil-method: settings.usage_limits.get
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      description: Retrieve usage limits for an organization.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageLimitsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      operationId: settings_usage_limits_update
      summary: Upsert usage limits for an organization. Creates or updates each metric limit.
      tags:
      - settings
      x-octomil-method: settings.usage_limits.update
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: null
      x-octomil-retryable-on: []
      x-octomil-side-effects:
      - creates or updates usage_limits rows
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      description: Upsert usage limits for an organization. Creates or updates each metric limit.
      parameters:
      - name: org_id
        in: query
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUsageLimitsRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageLimitsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/telemetry/agent/batches:
    post:
      operationId: telemetry_agent_batch
      summary: Ingest a batch of SDK/agent telemetry events at the /batches (plural) path. Hard ceiling is 4x MAX_BATCH_EVENTS;
        exceedi
      tags:
      - telemetry
      x-octomil-method: telemetry.agent.batch
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - writes agent telemetry events
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - payload_too_large
      - network_unavailable
      - request_timeout
      - server_error
      description: Ingest a batch of SDK/agent telemetry events at the /batches (plural) path. Hard ceiling is 4x MAX_BATCH_EVENTS;
        exceeding returns 413. Per-event rejections do not fail the request.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - events
              properties:
                events:
                  type: array
                  description: 'Batch of agent telemetry events. Hard ceiling is 4x MAX_BATCH_EVENTS; batches exceeding this
                    ceiling return 413.

                    '
                  items:
                    type: object
                    required:
                    - name
                    properties:
                      name:
                        type: string
                        description: Event name.
                      timestamp:
                        type: string
                        format: date-time
                        nullable: true
                      attributes:
                        type: object
                        additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - accepted
                properties:
                  accepted:
                    type: integer
                    description: Number of events accepted.
                  rejected:
                    type: integer
                    description: Number of events rejected.
                  errors:
                    type: array
                    items:
                      type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/telemetry/batches:
    post:
      operationId: telemetry_batch
      summary: Upload a batch of telemetry events from the device to the server ingestion pipeline.
      tags:
      - telemetry
      x-octomil-method: telemetry.batch
      x-octomil-since: 1.4.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: false
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      - rate_limited
      x-octomil-side-effects:
      - events ingested into telemetry pipeline (PostHog / OTLP collector)
      - server deduplicates by batchId
      x-octomil-errors:
      - device_not_registered
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Upload a batch of telemetry events from the device to the server ingestion pipeline.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/telemetry_batch'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - batch_id
                - acked_through_seq
                properties:
                  batch_id:
                    type: string
                    description: Echo of the submitted batch identifier.
                  acked_through_seq:
                    type: integer
                    description: Highest sequence number acknowledged; device may purge events up to this seq.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v2/telemetry/events:
    post:
      operationId: telemetry_v2_events
      summary: Accept telemetry events at the /api/v2/telemetry/events path in either simplified or OTLP envelope format.
        Events are ro
      tags:
      - telemetry
      x-octomil-method: telemetry.v2.events
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - routes events to subsystem handlers by name prefix
      - schedules OTLP forwarding as background task (for OTLP envelope)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Accept telemetry events at the /api/v2/telemetry/events path in either simplified or OTLP envelope format.
        Events are routed by name prefix to subsystem handlers. device.* events are forwarding-only (no DB write). OTLP forwarding
        is dispatched as a background task. Returns X-Device-Needs-Registration header when device is unregistered.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              description: 'Accepts two envelope formats: (1) Simplified: {"resource": {...}, "events": [...]} (2) OTLP: {"resourceLogs":
                [...], "scopeLogs": [...], "logRecords": [...]}

                '
              oneOf:
              - type: object
                required:
                - events
                properties:
                  resource:
                    type: object
                    additionalProperties: true
                  events:
                    type: array
                    items:
                      type: object
                      required:
                      - name
                      properties:
                        name:
                          type: string
                        timestamp:
                          type: string
                          format: date-time
                          nullable: true
                        attributes:
                          type: object
                          additionalProperties: true
              - type: object
                required:
                - resourceLogs
                properties:
                  resourceLogs:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  scopeLogs:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  logRecords:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - accepted
                properties:
                  accepted:
                    type: integer
                  device_needs_registration:
                    type: boolean
                    nullable: true
                    description: 'When true the response also includes X-Device-Needs-Registration header.

                      '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/traces/{trace_id}:
    get:
      operationId: traces_get
      summary: Retrieve all spans for a specific trace by trace_id. The org_id from the auth token is used to gate access.
      tags:
      - traces
      x-octomil-method: traces.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - resource_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Retrieve all spans for a specific trace by trace_id. The org_id from the auth token is used to gate access.
      parameters:
      - name: trace_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: 'All TraceResponse records sharing the given trace_id, ordered by created_at ascending. Returns
                  404 if no records exist for the trace_id within the caller''s org.

                  '
                items:
                  type: object
                  additionalProperties: true
                  description: 'TraceResponse: id, trace_id, span_id, parent_span_id, request_id, http_method, http_path,
                    http_status_code, org_id, device_id, total_duration_ms, status, error_code.

                    '
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/traces:
    get:
      operationId: traces_list
      summary: 'List distributed traces for the authenticated org within a lookback window. Scoped to the caller''s org_id
        from the auth '
      tags:
      - traces
      x-octomil-method: traces.list
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List distributed traces for the authenticated org within a lookback window. Scoped to the caller's org_id
        from the auth token.
      parameters:
      - name: hours
        in: query
        required: false
        schema:
          type: integer
        description: Lookback window in hours. Defaults to 24. Max 168 (7 days).
      - name: device_id
        in: query
        required: false
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - traces
                - total
                - limit
                - offset
                properties:
                  traces:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/traces/stats/summary:
    get:
      operationId: traces_stats_summary
      summary: Aggregate statistics for request traces within a time window. Returns total, success, failure, timeout, and
        auth-failure
      tags:
      - traces
      x-octomil-method: traces.stats.summary
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Aggregate statistics for request traces within a time window. Returns total, success, failure, timeout,
        and auth-failure counts plus average duration in ms.
      parameters:
      - name: hours
        in: query
        required: false
        schema:
          type: integer
        description: "Lookback window in hours (1\u2013168, default 24)."
      - name: http_path
        in: query
        required: false
        schema:
          type: string
        description: Filter by path prefix.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - total_requests
                - success_count
                - failure_count
                properties:
                  total_requests:
                    type: integer
                  success_count:
                    type: integer
                  failure_count:
                    type: integer
                  timeout_count:
                    type: integer
                  auth_failed_count:
                    type: integer
                  avg_duration_ms:
                    type: number
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/aggregate:
    post:
      operationId: training_aggregate
      summary: Aggregate device training updates into a new model version using the specified FL strategy. Loads the base
        version PyTor
      tags:
      - training
      x-octomil-method: training.aggregate
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates a new ModelVersion row
      - uploads PyTorch and ONNX artifacts to storage
      - if publish=true, publishes the new version
      - emits audit log entry (aggregate_training_updates)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - version_not_found
      - invalid_input
      - training_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Aggregate device training updates into a new model version using the specified FL strategy. Loads the base
        version PyTorch artifact from storage, runs FedAvg/FedProx/etc., saves PyTorch + ONNX artifacts, and creates a new
        ModelVersion row. If a completed SecAgg session exists for this model, uses the secure-aggregated result directly
        instead of loading raw device updates.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              - strategy
              properties:
                model_id:
                  type: string
                strategy:
                  type: string
                  enum:
                  - fedavg
                  - fedprox
                  - fedadam
                  - krum
                  - fedtrimmedavg
                  - ditto
                  - scaffold
                  - metrics
                  description: 'Aggregation strategy. "metrics" performs metrics-only aggregation without weight merging.

                    '
                base_version:
                  type: string
                  nullable: true
                  description: Base model version to aggregate on top of. Defaults to latest published version.
                new_version:
                  type: string
                  nullable: true
                  description: Version string for the aggregated output. Auto-increments patch if omitted.
                min_updates:
                  type: integer
                  default: 1
                  description: Minimum number of device updates required to proceed.
                update_format:
                  type: string
                  default: state_dict
                federation_id:
                  type: string
                  nullable: true
                  description: When set, limits aggregation to updates from federation member orgs only.
                publish:
                  type: boolean
                  default: false
                  description: If true, auto-publish the new version after aggregation.
                enable_differential_privacy:
                  type: boolean
                  default: false
                dp_clipping_norm:
                  type: number
                  nullable: true
                dp_epsilon:
                  type: number
                  nullable: true
                dp_delta:
                  type: number
                  nullable: true
                use_advanced_weighting:
                  type: boolean
                  default: false
                staleness_penalty_hours:
                  type: number
                  nullable: true
                trust_score_weight:
                  type: number
                  nullable: true
                norm_max:
                  type: number
                  nullable: true
                inbound_filters:
                  type: array
                  nullable: true
                  items:
                    type: object
                proximal_mu:
                  type: number
                  nullable: true
                  description: FedProx proximal term (strategy=fedprox only).
                server_learning_rate:
                  type: number
                  nullable: true
                client_learning_rate:
                  type: number
                  nullable: true
                beta_1:
                  type: number
                  nullable: true
                beta_2:
                  type: number
                  nullable: true
                tau:
                  type: number
                  nullable: true
                num_malicious_clients:
                  type: integer
                  nullable: true
                num_clients_to_keep:
                  type: integer
                  nullable: true
                trim_ratio:
                  type: number
                  nullable: true
                lambda_ditto:
                  type: number
                  nullable: true
                scaffold_learning_rate:
                  type: number
                  nullable: true
                architecture:
                  type: string
                  nullable: true
                input_dim:
                  type: integer
                  nullable: true
                hidden_dim:
                  type: integer
                  nullable: true
                output_dim:
                  type: integer
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Aggregation result \u2014 see schemas/training/AggregateResponse.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/cancel:
    post:
      operationId: training_cancel_round
      summary: Cancel a running or pending round. Selected devices stop local training on their next sync; any in-flight aggregation
        is
      tags:
      - training
      x-octomil-method: training.cancel_round
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets TrainingRound.state to "cancelled"
      - devices on the round receive the cancel signal on next sync
      - emits telemetry event (training.round_cancelled) with reason
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Cancel a running or pending round. Selected devices stop local training on their next sync; any in-flight
        aggregation is discarded.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The cancelled TrainingRound row.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds:
    post:
      operationId: training_create_round
      summary: Create a new federated training round. Round starts in "pending" until devices are selected (training.select_devices)
        an
      tags:
      - training
      x-octomil-method: training.create_round
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts a TrainingRound row
      - emits telemetry event (training.round_created)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - model_not_found
      - invalid_input
      - rate_limited
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new federated training round. Round starts in "pending" until devices are selected (training.select_devices)
        and the round is explicitly started (training.start_round).
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - federation_id
              - model_id
              properties:
                federation_id:
                  type: string
                model_id:
                  type: string
                target_device_count:
                  type: integer
                  description: How many devices to include in this round.
                aggregation_strategy:
                  type: string
                  description: "\"fedavg\" | \"fedprox\" | \"scaffold\" \u2014 see ml_service/trainer/."
                privacy_policy:
                  type: object
                  description: Round-specific DP / secure-aggregation overrides on top of the federation default.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The created TrainingRound row in "pending" state.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      operationId: training_list_rounds
      summary: List federated training rounds for the caller's org / federations.
      tags:
      - training
      x-octomil-method: training.list_rounds
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List federated training rounds for the caller's org / federations.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
      - name: federation_id
        in: query
        required: false
        schema:
          type: string
      - name: state
        in: query
        required: false
        schema:
          type: string
        description: "Filter \u2014 see enums/training_job_state.yaml."
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
          default: 50
      - name: cursor
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of rounds entries.
                items:
                  type: object
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/training-jobs/{job_id}/complete:
    post:
      operationId: training_device_jobs_complete
      summary: Mark a training job as complete with final metrics and optional adapter artifact reference. Transitions the
        job to a ter
      tags:
      - training
      x-octomil-method: training.device_jobs.complete
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - transitions TrainingJob to terminal state
      - stores final metrics and adapter reference
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Mark a training job as complete with final metrics and optional adapter artifact reference. Transitions
        the job to a terminal state on the server.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                metrics:
                  type: object
                  nullable: true
                  description: Final training metrics (loss, accuracy, etc.).
                adapter_path:
                  type: string
                  nullable: true
                  description: Storage path for the resulting adapter artifact.
                adapter_sha256:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Acknowledgement with final job state.
                properties:
                  job_id:
                    type: string
                  state:
                    type: string
                  updated:
                    type: boolean
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/training-jobs:
    post:
      operationId: training_device_jobs_create
      summary: Create a new on-device training job for a specific device. Called by the device agent when the device accepts
        a round of
      tags:
      - training
      x-octomil-method: training.device_jobs.create
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - creates TrainingJob row for the device
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - training_not_supported
      - network_unavailable
      - request_timeout
      - server_error
      description: Create a new on-device training job for a specific device. Called by the device agent when the device accepts
        a round offer or initiates personalization. Returns the job with its initial state.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - job_type
              - binding_key
              - base_model_id
              - base_version
              properties:
                job_type:
                  type: string
                  enum:
                  - personalization
                  - federated
                binding_key:
                  type: string
                  description: Slot binding key from the device's desired state.
                base_model_id:
                  type: string
                base_version:
                  type: string
                starting_adapter_version:
                  type: string
                  nullable: true
                dataset_id:
                  type: string
                  nullable: true
                round_id:
                  type: string
                  nullable: true
                participation_id:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "DeviceTrainingJobResponse \u2014 see schemas/training/DeviceTrainingJobResponse.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/devices/{device_id}/training-jobs/{job_id}/status:
    post:
      operationId: training_device_jobs_update_status
      summary: Device reports a state change or progress update for an active training job. Used for streaming progress (e.g.
        epoch com
      tags:
      - training
      x-octomil-method: training.device_jobs.update_status
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates TrainingJob state and progress fields
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Device reports a state change or progress update for an active training job. Used for streaming progress
        (e.g. epoch completion) and intermediate state transitions during training.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: job_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - state
              properties:
                state:
                  type: string
                  description: New job state value.
                progress:
                  type: object
                  nullable: true
                  description: Structured progress data (epoch, loss, etc.).
                last_error:
                  type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Acknowledgement with updated job state.
                properties:
                  job_id:
                    type: string
                  state:
                    type: string
                  updated:
                    type: boolean
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}:
    get:
      operationId: training_get_round
      summary: Fetch a training round by id.
      tags:
      - training
      x-octomil-method: training.get_round
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Fetch a training round by id.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Full TrainingRound row with state + selected devices + per-device contribution status.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/personalized/{device_id}:
    get:
      operationId: training_personalized_get
      summary: Get a device's personalized model state for a specific model + federation pair. Returns both the state record
        (null if n
      tags:
      - training
      x-octomil-method: training.personalized.get
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Get a device's personalized model state for a specific model + federation pair. Returns both the state
        record (null if no state exists yet) and a boolean indicating whether a personalized weights artifact is stored.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
        description: Device UUID.
      - name: model_id
        in: query
        required: true
        schema:
          type: string
      - name: federation_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Personalized model info for the device.
                properties:
                  state:
                    type: object
                    description: "PersonalizedState record \u2014 see schemas/training/PersonalizedState.json"
                    nullable: true
                    additionalProperties: true
                  has_personalized_weights:
                    type: boolean
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      operationId: training_personalized_update
      summary: 'Create or update a device''s personalized model state. Upserts the record keyed on (device_id, model_id, federation_id). '
      tags:
      - training
      x-octomil-method: training.personalized.update
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - upserts ClientModelState row for (device_id, model_id, federation_id)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Create or update a device's personalized model state. Upserts the record keyed on (device_id, model_id,
        federation_id). Returns HTTP 200 on both create and update.
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              - federation_id
              properties:
                model_id:
                  type: string
                federation_id:
                  type: string
                personalization_type:
                  type: string
                  enum:
                  - ditto
                  - fedper
                  - local_finetune
                  default: ditto
                weights_key:
                  type: string
                  nullable: true
                  description: S3/storage key for personalized weights artifact.
                metrics:
                  type: object
                  nullable: true
                  description: Local evaluation metrics (accuracy, loss, etc.).
                head_layers:
                  type: array
                  items:
                    type: string
                  nullable: true
                  description: Layer names for FedPer head split.
                round_number:
                  type: integer
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Updated PersonalizedState \u2014 see schemas/training/PersonalizedState.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/personalized:
    get:
      operationId: training_personalized_list
      summary: 'List all client personalization states for a federation. Returns the most recent per-device personalized model
        metadata '
      tags:
      - training
      x-octomil-method: training.personalized.list
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List all client personalization states for a federation. Returns the most recent per-device personalized
        model metadata including weights storage key, metrics, and the personalization strategy in use.
      parameters:
      - name: federation_id
        in: query
        required: true
        schema:
          type: string
        description: Federation to scope the listing. Required.
      - name: model_id
        in: query
        required: false
        schema:
          type: string
        description: Optional filter by model.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          default: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Array of PersonalizedState objects for all client devices in the federation.
                items:
                  type: object
                  description: See schemas/training/PersonalizedState.json
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/personalized/{device_id}/metrics:
    get:
      operationId: training_personalized_metrics
      summary: Get a device's personalization metrics (local accuracy, loss, etc.) for a specific model and federation. Returns
        only th
      tags:
      - training
      x-octomil-method: training.personalized.metrics
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: "Get a device's personalization metrics (local accuracy, loss, etc.) for a specific model and federation.\
        \ Returns only the metrics subset \u2014 use training.personalized.get for the full state including weights_key."
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
      - name: model_id
        in: query
        required: true
        schema:
          type: string
      - name: federation_id
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Personalization metric snapshot for the device.
                properties:
                  device_id:
                    type: string
                  model_id:
                    type: string
                  federation_id:
                    type: string
                  personalization_type:
                    type: string
                    nullable: true
                  local_metrics:
                    type: object
                    nullable: true
                  last_round_number:
                    type: integer
                    nullable: true
                  last_updated:
                    type: string
                    format: date-time
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/status:
    get:
      operationId: training_rounds_get_status
      summary: Get a lightweight progress snapshot of a training round. Returns counts of selected and received devices, status,
        and de
      tags:
      - training
      x-octomil-method: training.rounds.get_status
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Get a lightweight progress snapshot of a training round. Returns counts of selected and received devices,
        status, and deadline without the full device list. Prefer this over training.get_round for polling during an active
        round.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Lightweight round progress snapshot \u2014 see schemas/training/RoundStatusResponse.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/select-devices:
    post:
      operationId: training_rounds_select_devices
      summary: Select eligible devices for a training round based on optional criteria filters. The round must be in "pending"
        state. A
      tags:
      - training
      x-octomil-method: training.rounds.select_devices
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - updates TrainingRound.selected_devices
      - emits telemetry event (training.devices_selected)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Select eligible devices for a training round based on optional criteria filters. The round must be in "pending"
        state. After device selection, call training.start_round to begin collecting updates.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              nullable: true
              description: Optional device selection criteria. Omit to use round defaults.
              properties:
                device_group_id:
                  type: string
                  nullable: true
                platform:
                  type: string
                  nullable: true
                min_battery:
                  type: integer
                  minimum: 0
                  maximum: 100
                  nullable: true
                network_type:
                  type: string
                  nullable: true
                heartbeat_max_age_seconds:
                  type: integer
                  minimum: 60
                  default: 600
                fraction:
                  type: number
                  minimum: 0
                  maximum: 1
                  nullable: true
                  description: Fraction of eligible devices to select (0,1].
                stratify_by_platform:
                  type: boolean
                  default: false
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Full RoundResponse with updated selected_devices list.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/secagg/masked-vectors:
    post:
      operationId: training_secagg_masked_vectors
      summary: SecAgg+ Stage 2. Client submits masked and quantized model parameter vectors. Server stores vectors. When all
        active cli
      tags:
      - training
      x-octomil-method: training.secagg.masked_vectors
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - stores masked vectors in session state
      - advances session to unmask stage when all clients have submitted
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: SecAgg+ Stage 2. Client submits masked and quantized model parameter vectors. Server stores vectors. When
        all active clients have submitted, the server advances to Stage 3 (unmask). Returns HTTP 202 Accepted.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - client_id
              - masked_vectors
              properties:
                client_id:
                  type: string
                masked_vectors:
                  type: array
                  items:
                    type: string
                  description: Base64-encoded masked, quantized model parameter arrays.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Acknowledgement with current protocol stage.
                properties:
                  status:
                    type: string
                    enum:
                    - accepted
                  stage:
                    type: string
                  client_id:
                    type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/secagg/setup:
    post:
      operationId: training_secagg_setup
      summary: 'SecAgg+ Stage 0. Client submits ECDH public keys. Server initializes the protocol session on first call and
        returns the '
      tags:
      - training
      x-octomil-method: training.secagg.setup
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - initializes in-memory SecAgg protocol session on first client arrival
      - stores client public keys in session
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: SecAgg+ Stage 0. Client submits ECDH public keys. Server initializes the protocol session on first call
        and returns the session configuration and the client's neighbour graph. All participating clients must call this before
        the round can advance to Stage 1 (share-keys).
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - client_id
              - pk1
              - pk2
              properties:
                client_id:
                  type: string
                  description: Client identifier for this participant.
                pk1:
                  type: string
                  description: Base64-encoded X25519 public key for pairwise masks.
                pk2:
                  type: string
                  description: Base64-encoded X25519 public key for share encryption.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: SecAgg session config and neighbour graph for this client.
                properties:
                  session_id:
                    type: string
                  stage:
                    type: string
                  config:
                    type: object
                  neighbours:
                    type: object
                    description: Map of neighbour client_id to empty list (reserved for future key delivery).
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/secagg/share-keys:
    post:
      operationId: training_secagg_share_keys
      summary: SecAgg+ Stage 1. Client submits encrypted Shamir share bundles (one per neighbour). Server stores submissions
        and forwar
      tags:
      - training
      x-octomil-method: training.secagg.share_keys
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - stores encrypted shares in session forwarding tables
      - advances session stage to masked_vectors when all clients have submitted
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: SecAgg+ Stage 1. Client submits encrypted Shamir share bundles (one per neighbour). Server stores submissions
        and forwards shares destined for this client. When all active clients have submitted, the server advances the session
        to Stage 2.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - client_id
              - destinations
              - ciphertexts
              properties:
                client_id:
                  type: string
                destinations:
                  type: array
                  items:
                    type: string
                  description: Destination client IDs for the encrypted share bundles.
                ciphertexts:
                  type: array
                  items:
                    type: string
                  description: Base64-encoded encrypted share bundles, one per destination.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Forwarded shares destined for this client from other participants.
                properties:
                  stage:
                    type: string
                  sources:
                    type: array
                    items:
                      type: string
                  ciphertexts:
                    type: array
                    items:
                      type: string
                    description: Base64-encoded ciphertexts forwarded from other clients.
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/secagg/status:
    get:
      operationId: training_secagg_status
      summary: Query the current SecAgg+ protocol stage and participant counts for a training round. Returns live state from
        the in-mem
      tags:
      - training
      x-octomil-method: training.secagg.status
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Query the current SecAgg+ protocol stage and participant counts for a training round. Returns live state
        from the in-memory protocol session when available; falls back to the persisted SecAggSession DB row otherwise.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "SecAgg session status \u2014 see schemas/training/SecAggStatusResponse.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/secagg/unmask:
    post:
      operationId: training_secagg_unmask
      summary: 'SecAgg+ Stage 3. Surviving clients submit Shamir shares for dropout recovery. When threshold shares have been
        received, '
      tags:
      - training
      x-octomil-method: training.secagg.unmask
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - stores Shamir shares in session state
      - when threshold reached: unmasks weight vectors and produces aggregated result
      - aggregated result stored in SecAggSession.aggregated_model
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - training_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: SecAgg+ Stage 3. Surviving clients submit Shamir shares for dropout recovery. When threshold shares have
        been received, the server unmasks and aggregates the weight vectors. Returns "waiting" until threshold is reached,
        then "completed" or "failed".
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - client_id
              - shares
              properties:
                client_id:
                  type: string
                shares:
                  type: object
                  description: Map of owner_node_id to base64-encoded Shamir share bytes for dropout recovery.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Unmask result or waiting acknowledgement.
                properties:
                  status:
                    type: string
                    enum:
                    - completed
                    - failed
                    - waiting
                  stage:
                    type: string
                  aggregated_size:
                    type: integer
                    nullable: true
                  shares_received:
                    type: integer
                    nullable: true
                  threshold:
                    type: integer
                    nullable: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/rounds/{round_id}/start:
    post:
      operationId: training_start_round
      summary: Transition a round from "pending" / "selected" to "running". Selected devices receive the round assignment
        on their next
      tags:
      - training
      x-octomil-method: training.start_round
      x-octomil-since: 1.0.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - sets TrainingRound.state to "running" + started_at
      - signals selected devices via the device-agent push (devices.sync also picks it up on next poll)
      - emits telemetry event (training.round_started)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - assignment_not_found
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Transition a round from "pending" / "selected" to "running". Selected devices receive the round assignment
        on their next devices.sync; they execute local training and POST updates via training.weights.
      parameters:
      - name: round_id
        in: path
        required: true
        schema:
          type: string
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
        description: "Optional client-generated UUIDv4. Repeated requests with the same key within 24h replay the original\
          \ response without re-executing side effects. Required by STYLE_GUIDE \xA75 on every non-idempotent write (POST\
          \ / PATCH); GET / PUT / DELETE are inherently idempotent and don't need it."
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: The round in "running" state with selected device count + round_timeout_at.
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/updates/by-device:
    get:
      operationId: training_updates_by_device
      summary: 'Return per-device training update counts and cumulative sample totals for a model version. Groups updates
        by device and '
      tags:
      - training
      x-octomil-method: training.updates.by_device
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return per-device training update counts and cumulative sample totals for a model version. Groups updates
        by device and returns the devices with the most recent activity first.
      parameters:
      - name: model_id
        in: query
        required: true
        schema:
          type: string
      - name: version
        in: query
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 10
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Per-device update counts and sample totals, ordered by latest_update_at desc.
                items:
                  type: object
                  description: "DeviceUpdateSummary \u2014 see schemas/training/DeviceUpdateSummary.json"
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/updates/latest:
    get:
      operationId: training_updates_list_latest
      summary: List the most recent training update records for a model version, sorted by received_at descending. Useful
        for inspectin
      tags:
      - training
      x-octomil-method: training.updates.list_latest
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: List the most recent training update records for a model version, sorted by received_at descending. Useful
        for inspecting recent device submissions before triggering aggregation.
      parameters:
      - name: model_id
        in: query
        required: true
        schema:
          type: string
      - name: version
        in: query
        required: true
        schema:
          type: string
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 10
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                description: Most recent TrainingUpdateItem records, ordered by received_at desc.
                items:
                  type: object
                  description: "TrainingUpdateItem \u2014 see schemas/training/TrainingUpdateItem.json"
                  additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/updates/summary:
    get:
      operationId: training_updates_summary
      summary: Get aggregate training update counts and sample totals for a specific model version. Returns total update count,
        total s
      tags:
      - training
      x-octomil-method: training.updates.summary
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Get aggregate training update counts and sample totals for a specific model version. Returns total update
        count, total sample count, and the timestamp of the most recent update. Use this before triggering aggregation to
        check if enough updates exist.
      parameters:
      - name: model_id
        in: query
        required: true
        schema:
          type: string
      - name: version
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Aggregate training update summary \u2014 see schemas/training/TrainingUpdateSummary.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/training/weights:
    post:
      operationId: training_weights_upload
      summary: 'Upload a device''s local training update (gradient or full weight delta) for later aggregation. The payload
        is validated '
      tags:
      - training
      x-octomil-method: training.weights.upload
      x-octomil-since: 1.25.0
      x-octomil-stability: stable
      x-octomil-idempotent: false
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects:
      - inserts TrainingUpdate row
      - if round_id set, records update in TrainingRound.received_updates
      - emits audit log entry (training_update_upload)
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - model_not_found
      - invalid_input
      - weight_upload_failed
      - network_unavailable
      - request_timeout
      - server_error
      description: Upload a device's local training update (gradient or full weight delta) for later aggregation. The payload
        is validated for NaN/Inf before storage. If the model belongs to a federation, the device must be an active federation
        member.
      parameters:
      - name: Idempotency-Key
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - model_id
              - version
              - weights_data
              - sample_count
              properties:
                model_id:
                  type: string
                version:
                  type: string
                device_id:
                  type: string
                  nullable: true
                sample_count:
                  type: integer
                  minimum: 0
                weights_data:
                  type: string
                  format: byte
                  description: Base64-encoded or raw bytes of the weight update payload. Server accepts both.
                metrics:
                  type: object
                  nullable: true
                update_format:
                  type: string
                  default: state_dict
                round_id:
                  type: string
                  nullable: true
                  description: If set, links this update to the round and calls record_update.
                is_secure_aggregation:
                  type: boolean
                  nullable: true
                secret_shares:
                  type: object
                  nullable: true
                verification_tag:
                  type: string
                  nullable: true
                feature_coverage:
                  type: number
                  nullable: true
                missing_features:
                  type: array
                  items:
                    type: string
                  nullable: true
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: "Weight upload confirmation \u2014 see schemas/training/WeightUploadResponse.json"
                additionalProperties: true
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/usage:
    get:
      operationId: usage_get
      summary: 'Return usage metrics and warnings for the org for the current billing period. Metrics are keyed by slug; the
        key set is '
      tags:
      - usage
      x-octomil-method: usage.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - network_unavailable
      - request_timeout
      - server_error
      description: Return usage metrics and warnings for the org for the current billing period. Metrics are keyed by slug;
        the key set is plan-dependent.
      parameters:
      - name: org_id
        in: query
        required: false
        schema:
          type: string
        description: Org to query. Derived from auth token if omitted.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - org_id
                - plan
                - metrics
                - warnings
                properties:
                  org_id:
                    type: string
                  plan:
                    type: string
                    description: Current billing plan slug.
                  metrics:
                    type: object
                    additionalProperties: true
                    description: 'Map of metric slug to UsageMetricResponse (current, limit, percent_used, period_start, period_end).

                      '
                  warnings:
                    type: array
                    description: Active usage warnings for metrics approaching or exceeding limits.
                    items:
                      type: object
                      required:
                      - metric
                      - current
                      - limit
                      - percent_used
                      - severity
                      properties:
                        metric:
                          type: string
                        current:
                          type: number
                        limit:
                          type: integer
                        percent_used:
                          type: number
                        severity:
                          type: string
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/usage/savings:
    get:
      operationId: usage_savings_get
      summary: Return estimated cloud cost savings from on-device inference offloading for a given aggregation period, with
        per-model a
      tags:
      - usage
      x-octomil-method: usage.savings.get
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - network_unavailable
      - request_timeout
      - server_error
      description: Return estimated cloud cost savings from on-device inference offloading for a given aggregation period,
        with per-model and daily breakdowns.
      parameters:
      - name: period
        in: query
        required: false
        schema:
          type: string
        description: 'Aggregation period. Must be one of: 24h, 7d, 30d, all. Defaults to 30d.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - summary
                - by_model
                - daily
                properties:
                  summary:
                    type: object
                    required:
                    - total_requests
                    - on_device_count
                    - cloud_count
                    - on_device_pct
                    - cloud_cost_per_request
                    - estimated_cloud_cost_usd
                    - estimated_savings_usd
                    - period
                    properties:
                      total_requests:
                        type: integer
                      on_device_count:
                        type: integer
                      cloud_count:
                        type: integer
                      on_device_pct:
                        type: number
                      cloud_cost_per_request:
                        type: number
                      estimated_cloud_cost_usd:
                        type: number
                      estimated_savings_usd:
                        type: number
                      period:
                        type: string
                  by_model:
                    type: array
                    items:
                      type: object
                      required:
                      - model_id
                      - total_requests
                      - on_device_count
                      - cloud_count
                      - on_device_pct
                      - estimated_savings_usd
                      properties:
                        model_id:
                          type: string
                        total_requests:
                          type: integer
                        on_device_count:
                          type: integer
                        cloud_count:
                          type: integer
                        on_device_pct:
                          type: number
                        estimated_savings_usd:
                          type: number
                  daily:
                    type: array
                    items:
                      type: object
                      required:
                      - date
                      - on_device_count
                      - cloud_count
                      - savings_usd
                      properties:
                        date:
                          type: string
                        on_device_count:
                          type: integer
                        cloud_count:
                          type: integer
                        savings_usd:
                          type: number
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /api/v1/usage/savings/{model_id}:
    get:
      operationId: usage_savings_get_model
      summary: Return estimated cloud cost savings attributable to a specific model for a given aggregation period.
      tags:
      - usage
      x-octomil-method: usage.savings.get_model
      x-octomil-since: 1.28.0
      x-octomil-stability: stable
      x-octomil-idempotent: true
      x-octomil-blocking: true
      x-octomil-retryable-on:
      - network_unavailable
      - request_timeout
      - server_error
      x-octomil-side-effects: []
      x-octomil-errors:
      - authentication_failed
      - forbidden
      - invalid_input
      - model_not_found
      - network_unavailable
      - request_timeout
      - server_error
      description: Return estimated cloud cost savings attributable to a specific model for a given aggregation period.
      parameters:
      - name: model_id
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        required: false
        schema:
          type: string
        description: 'Aggregation period. Must be one of: 24h, 7d, 30d, all. Defaults to 30d.

          '
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                - model_id
                - estimated_savings_usd
                - total_requests
                - on_device_count
                - cloud_count
                - on_device_pct
                properties:
                  model_id:
                    type: string
                  estimated_savings_usd:
                    type: number
                  total_requests:
                    type: integer
                  on_device_count:
                    type: integer
                  cloud_count:
                    type: integer
                  on_device_pct:
                    type: number
                    description: Fraction of requests served on-device [0.0, 1.0].
        default:
          description: Error response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
components:
  schemas:
    telemetry_batch:
      title: TelemetryBatch
      description: Batched telemetry event payload uploaded by the device to the telemetry ingestion endpoint.
      type: object
      required:
      - batchId
      - deviceId
      - capturedAt
      - events
      additionalProperties: false
      properties:
        batchId:
          type: string
          description: Unique identifier for this batch, used for deduplication on the server.
        deviceId:
          type: string
          description: Opaque device identifier.
        sdkVersion:
          type: string
          description: Version of the Octomil SDK that generated this batch.
        capturedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this batch was assembled on-device.
        events:
          type: array
          minItems: 1
          description: List of telemetry events in this batch.
          items:
            type: object
            required:
            - eventId
            - name
            - timestamp
            - telemetryClass
            additionalProperties: false
            properties:
              eventId:
                type: string
                description: Unique event identifier (UUID or equivalent).
              name:
                type: string
                description: Event name from event_names.yaml (e.g. artifact.download_started).
              timestamp:
                type: string
                format: date-time
                description: ISO 8601 timestamp when the event occurred.
              telemetryClass:
                type: string
                enum:
                - must_keep
                - important
                - best_effort
                description: TelemetryClass enum value governing upload priority.
              traceId:
                type: string
                description: OTLP trace ID if this event is attached to a span.
              spanId:
                type: string
                description: OTLP span ID if this event is a span event.
              attributes:
                type: object
                description: Arbitrary key-value attributes for this event.
                additionalProperties:
                  oneOf:
                  - type: string
                  - type: number
                  - type: boolean
    UpdateUsageLimitsRequest:
      title: UpdateUsageLimitsRequest
      description: Request body for updating usage limits.
      type: object
      required:
      - limits
      properties:
        limits:
          type: array
          items:
            type: object
            required:
            - metric
            - limit_value
            properties:
              metric:
                type: string
              limit_value:
                type: integer
            additionalProperties: false
      additionalProperties: false
    UsageLimitsResponse:
      title: UsageLimitsResponse
      description: Usage limits for an organization.
      type: object
      required:
      - org_id
      - limits
      properties:
        org_id:
          type: string
          description: Organization identifier.
        limits:
          type: array
          items:
            type: object
            required:
            - metric
            - limit_value
            properties:
              metric:
                type: string
                description: Limit metric name (devices, training_rounds, models, etc.).
              limit_value:
                type: integer
                description: Maximum allowed value for this metric.
            additionalProperties: false
      additionalProperties: false
    UpdatePoliciesRequest:
      title: UpdatePoliciesRequest
      description: Partial update for org-level policies. All fields optional.
      type: object
      properties:
        serving_policy:
          type:
          - object
          - 'null'
        telemetry_enabled:
          type: boolean
        privacy_mode:
          type: string
          enum:
          - standard
          - strict
          - 'off'
      additionalProperties: false
    PoliciesResponse:
      title: PoliciesResponse
      description: Org-level serving and data-handling policy configuration.
      type: object
      required:
      - telemetry_enabled
      - privacy_mode
      properties:
        serving_policy:
          type:
          - object
          - 'null'
          description: Open-shape serving policy object. See ServingPolicy domain type.
        telemetry_enabled:
          type: boolean
        privacy_mode:
          type: string
          enum:
          - standard
          - strict
          - 'off'
      additionalProperties: false
    UpdateAuthConfigRequest:
      title: UpdateAuthConfigRequest
      description: Partial update for org-level authentication configuration. OAuth provider availability cannot be set here.
      type: object
      properties:
        identity_mode:
          type: string
          enum:
          - email_password
          - sso
          - passkey
        email_password_enabled:
          type: boolean
        sso_provider:
          type:
          - string
          - 'null'
        sso_domain:
          type:
          - string
          - 'null'
        scim_enabled:
          type: boolean
      additionalProperties: false
    AuthConfigResponse:
      title: AuthConfigResponse
      description: Authentication and identity provider configuration for the organization.
      type: object
      required:
      - identity_mode
      - email_password_enabled
      - google_enabled
      - apple_enabled
      - github_enabled
      - passkeys_enabled
      - scim_enabled
      properties:
        identity_mode:
          type: string
          enum:
          - email_password
          - sso
          - passkey
        email_password_enabled:
          type: boolean
        google_enabled:
          type: boolean
          description: Derived from server env vars; read-only from SDK.
        apple_enabled:
          type: boolean
          description: Derived from server env vars; read-only from SDK.
        github_enabled:
          type: boolean
          description: Derived from server env vars; read-only from SDK.
        passkeys_enabled:
          type: boolean
        sso_provider:
          type:
          - string
          - 'null'
        sso_domain:
          type:
          - string
          - 'null'
        scim_enabled:
          type: boolean
      additionalProperties: false
    UpdateSettingsRequest:
      title: UpdateSettingsRequest
      description: Partial update for organization-level settings. All fields are optional.
      type: object
      properties:
        audit_retention_days:
          type: integer
          minimum: 1
        require_mfa_for_admin:
          type: boolean
        require_admin_approval:
          type: boolean
        auto_rollback_enabled:
          type: boolean
        protect_production:
          type: boolean
        identity_mode:
          type: string
          enum:
          - email_password
          - sso
          - passkey
        device_battery_threshold:
          type: integer
          minimum: 0
          maximum: 100
        device_network_policy:
          type: string
          enum:
          - any
          - wifi_only
          - cellular_allowed
      additionalProperties: false
    OrgSettingsResponse:
      title: OrgSettingsResponse
      description: Organization-level governance and operational settings.
      type: object
      required:
      - org_id
      - audit_retention_days
      - require_mfa_for_admin
      - require_admin_approval
      - auto_rollback_enabled
      - protect_production
      - identity_mode
      - created_at
      - updated_at
      properties:
        org_id:
          type: string
          description: Organization identifier (UUIDv4).
        audit_retention_days:
          type: integer
          description: Number of days to retain audit log entries.
        require_mfa_for_admin:
          type: boolean
          description: When true, admin-role users must authenticate with MFA.
        require_admin_approval:
          type: boolean
          description: When true, deployments and other destructive actions require explicit admin approval.
        auto_rollback_enabled:
          type: boolean
          description: When true, the platform automatically rolls back failed deployments.
        protect_production:
          type: boolean
          description: When true, production environment changes require additional confirmation.
        identity_mode:
          type: string
          enum:
          - email_password
          - sso
          - passkey
          description: Primary identity authentication mode for the org.
        created_at:
          type: string
          format: date-time
          description: ISO-8601 UTC timestamp when the settings record was created.
        updated_at:
          type: string
          format: date-time
          description: ISO-8601 UTC timestamp of the last settings update.
      additionalProperties: false
    CreatePublishableKeyRequest:
      title: CreatePublishableKeyRequest
      description: Request body for creating a publishable key for client-side SDK use.
      type: object
      required:
      - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        environment:
          type: string
          enum:
          - live
          - test
          default: live
        allowed_app_ids:
          type:
          - array
          - 'null'
          items:
            type: string
        allowed_origins:
          type:
          - array
          - 'null'
          items:
            type: string
        scopes:
          type:
          - array
          - 'null'
          items:
            type: string
        rate_limit_rpm:
          type: integer
          minimum: 1
          maximum: 10000
      additionalProperties: false
    CreatePublishableKeyResponse:
      title: CreatePublishableKeyResponse
      description: Response for publishable key creation. key is the raw secret returned ONLY once.
      type: object
      required:
      - key
      - key_id
      - org_id
      - name
      - environment
      - scopes
      - rate_limit_rpm
      properties:
        key:
          type: string
          description: Full raw publishable key. Store immediately.
        key_id:
          type: string
        org_id:
          type: string
        name:
          type: string
        environment:
          type: string
          enum:
          - live
          - test
        scopes:
          type: array
          items:
            type: string
        allowed_app_ids:
          type:
          - array
          - 'null'
          items:
            type: string
        allowed_origins:
          type:
          - array
          - 'null'
          items:
            type: string
        rate_limit_rpm:
          type: integer
      additionalProperties: false
    IntegrationTestResponse:
      title: IntegrationTestResponse
      description: Result of an integration validation check.
      type: object
      required:
      - success
      - message
      properties:
        success:
          type: boolean
        message:
          type: string
      additionalProperties: false
    UpdateIntegrationRequest:
      title: UpdateIntegrationRequest
      description: Partial update fields for an integration.
      type: object
      properties:
        name:
          type: string
        enabled:
          type: boolean
        webhook_url:
          type: string
        webhook_events:
          type: string
        slack_workspace_id:
          type: string
        slack_channel_id:
          type: string
        email_recipients:
          type: string
        email_from:
          type: string
        siem_format:
          type: string
        siem_endpoint:
          type: string
        notify_on_deploy:
          type: boolean
        notify_on_incident:
          type: boolean
        notify_on_approval:
          type: boolean
      additionalProperties: false
    IntegrationDetailResponse:
      title: IntegrationDetailResponse
      description: Full integration configuration details.
      type: object
      required:
      - id
      - name
      - integration_type
      - enabled
      properties:
        id:
          type: string
        name:
          type: string
        integration_type:
          type: string
          enum:
          - slack
          - email
          - webhook
          - siem
        enabled:
          type: boolean
        webhook_url:
          type:
          - string
          - 'null'
        webhook_events:
          type:
          - string
          - 'null'
        slack_workspace_id:
          type:
          - string
          - 'null'
        slack_channel_id:
          type:
          - string
          - 'null'
        email_recipients:
          type:
          - string
          - 'null'
        email_from:
          type:
          - string
          - 'null'
        siem_format:
          type:
          - string
          - 'null'
        siem_endpoint:
          type:
          - string
          - 'null'
        notify_on_deploy:
          type: boolean
        notify_on_incident:
          type: boolean
        notify_on_approval:
          type: boolean
        created_at:
          type:
          - string
          - 'null'
        updated_at:
          type:
          - string
          - 'null'
      additionalProperties: false
    CreateIntegrationRequest:
      title: CreateIntegrationRequest
      description: Request body for creating a notification or data integration.
      type: object
      required:
      - name
      - integration_type
      properties:
        name:
          type: string
          minLength: 1
        integration_type:
          type: string
          enum:
          - slack
          - email
          - webhook
          - siem
        config:
          type: object
          additionalProperties: true
          description: Provider-specific config. Use PATCH after creation to set individual fields.
      additionalProperties: false
    IntegrationResponse:
      title: IntegrationResponse
      description: Summary integration record as returned by list and create operations.
      type: object
      required:
      - id
      - name
      - integration_type
      - enabled
      properties:
        id:
          type: string
        name:
          type: string
        integration_type:
          type: string
          enum:
          - slack
          - email
          - webhook
          - siem
        enabled:
          type: boolean
        created_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    UpdateRoutingRequest:
      title: UpdateRoutingRequest
      description: Partial update for cloud routing strategy and cooldown.
      type: object
      properties:
        cloud_routing_strategy:
          type:
          - string
          - 'null'
          enum:
          - ordered_failover
          - weighted_shuffle
          - least_busy
          - null
        cloud_cooldown_seconds:
          type:
          - integer
          - 'null'
          minimum: 0
      additionalProperties: false
    UpdateRoutingResponse:
      title: UpdateRoutingResponse
      description: Applied routing configuration after a PATCH.
      type: object
      required:
      - cloud_routing_strategy
      - cloud_cooldown_seconds
      properties:
        cloud_routing_strategy:
          type: string
          enum:
          - ordered_failover
          - weighted_shuffle
          - least_busy
        cloud_cooldown_seconds:
          type: integer
          minimum: 0
      additionalProperties: false
    UpdateLocalRuntimeRequest:
      title: UpdateLocalRuntimeRequest
      description: Partial update for a local runtime target.
      type: object
      properties:
        label:
          type:
          - string
          - 'null'
        endpoint:
          type:
          - string
          - 'null'
        model:
          type:
          - string
          - 'null'
        protocol:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
          enum:
          - active
          - disabled
          - null
        routing_priority:
          type:
          - integer
          - 'null'
      additionalProperties: false
    LocalTargetResponse:
      title: LocalTargetResponse
      description: A locally-reachable inference runtime target configured for cloud-execution routing.
      type: object
      required:
      - id
      - label
      - endpoint
      - model
      - protocol
      - status
      - routing_priority
      properties:
        id:
          type: string
        label:
          type: string
        endpoint:
          type: string
        model:
          type: string
        protocol:
          type: string
          description: e.g. 'openai_compatible'
        status:
          type: string
          enum:
          - active
          - disabled
        routing_priority:
          type: integer
        is_env_fallback:
          type: boolean
          default: false
        last_verified_at:
          type:
          - string
          - 'null'
          format: date-time
        verification_latency_ms:
          type:
          - number
          - 'null'
        last_verification_error:
          type:
          - string
          - 'null'
      additionalProperties: false
    TestConnectionResponse:
      title: TestConnectionResponse
      description: Result of an active probe against a cloud provider connection or local runtime.
      type: object
      required:
      - success
      properties:
        success:
          type: boolean
        error:
          type:
          - string
          - 'null'
        latency_ms:
          type:
          - number
          - 'null'
      additionalProperties: false
    CreateLocalRuntimeRequest:
      title: CreateLocalRuntimeRequest
      description: Request body for registering a new local runtime target in the execution config.
      type: object
      required:
      - label
      - endpoint
      properties:
        label:
          type: string
        endpoint:
          type: string
          description: Base URL for the local runtime, e.g. 'http://localhost:11434'.
        model:
          type: string
          default: ''
        protocol:
          type: string
          default: openai_compatible
        routing_priority:
          type: integer
          default: 0
      additionalProperties: false
    ExecutionHealthResponse:
      title: ExecutionHealthResponse
      description: Live health snapshot for all execution targets.
      type: object
      required:
      - targets
      - strategy
      - cooldown_seconds
      properties:
        targets:
          type: array
          items:
            type: object
            required:
            - target_id
            - target_type
            - provider
            properties:
              target_id:
                type: string
              target_type:
                type: string
              provider:
                type: string
              cooldown_remaining:
                type: number
                default: 0.0
              rpm:
                type: integer
                default: 0
              tpm:
                type: integer
                default: 0
              inflight:
                type: integer
                default: 0
              successes:
                type: integer
                default: 0
              failures:
                type: integer
                default: 0
              failure_rate:
                type: number
                default: 0.0
              last_verified_at:
                type:
                - string
                - 'null'
                format: date-time
              verification_latency_ms:
                type:
                - number
                - 'null'
              last_verification_error:
                type:
                - string
                - 'null'
            additionalProperties: false
        strategy:
          type: string
          enum:
          - ordered_failover
          - weighted_shuffle
          - least_busy
        cooldown_seconds:
          type: integer
          minimum: 0
      additionalProperties: false
    ExecutionConfigResponse:
      title: ExecutionConfigResponse
      description: 'Full execution configuration: local runtime targets, cloud targets, and routing settings.'
      type: object
      required:
      - local_targets
      - cloud_targets
      - routing_strategy
      - cooldown_seconds
      properties:
        local_targets:
          type: array
          items:
            $ref: '#/components/schemas/LocalTargetResponse'
        cloud_targets:
          type: array
          items:
            $ref: '#/components/schemas/CloudTargetResponse'
        routing_strategy:
          type: string
          enum:
          - ordered_failover
          - weighted_shuffle
          - least_busy
        cooldown_seconds:
          type: integer
          minimum: 0
      additionalProperties: false
    CloudTargetResponse:
      title: CloudTargetResponse
      description: A cloud provider target in the execution routing configuration.
      type: object
      required:
      - target_id
      - provider
      - connection_type
      properties:
        target_id:
          type: string
        provider:
          type: string
        connection_type:
          type: string
        connection_id:
          type:
          - string
          - 'null'
        status:
          type: string
          default: active
        routing_priority:
          type: integer
          default: 0
        routing_weight:
          type: integer
          default: 1
        rpm_limit:
          type: integer
          minimum: 0
          default: 0
        tpm_limit:
          type: integer
          minimum: 0
          default: 0
      additionalProperties: false
    UpdateCloudPolicyRequest:
      title: UpdateCloudPolicyRequest
      description: Partial update for the org cloud fallback policy.
      type: object
      properties:
        cloud_fallback_enabled:
          type:
          - boolean
          - 'null'
        cloud_credential_policy:
          type:
          - string
          - 'null'
          enum:
          - byok_only
          - byok_and_managed
          - managed_only
          - disabled
          - null
        cloud_allowed_providers:
          type:
          - array
          - 'null'
          items:
            type: string
        cloud_fallback_model:
          type:
          - string
          - 'null'
      additionalProperties: false
    CloudPolicyResponse:
      title: CloudPolicyResponse
      description: Cloud fallback policy configuration for the org.
      type: object
      required:
      - cloud_fallback_enabled
      - cloud_credential_policy
      properties:
        cloud_fallback_enabled:
          type: boolean
        cloud_credential_policy:
          type: string
          enum:
          - byok_only
          - byok_and_managed
          - managed_only
          - disabled
          description: Which credential sources are permitted for cloud inference.
        cloud_allowed_providers:
          type:
          - array
          - 'null'
          items:
            type: string
          description: Allowlist of provider identifiers. Null means all allowed.
        cloud_fallback_model:
          type:
          - string
          - 'null'
          description: Default cloud model id used when falling back.
      additionalProperties: false
    UpdateCredentialRequest:
      title: UpdateCredentialRequest
      description: Partial update for a BYOK cloud provider credential.
      type: object
      properties:
        api_key:
          type:
          - string
          - 'null'
          description: New raw API key. Stored encrypted; never returned.
        label:
          type:
          - string
          - 'null'
        base_url:
          type:
          - string
          - 'null'
        is_active:
          type:
          - boolean
          - 'null'
      additionalProperties: false
    CredentialResponse:
      title: CredentialResponse
      description: A BYOK cloud provider credential stored for an org. API key is never returned.
      type: object
      required:
      - id
      - provider
      - label
      - is_active
      properties:
        id:
          type: string
        provider:
          type: string
          description: Provider identifier, e.g. 'openai', 'anthropic', 'azure_openai'.
        label:
          type: string
        is_active:
          type: boolean
        base_url:
          type:
          - string
          - 'null'
        last_used_at:
          type:
          - string
          - 'null'
          format: date-time
        last_error:
          type:
          - string
          - 'null'
        created_at:
          type:
          - string
          - 'null'
          format: date-time
        updated_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    CreateCredentialRequest:
      title: CreateCredentialRequest
      description: Request body for creating a BYOK cloud provider credential.
      type: object
      required:
      - provider
      - api_key
      properties:
        provider:
          type: string
          description: Provider identifier, e.g. 'openai', 'anthropic', 'azure_openai'.
        api_key:
          type: string
          description: Raw API key. Stored encrypted; never returned.
        label:
          type: string
          default: default
        base_url:
          type:
          - string
          - 'null'
      additionalProperties: false
    UpdateConnectionRequest:
      title: UpdateConnectionRequest
      description: Partial update for a cloud provider connection.
      type: object
      properties:
        api_key:
          type:
          - string
          - 'null'
        label:
          type:
          - string
          - 'null'
        base_url:
          type:
          - string
          - 'null'
        status:
          type:
          - string
          - 'null'
      additionalProperties: false
    ConnectionResponse:
      title: ConnectionResponse
      description: A cloud provider connection (BYOK or managed).
      type: object
      required:
      - id
      - org_id
      - provider
      - connection_type
      - status
      - label
      - error_count
      - created_at
      properties:
        id:
          type: string
        org_id:
          type: string
        provider:
          type: string
        connection_type:
          type: string
          enum:
          - byok
          - managed
        status:
          type: string
        label:
          type: string
        base_url:
          type:
          - string
          - 'null'
        last_used_at:
          type:
          - string
          - 'null'
          format: date-time
        last_error:
          type:
          - string
          - 'null'
        error_count:
          type: integer
          minimum: 0
        created_at:
          type: string
          format: date-time
        verification_status:
          type:
          - string
          - 'null'
        verification_latency_ms:
          type:
          - number
          - 'null'
        last_verified_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    ConnectionModelsResponse:
      title: ConnectionModelsResponse
      description: List of model ids available from a cloud provider connection.
      type: object
      required:
      - models
      properties:
        models:
          type: array
          items:
            type: string
          description: Sorted list of model ids reported by the provider's /models endpoint.
      additionalProperties: false
    CreateConnectionRequest:
      title: CreateConnectionRequest
      description: Request body for creating a cloud provider connection.
      type: object
      required:
      - provider
      - connection_type
      properties:
        provider:
          type: string
          description: Provider identifier, e.g. 'openai', 'anthropic', 'azure_openai'.
        connection_type:
          type: string
          enum:
          - byok
          - managed
          description: Only 'byok' is currently accepted by the server.
        api_key:
          type:
          - string
          - 'null'
          description: Raw API key for BYOK connections. Stored encrypted; never returned.
        label:
          type: string
          default: default
        base_url:
          type:
          - string
          - 'null'
      additionalProperties: false
    UpdateBillingRequest:
      title: UpdateBillingRequest
      description: Request body for updating the local billing plan (admin/dev path).
      type: object
      required:
      - plan
      properties:
        plan:
          type: string
          description: Plan name (free, team, enterprise).
      additionalProperties: false
    BillingSupportContextResponse:
      title: BillingSupportContextResponse
      description: Billing context bundle for support handoff. Contains plan, subscription, and latest invoice summary.
      type: object
      required:
      - org_id
      - plan
      - status
      - support_tier
      properties:
        org_id:
          type: string
        plan:
          type: string
          enum:
          - free
          - starter
          - pro
          - enterprise
        status:
          type: string
        billing_interval:
          type:
          - string
          - 'null'
        stripe_customer_id:
          type:
          - string
          - 'null'
        stripe_subscription_id:
          type:
          - string
          - 'null'
        latest_invoice:
          type:
          - object
          - 'null'
          description: Latest Stripe invoice summary. Null if no customer or no invoices.
          additionalProperties: true
        support_tier:
          type: string
          description: Derived support tier label for the plan, e.g. 'community', 'standard', 'priority'.
      additionalProperties: false
    BillingInvoicesResponse:
      title: BillingInvoicesResponse
      description: Recent invoices sourced live from Stripe with a 90s server-side cache. Up to 20 invoices.
      type: object
      required:
      - invoices
      properties:
        invoices:
          type: array
          maxItems: 20
          items:
            type: object
            properties:
              id:
                type: string
              number:
                type:
                - string
                - 'null'
              status:
                type:
                - string
                - 'null'
              amount_due:
                type:
                - integer
                - 'null'
                description: Minor currency units (cents).
              amount_paid:
                type:
                - integer
                - 'null'
              currency:
                type:
                - string
                - 'null'
                description: ISO 4217 currency code.
              hosted_invoice_url:
                type:
                - string
                - 'null'
              invoice_pdf:
                type:
                - string
                - 'null'
              created:
                type:
                - integer
                - 'null'
                description: Unix epoch timestamp.
              period_start:
                type:
                - integer
                - 'null'
              period_end:
                type:
                - integer
                - 'null'
            additionalProperties: false
      additionalProperties: false
    BillingResponse:
      title: BillingResponse
      description: Current billing subscription state. Projection of the Stripe subscription cache; may lag by up to one webhook
        delivery cycle.
      type: object
      required:
      - org_id
      - plan
      - status
      properties:
        org_id:
          type: string
        plan:
          type: string
          enum:
          - free
          - starter
          - pro
          - enterprise
        status:
          type: string
          enum:
          - active
          - trialing
          - past_due
          - paused
          - incomplete
          - canceled
        billing_interval:
          type:
          - string
          - 'null'
          enum:
          - monthly
          - annual
          - null
        stripe_customer_id:
          type:
          - string
          - 'null'
        current_period_end:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    PortalRequest:
      title: PortalRequest
      description: Request body for creating a Stripe billing portal session.
      type: object
      required:
      - return_url
      properties:
        return_url:
          type: string
          format: uri
      additionalProperties: false
    PortalResponse:
      title: PortalResponse
      description: Stripe billing portal session URL.
      type: object
      required:
      - url
      properties:
        url:
          type: string
          format: uri
      additionalProperties: false
    CheckoutRequest:
      title: CheckoutRequest
      description: Request body for creating a Stripe checkout session.
      type: object
      required:
      - plan
      - success_url
      - cancel_url
      properties:
        plan:
          type: string
          description: Target plan (team, enterprise).
        success_url:
          type: string
          format: uri
        cancel_url:
          type: string
          format: uri
      additionalProperties: false
    CheckoutResponse:
      title: CheckoutResponse
      description: Stripe checkout session details.
      type: object
      required:
      - session_id
      - url
      properties:
        session_id:
          type: string
        url:
          type: string
          format: uri
      additionalProperties: false
    ReconcileCheckoutRequest:
      title: ReconcileCheckoutRequest
      description: Request body for reconciling a Stripe Checkout session after redirect-back.
      type: object
      required:
      - session_id
      properties:
        session_id:
          type: string
          description: Stripe checkout.Session id returned in the success_url ?session_id= query param.
      additionalProperties: false
    ReconcileCheckoutResponse:
      title: ReconcileCheckoutResponse
      description: Projected subscription state after reconciling a completed Stripe Checkout session.
      type: object
      required:
      - org_id
      - plan
      - status
      properties:
        org_id:
          type: string
        plan:
          type: string
          enum:
          - free
          - starter
          - pro
          - enterprise
        status:
          type: string
          enum:
          - active
          - trialing
          - past_due
          - paused
          - incomplete
          - canceled
        billing_interval:
          type:
          - string
          - 'null'
          enum:
          - monthly
          - annual
          - null
        stripe_customer_id:
          type:
          - string
          - 'null'
        stripe_subscription_id:
          type:
          - string
          - 'null'
        current_period_end:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    CreateAppKeyRequest:
      title: CreateAppKeyRequest
      description: Request body for creating an app key for server-side inference.
      type: object
      required:
      - name
      - app_id
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        app_id:
          type: string
          minLength: 1
          maxLength: 36
        environment:
          type: string
          enum:
          - live
          - test
          default: live
      additionalProperties: false
    CreateAppKeyResponse:
      title: CreateAppKeyResponse
      description: Response for app key creation. key is the raw secret returned ONLY once.
      type: object
      required:
      - key
      - key_id
      - org_id
      - name
      - app_id
      - environment
      - scopes
      properties:
        key:
          type: string
          description: Full raw app key. Store immediately.
        key_id:
          type: string
        org_id:
          type: string
        name:
          type: string
        app_id:
          type: string
        environment:
          type: string
          enum:
          - live
          - test
        scopes:
          type: array
          items:
            type: string
      additionalProperties: false
    CreateApiKeyResponse:
      title: CreateApiKeyResponse
      description: Response for API key creation. api_key is the raw secret returned ONLY once.
      type: object
      required:
      - id
      - org_id
      - name
      - prefix
      - scopes
      - created_at
      - api_key
      properties:
        id:
          type: string
        org_id:
          type: string
        name:
          type: string
        prefix:
          type: string
        scopes:
          type: object
          additionalProperties:
            type: boolean
        app_id:
          type:
          - string
          - 'null'
        federation_id:
          type:
          - string
          - 'null'
        model_id:
          type:
          - string
          - 'null'
        last_used_at:
          type:
          - string
          - 'null'
        revoked_at:
          type:
          - string
          - 'null'
        created_at:
          type:
          - string
          - 'null'
          format: date-time
        api_key:
          type: string
          description: "Full raw API key. Store immediately \u2014 not retrievable again."
      additionalProperties: false
    ApiKeyResponse:
      title: ApiKeyResponse
      description: API key record. Raw key value is never returned after creation.
      type: object
      required:
      - id
      - org_id
      - name
      - key_type
      - prefix
      - scopes
      - is_active
      - created_at
      properties:
        id:
          type: string
        org_id:
          type: string
        name:
          type: string
        key_type:
          type: string
          enum:
          - org_key
          - publishable_key
          - app_key
        prefix:
          type:
          - string
          - 'null'
        scopes:
          type: object
          additionalProperties:
            type: boolean
        is_active:
          type: boolean
        environment:
          type:
          - string
          - 'null'
          enum:
          - live
          - test
          - null
        app_id:
          type:
          - string
          - 'null'
        allowed_app_ids:
          type:
          - array
          - 'null'
          items:
            type: string
        allowed_origins:
          type:
          - array
          - 'null'
          items:
            type: string
        rate_limit_rpm:
          type:
          - integer
          - 'null'
        federation_id:
          type:
          - string
          - 'null'
        model_id:
          type:
          - string
          - 'null'
        last_used_at:
          type:
          - string
          - 'null'
          format: date-time
        revoked_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    ApiKeyScopesResponse:
      title: ApiKeyScopesResponse
      description: Canonical scope registry with key-type groupings and named templates.
      type: object
      required:
      - scopes
      - org_key
      - publishable_key
      - templates
      properties:
        scopes:
          type: object
          additionalProperties:
            type: object
            required:
            - category
            - description
            - status
            - eligible_key_types
            properties:
              category:
                type: string
              description:
                type: string
              status:
                type: string
                enum:
                - enforced
                - recognized
                - reserved
              eligible_key_types:
                type: array
                items:
                  type: string
                  enum:
                  - org_key
                  - publishable_key
                  - app_key
            additionalProperties: false
        org_key:
          $ref: '#/components/schemas/ApiKeyScopesResponse__KeyTypeScopesResponse'
        publishable_key:
          $ref: '#/components/schemas/ApiKeyScopesResponse__KeyTypeScopesResponse'
        app_key:
          oneOf:
          - $ref: '#/components/schemas/ApiKeyScopesResponse__KeyTypeScopesResponse'
          - type: 'null'
        templates:
          type: object
          additionalProperties:
            type: object
            required:
            - description
            - scopes
            properties:
              description:
                type: string
              scopes:
                type: array
                items:
                  type: string
            additionalProperties: false
      additionalProperties: false
    CreateApiKeyRequest:
      title: CreateApiKeyRequest
      description: Request body for creating an org-scoped API key.
      type: object
      required:
      - name
      - scopes
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        scopes:
          type: array
          items:
            type: string
          minItems: 1
        expires_in_days:
          type:
          - integer
          - 'null'
          minimum: 1
        app_id:
          type:
          - string
          - 'null'
      additionalProperties: false
    runtime_plan_response_schema:
      title: RuntimePlanResponse
      description: Response body from POST /api/v2/runtime/plan. Contains an ordered list of engine candidates the SDK should
        try.
      type: object
      required:
      - model
      - capability
      - policy
      - candidates
      - server_generated_at
      additionalProperties: false
      properties:
        plan_schema_version:
          type: integer
          minimum: 1
          default: 2
          description: Schema version of the plan response. Version 2 introduces gate_class, evaluation_phase, fallback_eligible
            on CandidateGate.
        model:
          type: string
          description: Model identifier the plan was generated for.
        capability:
          type: string
          description: Capability the plan was generated for.
        policy:
          type: string
          description: Routing policy that was applied (may differ from the request if defaulted).
        candidates:
          type: array
          items:
            $ref: '#/components/schemas/runtime_plan_response_schema__RuntimeCandidatePlan'
          description: Ordered engine candidates (highest priority first).
        fallback_candidates:
          type: array
          items:
            $ref: '#/components/schemas/runtime_plan_response_schema__RuntimeCandidatePlan'
          description: Fallback candidates if primary candidates all fail.
        plan_ttl_seconds:
          type: integer
          minimum: 0
          default: 604800
          description: 'How long this plan is valid, in seconds. Default: 604800 (7 days).'
        fallback_allowed:
          type: boolean
          default: true
          description: Whether the SDK is allowed to fall back to cloud inference if all local candidates fail. Private/local_only
            policies set this to false.
        public_client_allowed:
          type: boolean
          default: false
          description: Whether public clients (browser publishable keys, mobile bundle keys) are allowed to dispatch this
            plan against the hosted gateway. Default false. Server flips true only when the app capability has 'Allow public
            clients' enabled AND the routing policy is not Private/local_only AND the request key carries the cloud:inference:public
            scope. Server-key callers ignore this field.
        server_generated_at:
          type: string
          format: date-time
          description: ISO 8601 timestamp of when the server generated this plan.
        plan_correlation_id:
          type: string
          description: Opaque server-generated ID for correlating the plan with SDK route events and monitoring records.
        app_resolution:
          oneOf:
          - $ref: '#/components/schemas/app_resolution_schema'
          - type: 'null'
          description: Resolved application context when the request used an @app/{slug}/{capability} model ref or explicit
            app_slug. Null for plain model requests.
        resolution:
          oneOf:
          - $ref: '#/components/schemas/runtime_plan_response_schema__ModelResolution'
          - type: 'null'
          description: General model-reference resolution metadata for plain, capability-default, deployment, and experiment
            refs. App refs use app_resolution.
    model_ref_kind_schema:
      title: ModelRefKind
      description: 'Canonical classification of a model reference string. This is the single source of truth for how SDKs
        and the server categorize model references. Grammar-based parsing (local, no server round-trip): model, app, capability,
        deployment, experiment, alias, default, unknown. Server resolution required for: app (resolve slug to model), capability
        (resolve default model), deployment (resolve deploy_ prefix to model), experiment (resolve exp_ to variant and model).'
      type: string
      enum:
      - model
      - app
      - capability
      - deployment
      - experiment
      - alias
      - default
      - unknown
    candidate_gate_schema:
      title: CandidateGate
      description: A gate requirement attached to a candidate by the planner. SDKs evaluate gates per-request before selecting
        a candidate.
      type: object
      required:
      - code
      - required
      - source
      - gate_class
      - evaluation_phase
      - fallback_eligible
      additionalProperties: false
      properties:
        code:
          type: string
          enum:
          - artifact_verified
          - runtime_available
          - model_loads
          - context_fits
          - modality_supported
          - tool_support
          - min_tokens_per_second
          - max_ttft_ms
          - max_error_rate
          - min_free_memory_bytes
          - min_free_storage_bytes
          - benchmark_fresh
          - min_battery_pct
          - max_thermal_state
          - require_charging
          - require_wifi
          - schema_valid
          - tool_call_valid
          - safety_passed
          - evaluator_score_min
          - json_parseable
          - max_refusal_rate
          description: Gate code identifying the check to perform.
        required:
          type: boolean
          description: Whether the candidate must pass this gate to be selected. If false, gate failure is advisory only.
        threshold_number:
          type: number
          description: Numeric threshold for the gate (e.g. min tok/s, max TTFT ms, min bytes).
        threshold_string:
          type: string
          description: String threshold (e.g. modality name, format identifier).
        window_seconds:
          type: integer
          minimum: 0
          description: Time window for historical metrics (e.g. benchmark freshness in seconds).
        source:
          type: string
          enum:
          - server
          - sdk
          - runtime
          description: 'Who defined this gate: server (planner), sdk (local config), or runtime (engine self-report).'
        gate_class:
          type: string
          enum:
          - readiness
          - performance
          - output_quality
          description: Classification of this gate by evaluation concern. Inferred from code if absent.
        evaluation_phase:
          type: string
          enum:
          - pre_inference
          - during_inference
          - post_inference
          description: When this gate is evaluated relative to inference. Inferred from code if absent.
        fallback_eligible:
          type: boolean
          description: Whether failure of this gate can trigger fallback to the next candidate. May be suppressed by routing
            policy.
        blocking_default:
          type: boolean
          description: Default value for 'required' when the server does not specify. Used by SDKs for inference of gate severity.
    app_resolution_schema:
      title: AppResolution
      description: Resolved application context returned by the server planner when the model reference is an @app/{slug}/{capability}
        ref or an explicit app_slug. Provides enough local execution data for SDKs to run inference without a second server
        round-trip.
      type: object
      required:
      - app_id
      - capability
      - routing_policy
      - selected_model
      additionalProperties: false
      properties:
        app_id:
          type: string
          description: Server-assigned application identifier (UUID).
        app_slug:
          oneOf:
          - type: string
          - type: 'null'
          description: Human-readable app slug used in the @app/ reference.
        capability:
          type: string
          description: Resolved capability (e.g. 'chat', 'embeddings', 'transcription').
        routing_policy:
          type: string
          description: Effective routing policy from the app's serving configuration. Overrides request-level policy.
        selected_model:
          type: string
          description: Concrete model name resolved from the app's deployment (e.g. 'gemma3-1b').
        selected_model_variant_id:
          oneOf:
          - type: string
          - type: 'null'
          description: Catalog model variant identifier for the selected model.
        selected_model_version:
          oneOf:
          - type: string
          - type: 'null'
          description: Version string of the selected model.
        artifact_candidates:
          type: array
          items:
            $ref: '#/components/schemas/runtime_plan_response_schema__RuntimeArtifactPlan'
          description: Available artifact options for the resolved model, filtered for the requesting device.
        preferred_engines:
          type: array
          items:
            type: string
          description: Ordered list of preferred engine identifiers for this device and capability.
        fallback_policy:
          oneOf:
          - type: string
          - type: 'null'
          description: Fallback locality ('cloud') or null if no fallback is permitted (e.g. private/local_only).
        plan_ttl_seconds:
          type: integer
          minimum: 0
          default: 604800
          description: 'How long this resolution is valid, in seconds. Default: 604800 (7 days).'
        public_client_allowed:
          type: boolean
          default: false
          description: Per-app capability flag. True when the operator has explicitly enabled public-client (browser publishable
            / mobile bundle) calls against the hosted gateway for this app. Default false. Private/local_only routing keeps
            this false even if the flag is on. Server-side enforcement still requires the request key to carry cloud:inference:public.
    runtime_plan_request_schema:
      title: RuntimePlanRequest
      description: Request body for POST /api/v2/runtime/plan. Asks the server to resolve an optimized runtime execution plan
        for a model on a specific device.
      type: object
      required:
      - model
      - capability
      - device
      additionalProperties: false
      properties:
        model:
          type: string
          description: Model identifier to plan for (e.g. 'gemma-3-1b', 'smollm2-135m').
        capability:
          type: string
          enum:
          - chat
          - responses
          - embeddings
          - transcription
          - audio
          - tts
          description: Inference capability the plan should target.
        routing_policy:
          type: string
          enum:
          - private
          - local_only
          - local_first
          - cloud_first
          - cloud_only
          - performance_first
          - auto
          description: Routing policy controlling how candidates are prioritized and filtered. Defaults to local_first on
            the server. The auto value is retained for older SDK compatibility and is resolved by the server.
        app_id:
          type: string
          description: Application identifier registered with Octomil.
        app_slug:
          type: string
          description: Application slug for @app/{slug}/{capability} resolution. When present, the server resolves the app's
            configured model and routing policy.
        org_id:
          type: string
          description: Organization identifier. May also be derived from auth context on the server.
        device:
          $ref: '#/components/schemas/device_runtime_profile_schema'
          description: Device runtime profile describing the hardware and installed engines.
        allow_cloud_fallback:
          type: boolean
          description: When true (or omitted), cloud candidates may appear in fallback_candidates under local_first policy.
            Set to false to suppress cloud fallback entirely.
    device_runtime_profile_schema:
      title: DeviceRuntimeProfile
      description: Hardware and software profile sent to the server runtime planner endpoint. Collected by each SDK to describe
        the device requesting a plan.
      type: object
      required:
      - sdk
      - sdk_version
      - platform
      - arch
      additionalProperties: false
      properties:
        sdk:
          type: string
          enum:
          - python
          - node
          - ios
          - android
          - browser
          description: SDK platform identifier.
        sdk_version:
          type: string
          description: SDK version string (e.g. '1.15.0').
        platform:
          type: string
          description: Operating system platform (e.g. 'Darwin', 'Linux', 'iOS', 'macOS').
        arch:
          type: string
          description: CPU architecture (e.g. 'arm64', 'x86_64').
        os_version:
          type: string
          description: OS version string.
        chip:
          type: string
          description: Chip/SoC identifier (e.g. 'Apple M2', 'A17 Pro', 'Snapdragon 8 Gen 3').
        ram_total_bytes:
          type: integer
          minimum: 0
          description: Total system RAM in bytes.
        gpu_core_count:
          type: integer
          minimum: 0
          description: Number of GPU cores, if known.
        accelerators:
          type: array
          items:
            type: string
          description: Available hardware accelerators (e.g. 'metal', 'cuda', 'ane', 'nnapi', 'webgpu').
        installed_runtimes:
          type: array
          items:
            $ref: '#/components/schemas/device_runtime_profile_schema__InstalledRuntime'
          description: Locally-installed inference engines detected on this device.
        supported_gate_codes:
          type: array
          items:
            $ref: '#/components/schemas/candidate_gate_schema/properties/code'
          description: Device-environment gate codes this SDK can evaluate. Absent or empty = device-environment gates stripped,
            no candidate suppression. Non-empty = server may suppress local candidates with required unsupported device-environment
            gates.
    runtime_benchmark_submission_response_schema:
      title: RuntimeBenchmarkSubmissionResponse
      description: Response body for POST /api/v2/runtime/benchmarks after a benchmark is accepted for aggregation.
      type: object
      required:
      - id
      - accepted
      - created_at
      additionalProperties: false
      properties:
        id:
          type: string
          description: Server-generated benchmark submission identifier.
        accepted:
          type: boolean
          const: true
          description: True when the submission passed validation and was stored.
        created_at:
          type: string
          format: date-time
          description: ISO 8601 timestamp when the server accepted the submission.
    runtime_benchmark_submission_schema:
      title: RuntimeBenchmarkSubmission
      description: Request body for POST /api/v2/runtime/benchmarks. Submits real inference benchmark results from a device.
        Must NOT contain prompts, responses, audio, transcripts, or file paths.
      type: object
      required:
      - model
      - capability
      - engine
      - device
      - success
      additionalProperties: false
      properties:
        source:
          type: string
          enum:
          - planner
          - runner
          - manual
          default: planner
          description: Where this benchmark originated.
        model:
          type: string
          description: Model identifier that was benchmarked.
        model_version:
          type: string
          description: Model version string, if known.
        artifact_digest:
          type: string
          description: Content digest of the artifact that was benchmarked (e.g. SHA-256 hex).
        capability:
          type: string
          description: Capability that was benchmarked (e.g. 'chat', 'embeddings', 'transcription').
        engine:
          type: string
          description: Canonical engine identifier used for the benchmark (e.g. 'mlx-lm', 'llama.cpp').
        engine_version:
          type: string
          description: Engine version string, if known.
        quantization:
          type: string
          description: Quantization level used (e.g. 'q4_k_m', 'int8').
        device:
          $ref: '#/components/schemas/device_runtime_profile_schema'
          description: Device runtime profile describing the hardware where the benchmark ran.
        benchmark_tokens:
          type: integer
          minimum: 0
          description: Number of tokens generated during the benchmark run.
        ttft_ms:
          type: number
          minimum: 0
          description: Time to first token in milliseconds.
        tokens_per_second:
          type: number
          minimum: 0
          description: Token generation throughput in tokens per second.
        latency_ms:
          type: number
          minimum: 0
          description: Total inference latency in milliseconds.
        peak_memory_bytes:
          type: integer
          minimum: 0
          description: Peak memory usage during inference in bytes.
        success:
          type: boolean
          description: Whether the benchmark run completed successfully.
        error_code:
          type: string
          description: Error code if the benchmark failed.
        metadata:
          type: object
          additionalProperties: true
          description: Arbitrary metadata. Must NOT contain keys related to prompts, responses, audio, transcripts, or file
            paths.
    response_stream_event:
      oneOf:
      - $ref: '#/components/schemas/response_stream_event__TextDeltaEvent'
      - $ref: '#/components/schemas/response_stream_event__ToolCallDeltaEvent'
      - $ref: '#/components/schemas/response_stream_event__DoneEvent'
    response:
      type: object
      required:
      - id
      - model
      - output
      - finishReason
      properties:
        id:
          type: string
          pattern: ^resp_
        model:
          type: string
        output:
          type: array
          items:
            $ref: '#/components/schemas/response__OutputItem'
        finishReason:
          type: string
          enum:
          - stop
          - tool_calls
          - length
          - content_filter
        usage:
          $ref: '#/components/schemas/response__Usage'
      additionalProperties: false
    response_request:
      type: object
      required:
      - model
      - input
      properties:
        model:
          type: string
        input:
          oneOf:
          - type: string
          - type: array
            items:
              $ref: '#/components/schemas/response_request__ContentBlock'
        tools:
          type: array
          items:
            $ref: '#/components/schemas/response_request__ToolDef'
        instructions:
          type: string
        previousResponseId:
          type: string
        maxOutputTokens:
          type: integer
          minimum: 1
        temperature:
          type: number
          minimum: 0
          maximum: 2
        topP:
          type: number
          minimum: 0
          maximum: 1
        stop:
          type: array
          items:
            type: string
        responseFormat:
          $ref: '#/components/schemas/response_request__ResponseFormat'
        metadata:
          type: object
          additionalProperties: true
      additionalProperties: false
    Incident:
      title: Incident
      description: An incident record. Mirrors server/app/models/incident.py Incident.to_dict().
      type: object
      required:
      - id
      - org_id
      - title
      - severity
      - status
      properties:
        id:
          type: string
          description: Incident UUID.
        org_id:
          type: string
        alert_rule_id:
          type:
          - string
          - 'null'
          description: Source alert rule ID if the incident was auto-created; null for manually-created.
        title:
          type: string
        description:
          type:
          - string
          - 'null'
        severity:
          type: string
          enum:
          - info
          - warning
          - error
          - critical
          - medium
          description: Conventional values match IncidentSeverity (info|warning|error|critical). `"medium"` is included because
            the server's IncidentCreateRequest defaults to it and the DB stores arbitrary strings (the SAEnum column uses
            `create_type=False`); a generated SDK would otherwise reject the default create response. Future server-side enum
            tightening should remove `"medium"` from this list.
        status:
          type: string
          enum:
          - open
          - acknowledged
          - investigating
          - resolved
          - closed
        metric_name:
          type:
          - string
          - 'null'
        metric_value:
          type:
          - string
          - 'null'
        resource_type:
          type:
          - string
          - 'null'
        resource_id:
          type:
          - string
          - 'null'
        assigned_to:
          type:
          - string
          - 'null'
          description: User ID the incident is assigned to.
        acknowledged_at:
          type:
          - string
          - 'null'
          format: date-time
        acknowledged_by:
          type:
          - string
          - 'null'
        resolved_at:
          type:
          - string
          - 'null'
          format: date-time
        resolved_by:
          type:
          - string
          - 'null'
        resolution_notes:
          type:
          - string
          - 'null'
        closed_at:
          type:
          - string
          - 'null'
          format: date-time
        closed_by:
          type:
          - string
          - 'null'
        incident_metadata:
          type:
          - object
          - 'null'
          additionalProperties: true
        created_at:
          type:
          - string
          - 'null'
          format: date-time
        updated_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    IncidentListResponse:
      title: IncidentListResponse
      description: Bare array of incident objects. Newest first by created_at.
      type: array
      items:
        $ref: '#/components/schemas/Incident'
    UpdateAlertRuleRequest:
      title: UpdateAlertRuleRequest
      description: Partial update fields for an alert rule.
      type: object
      properties:
        name:
          type: string
        severity:
          type: string
          enum:
          - info
          - warning
          - error
          - critical
        metric_name:
          type: string
        threshold_value:
          type: number
        threshold_duration_minutes:
          type: integer
        enabled:
          type: boolean
        notify_channels:
          type: array
          items:
            type: string
      additionalProperties: false
    AlertRuleResponse:
      title: AlertRuleResponse
      description: Full alert rule configuration.
      type: object
      required:
      - id
      - name
      - alert_type
      - severity
      - metric_name
      - enabled
      properties:
        id:
          type: string
        name:
          type: string
        alert_type:
          type: string
          enum:
          - threshold
          - anomaly
          - sla
        severity:
          type: string
          enum:
          - info
          - warning
          - error
          - critical
        metric_name:
          type: string
        threshold_value:
          type:
          - number
          - 'null'
        threshold_duration_minutes:
          type:
          - integer
          - 'null'
        enabled:
          type: boolean
        notify_channels:
          type:
          - array
          - 'null'
          items:
            type: string
        created_at:
          type:
          - string
          - 'null'
        updated_at:
          type:
          - string
          - 'null'
      additionalProperties: false
    Job:
      title: Job
      description: Background job record.
      type: object
      required:
      - id
      - org_id
      - job_type
      - status
      - priority
      - progress_pct
      - attempts
      - max_attempts
      - created_at
      - updated_at
      properties:
        id:
          type: string
        org_id:
          type: string
        job_type:
          type: string
        status:
          type: string
          enum:
          - queued
          - scheduled
          - running
          - in_progress
          - completed
          - failed
          - cancelled
        priority:
          type: integer
        progress_pct:
          type: integer
          minimum: 0
          maximum: 100
        attempts:
          type: integer
          minimum: 0
        max_attempts:
          type: integer
          minimum: 1
        payload:
          type:
          - object
          - 'null'
          additionalProperties: true
        result:
          type:
          - object
          - 'null'
          additionalProperties: true
        error_message:
          type:
          - string
          - 'null'
        scheduled_at:
          type:
          - string
          - 'null'
          format: date-time
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        finished_at:
          type:
          - string
          - 'null'
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      additionalProperties: false
    JobsOverview:
      title: JobsOverview
      description: Aggregated job queue overview.
      type: object
      required:
      - queue_overview
      - job_types
      - automation
      - recent_jobs
      properties:
        queue_overview:
          type: object
          additionalProperties: true
        job_types:
          type: object
          additionalProperties:
            type: integer
        automation:
          type: object
          additionalProperties: true
        active_rollout_items:
          type: array
          items: {}
        running_experiment_items:
          type: array
          items: {}
        failed_integration_items:
          type: array
          items: {}
        recent_jobs:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: true
    training_plan:
      title: TrainingPlan
      description: Server-issued training plan defining local training configuration for a federated round.
      type: object
      required:
      - planId
      - roundId
      - jobId
      - modelArtifactId
      - hyperparameters
      - privacyConfig
      - expiresAt
      additionalProperties: false
      properties:
        planId:
          type: string
          description: Opaque unique identifier for this training plan.
        roundId:
          type: string
          description: Federated round this plan belongs to.
        jobId:
          type: string
          description: Training job this round is part of.
        modelArtifactId:
          type: string
          description: ArtifactId of the base model to train. Device must have this artifact staged.
        adapterArtifactId:
          type: string
          description: ArtifactId of an adapter to fine-tune, if applicable.
        hyperparameters:
          type: object
          required:
          - epochs
          - batchSize
          - learningRate
          additionalProperties: false
          description: Local training hyperparameters.
          properties:
            epochs:
              type: integer
              minimum: 1
              description: Number of local training epochs.
            batchSize:
              type: integer
              minimum: 1
              description: Mini-batch size for local SGD.
            learningRate:
              type: number
              exclusiveMinimum: 0
              description: Initial learning rate.
            maxGradNorm:
              type: number
              exclusiveMinimum: 0
              description: Gradient clipping norm bound for DP.
            weightDecay:
              type: number
              minimum: 0
            warmupSteps:
              type: integer
              minimum: 0
        privacyConfig:
          type: object
          required:
          - mechanism
          additionalProperties: false
          description: Differential privacy configuration.
          properties:
            mechanism:
              type: string
              enum:
              - none
              - gaussian
              - laplace
              description: DP noise mechanism to apply.
            epsilon:
              type: number
              exclusiveMinimum: 0
              description: Privacy budget epsilon.
            delta:
              type: number
              exclusiveMinimum: 0
              description: Privacy budget delta.
            noiseMultiplier:
              type: number
              exclusiveMinimum: 0
            clipNorm:
              type: number
              exclusiveMinimum: 0
              description: L2 sensitivity bound for clipping.
            secureAggregation:
              type: boolean
              default: false
              description: Whether secure aggregation is required for this round.
        dataConfig:
          type: object
          additionalProperties: false
          description: Local data selection constraints.
          properties:
            datasetKey:
              type: string
              description: SDK-registered dataset key to train on.
            maxSamples:
              type: integer
              minimum: 1
              description: Maximum number of local samples to use.
            minSamples:
              type: integer
              minimum: 1
              description: Minimum required samples; device aborts if below this.
        executionWindowSeconds:
          type: integer
          minimum: 60
          description: Maximum wall-clock seconds allowed for local training.
        expiresAt:
          type: string
          format: date-time
          description: Round deadline; device must complete and upload before this timestamp.
    device_sync_request:
      title: DeviceSyncRequest
      description: Unified sync request combining device inventory and observed state in a single round-trip.
      type: object
      required:
      - schemaVersion
      - deviceId
      - requestedAt
      additionalProperties: false
      properties:
        schemaVersion:
          type: string
          description: Contract schema version this payload conforms to (e.g. '1.12.0').
        deviceId:
          type: string
          description: Opaque device identifier.
        requestedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when the SDK initiated this sync.
        knownStateVersion:
          type: string
          description: Last applied desired state syncRevision. Server may short-circuit if unchanged.
        sdkVersion:
          type: string
          description: Version of the Octomil SDK.
        platform:
          type: string
          description: DevicePlatform enum value (ios, android, python, browser, node).
        appId:
          type: string
          description: Application identifier registered with Octomil.
        appVersion:
          type: string
          description: Version of the host application.
        modelInventory:
          type: array
          description: Installed model artifacts and their statuses.
          items:
            type: object
            required:
            - modelId
            - version
            - status
            additionalProperties: false
            properties:
              modelId:
                type: string
              version:
                type: string
              artifactId:
                type: string
              status:
                type: string
                description: ArtifactStatus enum value.
        activeVersions:
          type: array
          description: Models currently loaded and serving inference.
          items:
            type: object
            required:
            - modelId
            - version
            additionalProperties: false
            properties:
              modelId:
                type: string
              version:
                type: string
        observedState:
          $ref: '#/components/schemas/observed_state'
          description: Full observed state snapshot, inlined for single round-trip.
        availableStorageBytes:
          type: integer
          minimum: 0
          description: Free storage available on the device.
    observed_state:
      title: ObservedState
      description: Device-reported observed state sent to the server during control-plane sync.
      type: object
      required:
      - schemaVersion
      - deviceId
      - reportedAt
      additionalProperties: false
      properties:
        schemaVersion:
          type: string
          description: Contract schema version this payload conforms to.
        deviceId:
          type: string
          description: Opaque device identifier.
        reportedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this snapshot was captured on-device.
        activeModelPointer:
          $ref: '#/components/schemas/active_model_pointer'
          description: Currently active base model pointer. Null if no model is active.
        activeBinding:
          $ref: '#/components/schemas/active_binding'
          description: Currently active serving binding. Null if no binding is active.
        models:
          type: array
          description: Per-model observed state with installed version, active version, status, and health.
          items:
            type: object
            required:
            - modelId
            - status
            additionalProperties: false
            properties:
              modelId:
                type: string
                description: Model variant identifier.
              installedVersion:
                type: string
                description: Version of the model artifact currently on disk.
              activeVersion:
                type:
                - string
                - 'null'
                description: Version currently loaded and serving inference. Null if not active.
              status:
                type: string
                description: ArtifactStatus enum value.
              health:
                type: string
                enum:
                - healthy
                - degraded
                - unhealthy
                description: Current health of this model on-device.
              lastError:
                type:
                - string
                - 'null'
                description: Most recent error message if status indicates failure. Null otherwise.
        federationParticipations:
          type: array
          description: Current participation state for any active federated rounds.
          items:
            type: object
            required:
            - roundId
            - state
            additionalProperties: false
            properties:
              roundId:
                type: string
              state:
                type: string
                description: FederatedParticipationState enum value.
        sdkVersion:
          type: string
          description: Version of the Octomil SDK running on-device.
        osVersion:
          type: string
          description: Host OS version string.
        availableStorageBytes:
          type: integer
          minimum: 0
          description: Free storage available on the device at time of report.
    active_binding:
      title: ActiveBinding
      description: Serving binding describing the combination of a base model and zero or more adapters used for inference.
      type: object
      required:
      - bindingId
      - baseModelArtifactId
      - resolvedAt
      additionalProperties: false
      properties:
        bindingId:
          type: string
          description: Opaque unique identifier for this binding configuration.
        baseModelArtifactId:
          type: string
          description: ArtifactId of the base model for this binding.
        adapters:
          type: array
          description: Ordered list of adapter artifacts applied on top of the base model.
          items:
            type: object
            required:
            - artifactId
            - adapterType
            additionalProperties: false
            properties:
              artifactId:
                type: string
                description: ArtifactId of the adapter artifact.
              adapterType:
                type: string
                enum:
                - lora
                - qlora
                - prefix_tuning
                description: Adapter architecture type.
              priority:
                type: integer
                minimum: 0
                description: Application priority; lower value applied first.
              activationState:
                type: string
                description: AdapterActivationState enum value.
        resolvedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this binding was last resolved by the server.
        label:
          type: string
          description: Human-readable label for this binding (e.g. experiment name or rollout group).
    active_model_pointer:
      title: ActiveModelPointer
      description: Atomic pointer to the currently active base model version on-device. Written atomically on activation to
        avoid partial reads.
      type: object
      required:
      - modelId
      - version
      - artifactId
      - activatedAt
      additionalProperties: false
      properties:
        modelId:
          type: string
          description: Model variant identifier (matches catalog ModelVariant.id).
        version:
          type: string
          description: Model version string.
        artifactId:
          type: string
          description: ArtifactId of the artifact currently loaded in the runtime.
        format:
          type: string
          description: ArtifactFormat enum value for the active artifact.
        activatedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this model was promoted to active.
        previousArtifactId:
          type: string
          description: ArtifactId of the model this version replaced, if any. Used for rollback.
        isFallback:
          type: boolean
          default: false
          description: True if this pointer represents a rollback to a previous version rather than a forward activation.
    device_sync_response:
      title: DeviceSyncResponse
      description: Unified sync response returning desired state and server directives in a single round-trip.
      type: object
      required:
      - schemaVersion
      - deviceId
      - generatedAt
      - stateChanged
      additionalProperties: false
      properties:
        schemaVersion:
          type: string
          description: Contract schema version this payload conforms to.
        deviceId:
          type: string
          description: Opaque device identifier this response targets.
        generatedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this response was generated server-side.
        stateChanged:
          type: boolean
          description: False if the knownStateVersion matched and no update is needed.
        desiredState:
          $ref: '#/components/schemas/desired_state'
          description: Full desired state. Absent when stateChanged is false.
        nextPollIntervalSeconds:
          type: integer
          minimum: 1
          description: Server hint for how often the device should sync (seconds).
        serverTimestamp:
          type: string
          format: date-time
          description: Current server time for clock skew correction.
    desired_state:
      title: DesiredState
      description: Server-authoritative desired state delivered to a device during control-plane sync.
      type: object
      required:
      - schemaVersion
      - deviceId
      - generatedAt
      - activeBinding
      additionalProperties: false
      properties:
        schemaVersion:
          type: string
          description: Contract schema version this payload conforms to (e.g. '1.4.0').
        deviceId:
          type: string
          description: Opaque device identifier this state targets.
        generatedAt:
          type: string
          format: date-time
          description: ISO 8601 timestamp when this state was generated server-side.
        activeBinding:
          $ref: '#/components/schemas/active_binding'
          description: The target serving binding (base model + adapters) for this device.
        syncRevision:
          type: string
          description: Monotonic revision for change detection.
        rolloutId:
          type: string
          description: Opaque identifier for the active rollout targeting this device.
        models:
          type: array
          description: Per-model desired state entries with delivery mode, activation policy, and engine constraints.
          items:
            $ref: '#/components/schemas/desired_model_entry'
        policyConfig:
          $ref: '#/components/schemas/policy_config'
          description: Device-specific policy overrides. Absent means use defaults.
        federationOffers:
          type: array
          description: Active federated round offers the device should evaluate.
          items:
            type: object
            required:
            - roundId
            - jobId
            - expiresAt
            additionalProperties: false
            properties:
              roundId:
                type: string
              jobId:
                type: string
              expiresAt:
                type: string
                format: date-time
        gcEligibleArtifactIds:
          type: array
          description: Artifact IDs the server considers safe to evict from local storage.
          items:
            type: string
    policy_config:
      title: PolicyConfig
      description: Device-specific policy configuration governing resource usage, federation eligibility, and telemetry behaviour.
      type: object
      required:
      - version
      additionalProperties: false
      properties:
        version:
          type: string
          description: Monotonic version string for this policy config. Devices must apply the latest version.
        download:
          type: object
          additionalProperties: false
          description: Artifact download constraints.
          properties:
            requireWifi:
              type: boolean
              default: true
              description: Restrict artifact downloads to Wi-Fi connections only.
            requireCharging:
              type: boolean
              default: false
              description: Restrict artifact downloads to when device is charging.
            maxConcurrentChunks:
              type: integer
              minimum: 1
              default: 4
              description: Maximum number of chunk download requests in flight simultaneously.
        federation:
          type: object
          additionalProperties: false
          description: Federated learning eligibility constraints.
          properties:
            enabled:
              type: boolean
              default: true
              description: Whether this device is eligible to participate in federated rounds.
            requireWifi:
              type: boolean
              default: true
              description: Require Wi-Fi for upload of training updates.
            requireCharging:
              type: boolean
              default: true
              description: Require charging state for local training execution.
            minBatteryLevel:
              type: number
              minimum: 0
              maximum: 1
              description: Minimum battery level fraction required to start local training.
            maxWallTimeSeconds:
              type: integer
              minimum: 60
              description: Hard cap on wall-clock time for a single round's local training.
        telemetry:
          type: object
          additionalProperties: false
          description: Telemetry upload behaviour.
          properties:
            enabled:
              type: boolean
              default: true
              description: Whether telemetry upload is enabled for this device.
            requireWifi:
              type: boolean
              default: false
              description: Restrict telemetry uploads to Wi-Fi.
            uploadIntervalSeconds:
              type: integer
              minimum: 30
              default: 300
              description: Target interval between telemetry batch uploads.
            maxBatchSize:
              type: integer
              minimum: 1
              default: 500
              description: Maximum number of events per uploaded batch.
        cloudFallback:
          type: object
          additionalProperties: false
          description: Cloud inference fallback policy.
          properties:
            enabled:
              type: boolean
              default: true
              description: Whether cloud fallback is permitted when on-device inference fails.
            preferredProvider:
              type: string
              description: Cloud provider for fallback. Must be a CloudProvider enum value. Defaults to 'octomil'.
              enum:
              - octomil
              - openai
              - anthropic
              - groq
              - together
              - moonshot
              - minimax
              - deepseek
            providerOrder:
              type: array
              description: Ordered fallback chain. First provider with valid credentials is used. When omitted, only preferredProvider
                is attempted.
              items:
                type: string
                enum:
                - octomil
                - openai
                - anthropic
                - groq
                - together
                - moonshot
                - minimax
                - deepseek
              minItems: 1
              uniqueItems: true
            credentialPolicy:
              type: string
              description: How cloud credentials are resolved. 'byok_and_managed' tries org BYOK first, then Octomil-managed.
                'byok_only' requires org-provided credentials. 'managed_only' uses only Octomil-managed pool. 'disabled' blocks
                all cloud inference.
              enum:
              - byok_and_managed
              - byok_only
              - managed_only
              - disabled
              default: byok_and_managed
    desired_model_entry:
      title: DesiredModelEntry
      description: Per-model entry within desired state, specifying the target version, delivery mode, activation policy,
        and engine constraints.
      type: object
      required:
      - modelId
      - desiredVersion
      additionalProperties: false
      properties:
        modelId:
          type: string
          description: Model variant identifier.
        desiredVersion:
          type: string
          description: Target model version the device should converge to.
        currentChannel:
          type: string
          description: Release channel for this model (e.g. stable, beta, canary).
        deliveryMode:
          type: string
          description: "DeliveryMode enum value \u2014 how the model is delivered to the device."
        activationPolicy:
          type: string
          description: "ActivationPolicy enum value \u2014 when to activate a newly staged artifact."
        enginePolicy:
          type: object
          description: Engine constraints for runtime executor selection.
          additionalProperties: false
          properties:
            allowed:
              type: array
              description: RuntimeExecutor values this model may use.
              items:
                type: string
            forced:
              type: string
              description: If set, override engine selection and always use this executor.
        artifactManifest:
          $ref: '#/components/schemas/artifact_manifest'
          description: Download manifest for the target artifact version.
        rolloutId:
          type: string
          description: Opaque identifier for the rollout this entry belongs to.
    artifact_manifest:
      title: ArtifactManifest
      description: Chunked download manifest for a model artifact, including integrity metadata for each chunk.
      type: object
      required:
      - artifactId
      - modelId
      - version
      - format
      - totalBytes
      - chunks
      additionalProperties: false
      properties:
        artifactId:
          type: string
          description: Opaque unique identifier for this artifact version.
        modelId:
          type: string
          description: Model variant identifier this artifact belongs to.
        version:
          type: string
          description: Model version string.
        format:
          type: string
          description: ArtifactFormat enum value (e.g. coreml, tflite, gguf).
        totalBytes:
          type: integer
          minimum: 1
          description: Total size of the complete artifact in bytes.
        sha256:
          type: string
          pattern: ^[a-fA-F0-9]{64}$
          description: SHA-256 hex digest of the complete assembled artifact.
        chunks:
          type: array
          minItems: 1
          description: Ordered list of chunk descriptors. Download in order and concatenate.
          items:
            type: object
            required:
            - index
            - offset
            - size
            - sha256
            additionalProperties: false
            properties:
              index:
                type: integer
                minimum: 0
                description: Zero-based chunk sequence index.
              offset:
                type: integer
                minimum: 0
                description: Byte offset of this chunk within the assembled artifact.
              size:
                type: integer
                minimum: 1
                description: Size of this chunk in bytes.
              sha256:
                type: string
                pattern: ^[a-fA-F0-9]{64}$
                description: SHA-256 hex digest of this chunk.
        cdnBaseUrl:
          type: string
          format: uri
          description: Base URL for signed chunk downloads. Combined with chunk path to form the download URL.
        urlExpiresAt:
          type: string
          format: date-time
          description: Expiry time for any pre-signed URLs embedded in this manifest.
        entrypoint:
          type: string
          description: Primary file to load within the artifact.
        engineCompatibility:
          type: array
          description: RuntimeExecutor values this artifact supports (e.g. llamacpp, mlx).
          items:
            type: string
        isAdapter:
          type: boolean
          default: false
          description: True if this artifact is a LoRA adapter rather than a full base model.
        baseModelArtifactId:
          type: string
          description: 'For adapters: the artifactId of the base model this adapter targets.'
    ExperimentTargetGroups:
      title: ExperimentTargetGroups
      description: Device groups targeted by an experiment. Shared response shape for GET and PUT target-groups endpoints.
      type: object
      required:
      - experiment_id
      - target_groups
      properties:
        experiment_id:
          type: string
        target_groups:
          type: array
          description: Empty array when no groups are configured.
          items:
            type: object
            required:
            - id
            - name
            properties:
              id:
                type: string
              name:
                type: string
              group_type:
                type:
                - string
                - 'null'
            additionalProperties: true
      additionalProperties: true
    Experiment:
      title: Experiment
      description: An A/B experiment within a deployment. Mirrors server/app/models/experiment.py Experiment.to_dict().
      type: object
      required:
      - id
      - name
      - model_id
      - status
      - traffic_percentage
      - experiment_type
      properties:
        id:
          type: string
          description: Experiment UUID.
        name:
          type: string
        description:
          type:
          - string
          - 'null'
        model_id:
          type: string
          description: Internal Model registry UUID (see workspace memory `Model Registries`).
        status:
          type: string
          enum:
          - draft
          - running
          - paused
          - completed
          - cancelled
          description: "From ExperimentStatus enum. Lifecycle transitions: draft \u2192 running \u2192 (paused \u2194 running)\
            \ \u2192 completed; any \u2192 cancelled."
        traffic_percentage:
          type: number
          minimum: 0
          maximum: 100
          description: Percentage of eligible devices included (0-100).
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        ended_at:
          type:
          - string
          - 'null'
          format: date-time
        min_sample_size:
          type:
          - integer
          - 'null'
        confidence_level:
          type:
          - number
          - 'null'
        primary_metric:
          type:
          - string
          - 'null'
        experiment_type:
          type: string
          description: e.g. `"version"` (compare model versions), `"variant"` (compare variants).
        created_at:
          type:
          - string
          - 'null'
          format: date-time
        updated_at:
          type:
          - string
          - 'null'
          format: date-time
        variants:
          type:
          - array
          - 'null'
          description: Per-variant traffic allocation. Present when include_variants=true (the default).
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              model_version:
                type:
                - string
                - 'null'
              traffic_allocation:
                type: number
              is_control:
                type: boolean
        target_groups:
          type: array
          description: Device groups the experiment targets. Empty array when none configured.
          items:
            type: object
            required:
            - id
            - name
            properties:
              id:
                type: string
              name:
                type: string
              group_type:
                type:
                - string
                - 'null'
      additionalProperties: true
    ExperimentListResponse:
      title: ExperimentListResponse
      description: Bare array of experiment objects scoped to one deployment.
      type: array
      items:
        $ref: '#/components/schemas/Experiment'
    ExperimentTimeseries:
      title: ExperimentTimeseries
      description: Time-bucketed metric means per variant. Interval buckets are 'hour', 'day' (default), or 'week'. Mirrors
        get_experiment_timeseries() return shape.
      type: object
      required:
      - experiment_id
      - metric_name
      - interval
      - variants
      properties:
        experiment_id:
          type: string
        metric_name:
          type: string
        interval:
          type: string
          enum:
          - hour
          - day
          - week
          description: Bucket granularity. Values outside this set default to 'day' on the server.
        variants:
          type: array
          items:
            type: object
            required:
            - variant_id
            - variant_name
            - is_control
            - data_points
            properties:
              variant_id:
                type: string
              variant_name:
                type: string
              is_control:
                type: boolean
              data_points:
                type: array
                items:
                  type: object
                  required:
                  - timestamp
                  - value
                  - count
                  properties:
                    timestamp:
                      type: string
                      description: 'Bucket key. Format depends on interval: ISO datetime string for ''hour'', YYYY-MM-DD for
                        ''day'', YYYY-WWW for ''week''.'
                    value:
                      type: number
                      description: Mean metric value across all events in this bucket.
                    count:
                      type: integer
                      description: Number of events in this bucket.
      additionalProperties: true
    ExperimentSequentialAnalysis:
      title: ExperimentSequentialAnalysis
      description: "The server returns StatisticalAnalyzer.sequential_test() verbatim. No fixed required fields are guaranteed\
        \ by the server today \u2014 treat all fields as optional in SDK codegen until the analyzer interface is stabilised."
      type: object
      additionalProperties: true
    ExperimentSegmentAnalysis:
      title: ExperimentSegmentAnalysis
      description: Remaining fields are merged from StatisticalAnalyzer.segment_analysis() and are analyzer-defined.
      type: object
      required:
      - experiment_id
      - metric
      - segment_by
      properties:
        experiment_id:
          type: string
        metric:
          type: string
        segment_by:
          type: string
          description: Device metadata key used to partition events (e.g. 'platform', 'os_version'). Defaults to 'platform'
            when the query param is omitted.
      additionalProperties: true
    ExperimentSampleSize:
      title: ExperimentSampleSize
      description: Required sample size for the experiment's primary metric and current enrollment progress. Mirrors get_experiment_sample_size()
        return shape.
      type: object
      required:
      - sample_size_per_group
      - total_sample_size
      - power
      - alpha
      - minimum_detectable_effect
      - effect_type
      - current_control_n
      - current_treatment_n
      - progress_percent
      properties:
        sample_size_per_group:
          type: integer
          description: Required observations per variant group.
        total_sample_size:
          type: integer
          description: sample_size_per_group * 2.
        power:
          type: number
          description: Target statistical power (e.g. 0.80).
        alpha:
          type: number
          description: Significance level (e.g. 0.05).
        minimum_detectable_effect:
          type: number
          description: Minimum relative effect size the experiment is powered to detect.
        effect_type:
          type: string
          description: Effect type label returned by StatisticalAnalyzer (e.g. 'relative').
        current_control_n:
          type: integer
          description: Current number of control-group observations.
        current_treatment_n:
          type: integer
          description: Current number of treatment-group observations.
        progress_percent:
          type: number
          minimum: 0
          maximum: 100
          description: Enrollment progress as percentage of total_sample_size. Rounded to 1 decimal place.
      additionalProperties: true
    ExperimentNonparametric:
      title: ExperimentNonparametric
      description: 'Nonparametric analysis results: Mann-Whitney U test plus per-variant normality assessment. Mirrors get_nonparametric_analysis()
        return shape.'
      type: object
      required:
      - experiment_id
      - metric
      - recommended_test
      properties:
        experiment_id:
          type: string
        metric:
          type: string
        mann_whitney:
          type: object
          description: Mann-Whitney U test output from StatisticalAnalyzer.mann_whitney_test(). Shape is analyzer-defined;
            treat all sub-fields as optional.
          additionalProperties: true
        normality_control:
          type: object
          description: Normality test output for the control group from StatisticalAnalyzer.test_normality().
          properties:
            is_normal:
              type: boolean
          additionalProperties: true
        normality_treatment:
          type: object
          description: Normality test output for the treatment group.
          properties:
            is_normal:
              type: boolean
          additionalProperties: true
        recommended_test:
          type: string
          enum:
          - t-test
          - mann_whitney_u
          description: '''t-test'' when both groups pass normality; ''mann_whitney_u'' otherwise.'
      additionalProperties: true
    ExperimentGuardrailsResult:
      title: ExperimentGuardrailsResult
      description: Per-metric guardrail check results. Mirrors check_experiment_guardrails() return shape.
      type: object
      required:
      - experiment_id
      - guardrails
      - any_violation
      - recommendation
      properties:
        experiment_id:
          type: string
        guardrails:
          type: array
          description: One entry per metric in the request body. Shape of each entry is returned from StatisticalAnalyzer.check_guardrails().
          items:
            type: object
            properties:
              metric_name:
                type: string
              status:
                type: string
                enum:
                - pass
                - violation
                description: pass = guardrail held; violation = threshold breached.
            additionalProperties: true
        any_violation:
          type: boolean
          description: True if any metric status == 'violation'.
        recommendation:
          type: string
          description: "Human-readable summary: 'All guardrails pass' or 'Guardrail violation detected \u2014 review before\
            \ proceeding'."
      additionalProperties: true
    ExperimentAnalytics:
      title: ExperimentAnalytics
      description: 'Full analytics summary for an experiment: per-variant stats, t-test significance, and statistical power.
        Mirrors get_experiment_analytics() return shape.'
      type: object
      required:
      - experiment_id
      - experiment_name
      - status
      - primary_metric
      - total_events
      - total_devices
      - variants
      properties:
        experiment_id:
          type: string
          description: Experiment UUID.
        experiment_name:
          type: string
        status:
          type: string
          enum:
          - draft
          - running
          - paused
          - completed
          - cancelled
        primary_metric:
          type: string
          description: Metric name used for analysis. Defaults to experiment.primary_metric when metric_name query param is
            omitted.
        total_events:
          type: integer
        total_devices:
          type: integer
        started_at:
          type:
          - string
          - 'null'
          format: date-time
        variants:
          type: array
          description: Per-variant analytics summary.
          items:
            $ref: '#/components/schemas/ExperimentAnalytics__VariantAnalytics'
        significance_result:
          oneOf:
          - $ref: '#/components/schemas/ExperimentAnalytics__SignificanceResult'
          - type: 'null'
          description: Welch t-test result. Null when either variant has fewer than 2 observations.
        statistical_power:
          type:
          - number
          - 'null'
          description: Observed statistical power. Null when significance_result is null.
        recommendation:
          type:
          - string
          - 'null'
        warnings:
          type: array
          items:
            type: string
      additionalProperties: true
    chat_turn_request:
      title: ChatTurnRequest
      description: Request to submit a user turn and receive an assistant response.
      type: object
      required:
      - threadId
      - input
      properties:
        threadId:
          type: string
          pattern: ^thread_
          description: Thread to append the turn to.
        input:
          type: string
          description: User message text.
        inputParts:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/content_part'
          description: "Typed input parts (transitional \u2014 mirrors contentParts on messages). When present, takes precedence\
            \ over input string."
        config:
          type: object
          properties:
            maxTokens:
              type: integer
            temperature:
              type: number
              minimum: 0
              maximum: 2
            topP:
              type: number
              minimum: 0
              maximum: 1
            stop:
              type: array
              items:
                type: string
          additionalProperties: false
          description: Optional generation configuration.
      additionalProperties: false
    content_part:
      description: Typed content part for multimodal messages. Each media part requires exactly one source (assetId, url,
        or data).
      oneOf:
      - $ref: '#/components/schemas/content_part__InputText'
      - $ref: '#/components/schemas/content_part__InputImage'
      - $ref: '#/components/schemas/content_part__InputAudio'
      - $ref: '#/components/schemas/content_part__InputVideo'
    chat_turn_result:
      title: ChatTurnResult
      description: Result of a chat turn containing the user and assistant messages.
      type: object
      required:
      - userMessage
      - assistantMessage
      properties:
        userMessage:
          $ref: '#/components/schemas/chat_message'
          description: The user message that initiated the turn.
        assistantMessage:
          $ref: '#/components/schemas/chat_message'
          description: The assistant response message.
      additionalProperties: false
    chat_message:
      title: ChatMessage
      description: A single message within a chat thread.
      type: object
      required:
      - id
      - threadId
      - role
      - createdAt
      properties:
        id:
          type: string
          pattern: ^msg_
          description: Unique message identifier.
        threadId:
          type: string
          pattern: ^thread_
          description: Parent thread identifier.
        role:
          type: string
          enum:
          - system
          - user
          - assistant
          - tool
          description: Message author role.
        content:
          type:
          - string
          - 'null'
          description: Text content of the message.
        toolCalls:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/response__ToolCall'
          description: Tool calls emitted by the assistant.
        toolCallId:
          type:
          - string
          - 'null'
          description: For role=tool, the ID of the tool call being responded to.
        metrics:
          oneOf:
          - $ref: '#/components/schemas/chat_message__GenerationMetrics'
          - type: 'null'
          description: Optional generation performance metrics.
        contentParts:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/content_part'
          description: 'Typed content parts (canonical when present). content field becomes derived shorthand: concatenation
            of all InputText parts joined by newline, null if no text parts. Both present with conflicting values is invalid.
            Assistant messages use content only (output parts reserved for future).'
        createdAt:
          type: string
          format: date-time
          description: ISO 8601 creation timestamp.
      additionalProperties: false
    chat_thread:
      title: ChatThread
      description: A conversation thread containing messages.
      type: object
      required:
      - id
      - model
      - createdAt
      - updatedAt
      properties:
        id:
          type: string
          pattern: ^thread_
          description: Unique thread identifier.
        title:
          type: string
          description: Optional display title for the thread.
        model:
          type: string
          description: Model used for inference in this thread.
        createdAt:
          type: string
          format: date-time
          description: ISO 8601 creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: ISO 8601 last-updated timestamp.
        metadata:
          type: object
          additionalProperties: true
          description: Arbitrary key-value metadata.
      additionalProperties: false
    audio_speech_result:
      description: Result schema for audio.speech.create. Carries audio bytes plus routing metadata.
      type: object
      required:
      - audio_bytes
      - content_type
      - format
      - model
      properties:
        audio_bytes:
          type: string
          contentEncoding: base64
          description: Synthesized audio. Wire encoding is platform-native bytes for SDK transports; the contract reference
            uses base64 for inspectability.
        content_type:
          type: string
          description: MIME type of the audio bytes. e.g. 'audio/wav', 'audio/mpeg', 'audio/ogg'.
        format:
          type: string
          enum:
          - wav
          - mp3
          - ogg
          - opus
          - flac
          - aac
          - pcm
          description: Audio format actually returned. May differ from request.response_format if the locality cannot honor
            the request and cloud fallback was selected.
        model:
          type: string
          description: Resolved model id (after app ref resolution). For @app/<slug>/tts requests this is the underlying model
            the app maps to.
        provider:
          type:
          - string
          - 'null'
          description: Upstream provider key when cloud execution was selected ('openai', 'elevenlabs', ...). Null when execution
            was local.
        voice:
          type:
          - string
          - 'null'
          description: Voice actually used. May echo the caller's voice or be the locality default when the caller omitted
            it.
        sample_rate:
          type:
          - integer
          - 'null'
          description: Sample rate of the audio in Hz, when known. Local sherpa returns the model's native rate (typically
            24000 for Kokoro, 22050 for Piper). Cloud providers may not surface this.
        duration_ms:
          type:
          - integer
          - 'null'
          minimum: 0
          description: Audio duration in milliseconds, when computable. Local execution always populates this; cloud execution
            may leave it null.
        latency_ms:
          type: number
          minimum: 0
          description: End-to-end latency of the synthesis call in milliseconds, measured from request start to bytes received.
        route:
          type: object
          description: Routing metadata. No user content (input text, raw audio bytes, file paths, provider request ids).
          required:
          - locality
          properties:
            locality:
              type: string
              enum:
              - on_device
              - cloud
              description: Where the synthesis ran.
            engine:
              type:
              - string
              - 'null'
              description: Engine id. 'sherpa-onnx' for on-device, null or provider name for cloud.
            policy:
              type:
              - string
              - 'null'
              description: Resolved routing policy preset (e.g. 'local_only', 'cloud_only', 'auto', 'local_first').
            fallback_used:
              type: boolean
              default: false
              description: True when the primary locality failed and the request succeeded via fallback (e.g. local failed,
                cloud_first cloud succeeded).
          additionalProperties: false
        billed_units:
          type:
          - integer
          - 'null'
          minimum: 0
          description: Generic billing unit count for cloud execution. Pairs with unit_kind. Null on local.
        unit_kind:
          type:
          - string
          - 'null'
          enum:
          - characters
          - milliseconds
          - null
          description: Unit kind for billed_units. 'characters' for OpenAI tts-1 / tts-1-hd, 'milliseconds' for per-minute
            billed providers. Null on local.
      additionalProperties: false
    audio_speech_request:
      description: Request schema for audio.speech.create. Synthesizes speech from text via the unified facade (local sherpa-onnx
        or hosted cloud, decided by routing policy).
      type: object
      required:
      - model
      - input
      properties:
        model:
          type: string
          description: 'Model ref. Common forms: an Octomil app ref like ''@app/<slug>/tts'', a hosted provider model id like
            ''tts-1'', or a local model id like ''kokoro-82m''. Must resolve to a model with the tts ModelCapability.'
        input:
          type: string
          minLength: 1
          maxLength: 4096
          description: Text to synthesize. Must not be empty. Maximum 4096 characters per call (matches OpenAI tts-1 input
            cap).
        voice:
          type: string
          description: 'Voice id. Pass-through to the routed locality: cloud accepts provider voices (''alloy'', ''echo'',
            ''fable'', ''onyx'', ''nova'', ''shimmer'' for OpenAI), local accepts model-specific voices (e.g. Kokoro ''af_bella'',
            ''am_adam''). Mismatches raise voice_not_supported_for_locality. Defaults to the model''s locality-appropriate
            first voice when omitted.'
        response_format:
          type: string
          enum:
          - wav
          - mp3
          - ogg
          - opus
          - flac
          - aac
          - pcm
          default: wav
          description: 'Output audio format. ''wav'' is supported on every locality. mp3/ogg/opus/flac/aac/pcm depend on the
            routed locality: cloud providers cover all, local sherpa-onnx is wav-only until local transcoding ships (see PR
            6 in the implementation plan).'
        speed:
          type: number
          minimum: 0.25
          maximum: 4.0
          default: 1.0
          description: Playback speed multiplier. Bounds match OpenAI audio.speech.create.
      additionalProperties: false
    Thread:
      title: Thread
      description: Agent conversation thread.
      type: object
      required:
      - id
      - agent_type
      - created_at
      - updated_at
      properties:
        id:
          type: string
        agent_type:
          type: string
        model:
          type:
          - string
          - 'null'
        title:
          type:
          - string
          - 'null'
        metadata:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        messages:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/Message'
      additionalProperties: false
    Message:
      title: Message
      description: A message within an agent thread.
      type: object
      required:
      - id
      - thread_id
      - role
      - created_at
      properties:
        id:
          type: string
        thread_id:
          type: string
        role:
          type: string
          enum:
          - user
          - assistant
          - tool
        content:
          type:
          - string
          - 'null'
        tool_calls:
          type:
          - array
          - 'null'
          items:
            type: object
            additionalProperties: true
        tool_call_id:
          type:
          - string
          - 'null'
        metrics:
          type:
          - object
          - 'null'
          additionalProperties: true
        created_at:
          type: string
          format: date-time
      additionalProperties: false
    CreateSessionResponse:
      title: CreateSessionResponse
      description: Response to POST /agents/sessions.
      type: object
      required:
      - session_id
      - model
      - use_case
      - binding_key
      - tools
      - instructions
      properties:
        session_id:
          type: string
        model:
          type: string
        model_ref:
          type:
          - string
          - 'null'
        model_name:
          type:
          - string
          - 'null'
        model_version:
          type:
          - string
          - 'null'
        use_case:
          type: string
        binding_key:
          type: string
        device_id:
          type:
          - string
          - 'null'
        deployment_id:
          type:
          - string
          - 'null'
        deployment_key:
          type:
          - string
          - 'null'
        tools:
          type: array
          items:
            type: object
            additionalProperties: true
        instructions:
          type: string
      additionalProperties: false
    Run:
      title: Run
      description: One agent invocation within a thread.
      type: object
      required:
      - id
      - thread_id
      - agent_type
      - model
      - status
      - query
      - created_at
      properties:
        id:
          type: string
        thread_id:
          type: string
        agent_type:
          type: string
        model:
          type: string
        status:
          type: string
          enum:
          - queued
          - running
          - completed
          - failed
          - awaiting_approval
          - cancelled
        query:
          type: string
          maxLength: 2000
        summary:
          type:
          - string
          - 'null'
        confidence:
          type:
          - number
          - 'null'
          minimum: 0
          maximum: 1
        error:
          type:
          - string
          - 'null'
        steps:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/Step'
        pending_approvals:
          type:
          - array
          - 'null'
          items:
            $ref: '#/components/schemas/ApprovalRequest'
        created_at:
          type: string
          format: date-time
        completed_at:
          type:
          - string
          - 'null'
          format: date-time
      additionalProperties: false
    ApprovalRequest:
      title: ApprovalRequest
      description: A high-risk tool action awaiting approval.
      type: object
      required:
      - id
      - tool_name
      - payload
      - risk_level
      - status
      properties:
        id:
          type: string
        tool_name:
          type: string
        payload:
          type: object
          additionalProperties: true
        risk_level:
          type: string
          enum:
          - low
          - medium
          - high
          - critical
        status:
          type: string
          enum:
          - pending
          - approved
          - rejected
          - expired
      additionalProperties: false
    Step:
      title: Step
      description: Atomic trace unit within a run.
      type: object
      required:
      - id
      - step_index
      - step_type
      - status
      - latency_ms
      properties:
        id:
          type: string
        step_index:
          type: integer
          minimum: 0
        step_type:
          type: string
          enum:
          - llm_call
          - tool_call
          - approval_gate
        tool_name:
          type:
          - string
          - 'null'
        tool_call_id:
          type:
          - string
          - 'null'
        status:
          type: string
        error_code:
          type:
          - string
          - 'null'
        latency_ms:
          type: integer
          minimum: 0
      additionalProperties: false
    AgentManifest:
      title: AgentManifest
      description: Registry-driven metadata for all registered agents.
      type: object
      additionalProperties: true
    ApiKeyScopesResponse__KeyTypeScopesResponse:
      type: object
      required:
      - groups
      properties:
        groups:
          type: object
          additionalProperties:
            type: object
            required:
            - label
            - description
            - scopes
            properties:
              label:
                type: string
              description:
                type: string
              scopes:
                type: array
                items:
                  type: string
            additionalProperties: false
      additionalProperties: false
    runtime_plan_response_schema__ModelResolution:
      type: object
      required:
      - ref_kind
      - original_ref
      - resolved_model
      additionalProperties: false
      description: Resolved model-reference metadata used to trace app, capability, deployment, and experiment routing decisions.
      properties:
        ref_kind:
          $ref: '#/components/schemas/model_ref_kind_schema'
          description: The parsed model reference kind. Must be one of the canonical model_ref_kind values.
        original_ref:
          type: string
          description: The original model reference supplied by the caller.
        resolved_model:
          type: string
          description: The concrete model selected for runtime planning.
        deployment_id:
          type: string
          description: Deployment identifier when the ref resolved through a deployment.
        deployment_key:
          type: string
          description: Human-readable or external deployment key when available.
        experiment_id:
          type: string
          description: Experiment identifier when the ref resolved through an experiment.
        variant_id:
          type: string
          description: Experiment or catalog variant identifier when available.
        variant_name:
          type: string
          description: Experiment variant name when available.
        capability:
          type: string
          description: Resolved capability when it differs from or clarifies the request capability.
        routing_policy:
          type: string
          description: Routing policy supplied by the resolved deployment, experiment, or capability default when present.
    runtime_plan_response_schema__RuntimeCandidatePlan:
      type: object
      required:
      - locality
      - priority
      - confidence
      - reason
      additionalProperties: false
      description: A single candidate in a runtime plan, representing either a local engine or cloud inference.
      properties:
        locality:
          type: string
          enum:
          - local
          - cloud
          description: Where this candidate would run inference.
        engine:
          type: string
          description: Canonical engine identifier (e.g. 'mlx-lm', 'llama.cpp', 'cloud').
        engine_version_constraint:
          type: string
          description: Semver constraint on engine version (e.g. '>=0.18.0').
        artifact:
          $ref: '#/components/schemas/runtime_plan_response_schema__RuntimeArtifactPlan'
          description: Recommended model artifact to download or use.
        priority:
          type: integer
          minimum: 0
          description: Candidate priority. Lower is higher priority.
        confidence:
          type: number
          minimum: 0
          maximum: 1
          description: Server confidence score for this candidate (0.0-1.0).
        reason:
          type: string
          description: Human-readable reason for this recommendation.
        benchmark_required:
          type: boolean
          default: false
          description: Whether a local benchmark is required before the SDK should use this candidate.
        gates:
          type: array
          items:
            $ref: '#/components/schemas/candidate_gate_schema'
          description: Gate requirements the SDK must evaluate per-request before selecting this candidate. Gates are checked
            in the attempt loop; failure of a required gate causes the attempt to fail and fallback to the next candidate.
        delivery_mode:
          type: string
          enum:
          - hosted_gateway
          - sdk_runtime
          - external_endpoint
          description: How the SDK should reach this candidate. 'hosted_gateway' = call api.octomil.com /v1 routes; 'sdk_runtime'
            = run on-device with a prepared local runtime + artifact; 'external_endpoint' = caller-supplied OpenAI-compatible
            URL.
        prepare_required:
          type: boolean
          default: false
          description: Whether the SDK must prepare (install runtime + stage artifacts) before this candidate can serve a
            request. Always false for hosted_gateway. Typically true for sdk_runtime when the artifact is not yet staged.
        prepare_policy:
          type: string
          enum:
          - lazy
          - explicit_only
          - disabled
          default: lazy
          description: When the SDK is allowed to prepare this candidate. 'lazy' = at first .create() request; 'explicit_only'
            = only via client.prepare()/initialize(warm=...); 'disabled' = never prepare (e.g. hosted_gateway).
    runtime_plan_response_schema__RuntimeArtifactPlan:
      type: object
      required:
      - model_id
      additionalProperties: false
      description: Artifact recommendation from the server planner.
      properties:
        model_id:
          type: string
          description: Model identifier.
        artifact_id:
          type: string
          description: Server-assigned artifact ID.
        model_version:
          type: string
          description: Model version string.
        format:
          type: string
          description: Model format (e.g. 'gguf', 'safetensors', 'mlmodelc', 'onnx').
        quantization:
          type: string
          description: Quantization level (e.g. 'q4_k_m', 'int8', 'f16').
        uri:
          type: string
          format: uri
          description: Download URI for the artifact.
        digest:
          type: string
          description: Content digest (e.g. SHA-256 hex).
        size_bytes:
          type: integer
          minimum: 0
          description: Artifact size in bytes.
        min_ram_bytes:
          type: integer
          minimum: 0
          description: Minimum RAM required in bytes to run this artifact.
        required_files:
          type: array
          items:
            type: string
          description: Relative paths the prepare adapter must stage before the runtime can load. e.g. ['model.onnx','tokens.txt','voices.bin','espeak-ng-data/'].
            Trailing slash marks a directory bundle.
        download_urls:
          type: array
          items:
            type: object
            required:
            - url
            additionalProperties: false
            properties:
              url:
                type: string
                format: uri
              expires_at:
                type: string
                format: date-time
              headers:
                type: object
                additionalProperties:
                  type: string
                description: 'Optional request headers (e.g. signed-URL auth) the SDK must send with the GET. No bearer leaks:
                  server-issued only.'
          description: One or more download endpoints for the artifact. Multi-URL supports CDN/origin failover and parallel
            chunk fetches. Hosted_gateway candidates leave this empty.
        manifest_uri:
          type: string
          format: uri
          description: Optional URI to an artifact manifest describing per-file digests, chunk layouts, and total assembled
            size. When present, the SDK fetches this before downloading and uses it to drive chunked, resumable downloads
            with per-chunk verification.
    device_runtime_profile_schema__InstalledRuntime:
      type: object
      required:
      - engine
      additionalProperties: false
      description: A locally-installed inference engine detected on the device.
      properties:
        engine:
          type: string
          description: Canonical engine identifier (e.g. 'coreml', 'mlx-lm', 'llama.cpp', 'onnxruntime').
        version:
          type: string
          description: Engine version string, if known.
        available:
          type: boolean
          default: true
          description: Whether the engine is currently usable.
        accelerator:
          type: string
          description: Hardware accelerator used by this engine (e.g. 'metal', 'ane', 'cuda').
        metadata:
          type: object
          additionalProperties: true
          description: Arbitrary metadata about the engine installation.
    response_stream_event__TextDeltaEvent:
      type: object
      required:
      - type
      - delta
      properties:
        type:
          type: string
          const: text_delta
        delta:
          type: string
      additionalProperties: false
    response_stream_event__ToolCallDeltaEvent:
      type: object
      required:
      - type
      - index
      properties:
        type:
          type: string
          const: tool_call_delta
        index:
          type: integer
          minimum: 0
        id:
          type: string
        name:
          type: string
        argumentsDelta:
          type: string
      additionalProperties: false
    response_stream_event__DoneEvent:
      type: object
      required:
      - type
      - response
      properties:
        type:
          type: string
          const: done
        response:
          $ref: '#/components/schemas/response'
      additionalProperties: false
    response__OutputItem:
      oneOf:
      - $ref: '#/components/schemas/response__TextOutput'
      - $ref: '#/components/schemas/response__ToolCallOutput'
    response__TextOutput:
      type: object
      required:
      - type
      - text
      properties:
        type:
          type: string
          const: text
        text:
          type: string
      additionalProperties: false
    response__ToolCallOutput:
      type: object
      required:
      - type
      - toolCall
      properties:
        type:
          type: string
          const: tool_call
        toolCall:
          $ref: '#/components/schemas/response__ToolCall'
      additionalProperties: false
    response__ToolCall:
      type: object
      required:
      - id
      - name
      - arguments
      properties:
        id:
          type: string
        name:
          type: string
        arguments:
          type: string
      additionalProperties: false
    response__Usage:
      type: object
      required:
      - promptTokens
      - completionTokens
      - totalTokens
      properties:
        promptTokens:
          type: integer
          minimum: 0
        completionTokens:
          type: integer
          minimum: 0
        totalTokens:
          type: integer
          minimum: 0
      additionalProperties: false
    response_request__ContentBlock:
      type: object
      required:
      - role
      properties:
        role:
          type: string
          enum:
          - system
          - user
          - assistant
          - tool
        content: {}
        toolCallId:
          type: string
    response_request__ToolDef:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        description:
          type: string
        parameters:
          type: object
        input_schema:
          type: object
    response_request__ResponseFormat:
      oneOf:
      - type: string
        enum:
        - text
        - json_object
      - type: object
        required:
        - type
        - schema
        properties:
          type:
            type: string
            const: json_schema
          schema:
            type: object
    ExperimentAnalytics__ConfidenceInterval:
      type: object
      properties:
        lower:
          type: number
        upper:
          type: number
        confidence_level:
          type: number
        mean:
          type: number
        margin_of_error:
          type: number
    ExperimentAnalytics__VariantAnalytics:
      type: object
      required:
      - variant_id
      - variant_name
      - is_control
      - sample_count
      - unique_devices
      properties:
        variant_id:
          type: string
        variant_name:
          type: string
        is_control:
          type: boolean
        sample_count:
          type: integer
        unique_devices:
          type: integer
        mean:
          type:
          - number
          - 'null'
        std_dev:
          type:
          - number
          - 'null'
        confidence_interval:
          oneOf:
          - $ref: '#/components/schemas/ExperimentAnalytics__ConfidenceInterval'
          - type: 'null'
    ExperimentAnalytics__SignificanceResult:
      type: object
      required:
      - is_significant
      - p_value
      properties:
        is_significant:
          type: boolean
        p_value:
          type: number
        t_statistic:
          type:
          - number
          - 'null'
        significance_level:
          type:
          - number
          - 'null'
        confidence_level:
          type:
          - number
          - 'null'
        control_mean:
          type:
          - number
          - 'null'
        treatment_mean:
          type:
          - number
          - 'null'
        relative_difference:
          type:
          - number
          - 'null'
        absolute_difference:
          type:
          - number
          - 'null'
        effect_size:
          type:
          - number
          - 'null'
        effect_magnitude:
          type:
          - string
          - 'null'
        control_n:
          type:
          - integer
          - 'null'
        treatment_n:
          type:
          - integer
          - 'null'
        degrees_of_freedom:
          type:
          - number
          - 'null'
        is_sufficient_sample:
          type:
          - boolean
          - 'null'
    content_part__InputText:
      type: object
      required:
      - type
      - text
      properties:
        type:
          const: input_text
        text:
          type: string
      additionalProperties: false
    content_part__InputImage:
      type: object
      required:
      - type
      oneOf:
      - required:
        - assetId
        not:
          anyOf:
          - required:
            - url
          - required:
            - data
      - required:
        - url
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - data
      - required:
        - data
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - url
      properties:
        type:
          const: input_image
        assetId:
          type: string
          description: Asset service reference (preferred for persisted messages)
        url:
          type: string
          format: uri
        data:
          type: string
          maxLength: 13981014
          description: Base64 offline fallback, ~10MB max. Do not use for persisted chat history.
        mediaType:
          type: string
          enum:
          - image/jpeg
          - image/png
          - image/webp
          - image/gif
        detail:
          type: string
          enum:
          - auto
          - low
          - high
          default: auto
          description: Vision model routing hint
      additionalProperties: false
    content_part__InputAudio:
      type: object
      required:
      - type
      oneOf:
      - required:
        - assetId
        not:
          anyOf:
          - required:
            - url
          - required:
            - data
      - required:
        - url
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - data
      - required:
        - data
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - url
      properties:
        type:
          const: input_audio
        assetId:
          type: string
        url:
          type: string
          format: uri
        data:
          type: string
        mediaType:
          type: string
          enum:
          - audio/mpeg
          - audio/wav
          - audio/ogg
      additionalProperties: false
    content_part__InputVideo:
      type: object
      required:
      - type
      oneOf:
      - required:
        - assetId
        not:
          anyOf:
          - required:
            - url
          - required:
            - data
      - required:
        - url
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - data
      - required:
        - data
        - mediaType
        not:
          anyOf:
          - required:
            - assetId
          - required:
            - url
      properties:
        type:
          const: input_video
        assetId:
          type: string
        url:
          type: string
          format: uri
        data:
          type: string
        mediaType:
          type: string
          enum:
          - video/mp4
          - video/webm
        maxFrames:
          type: integer
          description: Runtime hint for frame extraction
      additionalProperties: false
    chat_message__GenerationMetrics:
      type: object
      required:
      - ttftMs
      - decodeTokensPerSec
      - totalTokens
      - totalLatencyMs
      properties:
        ttftMs:
          type: integer
          description: Time to first token in milliseconds.
        decodeTokensPerSec:
          type: number
          description: Decode throughput in tokens per second.
        totalTokens:
          type: integer
          description: Total tokens generated.
        totalLatencyMs:
          type: integer
          description: Total generation latency in milliseconds.
      additionalProperties: false
    ErrorEnvelope:
      type: object
      required:
      - error
      additionalProperties: false
      properties:
        error:
          type: object
          required:
          - code
          - message
          additionalProperties: false
          properties:
            code:
              type: string
              enum:
              - accelerator_unavailable
              - action_not_found
              - action_state_invalid
              - agent_system_unavailable
              - alert_rule_not_found
              - api_key_already_revoked
              - api_key_not_found
              - app_backgrounded
              - app_context_conflict
              - app_not_found
              - approval_already_resolved
              - approval_not_found
              - assignment_not_found
              - authentication_failed
              - billing_customer_not_found
              - billing_export_not_found
              - cancelled
              - capability_not_configured
              - capability_not_supported
              - catalog_family_not_found
              - catalog_lifecycle_invalid
              - catalog_package_not_found
              - catalog_resource_not_found
              - catalog_slug_conflict
              - catalog_variant_not_found
              - catalog_version_not_found
              - checkout_not_complete
              - checksum_mismatch
              - cloud_catalog_mapping_not_found
              - cloud_catalog_run_not_found
              - cloud_catalog_source_not_found
              - cloud_credentials_missing
              - cloud_credentials_revoked
              - cloud_fallback_disallowed
              - cloud_inference_not_allowed
              - cloud_provider_auth_failed
              - conflict
              - connection_not_found
              - context_too_large
              - control_sync_failed
              - credential_not_found
              - deployment_not_found
              - device_not_registered
              - device_revoked
              - download_failed
              - email_already_in_use
              - email_already_verified
              - experiment_not_found
              - experiment_state_invalid
              - forbidden
              - gone
              - hosted_tts_disabled
              - incident_not_found
              - inference_failed
              - insufficient_memory
              - insufficient_scope
              - insufficient_storage
              - integration_not_found
              - invalid_api_key
              - invalid_input
              - invalid_model_ref
              - invalid_token
              - job_not_found
              - job_state_invalid
              - last_auth_method
              - local_runtime_not_found
              - max_tool_rounds_exceeded
              - missing_org_context
              - model_disabled
              - model_load_failed
              - model_not_found
              - network_unavailable
              - no_default_model
              - oauth_provider_not_linked
              - passkey_challenge_expired
              - passkey_credential_not_found
              - payload_too_large
              - plan_limit_exceeded
              - policy_denied
              - previous_response_not_found
              - provider_error
              - rate_limited
              - request_timeout
              - resource_not_found
              - run_not_found
              - run_state_invalid
              - runtime_unavailable
              - server_error
              - stream_interrupted
              - thread_not_found
              - token_expired
              - too_many_tools
              - training_failed
              - training_not_supported
              - unknown
              - unsupported_modality
              - unsupported_tool_calling
              - upstream_provider_error
              - upstream_provider_unavailable
              - version_not_found
              - weight_upload_failed
            message:
              type: string
            details:
              type: object
              additionalProperties: true
            request_id:
              type: string
