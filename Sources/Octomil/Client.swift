// HAND-MAINTAINED — NOT auto-generated.
//
// Despite the historical name, this is a hand-owned compatibility shim, not
// swift-openapi-generator output. iOS does not currently consume a generated
// swift-openapi client; adopting one is a separate migration (facade rework
// against the 1.12.2 output), tracked separately. Do NOT regenerate over it.
//
// The contract spec copy lives at Sources/Octomil/openapi.yaml and is
// freshness-gated by .github/workflows/openapi-types-fresh.yml.

import OpenAPIRuntime
import OpenAPIURLSession
import Foundation

// MARK: - Client placeholder
//
// Minimal placeholder protocol retained for source compatibility. The SDK's
// real networking lives in the hand-written facade (APIClient / OctomilClient).

/// Placeholder protocol; retained for source compatibility until/if the SDK
/// migrates onto a generated swift-openapi client.
internal protocol _OctomilGeneratedClientProtocol: Sendable {}
