# SDK Parity Fixtures

Vendored from octomil-contracts. SDK CI does not require monorepo checkout.

These fixtures validate that the iOS SDK can decode planner responses, evaluate
candidate gates, and produce correct route metadata. Each fixture is self-contained
and exercises a specific routing scenario.
